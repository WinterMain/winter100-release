System.register(["./9SNp4SVz-legacy.js","./DRAA_xGX-legacy.js","./CnNAL0qv-legacy.js","./MB4iT-_Z-legacy.js","./CJg-Door-legacy.js"],(function(n,e){"use strict";var t,a,s,o,r,i,p,l,u,c,d,m,h,f,g,x,y,v,b,S,w,N,k,P,R,C,T;return{setters:[n=>{t=n._,a=n.o,s=n.c,o=n.b,r=n.d,i=n.s,p=n.t,l=n.F,u=n.q,c=n.e,d=n.W,m=n.u,h=n.y,f=n.k,g=n.f,x=n.i,y=n.w,v=n.bK,b=n.j,S=n.aL,w=n.m},n=>{N=n.V},n=>{k=n.u,P=n._,R=n.a},n=>{C=n.u},n=>{T=n.u}],execute:function(){var e=document.createElement("style");e.textContent='.c-books-sidebar .sidebar-header[data-v-8fabe94b]{padding:16px 16px 8px}.c-books-sidebar .sidebar-back[data-v-8fabe94b]{align-items:center;color:#9299a6;display:inline-flex;font-size:12px;gap:4px;line-height:20px;transition-duration:.15s;transition-property:all;transition-timing-function:cubic-bezier(.4,0,.2,1)}.c-books-sidebar .sidebar-back[data-v-8fabe94b]:hover{--tw-text-opacity:1;color:rgb(237 61 161/var(--tw-text-opacity))}.c-books-sidebar .side-title[data-v-8fabe94b]{background:linear-gradient(95deg,rgba(255,182,220,.45),hsla(0,0%,100%,.4) 55%,transparent);border-bottom:3px dotted #d5d9dd;color:#22262a;font-family:SF Pro Display,-apple-system,BlinkMacSystemFont,Segoe UI,Inter,Nunito,sans-serif;font-size:18px;font-weight:400;letter-spacing:.02em;line-height:26px;margin:0;padding:12px 18px;position:relative;text-shadow:1px 1px 0 hsla(0,0%,100%,.95);z-index:1}.c-books-sidebar .sidebar-nav[data-v-8fabe94b]{padding-bottom:8px;padding-top:8px}.c-books-sidebar .sidebar-item[data-v-8fabe94b]{align-items:center;border-left:3px solid transparent;color:#5e6773;display:flex;font-size:14px;gap:8px;line-height:22px;padding:10px 20px;transition-duration:.15s;transition-property:all;transition-timing-function:cubic-bezier(.4,0,.2,1)}.c-books-sidebar .sidebar-item[data-v-8fabe94b]:hover{background:rgba(237,61,161,.04);color:#22262a}.c-books-sidebar .sidebar-item.active[data-v-8fabe94b]{background:rgba(237,61,161,.08);border-left-color:#ed3da1;color:#ed3da1;font-weight:600}.c-books-sidebar .sidebar-index[data-v-8fabe94b]{color:#9299a6;font-size:12px;line-height:20px;min-width:20px}.c-books-sidebar .sidebar-item.active .sidebar-index[data-v-8fabe94b]{color:#ed3da1}body:not([data-theme=cartoon]) .c-books-sidebar .sidebar-item.active[data-v-8fabe94b],body:not([data-theme=cartoon]) .c-books-sidebar .sidebar-item[data-v-8fabe94b]:hover{background:#f5f6fa}.c-chapter-content{padding:24px}.c-chapter-content .mark-down h1{border-bottom:3px dotted #d5d9dd;font-size:24px;line-height:32px;margin-bottom:20px;padding-bottom:16px}.c-chapter-content .mark-down h1,.c-chapter-content .mark-down h2{color:#22262a;font-family:SF Pro Display,-apple-system,BlinkMacSystemFont,Segoe UI,Inter,Nunito,sans-serif;font-weight:500}.c-chapter-content .mark-down h2{font-size:20px;line-height:28px;margin-bottom:12px;margin-top:40px}.c-chapter-content .mark-down h2:before{background:#ed3da1;border-radius:2px;content:"";display:inline-block;height:20px;margin-right:8px;vertical-align:text-bottom;width:4px}.c-chapter-content .mark-down h3{color:#22262a;font-size:18px;font-weight:500;line-height:26px;margin-bottom:8px;margin-top:28px}.c-chapter-content .mark-down p{font-size:15px;line-height:1.8;margin-bottom:12px}.c-chapter-content .mark-down menu,.c-chapter-content .mark-down ol,.c-chapter-content .mark-down ul{list-style:inside;margin:0 0 12px;padding:0}.c-chapter-content .mark-down li{font-size:15px;line-height:1.8}.c-chapter-content .mark-down table{border:2px solid #eff1f5;border-collapse:collapse;border-radius:8px;margin-bottom:16px;overflow:hidden;width:100%}.c-chapter-content .mark-down table th{background:linear-gradient(95deg,rgba(255,182,220,.2),hsla(0,0%,100%,.4) 55%,transparent);border-bottom:2px solid #eff1f5;color:#22262a;font-size:14px;font-weight:500;line-height:22px;padding:10px 16px;text-align:left}.c-chapter-content .mark-down table td{border-bottom:1px solid #eff1f5;color:#3d424a;font-size:14px;line-height:22px;padding:8px 16px}.c-chapter-content .mark-down table tr:last-child td{border-bottom:none}.c-chapter-content .mark-down blockquote{background:rgba(237,61,161,.04);border-left:4px solid #ed3da1;border-radius:12px;margin-bottom:16px;padding:16px 20px}.c-chapter-content .mark-down blockquote p{color:#5e6773!important;margin-bottom:0}.c-chapter-content .mark-down strong{color:#22262a;font-weight:500}.c-chapter-content .mark-down hr{border:none;border-top:3px dotted #d5d9dd;margin-bottom:32px;margin-top:32px}@media (max-width:767px){.c-chapter-content{padding:16px}.c-chapter-content .mark-down h1{font-size:20px;line-height:28px}.c-chapter-content .mark-down h2{font-size:18px;line-height:26px;margin-top:28px}}.c-chapter-nav[data-v-01b3ab8d]{border-top:3px dotted #d5d9dd;display:flex;gap:16px;justify-content:space-between;padding:24px}.c-chapter-nav .nav-link[data-v-01b3ab8d]{align-items:center;display:flex;gap:8px;max-width:50%;padding:16px 20px}.c-chapter-nav .nav-link[data-v-01b3ab8d]:hover{transform:translateY(-4px) rotate(-.3deg)}.c-chapter-nav .nav-next[data-v-01b3ab8d]{margin-left:auto;text-align:right}.c-chapter-nav .nav-label[data-v-01b3ab8d]{display:flex;flex-direction:column}.c-chapter-nav .nav-hint[data-v-01b3ab8d]{color:#9299a6;font-size:12px;line-height:20px}.c-chapter-nav .nav-title[data-v-01b3ab8d]{color:#22262a;font-family:SF Pro Display,-apple-system,BlinkMacSystemFont,Segoe UI,Inter,Nunito,sans-serif;font-size:14px;font-weight:600;line-height:22px;margin-top:2px}@media (max-width:767px){.c-chapter-nav[data-v-01b3ab8d]{flex-direction:column;padding:16px}.c-chapter-nav .nav-link[data-v-01b3ab8d]{max-width:100%;padding:12px}}body:not([data-theme=cartoon]) .c-chapter-nav[data-v-01b3ab8d]{border-top:1px solid rgba(0,0,0,.06)}body:not([data-theme=cartoon]) .c-chapter-nav .nav-link[data-v-01b3ab8d]:hover{transform:translateY(-2px)}.p-book-chapter{overflow-x:hidden;padding-bottom:80px;position:relative}.p-book-chapter>:not(.u-cartoon-sky){position:relative;z-index:1}.p-book-chapter .page-container .c-aside{width:260px}.p-book-chapter .page-container .c-aside .fixed-area{height:auto}.p-book-chapter .page-container .c-aside .fixed-area[fixed=true]{bottom:20px;position:fixed}.p-book-chapter .page-container .c-outer{border-radius:10px;flex:1 1 0%;margin-left:20px;overflow-x:hidden;--tw-bg-opacity:1;background-color:rgb(255 255 255/var(--tw-bg-opacity))}.p-book-chapter .multi-lang-stage .multi-lang-pane{height:0;overflow:hidden;padding:0}.p-book-chapter .multi-lang-stage.en_US .multi-lang-pane[data-lang=en_US],.p-book-chapter .multi-lang-stage.zh_Hans_CN .multi-lang-pane[data-lang=zh_Hans_CN],.p-book-chapter .multi-lang-stage.zh_Hant_HK .multi-lang-pane[data-lang=zh_Hant_HK]{height:auto;overflow:auto;padding:24px}.p-book-chapter .sidebar-overlay,.p-book-chapter .sidebar-toggle{display:none}@media (max-width:767px){.p-book-chapter .c-aside{display:none}.p-book-chapter .c-aside.sidebar-visible{display:block}.p-book-chapter .sidebar-toggle{align-items:center;border-bottom-right-radius:12px;border-top-right-radius:12px;display:flex;justify-content:center;position:fixed;z-index:10;--tw-bg-opacity:1;background-color:rgb(255 255 255/var(--tw-bg-opacity));border:2px solid #eff1f5;border-left:none;bottom:80px;box-shadow:4px 4px rgba(237,61,161,.12);height:40px;left:0;width:40px}.p-book-chapter .sidebar-overlay{background:rgba(0,0,0,.3);display:block;right:0;bottom:0;left:0;position:fixed;top:60px;z-index:1}.p-book-chapter .c-books-sidebar{background:#fff;border-left:none;border-radius:0;border-right:3px solid rgba(237,61,161,.35);bottom:0;left:0;overflow-y:auto;position:fixed;top:60px;transform:translate(-100%);transition:transform .3s ease;width:280px;z-index:10}.p-book-chapter .c-books-sidebar.sidebar-open{box-shadow:8px 0 24px rgba(0,0,0,.1);transform:translate(0)}}\n',document.head.appendChild(e);const L={en_US:{"全部书籍":"All Books"},zh_Hans_CN:{"全部书籍":"全部书籍"},zh_Hant_HK:{"全部书籍":"全部書籍"}},A={class:"c-books-sidebar"},j={class:"sidebar-header"},I={class:"side-title"},H={class:"sidebar-nav"},E={class:"sidebar-index"},_=t({__name:"Sidebar",props:{bookConfig:{type:Object,default:()=>({title:"",chapters:[]})},bookSlug:{type:String,default:""},currentChapter:{type:String,default:""}},setup(n){const e=n=>{const e=m().$lang.value||"zh_Hans_CN",t=L[e]||L.zh_Hans_CN||{};return void 0===t[n]?n:t[n]};return(t,m)=>{const f=h;return a(),s("aside",A,[o("div",j,[r(f,{to:"/books/",class:"sidebar-back"},{default:i((()=>[m[0]||(m[0]=o("i",{class:"iconfont icon-arrow_left text-12"},null,-1)),o("span",null,p(e("全部书籍")),1)])),_:1})]),o("p",I,p(n.bookConfig.title),1),o("nav",H,[(a(!0),s(l,null,u(n.bookConfig.chapters,((e,t)=>(a(),c(f,{key:e.slug,to:`/books/${n.bookSlug}/${e.slug}/`,class:d(["sidebar-item",{active:n.currentChapter===e.slug}])},{default:i((()=>[o("span",E,p(String(t+1).padStart(2,"0")),1),o("span",null,p(e.title),1)])),_:2},1032,["to","class"])))),128))])])}}},[["__scopeId","data-v-8fabe94b"]]),D={class:"c-chapter-content"},M={__name:"ChapterContent",props:{content:{type:String,default:""}},setup:n=>(e,t)=>(a(),s("article",D,[r(f(N),{class:"mark-down code-content",source:n.content},null,8,["source"])]))},U={en_US:{"上一章":"Previous Chapter","下一章":"Next Chapter"},zh_Hans_CN:{"上一章":"上一章","下一章":"下一章"},zh_Hant_HK:{"上一章":"上一章","下一章":"下一章"}},O={class:"c-chapter-nav"},F={class:"nav-label"},z={class:"nav-hint"},B={class:"nav-title"},W={key:1},q={class:"nav-label"},V={class:"nav-hint"},G={class:"nav-title"},$={key:3},K=t({__name:"ChapterNav",props:{bookSlug:{type:String,default:""},prev:{type:Object,default:null},next:{type:Object,default:null}},setup(n){const e=n=>{const e=m().$lang.value||"zh_Hans_CN",t=U[e]||U.zh_Hans_CN||{};return void 0===t[n]?n:t[n]};return(t,r)=>{const l=h;return a(),s("div",O,[n.prev?(a(),c(l,{key:0,to:`/books/${n.bookSlug}/${n.prev.slug}/`,class:"nav-link nav-prev u-cartoon-box"},{default:i((()=>[r[0]||(r[0]=o("i",{class:"iconfont icon-arrow_left text-12"},null,-1)),o("div",F,[o("span",z,p(e("上一章")),1),o("span",B,p(n.prev.title),1)])])),_:1},8,["to"])):(a(),s("div",W)),n.next?(a(),c(l,{key:2,to:`/books/${n.bookSlug}/${n.next.slug}/`,class:"nav-link nav-next u-cartoon-box"},{default:i((()=>[o("div",q,[o("span",V,p(e("下一章")),1),o("span",G,p(n.next.title),1)]),r[1]||(r[1]=o("i",{class:"iconfont icon-arrow_right text-12"},null,-1))])),_:1},8,["to"])):(a(),s("div",$))])}}},[["__scopeId","data-v-01b3ab8d"]]),J={en_US:{"书籍不存在":"Book not found","章节不存在":"Chapter not found"},zh_Hans_CN:{"书籍不存在":"书籍不存在","章节不存在":"章节不存在"},zh_Hant_HK:{"书籍不存在":"書籍不存在","章节不存在":"章節不存在"}},Y={class:"p-book-chapter u-cartoon-page"},X={class:"c-aside"},Q={class:"fixed-area"},Z={class:"c-outer bg-white u-cartoon-box u-cartoon-box--no-hover"};n("default",{__name:"[chapter]",setup(n){const e=n=>{const e=m().$lang.value||"zh_Hans_CN",t=J[e]||J.zh_Hans_CN||{};return void 0===t[n]?n:t[n]},t=Object.assign({"/books/nextjs/config.js":P,"/books/nuxtjs/config.js":R}),i=Object.assign({"/books/nextjs/chapters/en_US/01-introduction.md":"# Introduction to Next.js\n\nNext.js is a React framework for building full-stack web applications. It adds file-based routing, optimized bundling, and first-class support for server rendering and static generation on top of React, so you can ship fast, SEO-friendly pages without assembling tooling from scratch.\n\n## Create a project\n\nUse the official starter with your preferred package manager:\n\n```bash\nnpx create-next-app@latest my-app\ncd my-app\nnpm run dev\n```\n\nDuring setup you can enable TypeScript, ESLint, Tailwind CSS, and the App Router (`app/`). The dev server typically runs at `http://localhost:3000`.\n\n## Project structure (App Router)\n\nA typical layout looks like this:\n\n```\nmy-app/\n├── app/\n│   ├── layout.tsx    # Root layout (shell, fonts, providers)\n│   ├── page.tsx      # Home route (/)\n│   └── globals.css\n├── public/           # Static assets\n├── next.config.mjs\n└── package.json\n```\n\n`app/page.tsx` is the entry for `/`. Nested folders under `app/` map to URL segments; `layout.tsx` wraps child routes and persists across navigations.\n\n## Your first page\n\n```tsx\n// app/page.tsx\nexport default function Home() {\n  return (\n    <main>\n      <h1>Hello, Next.js</h1>\n    </main>\n  );\n}\n```\n\nExport a default React component from `page.tsx` for each route. Metadata and data loading integrate here without a separate router config file.\n\n## Next.js vs Nuxt\n\nBoth are **meta-frameworks** with file-based routing and SSR/SSG. **Nuxt** is Vue-centric (Vite or webpack under the hood) with conventions like `pages/` and `layouts/`. **Next.js** is **React-centric** and uses the `app/` directory (or legacy `pages/`) with React Server Components by default in the App Router. If your team is on Vue, Nuxt is the natural fit; for React ecosystems and hiring pools, Next.js is the standard choice. Conceptually—layouts, dynamic routes, and hybrid rendering—map closely between the two.\n","/books/nextjs/chapters/en_US/02-installation.md":'# Installation\n\nThis chapter walks you through bootstrapping a Next.js app from scratch: system requirements, the CLI, manual setup, and baseline configuration for TypeScript, ESLint, and path aliases.\n\n## System requirements\n\n- Node.js **20.9** or higher.\n- macOS, Windows (including WSL), or Linux.\n- Browsers: Chrome 111+, Edge 111+, Firefox 111+, Safari 16.4+.\n\n## Create with the CLI\n\nThe quickest path is the official scaffold `create-next-app`:\n\n```bash\npnpm create next-app@latest my-app --yes\ncd my-app\npnpm dev\n```\n\n`--yes` skips every prompt and uses the recommended defaults: **TypeScript + Tailwind CSS + ESLint + App Router + Turbopack**, with the `@/*` import alias wired in.\n\nDrop `--yes` to see the full interactive prompts:\n\n```txt\nWould you like to use TypeScript? Yes\nWhich linter would you like to use? ESLint / Biome / None\nWould you like to use React Compiler? No\nWould you like to use Tailwind CSS? Yes\nWould you like your code inside a `src/` directory? No\nWould you like to use App Router? Yes\nWould you like to customize the import alias? No\n```\n\n## Manual install\n\nFor full control, install the packages yourself:\n\n```bash\nnpm i next@latest react@latest react-dom@latest\n```\n\nThen add the scripts to `package.json`:\n\n```json\n{\n  "scripts": {\n    "dev": "next dev",\n    "build": "next build",\n    "start": "next start",\n    "lint": "eslint"\n  }\n}\n```\n\n> Turbopack is the default bundler. Pass `--webpack` to `next dev` or `next build` to fall back to webpack.\n\n## Create the `app` directory\n\nNext.js uses file-system routing. Create an `app/` folder in the project root, and add a **root layout** — it\'s required and must contain `html` and `body` tags:\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return (\n    <html lang="en">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\nThen add a home page:\n\n```tsx\n// app/page.tsx\nexport default function Page() {\n  return <h1>Hello, Next.js!</h1>;\n}\n```\n\nRun `npm run dev` and open `http://localhost:3000` — your first page is live.\n\n## The `public` folder\n\nPut static assets (images, fonts, etc.) under `public/`. Files inside are served from the root URL:\n\n```tsx\nimport Image from \'next/image\';\n\n<Image src="/profile.png" alt="Profile" width={100} height={100} />;\n```\n\n## TypeScript and path aliases\n\nNext.js has built-in TypeScript support: rename a file to `.ts` / `.tsx` and run `next dev` — it auto-generates `tsconfig.json` with recommended settings.\n\nPath aliases in `tsconfig.json` (or `jsconfig.json`) keep imports tidy:\n\n```json\n{\n  "compilerOptions": {\n    "baseUrl": ".",\n    "paths": {\n      "@/components/*": ["components/*"],\n      "@/styles/*": ["styles/*"]\n    }\n  }\n}\n```\n\nYou can then write `import { Button } from \'@/components/button\'` instead of `../../../components/button`.\n',"/books/nextjs/chapters/en_US/03-app-router.md":'# App Router\n\nThe App Router (`app/`) is the modern way to structure routes in Next.js. Folders and special files define URLs, layouts, and loading behavior without a central route table.\n\n## Routing conventions\n\n- **`page.tsx`** — UI for a segment; makes the route public.\n- **`layout.tsx`** — Shared shell for that segment and its children.\n- **`loading.tsx`** — Suspense boundary for loading UI.\n- **`error.tsx`** — Error boundary for the segment.\n- **`not-found.tsx`** — Custom 404 for the subtree.\n\nA file at `app/dashboard/page.tsx` renders `/dashboard`; only folders with a `page.tsx` (or a route handler) are navigable routes.\n\n## Layouts\n\nLayouts wrap nested routes and preserve state on client-side navigation:\n\n```tsx\n// app/dashboard/layout.tsx\nexport default function DashboardLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return (\n    <section className="dashboard">\n      <nav>{/* sidebar */}</nav>\n      <div>{children}</div>\n    </section>\n  );\n}\n```\n\nRoot `app/layout.tsx` should include `<html>` and `<body>` and is required for the tree.\n\n## Dynamic routes\n\nUse bracket folders for dynamic segments:\n\n```\napp/blog/[slug]/page.tsx   → /blog/hello-world\napp/shop/[...slug]/page.tsx → catch-all segments\n```\n\n```tsx\n// Next.js 15+: params is a Promise\nexport default async function Post({\n  params,\n}: {\n  params: Promise<{ slug: string }>;\n}) {\n  const { slug } = await params;\n  return <article>{slug}</article>;\n}\n```\n\n## Route groups\n\nParentheses **do not** affect the URL: `(marketing)` and `(shop)` organize code while keeping flat URLs.\n\n```\napp/\n├── (marketing)/\n│   └── page.tsx      → /\n└── (shop)/\n    └── cart/page.tsx → /cart\n```\n\nUse groups to split layouts or concerns without extra path segments.\n',"/books/nextjs/chapters/en_US/04-layouts-and-pages.md":'# Layouts and Pages\n\nNext.js uses file-system routing: folders define URL segments while `page.tsx` and `layout.tsx` define the UI for each segment. This chapter covers how they compose, plus dynamic segments and `searchParams`.\n\n## Creating a page\n\nAdd a `page.tsx` anywhere under `app/` and default-export a React component to make that route public:\n\n```tsx\n// app/page.tsx → /\nexport default function Page() {\n  return <h1>Hello Next.js!</h1>;\n}\n```\n\n## Creating a layout\n\nA layout is UI shared across multiple pages. On client-side navigation, layouts preserve state, stay interactive, and do not rerender.\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return (\n    <html lang="en">\n      <body>\n        <main>{children}</main>\n      </body>\n    </html>\n  );\n}\n```\n\nThe layout at the root of `app/` is the **root layout** — it\'s required, and only it may include the `<html>` and `<body>` tags.\n\n## Nested routes and layouts\n\nNested folders map to nested URL segments. `/blog/[slug]` corresponds to:\n\n```\napp/\n├── layout.tsx         # root layout\n├── blog/\n│   ├── layout.tsx     # blog-section layout (optional)\n│   ├── page.tsx       # /blog\n│   └── [slug]/\n│       └── page.tsx   # /blog/hello-world\n```\n\nLayouts nest automatically: root layout wraps the blog layout which wraps the individual post page. Use this to share navigation, sidebars, or breadcrumbs at the appropriate level.\n\n```tsx\n// app/blog/layout.tsx\nexport default function BlogLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return <section className="blog">{children}</section>;\n}\n```\n\n## Dynamic segments\n\nBrackets create dynamic segments: `[slug]`, `[id]`. In Next.js 15+, `params` is a Promise and must be awaited:\n\n```tsx\n// app/blog/[slug]/page.tsx\nexport default async function Post({\n  params,\n}: {\n  params: Promise<{ slug: string }>;\n}) {\n  const { slug } = await params;\n  const post = await getPost(slug);\n\n  return (\n    <article>\n      <h1>{post.title}</h1>\n      <p>{post.content}</p>\n    </article>\n  );\n}\n```\n\nPair dynamic segments with `generateStaticParams` to prerender every permutation at build time for a fully static site.\n\n## searchParams\n\nIn a Server Component page, read the query string via the `searchParams` prop:\n\n```tsx\nexport default async function Page({\n  searchParams,\n}: {\n  searchParams: Promise<{ [k: string]: string | string[] | undefined }>;\n}) {\n  const { q } = await searchParams;\n  return <p>Search term: {q}</p>;\n}\n```\n\n> Heads up: accessing `searchParams` opts the page into **dynamic rendering**, because it depends on the incoming request.\n\nClient Components should use the `useSearchParams` hook instead.\n\n## Linking between pages\n\nUse `next/link` for fast client-side navigation with built-in prefetching:\n\n```tsx\nimport Link from \'next/link\';\n\nexport default async function Posts() {\n  const posts = await getPosts();\n  return (\n    <ul>\n      {posts.map(p => (\n        <li key={p.slug}>\n          <Link href={`/blog/${p.slug}`}>{p.title}</Link>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```\n\nThe next chapter dives deeper into prefetching, streaming, and client-side transitions.\n',"/books/nextjs/chapters/en_US/05-linking-and-navigating.md":"# Linking and Navigating\n\nNext.js renders routes on the server by default. To keep transitions feeling instant, it bakes in **prefetching**, **streaming**, and **client-side transitions**. This chapter unpacks how they work together and how to tune them for slow networks and dynamic routes.\n\n## The four layers of navigation\n\n1. **Server Rendering**: layouts and pages are React Server Components by default; the server generates an RSC payload and sends it to the client.\n2. **Prefetching**: when a `<Link>` enters the viewport, Next.js fetches the target route in the background.\n3. **Streaming**: the server sends HTML progressively; `loading.tsx` lets you show a skeleton immediately.\n4. **Client-side transitions**: `<Link>` swaps page content without a full reload — preserving shared layouts, scroll position, and interactivity.\n\n## Use `<Link>` instead of `<a>`\n\n```tsx\nimport Link from 'next/link';\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <html>\n      <body>\n        <nav>\n          <Link href=\"/blog\">Blog</Link>         {/* prefetched */}\n          <a href=\"/contact\">Contact</a>          {/* not prefetched */}\n        </nav>\n        {children}\n      </body>\n    </html>\n  );\n}\n```\n\n- **Static routes**: the entire route is prefetched.\n- **Dynamic routes**: prefetching is skipped, or *partial* if a `loading.tsx` exists (shared layout + skeleton).\n\n## Stream with `loading.tsx`\n\nDrop a `loading.tsx` inside a route folder and Next.js will automatically wrap `page.tsx` in a `<Suspense>` boundary:\n\n```tsx\n// app/blog/loading.tsx\nexport default function Loading() {\n  return <BlogListSkeleton />;\n}\n```\n\nThe user sees the skeleton instantly and real content swaps in when ready. This directly improves **Core Web Vitals** (TTFB / FCP / TTI).\n\n## Add `generateStaticParams` for dynamic segments\n\nIf a dynamic segment *could* be prerendered but isn't, every request falls back to dynamic rendering. Fix it:\n\n```tsx\nexport async function generateStaticParams() {\n  const posts = await fetch('https://.../posts').then(r => r.json());\n  return posts.map((p: any) => ({ slug: p.slug }));\n}\n```\n\n## Optimizing for slow networks\n\nOn flaky connections, prefetch may not finish before a click. Use `useLinkStatus` to show immediate feedback:\n\n```tsx\n'use client';\nimport { useLinkStatus } from 'next/link';\n\nexport default function LinkIndicator() {\n  const { pending } = useLinkStatus();\n  return <span aria-hidden className={pending ? 'is-pending' : ''} />;\n}\n```\n\nPair it with a CSS delay (e.g. show only after 100ms) to avoid flashes on fast transitions.\n\n## Disabling prefetch selectively\n\nIn huge lists (infinite-scroll tables) prefetching wastes resources. Turn it off:\n\n```tsx\n<Link prefetch={false} href=\"/blog\">Blog</Link>\n```\n\nOr only prefetch on hover:\n\n```tsx\n'use client';\nimport Link from 'next/link';\nimport { useState } from 'react';\n\nfunction HoverPrefetchLink({ href, children }: { href: string; children: React.ReactNode }) {\n  const [active, setActive] = useState(false);\n  return (\n    <Link\n      href={href}\n      prefetch={active ? null : false}\n      onMouseEnter={() => setActive(true)}\n    >\n      {children}\n    </Link>\n  );\n}\n```\n\n## Native History API\n\nWant to update the URL without re-rendering the UI? Use `pushState` directly — it integrates with `usePathname` and `useSearchParams`:\n\n```tsx\n'use client';\nimport { useSearchParams } from 'next/navigation';\n\nexport default function SortProducts() {\n  const searchParams = useSearchParams();\n  function update(order: string) {\n    const params = new URLSearchParams(searchParams.toString());\n    params.set('sort', order);\n    window.history.pushState(null, '', `?${params.toString()}`);\n  }\n  return (\n    <>\n      <button onClick={() => update('asc')}>Ascending</button>\n      <button onClick={() => update('desc')}>Descending</button>\n    </>\n  );\n}\n```\n","/books/nextjs/chapters/en_US/06-server-and-client-components.md":"# Server and Client Components\n\nIn the App Router, layouts and pages are **Server Components** by default. They run on the server, can reach databases and secrets directly, and ship very little JavaScript to the browser. When you need interactivity, you layer in **Client Components**.\n\n## When to use each\n\n**Server Components** — use when you need to:\n- Fetch data close to its source (databases, APIs).\n- Use API keys, tokens, and other secrets.\n- Reduce client JS and improve First Contentful Paint.\n- Stream content progressively.\n\n**Client Components** — use when you need:\n- Hooks like `useState`, `useEffect`.\n- Browser-only APIs (`localStorage`, `window`, `Navigator.geolocation`).\n- Event handlers like `onClick`, `onChange`.\n\n## How do they work in Next.js?\n\n**On the server**:\n- Server Components render into a special data format called the **RSC Payload**.\n- Together with placeholders for Client Components, it's used to produce the initial HTML.\n\n**On the client (first load)**:\n1. HTML shows a fast, non-interactive preview.\n2. The RSC Payload reconciles the Server and Client trees.\n3. JavaScript **hydrates** Client Components to make the page interactive.\n\n**Subsequent navigations**:\n- The RSC Payload is prefetched and cached for instant transitions.\n- Client Components render entirely in the browser.\n\n## Writing a Client Component\n\nAdd the `'use client'` directive at the top of the file:\n\n```tsx\n'use client';\n\nimport { useState } from 'react';\n\nexport default function Counter() {\n  const [n, setN] = useState(0);\n  return (\n    <div>\n      <p>{n} clicks</p>\n      <button onClick={() => setN(n + 1)}>Click me</button>\n    </div>\n  );\n}\n```\n\n`'use client'` declares a boundary: from this file onwards, all imports are part of the client bundle — you don't need the directive in every file.\n\n## Keep bundles lean\n\n**Push `'use client'` as deep as possible.** Mark only the interactive leaves, not whole pages:\n\n```tsx\n// Layout: Server Component\nimport Search from './search';   // Client Component\nimport Logo from './logo';       // Server Component\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      <nav>\n        <Logo />\n        <Search />\n      </nav>\n      <main>{children}</main>\n    </>\n  );\n}\n```\n\nThe layout remains a Server Component; only `Search` ships as client JS.\n\n## Passing data from Server to Client\n\nJust use props — they must be serializable by React:\n\n```tsx\n// Server Component\nimport LikeButton from '@/app/ui/like-button';\nimport { getPost } from '@/lib/data';\n\nexport default async function Page({\n  params,\n}: {\n  params: Promise<{ id: string }>;\n}) {\n  const { id } = await params;\n  const post = await getPost(id);\n  return <LikeButton likes={post.likes} />;\n}\n```\n\n```tsx\n// Client Component\n'use client';\nexport default function LikeButton({ likes }: { likes: number }) {\n  // ...\n}\n```\n\n## Interleaving: Server inside Client\n\nClient Components can't `import` Server Components directly, but they can receive them as `children`:\n\n```tsx\n// Client Component: Modal\n'use client';\nexport default function Modal({ children }: { children: React.ReactNode }) {\n  return <div className=\"modal\">{children}</div>;\n}\n```\n\n```tsx\n// Server Component: Page\nimport Modal from './ui/modal';\nimport Cart from './ui/cart'; // Server Component\n\nexport default function Page() {\n  return (\n    <Modal>\n      <Cart />\n    </Modal>\n  );\n}\n```\n\nThis \"client shell, server content\" pattern is everywhere in practice.\n\n## Context providers\n\nReact Context isn't supported in Server Components. Wrap providers in a Client Component and mount them from the root layout:\n\n```tsx\n'use client';\nimport { createContext } from 'react';\n\nexport const ThemeContext = createContext({});\n\nexport default function ThemeProvider({ children }: { children: React.ReactNode }) {\n  return <ThemeContext.Provider value=\"dark\">{children}</ThemeContext.Provider>;\n}\n```\n\n## Preventing environment poisoning\n\nServer code can accidentally be imported into the client. Use the `server-only` package to fail the build when that happens:\n\n```ts\nimport 'server-only';\n\nexport async function getData() {\n  const res = await fetch('https://external-service.com/data', {\n    headers: { authorization: process.env.API_KEY },\n  });\n  return res.json();\n}\n```\n\n`client-only` is the counterpart for browser-only modules.\n","/books/nextjs/chapters/en_US/07-rendering.md":'# Rendering Modes\n\nNext.js lets you mix **server** and **client** rendering in one app. Choosing the right mode balances interactivity, SEO, freshness, and infrastructure cost.\n\n## Server Components (default)\n\nIn the App Router, components in `app/` are **Server Components** unless marked otherwise. They run on the server, can access databases and secrets directly, and send minimal JavaScript to the client.\n\n```tsx\n// app/items/page.tsx — Server Component\nimport { db } from "@/lib/db";\n\nexport default async function ItemsPage() {\n  const items = await db.item.findMany();\n  return (\n    <ul>\n      {items.map((i) => (\n        <li key={i.id}>{i.name}</li>\n      ))}\n    </ul>\n  );\n}\n```\n\nNo `useState` or browser-only APIs here—split interactive pieces into Client Components.\n\n## Client Components\n\nAdd `"use client"` at the top of files that need hooks, event handlers, or browser APIs:\n\n```tsx\n"use client";\n\nimport { useState } from "react";\n\nexport function Counter() {\n  const [n, setN] = useState(0);\n  return <button onClick={() => setN(n + 1)}>{n}</button>;\n}\n```\n\nCompose small client islands inside larger server trees to keep bundles lean.\n\n## Static generation and ISR\n\n**Static** pages are built at build time (`output: "export"` or default static segments). **ISR** (Incremental Static Regeneration) rebuilds pages in the background after a time window:\n\n```tsx\nexport const revalidate = 3600; // seconds\n\nexport default async function Page() {\n  const data = await fetch("https://api.example.com/stats", {\n    next: { revalidate: 3600 },\n  }).then((r) => r.json());\n  return <pre>{JSON.stringify(data)}</pre>;\n}\n```\n\nUse static/ISR for content that changes occasionally; use **dynamic rendering** (no cache, or `force-dynamic`) for per-request personalization.\n\n## Choosing a mode\n\n| Need | Prefer |\n|------|--------|\n| SEO, fast TTFB, no client JS for content | Server Components + static or ISR |\n| Forms, animations, charts | Client Components (or mix) |\n| Real-time or user-specific data every request | Dynamic server rendering |\n| CDN-cached marketing pages | Static or ISR with `revalidate` |\n\nStart with Server Components and static/ISR; add Client Components and dynamic rendering only where the UX requires it.\n',"/books/nextjs/chapters/en_US/08-fetching-data.md":"# Fetching Data\n\nFetching data in the App Router is remarkably natural: Server Components `await` directly; Client Components use `use`, SWR, or React Query. This chapter covers both sides, plus using Suspense to stream the slow parts.\n\n## Fetching in Server Components\n\n### With `fetch`\n\nMake the component `async` and await the request:\n\n```tsx\nexport default async function Page() {\n  const res = await fetch('https://api.vercel.app/blog');\n  const posts = await res.json();\n  return (\n    <ul>\n      {posts.map((p: any) => (\n        <li key={p.id}>{p.title}</li>\n      ))}\n    </ul>\n  );\n}\n```\n\nKey points:\n- React automatically **memoizes** identical `fetch` calls within one component tree, so you can fetch where you need instead of drilling props.\n- `fetch` is **not cached** by default. Use the `use cache` directive (chapter 10) or `next: { revalidate }` on the fetch to cache.\n\n### With an ORM or database client\n\nServer Components only run on the server, so it's safe to query your database directly:\n\n```tsx\nimport { db, posts } from '@/lib/db';\n\nexport default async function Page() {\n  const all = await db.select().from(posts);\n  return (\n    <ul>\n      {all.map(p => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n> Still authenticate and authorize each query. See the *Data Security* guide.\n\n## Streaming\n\nWhen some data is slow, stream it while the rest of the page renders instantly.\n\n### `loading.js`\n\nDrop a `loading.tsx` in a route folder to wrap the whole page in a Suspense boundary:\n\n```tsx\n// app/blog/loading.tsx\nexport default function Loading() {\n  return <div>Loading…</div>;\n}\n```\n\n### `<Suspense>` for fine-grained control\n\nStream only the slow fragment:\n\n```tsx\nimport { Suspense } from 'react';\nimport BlogList from '@/components/BlogList';\nimport BlogListSkeleton from '@/components/BlogListSkeleton';\n\nexport default function BlogPage() {\n  return (\n    <div>\n      <header>\n        <h1>Welcome to the Blog</h1>\n      </header>\n      <main>\n        <Suspense fallback={<BlogListSkeleton />}>\n          <BlogList />\n        </Suspense>\n      </main>\n    </div>\n  );\n}\n```\n\n### Design meaningful loading states\n\nDon't ship a bare \"Loading…\" string. Skeletons, placeholder cards, cover images, and titles all reassure the user that the app is responding.\n\n## Fetching in Client Components\n\n### Server → Client streaming with `use`\n\nKick off the promise on the server, pass it as a prop, and consume it with React's `use`:\n\n```tsx\n// Server Component\nimport Posts from '@/components/Posts';\nimport { Suspense } from 'react';\n\nexport default function Page() {\n  const postsPromise = fetch('https://api.vercel.app/blog').then(r => r.json());\n  return (\n    <Suspense fallback={<p>Loading…</p>}>\n      <Posts postsPromise={postsPromise} />\n    </Suspense>\n  );\n}\n```\n\n```tsx\n'use client';\nimport { use } from 'react';\n\nexport default function Posts({ postsPromise }: { postsPromise: Promise<any[]> }) {\n  const posts = use(postsPromise);\n  return (\n    <ul>\n      {posts.map(p => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n### Community libraries: SWR / React Query\n\nFor client-side caching, retries, pagination, and mutations, SWR or TanStack Query are the ergonomic choices:\n\n```tsx\n'use client';\nimport useSWR from 'swr';\n\nconst fetcher = (url: string) => fetch(url).then(r => r.json());\n\nexport function Posts() {\n  const { data, error, isLoading } = useSWR('/api/posts', fetcher);\n  if (isLoading) return <p>Loading…</p>;\n  if (error) return <p>Failed to load</p>;\n  return (\n    <ul>\n      {data.map((p: any) => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n## Nuxt comparison\n\nNuxt reaches for `useFetch` / `useAsyncData`; Next.js leans on native `fetch` and async components, which is closer to web standards. Both share a goal: data fetched on the server is serialized into the HTML and rehydrated on the client — no round-trip on first paint.\n","/books/nextjs/chapters/en_US/09-mutating-data.md":"# Mutating Data with Server Actions\n\nNext.js handles writes through React's **Server Functions** — async functions marked with `'use server'` that the client can invoke over the network. When wired to a `<form>` or `formAction`, we call them **Server Actions**.\n\n## Creating a Server Function\n\nPut `'use server'` at the top of a file to mark every export as a Server Function:\n\n```ts\n// app/actions.ts\n'use server';\n\nimport { auth } from '@/lib/auth';\n\nexport async function createPost(formData: FormData) {\n  const session = await auth();\n  if (!session?.user) throw new Error('Unauthorized');\n\n  const title = formData.get('title');\n  const content = formData.get('content');\n  // write to the database…\n}\n```\n\n> Security warning: Server Functions are exposed as POST endpoints. **Authenticate and authorize inside every function.** Don't rely on whether a button is rendered.\n\nYou can also mark a single function with `'use server'`:\n\n```tsx\nexport default function Page() {\n  async function createPost(formData: FormData) {\n    'use server';\n    // …\n  }\n  return <form action={createPost}>…</form>;\n}\n```\n\n## Invoking from a form\n\nReact extends `<form>`'s `action` prop to accept Server Functions:\n\n```tsx\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  return (\n    <form action={createPost}>\n      <input name=\"title\" />\n      <textarea name=\"content\" />\n      <button type=\"submit\">Publish</button>\n    </form>\n  );\n}\n```\n\n- Forms submit even before JavaScript has loaded (progressive enhancement).\n- After submit, Next.js returns the updated UI in the same round-trip.\n\n## Invoking from an event handler\n\nClient Components can call actions from handlers:\n\n```tsx\n'use client';\nimport { incrementLike } from './actions';\nimport { useState } from 'react';\n\nexport default function LikeButton({ initialLikes }: { initialLikes: number }) {\n  const [likes, setLikes] = useState(initialLikes);\n  return (\n    <button onClick={async () => setLikes(await incrementLike())}>\n      ❤ {likes}\n    </button>\n  );\n}\n```\n\n## Handling expected errors\n\nFor validation-style \"expected errors\", **don't throw** — return them as values:\n\n```ts\n'use server';\nexport async function createPost(prevState: any, formData: FormData) {\n  const res = await fetch('https://api.vercel.app/posts', {\n    method: 'POST',\n    body: JSON.stringify({\n      title: formData.get('title'),\n      content: formData.get('content'),\n    }),\n  });\n  if (!res.ok) return { message: 'Failed to create post' };\n  return { message: '' };\n}\n```\n\nOn the client, manage it with `useActionState`:\n\n```tsx\n'use client';\nimport { useActionState } from 'react';\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  const [state, formAction, pending] = useActionState(createPost, { message: '' });\n  return (\n    <form action={formAction}>\n      <input name=\"title\" required />\n      <textarea name=\"content\" required />\n      {state?.message && <p aria-live=\"polite\">{state.message}</p>}\n      <button disabled={pending}>Publish</button>\n    </form>\n  );\n}\n```\n\n## Refreshing UI after a mutation\n\nAfter a write you usually need to refresh data. Pick the right tool:\n\n```ts\n'use server';\nimport { revalidatePath, revalidateTag } from 'next/cache';\nimport { redirect } from 'next/navigation';\n\nexport async function createPost(formData: FormData) {\n  // mutate…\n  revalidatePath('/posts');   // invalidate by path\n  // revalidateTag('posts'); // invalidate by tag\n  redirect('/posts');         // navigate away\n}\n```\n\n- `revalidatePath` / `revalidateTag`: invalidate the cache so the next read is fresh.\n- `refresh()` (from `next/cache`): refreshes the client router — does *not* revalidate tag caches.\n- `redirect()`: throws a framework-handled signal and navigates; nothing after runs.\n\n## Cookies\n\n```ts\n'use server';\nimport { cookies } from 'next/headers';\n\nexport async function signIn() {\n  const store = await cookies();\n  store.set('token', 'xxx');    // set\n  store.get('token')?.value;    // read\n  store.delete('token');         // delete\n}\n```\n\nAfter a Server Action sets or deletes a cookie, Next.js re-renders the current page and its layouts so the UI reflects the new value.\n","/books/nextjs/chapters/en_US/10-caching.md":"# Caching\n\nCaching stores the result of computations so later requests don't repeat the work. Next.js in the App Router uses the **Cache Components** model: declare cacheable work with `use cache`, and wrap runtime data in `<Suspense>`. The combined rendering strategy is **Partial Prerendering (PPR)**.\n\n## Enable Cache Components\n\nTurn it on in `next.config.ts`:\n\n```ts\nimport type { NextConfig } from 'next';\n\nconst nextConfig: NextConfig = {\n  cacheComponents: true,\n};\n\nexport default nextConfig;\n```\n\n## The `use cache` directive\n\n`use cache` caches the return value of async functions and components at two levels.\n\n### Data-level caching\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport async function getUsers() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM users');\n}\n```\n\nThe function's **arguments** and any closed-over values become part of the cache key, so different inputs produce separate cache entries.\n\n### UI-level caching\n\nYou can cache an entire component, page, or layout:\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport default async function Page() {\n  'use cache';\n  cacheLife('hours');\n\n  const users = await db.query('SELECT * FROM users');\n  return (\n    <ul>\n      {users.map(u => <li key={u.id}>{u.name}</li>)}\n    </ul>\n  );\n}\n```\n\n> Add `'use cache'` at the top of a file and every exported function is cached.\n\n## Streaming uncached data\n\nFor data that must be fresh on every request (APIs, DBs), **do not** add `'use cache'`. Wrap it in `<Suspense>`:\n\n```tsx\nimport { Suspense } from 'react';\n\nasync function LatestPosts() {\n  const data = await fetch('https://api.example.com/posts');\n  const posts = await data.json();\n  return <ul>{posts.map((p: any) => <li key={p.id}>{p.title}</li>)}</ul>;\n}\n\nexport default function Page() {\n  return (\n    <>\n      <h1>My Blog</h1>\n      <Suspense fallback={<p>Loading latest posts…</p>}>\n        <LatestPosts />\n      </Suspense>\n    </>\n  );\n}\n```\n\nThe fallback lands in the static shell; real content streams in at request time.\n\n## Runtime APIs\n\n`cookies`, `headers`, `searchParams`, and `params` (unless `generateStaticParams` provides a sample) require runtime information. Any component touching them **must be wrapped in `<Suspense>`**:\n\n```tsx\nimport { cookies } from 'next/headers';\nimport { Suspense } from 'react';\n\nasync function UserGreeting() {\n  const theme = (await cookies()).get('theme')?.value || 'light';\n  return <p>Your theme: {theme}</p>;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<p>Loading…</p>}>\n      <UserGreeting />\n    </Suspense>\n  );\n}\n```\n\n## Feeding runtime values to cached functions\n\nExtract a runtime value and pass it as an argument to a cached function:\n\n```tsx\nimport { cookies } from 'next/headers';\nimport { Suspense } from 'react';\n\nasync function CachedContent({ sessionId }: { sessionId: string }) {\n  'use cache';\n  return <div>{await fetchUserData(sessionId)}</div>;\n}\n\nasync function ProfileContent() {\n  const session = (await cookies()).get('session')?.value;\n  return <CachedContent sessionId={session!} />;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<div>Loading…</div>}>\n      <ProfileContent />\n    </Suspense>\n  );\n}\n```\n\n## Non-deterministic operations\n\n`Math.random()`, `Date.now()`, and `crypto.randomUUID()` return different values each time. To get unique values per request, call `connection()` to defer rendering to request time:\n\n```tsx\nimport { connection } from 'next/server';\nimport { Suspense } from 'react';\n\nasync function Unique() {\n  await connection();\n  return <p>Request ID: {crypto.randomUUID()}</p>;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<p>Loading…</p>}>\n      <Unique />\n    </Suspense>\n  );\n}\n```\n\n## Partial Prerendering in one picture\n\nWith Cache Components enabled, each route produces a **static shell** at build time:\n- `'use cache'` results are baked into the shell.\n- Anything under `<Suspense>` that touches runtime data streams in at request time.\n- Pure computations (sync I/O, module imports) are included automatically.\n\nThat's the default PPR behavior — the same route delivers fast TTFB *and* up-to-date personalized content.\n","/books/nextjs/chapters/en_US/11-revalidating.md":"# Revalidating\n\nCaching is great — *expiring* caches is what makes it safe. Next.js offers two strategies: **time-based** with `cacheLife`, and **on-demand** with `revalidateTag` / `updateTag` / `revalidatePath`. This chapter covers both.\n\n## Time-based: `cacheLife`\n\n`cacheLife` sets the cache lifetime inside a `use cache` scope:\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport async function getProducts() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM products');\n}\n```\n\nBuilt-in profiles:\n\n| profile   | stale | revalidate | expire       |\n|-----------|-------|------------|--------------|\n| `seconds` | 0     | 1s         | 60s          |\n| `minutes` | 5m    | 1m         | 1h           |\n| `hours`   | 5m    | 1h         | 1d           |\n| `days`    | 5m    | 1d         | 1w           |\n| `weeks`   | 5m    | 1w         | 30d          |\n| `max`     | 5m    | 30d        | ~indefinite  |\n\nOr pass a custom object:\n\n```tsx\ncacheLife({\n  stale: 3600,       // 1 hour before considered stale\n  revalidate: 7200,  // 2 hours before regenerated\n  expire: 86400,     // 1 day before fully expired\n});\n```\n\n## Tag-based: `cacheTag`\n\nTag cached work so you can invalidate it in batches later:\n\n```tsx\nimport { cacheTag } from 'next/cache';\n\nexport async function getProducts() {\n  'use cache';\n  cacheTag('products');\n  return db.query('SELECT * FROM products');\n}\n```\n\n## `revalidateTag`: background refresh (SWR)\n\nBest for \"a slight delay is OK\" — blogs, product catalogs. Stale-while-revalidate: serve the old version, regenerate in the background.\n\n```tsx\nimport { revalidateTag } from 'next/cache';\n\nexport async function updateUser() {\n  // mutate…\n  revalidateTag('user', 'max'); // 'max' = longest stale window\n}\n```\n\nCan be called from a Server Action or a Route Handler.\n\n## `updateTag`: immediate invalidation (read-your-own-writes)\n\nBest when a user must see their own change immediately:\n\n```tsx\nimport { updateTag } from 'next/cache';\nimport { redirect } from 'next/navigation';\n\nexport async function createPost(formData: FormData) {\n  const post = await db.post.create({\n    data: { title: formData.get('title') },\n  });\n\n  updateTag('posts');\n  redirect(`/posts/${post.id}`);\n}\n```\n\nOnly available inside **Server Actions**.\n\n### Side by side\n\n|                | `updateTag`          | `revalidateTag`                |\n|----------------|----------------------|--------------------------------|\n| Where          | Server Actions only  | Server Actions + Route Handlers |\n| Behavior       | Expires immediately  | Stale-while-revalidate          |\n| Typical use    | Read-your-own-writes | Background refresh              |\n\n## `revalidatePath`: coarse by URL\n\nInvalidate all cached work tied to a route when you're not sure which tags apply:\n\n```tsx\nimport { revalidatePath } from 'next/cache';\n\nexport async function updateUser() {\n  // mutate…\n  revalidatePath('/profile');\n}\n```\n\n> Prefer tag-based invalidation when possible — it's precise and avoids over-invalidating.\n\n## What should I cache?\n\nQuick heuristic:\n- Doesn't depend on runtime data (`cookies` / `headers` / `searchParams`) and can tolerate some staleness? → **Cache it.**\n- Must be fresh on every request? → Don't cache; use `<Suspense>` to stream.\n- CMS content? → Use tags + long `cacheLife`, and trigger `revalidateTag` when content is published.\n","/books/nextjs/chapters/en_US/12-error-handling.md":"# Error Handling\n\nErrors come in two flavours:\n- **Expected errors**: form validation, failed requests — return them as values; don't throw.\n- **Uncaught exceptions**: actual bugs — throw them so error boundaries can catch them.\n\n## Handling expected errors\n\n### Server Functions\n\nFor validation failures or non-2xx responses, return an error object:\n\n```ts\n'use server';\nexport async function createPost(prevState: any, formData: FormData) {\n  const res = await fetch('https://api.vercel.app/posts', {\n    method: 'POST',\n    body: JSON.stringify({\n      title: formData.get('title'),\n      content: formData.get('content'),\n    }),\n  });\n  if (!res.ok) return { message: 'Failed to create post' };\n  return { message: '' };\n}\n```\n\nConsume it with `useActionState` on the client:\n\n```tsx\n'use client';\nimport { useActionState } from 'react';\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  const [state, formAction, pending] = useActionState(createPost, { message: '' });\n  return (\n    <form action={formAction}>\n      <input name=\"title\" required />\n      <textarea name=\"content\" required />\n      {state?.message && <p aria-live=\"polite\">{state.message}</p>}\n      <button disabled={pending}>Submit</button>\n    </form>\n  );\n}\n```\n\n### In Server Components\n\nBranch or redirect based on the response:\n\n```tsx\nexport default async function Page() {\n  const res = await fetch('https://…');\n  if (!res.ok) return <p>Something went wrong</p>;\n  const data = await res.json();\n  return <Posts data={data} />;\n}\n```\n\n### 404: `notFound`\n\nCall `notFound()` inside a segment to render the nearest `not-found.tsx`:\n\n```tsx\nimport { notFound } from 'next/navigation';\nimport { getPostBySlug } from '@/lib/posts';\n\nexport default async function Page({\n  params,\n}: {\n  params: Promise<{ slug: string }>;\n}) {\n  const { slug } = await params;\n  const post = getPostBySlug(slug);\n  if (!post) notFound();\n  return <div>{post.title}</div>;\n}\n```\n\nPair with `app/blog/not-found.tsx`:\n\n```tsx\nexport default function NotFound() {\n  return <div>404 - Page not found</div>;\n}\n```\n\n## Handling uncaught exceptions\n\n### Nested error boundaries\n\nDrop an `error.tsx` inside any segment to create an error boundary for that subtree. **It must be a Client Component**:\n\n```tsx\n'use client';\nimport { useEffect } from 'react';\n\nexport default function ErrorPage({\n  error,\n  unstable_retry,\n}: {\n  error: Error & { digest?: string };\n  unstable_retry: () => void;\n}) {\n  useEffect(() => console.error(error), [error]);\n\n  return (\n    <div>\n      <h2>Something went wrong</h2>\n      <button onClick={() => unstable_retry()}>Try again</button>\n    </div>\n  );\n}\n```\n\nErrors bubble up to the closest `error.tsx`, so you can define them per subtree. `/dashboard/settings/error.tsx` handles the settings page; other pages fall back to a higher boundary.\n\n### Component-level: `unstable_catchError`\n\nFor even finer boundaries, wrap a component with `unstable_catchError`:\n\n```tsx\n'use client';\nimport { unstable_catchError as catchError, type ErrorInfo } from 'next/error';\n\nfunction Fallback(\n  props: { title: string },\n  { error, unstable_retry }: ErrorInfo\n) {\n  return (\n    <div>\n      <h2>{props.title}</h2>\n      <p>{error.message}</p>\n      <button onClick={() => unstable_retry()}>Try again</button>\n    </div>\n  );\n}\n\nexport default catchError(Fallback);\n```\n\n### Event handlers\n\nError boundaries only catch errors during rendering. For event handlers and async callbacks, `try/catch` yourself and store in state:\n\n```tsx\n'use client';\nimport { useState } from 'react';\n\nexport function Button() {\n  const [error, setError] = useState<any>(null);\n  function handleClick() {\n    try {\n      throw new Error('Oops');\n    } catch (e) {\n      setError(e);\n    }\n  }\n  if (error) return <p>Error: {error.message}</p>;\n  return <button onClick={handleClick}>Click me</button>;\n}\n```\n\nNote: errors inside `startTransition` **do** bubble up to the nearest error boundary.\n\n### Last-resort: `global-error.js`\n\nWhat if the root layout itself crashes? `app/global-error.tsx` replaces the root layout and must include its own `<html>` and `<body>`:\n\n```tsx\n'use client';\n\nexport default function GlobalError({\n  error,\n  unstable_retry,\n}: {\n  error: Error & { digest?: string };\n  unstable_retry: () => void;\n}) {\n  return (\n    <html>\n      <body>\n        <h2>Something went wrong</h2>\n        <button onClick={() => unstable_retry()}>Try again</button>\n      </body>\n    </html>\n  );\n}\n```\n","/books/nextjs/chapters/en_US/13-css-styling.md":"# CSS Styling\n\nNext.js supports a broad lineup of CSS options: **Tailwind CSS**, **CSS Modules**, **global CSS**, **external stylesheets**, **Sass**, and **CSS-in-JS**. This chapter focuses on the four most common choices.\n\n## Tailwind CSS (recommended)\n\n`create-next-app` already installs Tailwind for you. For a manual setup:\n\n```bash\nnpm install -D tailwindcss @tailwindcss/postcss\n```\n\nEnable the PostCSS plugin in `postcss.config.mjs`:\n\n```js\nexport default {\n  plugins: {\n    '@tailwindcss/postcss': {},\n  },\n};\n```\n\nImport Tailwind from your global stylesheet:\n\n```css\n/* app/globals.css */\n@import 'tailwindcss';\n```\n\nImport the stylesheet from the root layout:\n\n```tsx\nimport './globals.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\nNow compose utility classes directly in your components:\n\n```tsx\nexport default function Page() {\n  return (\n    <main className=\"flex min-h-screen flex-col items-center justify-between p-24\">\n      <h1 className=\"text-4xl font-bold\">Welcome to Next.js!</h1>\n    </main>\n  );\n}\n```\n\n## CSS Modules\n\nGreat for component-scoped styles that avoid class-name collisions. Name files `*.module.css`:\n\n```css\n/* blog.module.css */\n.blog {\n  padding: 24px;\n}\n```\n\n```tsx\nimport styles from './blog.module.css';\n\nexport default function Page() {\n  return <main className={styles.blog} />;\n}\n```\n\nNext.js generates unique class names so the same selector in different files won't clash.\n\n## Global CSS\n\nUse it for genuinely global styles (Tailwind's base layer, resets, etc.):\n\n```css\n/* app/global.css */\nbody {\n  padding: 20px 20px 60px;\n  max-width: 680px;\n  margin: 0 auto;\n}\n```\n\n```tsx\nimport './global.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n> Heads up: on navigation, global stylesheets aren't unmounted automatically, which can lead to subtle conflicts. Keep \"global\" for styles that really are global.\n\n## External stylesheets\n\nThird-party CSS can be imported anywhere inside `app/`:\n\n```tsx\nimport 'bootstrap/dist/css/bootstrap.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\">\n      <body className=\"container\">{children}</body>\n    </html>\n  );\n}\n```\n\n## Ordering and merging\n\nIn production, Next.js chunks and merges your CSS. Final order depends on the order of `import` statements:\n\n```tsx\nimport { BaseButton } from './base-button';  // imports base-button.module.css internally\nimport styles from './page.module.css';\n```\n\nHere the order is `base-button.module.css` → `page.module.css`.\n\nPractical rules of thumb:\n- Keep CSS imports in a single entry file per area.\n- Import Tailwind and global stylesheets at the root.\n- Use Tailwind for most styling, CSS Modules for component-specific needs, and global CSS sparingly.\n- Disable auto-sort lint rules like `sort-imports` so they don't shuffle CSS order.\n- Dev and build can differ; always verify with `next build`.\n\n## Development vs production\n\n- `next dev` hot-reloads CSS for instant feedback.\n- `next build` minifies and chunks CSS so each route loads the minimum set.\n- CSS still loads with JS disabled in production; dev needs JS for Fast Refresh.\n","/books/nextjs/chapters/en_US/14-images.md":"# Image Optimization\n\n`next/image` replaces the HTML `<img>` element and gives you:\n- **Size optimization**: correctly sized images for each device, modern formats like WebP.\n- **Visual stability**: prevents layout shift (CLS) while images load.\n- **Faster loads**: native lazy loading with optional blur-up placeholders.\n- **Asset flexibility**: on-demand resizing, even for remote images.\n\n## Basic usage\n\n```tsx\nimport Image from 'next/image';\n\nexport default function Page() {\n  return <Image src=\"/profile.png\" alt=\"Profile\" width={500} height={500} />;\n}\n```\n\n`src` can be a local path or a remote URL.\n\n## Local images\n\nDrop assets into `public/` at the project root and reference them as `/xxx.png`.\n\nThe best experience is a **static import** — `width`, `height`, and `blurDataURL` are inferred automatically:\n\n```tsx\nimport Image from 'next/image';\nimport ProfileImage from './profile.png';\n\nexport default function Page() {\n  return (\n    <Image\n      src={ProfileImage}\n      alt=\"Profile\"\n      // width / height / blurDataURL inferred\n      placeholder=\"blur\"\n    />\n  );\n}\n```\n\n### Filenames not known statically?\n\nFor things like blog cover images coming from data, use dynamic `import()` inside a Server Component:\n\n```tsx\nimport Image from 'next/image';\n\nasync function PostImage({ imageFilename, alt }: { imageFilename: string; alt: string }) {\n  const { default: image } = await import(`../content/blog/images/${imageFilename}`);\n  return <Image src={image} alt={alt} />;\n}\n```\n\nThe path must include a **static prefix** (`../content/blog/images/`). Next.js bundles every file under that prefix, and external input can't escape it.\n\n## Remote images\n\nFor remote URLs you must supply `width` / `height` (or use `fill`) — Next.js can't access them at build time:\n\n```tsx\n<Image\n  src=\"https://s3.amazonaws.com/my-bucket/profile.png\"\n  alt=\"Profile\"\n  width={500}\n  height={500}\n/>\n```\n\nWhitelist the host in `next.config.ts` to prevent abuse:\n\n```ts\nimport type { NextConfig } from 'next';\n\nconst config: NextConfig = {\n  images: {\n    remotePatterns: [\n      {\n        protocol: 'https',\n        hostname: 's3.amazonaws.com',\n        pathname: '/my-bucket/**',\n      },\n    ],\n  },\n};\n\nexport default config;\n```\n\nBe as specific as possible.\n\n## `fill` mode\n\nWhen you want the image to fill a sized, `position: relative` parent:\n\n```tsx\n<div style={{ position: 'relative', width: 320, height: 180 }}>\n  <Image src=\"/cover.jpg\" alt=\"Cover\" fill sizes=\"320px\" />\n</div>\n```\n\nAlways supply `sizes` so the browser can pick the best responsive source.\n\n## Performance tips\n\n- Add `priority` to the key above-the-fold image so it's loaded eagerly.\n- For thumbnails in lists, provide accurate `sizes` for proper source selection.\n- Static imports give you a free blur placeholder — noticeable UX boost.\n- Don't use `<Image>` for decorative icons; plain `<img>` or inline SVG is lighter.\n","/books/nextjs/chapters/en_US/15-fonts.md":"# Font Optimization\n\n`next/font` automatically optimizes font loading:\n- **Self-hosts** every font file, so the browser never hits Google.\n- Eliminates layout shift (CLS) from font swaps.\n- Scopes fonts to the component they're used in.\n\n## Google Fonts\n\nImport a font from `next/font/google` and call it as a function:\n\n```tsx\nimport { Geist } from 'next/font/google';\n\nconst geist = Geist({ subsets: ['latin'] });\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\" className={geist.className}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n- **Prefer variable fonts** — flexible and reasonably sized.\n- For non-variable fonts, specify a `weight`:\n\n```tsx\nimport { Roboto } from 'next/font/google';\n\nconst roboto = Roboto({ weight: '400', subsets: ['latin'] });\n```\n\n## Local fonts\n\nDrop `.woff2` files into the project (e.g. `app/fonts/` or `public/fonts/`) and use `next/font/local`:\n\n```tsx\nimport localFont from 'next/font/local';\n\nconst myFont = localFont({ src: './my-font.woff2' });\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\" className={myFont.className}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\nFor multiple weights/styles in one family, pass an array:\n\n```tsx\nconst roboto = localFont({\n  src: [\n    { path: './Roboto-Regular.woff2', weight: '400', style: 'normal' },\n    { path: './Roboto-Italic.woff2',  weight: '400', style: 'italic' },\n    { path: './Roboto-Bold.woff2',    weight: '700', style: 'normal' },\n    { path: './Roboto-BoldItalic.woff2', weight: '700', style: 'italic' },\n  ],\n});\n```\n\n## Scope\n\nFonts are scoped to where they're used. Apply one at the root layout for the whole app, or at `app/blog/layout.tsx` to style only the blog area. Different routes can use different fonts without interfering.\n\n## Pairing with Tailwind\n\nExpose the font as a CSS variable and map it to a Tailwind font family:\n\n```tsx\nimport { Geist } from 'next/font/google';\n\nconst geist = Geist({\n  subsets: ['latin'],\n  variable: '--font-geist',\n});\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html className={geist.variable}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n```css\n/* app/globals.css */\n@theme {\n  --font-sans: var(--font-geist);\n}\n```\n\nNow the `font-sans` utility resolves to Geist.\n","/books/nextjs/chapters/en_US/16-metadata.md":"# Metadata and OG Images\n\nThe Metadata API lets you define `<title>`, `<meta>`, and `<link>` tags per page for SEO and social sharing. Three paths:\n1. A **static `metadata` object**\n2. A **dynamic `generateMetadata` function**\n3. **File conventions** (`favicon.ico`, `opengraph-image.tsx`, `robots.txt`, `sitemap.xml`, …)\n\n> `metadata` and `generateMetadata` are supported **only in Server Components**.\n\n## Default tags\n\nEven without custom metadata, Next.js injects the essentials:\n\n```html\n<meta charset=\"utf-8\" />\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" />\n```\n\n## Static metadata\n\nExport a `Metadata` object from `layout.tsx` or `page.tsx`:\n\n```tsx\nimport type { Metadata } from 'next';\n\nexport const metadata: Metadata = {\n  title: 'My Blog',\n  description: 'Notes on front-end and product.',\n};\n\nexport default function Layout() {\n  /* … */\n}\n```\n\n## Dynamic metadata\n\nFetch data based on `params` and generate title/description:\n\n```tsx\nimport type { Metadata } from 'next';\n\ntype Props = {\n  params: Promise<{ slug: string }>;\n};\n\nexport async function generateMetadata({ params }: Props): Promise<Metadata> {\n  const { slug } = await params;\n  const post = await fetch(`https://api.vercel.app/blog/${slug}`).then(r => r.json());\n\n  return {\n    title: post.title,\n    description: post.description,\n  };\n}\n\nexport default function Page() {\n  /* … */\n}\n```\n\n### Avoiding duplicate requests\n\nThe page itself and `generateMetadata` often need the same data. Wrap the fetcher with React's `cache` so it runs once:\n\n```ts\nimport { cache } from 'react';\nimport { db } from '@/app/lib/db';\n\nexport const getPost = cache(async (slug: string) => {\n  return db.query.posts.findFirst({ where: eq(posts.slug, slug) });\n});\n```\n\n### Streaming metadata\n\nFor dynamically rendered pages Next.js injects metadata as a separate stream, so the UI isn't blocked. Crawlers like Twitterbot or Slackbot that need metadata in the initial `<head>` are detected and served a blocking response instead.\n\n## File conventions\n\nThese special files can be static assets or generated in code:\n\n- `favicon.ico`, `icon.jpg`, `apple-icon.jpg`\n- `opengraph-image.jpg`, `twitter-image.jpg`\n- `robots.txt`, `sitemap.xml`\n\n### Static favicon / OG\n\nPlace `favicon.ico` at the root of `app/`, and `opengraph-image.jpg` wherever you want a route-specific image. The closer-to-the-leaf image wins.\n\n### Dynamic OG images\n\nUse `ImageResponse` to generate 1200×630 PNGs with JSX + CSS — perfect for per-post covers:\n\n```tsx\n// app/blog/[slug]/opengraph-image.tsx\nimport { ImageResponse } from 'next/og';\nimport { getPost } from '@/app/lib/data';\n\nexport const size = { width: 1200, height: 630 };\nexport const contentType = 'image/png';\n\nexport default async function Image({ params }: { params: { slug: string } }) {\n  const post = await getPost(params.slug);\n  return new ImageResponse(\n    (\n      <div\n        style={{\n          fontSize: 128,\n          background: 'white',\n          width: '100%',\n          height: '100%',\n          display: 'flex',\n          alignItems: 'center',\n          justifyContent: 'center',\n        }}\n      >\n        {post.title}\n      </div>\n    )\n  );\n}\n```\n\nUnder the hood `ImageResponse` uses `satori + resvg`. Only flexbox and a subset of CSS are supported (no `grid`). Think of it as a programmable screenshot tool, not a full browser.\n","/books/nextjs/chapters/en_US/17-route-handlers.md":"# Route Handlers\n\n**Route Handlers** let you build API endpoints directly in the App Router using the standard `Request` / `Response` APIs. They replace the legacy `pages/api/*` — no need to mix the two.\n\n## Convention\n\nAdd a `route.ts` (or `.js`) anywhere under `app/` and export functions named after HTTP methods:\n\n```ts\n// app/api/hello/route.ts\nexport async function GET(request: Request) {\n  return Response.json({ hello: 'world' });\n}\n```\n\nSupported methods: `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `HEAD`, `OPTIONS`. Unimplemented methods return `405 Method Not Allowed`.\n\n> Constraint: `route.ts` and `page.tsx` can't coexist at the same segment.\n\n## `NextRequest` / `NextResponse`\n\nNext.js extends the standard primitives with convenience helpers:\n\n```ts\nimport { NextRequest, NextResponse } from 'next/server';\n\nexport async function GET(req: NextRequest) {\n  const q = req.nextUrl.searchParams.get('q') ?? '';\n  return NextResponse.json({ query: q });\n}\n```\n\n## Caching\n\nRoute Handlers are **not cached by default**. Opt in for `GET` by exporting:\n\n```ts\nexport const dynamic = 'force-static';\n\nexport async function GET() {\n  const res = await fetch('https://data.mongodb-api.com/…', {\n    headers: { 'API-Key': process.env.DATA_API_KEY! },\n  });\n  return Response.json({ data: await res.json() });\n}\n```\n\nOther HTTP methods (POST, etc.) aren't cached even if they share a file with a cached `GET`.\n\n### With Cache Components\n\nWhen `cacheComponents: true` is enabled, `GET` handlers follow the same rendering model as pages:\n- Prerendered at build time if they don't touch runtime APIs or non-deterministic operations.\n- Deferred to request time when they do (`headers()`, `cookies()`, `Math.random()`, request props…).\n- To cache expensive work (DB queries), extract it into a helper with `'use cache'`:\n\n```ts\nimport { cacheLife } from 'next/cache';\n\nexport async function GET() {\n  const products = await getProducts();\n  return Response.json(products);\n}\n\nasync function getProducts() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM products');\n}\n```\n\n> `'use cache'` can't sit directly in the handler body — always extract it.\n\n## Typing params in TypeScript\n\nUse the built-in `RouteContext` helper for strongly-typed dynamic segments:\n\n```ts\n// app/users/[id]/route.ts\nimport type { NextRequest } from 'next/server';\n\nexport async function GET(_req: NextRequest, ctx: RouteContext<'/users/[id]'>) {\n  const { id } = await ctx.params;\n  return Response.json({ id });\n}\n```\n\nTypes are generated during `next dev` / `next build`.\n\n## Common patterns\n\n### Forms or webhooks\n\n```ts\nexport async function POST(req: Request) {\n  const data = await req.json();\n  // process webhook / write to DB…\n  return new Response('ok');\n}\n```\n\n### Streaming responses\n\nReturn a `ReadableStream` directly:\n\n```ts\nexport async function GET() {\n  const stream = new ReadableStream({\n    async start(controller) {\n      controller.enqueue('hello ');\n      controller.enqueue('world');\n      controller.close();\n    },\n  });\n  return new Response(stream, { headers: { 'Content-Type': 'text/plain' } });\n}\n```\n\n### Special Route Handlers\n\n`sitemap.ts`, `opengraph-image.tsx`, `icon.tsx`, and the other metadata files are Route Handlers too — static by default, and become dynamic only if they use runtime APIs or explicit dynamic config.\n\n## Route Handler or Server Action?\n\n| Situation                                     | Pick                  |\n|-----------------------------------------------|------------------------|\n| Internal forms, UI-driven mutations           | **Server Action**      |\n| External integrations (webhooks, OAuth)       | **Route Handler**      |\n| APIs for mobile or third-party clients        | **Route Handler**      |\n| You need strict HTTP method semantics         | **Route Handler**      |\n\nRule of thumb: use Server Actions for your own UI, Route Handlers when you need a clean HTTP surface for the outside world.\n","/books/nextjs/chapters/en_US/18-deploying.md":"# Deploying\n\nNext.js can be deployed in several ways: managed platforms (Vercel, Netlify, Cloudflare…), a self-hosted Node.js server, Docker, static export, or through adapters that target other runtimes. This chapter surveys those paths so you can pick the right one for production.\n\n## Build output\n\n```bash\npnpm build   # next build\npnpm start   # next start\n```\n\n`next build` produces:\n- `.next/server/` — SSR code, Server Components, Route Handlers.\n- `.next/static/` — client JS, CSS, static assets (content-hashed; safe to cache forever).\n- `.next/standalone/` (optional) — the minimal Node app plus the `node_modules` it needs.\n\n`next start` launches the Node.js server in production mode.\n\n## 1. Hosted platforms (Vercel & co.)\n\nNext.js has its smoothest zero-config experience on **Vercel**. Connect the repo and the platform auto-detects:\n- Server Components / SSR / ISR\n- Partial Prerendering (PPR)\n- Image Optimization\n- Middleware / Edge Runtime\n- Cron Jobs, Skew Protection, etc.\n\nNetlify, Cloudflare Pages, AWS Amplify, and others provide similar capabilities through their respective **adapters**.\n\n## 2. Self-hosted Node.js server\n\nAny environment with **Node.js 20.9+** can run `next start` directly:\n\n```bash\nnode_modules/.bin/next start -p 3000\n```\n\nTypical deployment steps:\n1. In CI, run `pnpm install --prod=false && pnpm build`.\n2. Ship `.next/`, `public/`, `package.json`, `node_modules` (or `standalone`) to the server.\n3. Use PM2 / systemd / supervisor to keep the process alive.\n4. Put Nginx / Caddy in front for TLS, gzip, and static caching.\n\n### Standalone output\n\nEnable in `next.config.js`:\n\n```js\nmodule.exports = { output: 'standalone' };\n```\n\nCopy `.next/standalone` + `.next/static` + `public` and launch:\n\n```bash\nnode .next/standalone/server.js\n```\n\nThe resulting image is dramatically smaller — ideal for containers.\n\n## 3. Docker\n\nA multi-stage Dockerfile built around `output: 'standalone'`:\n\n```dockerfile\nFROM node:20-alpine AS deps\nWORKDIR /app\nCOPY package.json pnpm-lock.yaml ./\nRUN corepack enable && pnpm install --frozen-lockfile\n\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY --from=deps /app/node_modules ./node_modules\nCOPY . .\nRUN corepack enable && pnpm build\n\nFROM node:20-alpine AS runner\nWORKDIR /app\nENV NODE_ENV=production\nCOPY --from=builder /app/public ./public\nCOPY --from=builder /app/.next/standalone ./\nCOPY --from=builder /app/.next/static ./.next/static\nEXPOSE 3000\nCMD [\"node\", \"server.js\"]\n```\n\n## 4. Static export\n\nIf the site is fully static (no Server Actions, no SSR), export HTML files:\n\n```js\n// next.config.js\nmodule.exports = { output: 'export' };\n```\n\nArtifacts land in `out/` and can be served by any static host (Nginx, CDN, GitHub Pages). Caveats:\n- No Server Actions, no dynamic Route Handler APIs, no default `next/image` optimization.\n- Use `next/image`'s `unoptimized` mode or a third-party loader instead.\n\n## 5. Adapters: other runtimes\n\nAdapters make Cloudflare Workers, Netlify Edge, Deno, and more possible:\n- `@opennextjs/cloudflare`\n- `@netlify/next`\n- `@vercel/next` (first-party)\n\nWire up the adapter in `next.config.js`, then follow the platform's docs. The adapter transforms the build output into something the target runtime can execute.\n\n## Production checklist\n\n1. **Environment variables** — inject secrets via the platform. Use `.env.local` locally; never commit it.\n2. **Caching strategy** — `.next/static/` is immutable; tune page responses with `cacheLife` / `revalidateTag`.\n3. **Image hardening** — use multi-stage builds and run the `runner` stage as a non-root user.\n4. **Observability** — enable `instrumentation.ts` and wire up OpenTelemetry / Sentry / Datadog.\n5. **Skew protection** — long-lived users should still be able to fetch their original chunk versions. Vercel enables this automatically; self-host with `deploymentId`.\n\n## Compared to Nuxt\n\n- Nuxt uses Nitro, which produces presets for Node, Cloudflare, Lambda, etc. out of the box.\n- Next.js adapters aim in the same direction as Nitro presets but require additional adapter packages.\n\nBefore picking a deployment target, confirm:\n- Whether you need SSR / Server Actions;\n- Whether you need fine-grained caching or edge capabilities;\n- How much operations overhead you're willing to absorb (managed vs self-hosted).\n\nLine those up, then pick the path above that fits.\n","/books/nextjs/chapters/zh_Hans_CN/01-introduction.md":"# 初识 Next.js\n\nNext.js 是由 Vercel 开发的 React 全栈框架，提供了服务端渲染、静态生成、API 路由等开箱即用的功能。\n\n## 创建项目\n\n```bash\nnpx create-next-app@latest my-next-app\ncd my-next-app\nnpm run dev\n```\n\n## 项目结构（App Router）\n\nNext.js 13+ 推荐使用 App Router：\n\n```\nmy-next-app/\n├── app/\n│   ├── layout.tsx       # 根布局\n│   ├── page.tsx         # 首页\n│   ├── globals.css      # 全局样式\n│   └── blog/\n│       └── page.tsx     # /blog 页面\n├── public/              # 静态资源\n├── next.config.js       # 配置文件\n└── package.json\n```\n\n## 第一个页面\n\n```tsx\n// app/page.tsx\nexport default function Home() {\n  return (\n    <main>\n      <h1>Hello Next.js!</h1>\n      <p>这是我的第一个 Next.js 页面</p>\n    </main>\n  )\n}\n```\n\n## 与 Nuxt.js 的对比\n\n| 特性 | Next.js | Nuxt.js |\n|------|---------|---------|\n| 基础框架 | React | Vue |\n| 路由方式 | 文件系统路由 | 文件系统路由 |\n| 渲染模式 | SSR/SSG/ISR | SSR/SSG/SPA |\n| 部署 | Vercel / 自建 | 任意 Node 环境 |\n| 状态管理 | Zustand / Redux | Pinia |\n","/books/nextjs/chapters/zh_Hans_CN/02-installation.md":'# 安装与创建项目\n\n本章带你从零搭建一个 Next.js 应用，了解系统要求、CLI 工具、手动安装流程以及 TypeScript、ESLint、路径别名等基础配置。\n\n## 系统要求\n\n- Node.js **20.9** 及以上。\n- macOS、Windows（含 WSL）或 Linux。\n- 浏览器：Chrome 111+、Edge 111+、Firefox 111+、Safari 16.4+。\n\n## 通过 CLI 快速创建\n\n推荐使用官方脚手架 `create-next-app`，一键生成带最佳实践的项目：\n\n```bash\npnpm create next-app@latest my-app --yes\ncd my-app\npnpm dev\n```\n\n`--yes` 会跳过提示，使用默认配置：**TypeScript + Tailwind CSS + ESLint + App Router + Turbopack**，并自动设置 `@/*` 路径别名。\n\n去掉 `--yes` 则会出现交互提示：\n\n```txt\nWould you like to use TypeScript? Yes\nWhich linter would you like to use? ESLint / Biome / None\nWould you like to use React Compiler? No\nWould you like to use Tailwind CSS? Yes\nWould you like your code inside a `src/` directory? No\nWould you like to use App Router? Yes\nWould you like to customize the import alias? No\n```\n\n## 手动安装\n\n如果你想对项目有完全掌控，也可以手动初始化：\n\n```bash\nnpm i next@latest react@latest react-dom@latest\n```\n\n然后在 `package.json` 写入 scripts：\n\n```json\n{\n  "scripts": {\n    "dev": "next dev",\n    "build": "next build",\n    "start": "next start",\n    "lint": "eslint"\n  }\n}\n```\n\n> Turbopack 已成为默认打包器。如需使用 Webpack 可运行 `next dev --webpack`。\n\n## 创建 `app` 目录\n\nNext.js 使用文件系统路由。在根目录新建 `app/`，并创建 **根布局**（必须包含 `html` 与 `body`）：\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return (\n    <html lang="zh-CN">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n再创建首页：\n\n```tsx\n// app/page.tsx\nexport default function Page() {\n  return <h1>Hello, Next.js!</h1>;\n}\n```\n\n启动 `npm run dev`，访问 `http://localhost:3000` 即可看到效果。\n\n## 静态资源目录\n\n在项目根新建 `public/` 用于存放图片、字体等静态资源，文件可直接以 `/` 根路径访问：\n\n```tsx\nimport Image from \'next/image\';\n\n<Image src="/profile.png" alt="Profile" width={100} height={100} />;\n```\n\n## TypeScript 与路径别名\n\nNext.js 原生支持 TypeScript，只需将文件改为 `.ts` / `.tsx`，再运行 `next dev`，它会自动写入 `tsconfig.json`。\n\n在 `tsconfig.json` 或 `jsconfig.json` 中可配置绝对引用，避免冗长的相对路径：\n\n```json\n{\n  "compilerOptions": {\n    "baseUrl": ".",\n    "paths": {\n      "@/components/*": ["components/*"],\n      "@/styles/*": ["styles/*"]\n    }\n  }\n}\n```\n\n从此就可以写 `import { Button } from \'@/components/button\'`，而不是 `../../../components/button`。\n',"/books/nextjs/chapters/zh_Hans_CN/03-app-router.md":'# App Router\n\nNext.js 13 引入了全新的 App Router，基于 React Server Components 构建，提供了更强大的路由和布局能力。\n\n## 路由约定\n\n在 `app/` 目录下，文件夹结构即路由结构：\n\n```\napp/\n├── page.tsx            → /\n├── about/\n│   └── page.tsx        → /about\n├── blog/\n│   ├── page.tsx        → /blog\n│   └── [slug]/\n│       └── page.tsx    → /blog/:slug\n```\n\n每个路由段可包含以下特殊文件：\n\n- `page.tsx` — 页面 UI\n- `layout.tsx` — 共享布局\n- `loading.tsx` — 加载状态\n- `error.tsx` — 错误处理\n- `not-found.tsx` — 404 页面\n\n## Layout（布局）\n\n布局是在多个页面之间共享的 UI：\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode\n}) {\n  return (\n    <html lang="zh">\n      <body>\n        <nav>导航栏</nav>\n        {children}\n        <footer>页脚</footer>\n      </body>\n    </html>\n  )\n}\n```\n\n## 动态路由\n\n使用方括号 `[param]` 创建动态段：\n\n```tsx\n// app/blog/[slug]/page.tsx\nexport default function BlogPost({\n  params,\n}: {\n  params: { slug: string }\n}) {\n  return <h1>文章：{params.slug}</h1>\n}\n```\n\n## 路由组\n\n用圆括号 `(group)` 组织路由，不影响 URL：\n\n```\napp/\n├── (marketing)/\n│   ├── about/page.tsx      → /about\n│   └── contact/page.tsx    → /contact\n├── (dashboard)/\n│   ├── settings/page.tsx   → /settings\n│   └── profile/page.tsx    → /profile\n```\n',"/books/nextjs/chapters/zh_Hans_CN/04-layouts-and-pages.md":'# 布局与页面\n\nNext.js 基于文件系统路由：文件夹决定 URL 段，`page.tsx` 和 `layout.tsx` 决定每段对应的 UI。本章讲清楚二者如何协作，以及动态段和 `searchParams` 的用法。\n\n## 创建页面\n\n在 `app` 下放一个 `page.tsx` 并默认导出组件，这个路由就变得公开可访问：\n\n```tsx\n// app/page.tsx → /\nexport default function Page() {\n  return <h1>Hello Next.js!</h1>;\n}\n```\n\n## 创建布局\n\n布局是多个页面共享的 UI。在客户端导航时布局会保留状态、保持交互性，且不会重新渲染。\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return (\n    <html lang="zh-CN">\n      <body>\n        <main>{children}</main>\n      </body>\n    </html>\n  );\n}\n```\n\n位于 `app/` 根部的叫 **根布局**，是必须的，而且只有它能写 `<html>` 和 `<body>`。\n\n## 嵌套路由与嵌套布局\n\n嵌套文件夹 = 嵌套 URL 段。例如 `/blog/[slug]` 对应：\n\n```\napp/\n├── layout.tsx         # 根布局\n├── blog/\n│   ├── layout.tsx     # 博客区布局（可选）\n│   ├── page.tsx       # /blog\n│   └── [slug]/\n│       └── page.tsx   # /blog/hello-world\n```\n\n布局会自动嵌套包裹：根布局 → 博客布局 → 博客页。这样可以在各层按需复用导航、侧栏、面包屑等结构。\n\n```tsx\n// app/blog/layout.tsx\nexport default function BlogLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return <section className="blog">{children}</section>;\n}\n```\n\n## 动态段\n\n用方括号创建动态段：`[slug]`、`[id]`。Next.js 15+ 的 `params` 是一个 Promise，需要 `await`：\n\n```tsx\n// app/blog/[slug]/page.tsx\nexport default async function Post({\n  params,\n}: {\n  params: Promise<{ slug: string }>;\n}) {\n  const { slug } = await params;\n  const post = await getPost(slug);\n\n  return (\n    <article>\n      <h1>{post.title}</h1>\n      <p>{post.content}</p>\n    </article>\n  );\n}\n```\n\n配合 `generateStaticParams` 可以在构建时预生成所有动态路由，获得纯静态站点的性能。\n\n## searchParams\n\n在服务端组件页面中通过 `searchParams` 读取查询参数：\n\n```tsx\nexport default async function Page({\n  searchParams,\n}: {\n  searchParams: Promise<{ [k: string]: string | string[] | undefined }>;\n}) {\n  const { q } = await searchParams;\n  return <p>搜索词：{q}</p>;\n}\n```\n\n> 注意：使用 `searchParams` 会让页面自动变成 **动态渲染**，因为它依赖进来的请求。\n\n客户端组件可以用 `useSearchParams` 这个 hook。\n\n## 页面间导航\n\n使用 `next/link` 实现客户端导航（带自动预加载）：\n\n```tsx\nimport Link from \'next/link\';\n\nexport default async function Posts() {\n  const posts = await getPosts();\n  return (\n    <ul>\n      {posts.map(p => (\n        <li key={p.slug}>\n          <Link href={`/blog/${p.slug}`}>{p.title}</Link>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```\n\n下一章我们会深入讲解 `Link` 的预取、流式渲染与客户端过渡。\n',"/books/nextjs/chapters/zh_Hans_CN/05-linking-and-navigating.md":"# 链接与导航\n\nNext.js 的路由默认在服务端渲染，为了让用户在路由切换时也感觉流畅，框架内置了 **预取**、**流式传输** 和 **客户端过渡** 三大优化。本章讲透它们是如何协作的，以及怎样为慢网络和动态路由做优化。\n\n## 导航的四层原理\n\n1. **Server Rendering**：布局和页面默认是 RSC（React Server Components），服务端生成 RSC 载荷后发给客户端。\n2. **Prefetching**：当链接进入视口时，Next.js 自动在后台预取目标路由。\n3. **Streaming**：允许服务端边渲染边发送，配合 `loading.tsx` 立即显示加载骨架。\n4. **Client-side Transitions**：`<Link>` 使用客户端过渡替换页面内容，保留共享布局、滚动状态与交互。\n\n## 用 `<Link>` 替代 `<a>`\n\n```tsx\nimport Link from 'next/link';\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <html>\n      <body>\n        <nav>\n          <Link href=\"/blog\">博客</Link>        {/* 自动预取 */}\n          <a href=\"/contact\">联系我们</a>        {/* 不会预取 */}\n        </nav>\n        {children}\n      </body>\n    </html>\n  );\n}\n```\n\n- **静态路由**：整条路由都会被预取。\n- **动态路由**：默认不预取；若存在 `loading.tsx`，则会部分预取（共享布局 + 骨架）。\n\n## 用 `loading.tsx` 开启流式渲染\n\n在路由文件夹里放 `loading.tsx`，Next.js 会自动把 `page.tsx` 包在 `<Suspense>` 里：\n\n```tsx\n// app/blog/loading.tsx\nexport default function Loading() {\n  return <BlogListSkeleton />;\n}\n```\n\n效果：用户点击链接后立即看到骨架屏，真实内容准备好再替换进来。对 **Core Web Vitals**（TTFB / FCP / TTI）都有明显提升。\n\n## 动态段建议加 `generateStaticParams`\n\n若动态段本可以在构建期预渲染，却没有 `generateStaticParams`，每次访问都会退化为请求时渲染：\n\n```tsx\nexport async function generateStaticParams() {\n  const posts = await fetch('https://.../posts').then(r => r.json());\n  return posts.map((p: any) => ({ slug: p.slug }));\n}\n```\n\n## 慢网络下的体验优化\n\n网络不稳时预取可能没跑完，骨架也来不及显示。用 `useLinkStatus` 立刻给用户一个视觉反馈：\n\n```tsx\n'use client';\nimport { useLinkStatus } from 'next/link';\n\nexport default function LinkIndicator() {\n  const { pending } = useLinkStatus();\n  return <span aria-hidden className={pending ? 'is-pending' : ''} />;\n}\n```\n\n结合 CSS 延迟动画（比如 100ms 才显示）可以避免快速导航时的「闪屏」。\n\n## 按需关闭预取\n\n长列表（无限滚动表格等）预取会很浪费，可以直接关掉：\n\n```tsx\n<Link prefetch={false} href=\"/blog\">博客</Link>\n```\n\n或者只在鼠标悬停时预取，兼顾资源与体验：\n\n```tsx\n'use client';\nimport Link from 'next/link';\nimport { useState } from 'react';\n\nfunction HoverPrefetchLink({ href, children }: { href: string; children: React.ReactNode }) {\n  const [active, setActive] = useState(false);\n  return (\n    <Link\n      href={href}\n      prefetch={active ? null : false}\n      onMouseEnter={() => setActive(true)}\n    >\n      {children}\n    </Link>\n  );\n}\n```\n\n## 使用原生 History API\n\n不想触发 UI 重新渲染但又想同步 URL，可以直接用 `pushState`：\n\n```tsx\n'use client';\nimport { useSearchParams } from 'next/navigation';\n\nexport default function SortProducts() {\n  const searchParams = useSearchParams();\n  function update(order: string) {\n    const params = new URLSearchParams(searchParams.toString());\n    params.set('sort', order);\n    window.history.pushState(null, '', `?${params.toString()}`);\n  }\n  return (\n    <>\n      <button onClick={() => update('asc')}>升序</button>\n      <button onClick={() => update('desc')}>降序</button>\n    </>\n  );\n}\n```\n\n`pushState` / `replaceState` 都会与 `usePathname`、`useSearchParams` 同步。\n","/books/nextjs/chapters/zh_Hans_CN/06-server-and-client-components.md":"# 服务端与客户端组件\n\nApp Router 里，布局和页面默认都是 **服务端组件（Server Components）**。它们在服务器上运行，可以直接访问数据库、使用密钥，并把极少量 JavaScript 发到浏览器。需要交互时，你再加入 **客户端组件（Client Components）**。\n\n## 何时选哪一种？\n\n**选服务端组件**：\n- 靠近数据源获取数据（数据库、API）。\n- 使用 API Key、Token 等不能暴露给客户端的机密。\n- 减少发到浏览器的 JS，优化 FCP。\n- 渐进式流式输出。\n\n**选客户端组件**：\n- 需要 `useState`、`useEffect` 等 hooks。\n- 需要浏览器 API（`localStorage`、`window`、`Navigator.geolocation`）。\n- 监听 `onClick`、`onChange` 等事件。\n\n## 在 Next.js 里，它们如何运作？\n\n**服务器侧**：\n- 服务端组件被渲染为一种特殊的数据格式：**RSC Payload**。\n- 它与客户端组件的占位符一起，在服务端拼出首屏 HTML。\n\n**客户端首次加载**：\n1. HTML 立刻呈现一个非交互预览。\n2. RSC Payload 协调服务端/客户端组件树。\n3. JavaScript 加载并 **水合** 客户端组件，让页面变得可交互。\n\n**后续导航**：\n- RSC Payload 被预取并缓存，切换瞬间完成。\n- 客户端组件完全在浏览器里渲染。\n\n## 写一个客户端组件\n\n在文件顶部加 `'use client'` 指令：\n\n```tsx\n'use client';\n\nimport { useState } from 'react';\n\nexport default function Counter() {\n  const [n, setN] = useState(0);\n  return (\n    <div>\n      <p>{n} 次点击</p>\n      <button onClick={() => setN(n + 1)}>点我</button>\n    </div>\n  );\n}\n```\n\n`'use client'` 声明了一道分界线：从该文件开始，它所有的 import 都属于客户端 bundle，无需每个文件都重复写指令。\n\n## 减小 JS 体积的诀窍\n\n**把 `'use client'` 放在尽可能小的叶子组件上**，而不是整块页面。例如：\n\n```tsx\n// 布局：服务端组件\nimport Search from './search';   // 客户端组件\nimport Logo from './logo';       // 服务端组件\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      <nav>\n        <Logo />\n        <Search />\n      </nav>\n      <main>{children}</main>\n    </>\n  );\n}\n```\n\n整个布局依然是服务端组件，只有 `Search` 是客户端 JS。\n\n## 服务端传递数据到客户端\n\n直接用 props 即可（注意 props 必须可序列化）：\n\n```tsx\n// 服务端组件\nimport LikeButton from '@/app/ui/like-button';\nimport { getPost } from '@/lib/data';\n\nexport default async function Page({\n  params,\n}: {\n  params: Promise<{ id: string }>;\n}) {\n  const { id } = await params;\n  const post = await getPost(id);\n  return <LikeButton likes={post.likes} />;\n}\n```\n\n```tsx\n// 客户端组件\n'use client';\nexport default function LikeButton({ likes }: { likes: number }) {\n  // ...\n}\n```\n\n## 在客户端组件里嵌套服务端组件\n\n客户端组件不能直接 `import` 服务端组件，但可以通过 **children 插槽** 把它传进来：\n\n```tsx\n// 客户端组件：Modal\n'use client';\nexport default function Modal({ children }: { children: React.ReactNode }) {\n  return <div className=\"modal\">{children}</div>;\n}\n```\n\n```tsx\n// 服务端组件：Page\nimport Modal from './ui/modal';\nimport Cart from './ui/cart'; // 服务端组件\n\nexport default function Page() {\n  return (\n    <Modal>\n      <Cart />\n    </Modal>\n  );\n}\n```\n\n这种「客户端外壳 + 服务端内容」的组合非常常见。\n\n## Context Provider\n\nReact Context 不被服务端组件支持。把 Provider 封装成客户端组件，在根布局里使用：\n\n```tsx\n'use client';\nimport { createContext } from 'react';\n\nexport const ThemeContext = createContext({});\n\nexport default function ThemeProvider({ children }: { children: React.ReactNode }) {\n  return <ThemeContext.Provider value=\"dark\">{children}</ThemeContext.Provider>;\n}\n```\n\n## 防止「环境污染」\n\n服务器代码可能意外 import 到客户端。用 `server-only` 包在构建期抛出错误：\n\n```ts\nimport 'server-only';\n\nexport async function getData() {\n  const res = await fetch('https://external-service.com/data', {\n    headers: { authorization: process.env.API_KEY },\n  });\n  return res.json();\n}\n```\n\n对应地也有 `client-only` 用于标记浏览器专属的模块。\n","/books/nextjs/chapters/zh_Hans_CN/07-rendering.md":"# 渲染模式\n\nNext.js 支持多种渲染策略，可以针对不同页面选择最合适的方式。\n\n## Server Components（服务端组件）\n\nApp Router 中的组件默认为 Server Components：\n\n```tsx\n// 这是一个 Server Component（默认）\nasync function PostList() {\n  const posts = await fetch('https://api.example.com/posts')\n  const data = await posts.json()\n\n  return (\n    <ul>\n      {data.map(post => (\n        <li key={post.id}>{post.title}</li>\n      ))}\n    </ul>\n  )\n}\n```\n\n优势：\n- 直接在服务端获取数据，减少客户端 JavaScript\n- 可以直接访问数据库、文件系统等后端资源\n- 敏感信息（API 密钥等）不会泄露到客户端\n\n## Client Components（客户端组件）\n\n需要交互或浏览器 API 时使用 `'use client'` 指令：\n\n```tsx\n'use client'\n\nimport { useState } from 'react'\n\nexport default function Counter() {\n  const [count, setCount] = useState(0)\n\n  return (\n    <button onClick={() => setCount(count + 1)}>\n      计数：{count}\n    </button>\n  )\n}\n```\n\n## Static Generation（静态生成）\n\n在构建时生成 HTML：\n\n```tsx\n// 构建时生成所有文章路径\nexport async function generateStaticParams() {\n  const posts = await fetch('https://api.example.com/posts').then(r => r.json())\n\n  return posts.map(post => ({\n    slug: post.slug,\n  }))\n}\n```\n\n## ISR（增量静态再生）\n\n结合静态生成和服务端渲染的优势：\n\n```tsx\nasync function Page() {\n  const data = await fetch('https://api.example.com/data', {\n    next: { revalidate: 60 } // 每 60 秒重新验证\n  })\n\n  return <div>{/* ... */}</div>\n}\n```\n\n## 渲染模式选择指南\n\n| 场景 | 推荐模式 |\n|------|---------|\n| 静态内容（博客、文档） | SSG |\n| 动态内容 + SEO | SSR |\n| 不常变化的动态内容 | ISR |\n| 纯交互页面（Dashboard） | CSR |\n","/books/nextjs/chapters/zh_Hans_CN/08-fetching-data.md":"# 数据获取\n\n在 App Router 里，获取数据是最自然的事情：服务端组件直接 `await`，客户端组件用 `use`、SWR 或 React Query。本章覆盖两端的获取方式，以及用 Suspense 流式推送不确定耗时的部分。\n\n## 在服务端组件获取数据\n\n### 使用 `fetch`\n\n把组件写成 `async` 函数，直接 `await`：\n\n```tsx\nexport default async function Page() {\n  const res = await fetch('https://api.vercel.app/blog');\n  const posts = await res.json();\n  return (\n    <ul>\n      {posts.map((p: any) => (\n        <li key={p.id}>{p.title}</li>\n      ))}\n    </ul>\n  );\n}\n```\n\n要点：\n- React 会自动对同一组件树里相同的 `fetch` 做 **memoization**，你可以在真正需要数据的组件里取，不用层层传 props。\n- `fetch` 默认 **不缓存**。如需缓存，请搭配 `use cache` 指令（第 10 章）或在 `fetch` 上配合 `next: { revalidate }`。\n\n### 使用 ORM 或数据库客户端\n\n既然服务端组件只在服务端跑，你可以直接调用 ORM：\n\n```tsx\nimport { db, posts } from '@/lib/db';\n\nexport default async function Page() {\n  const all = await db.select().from(posts);\n  return (\n    <ul>\n      {all.map(p => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n> 仍需对每个查询做鉴权与授权，详见官方 *Data Security* 指南。\n\n## 流式渲染\n\n若某些数据较慢，让它们先流着，页面其余部分立刻可见。\n\n### `loading.js`\n\n在路由文件夹里放 `loading.tsx`，Next.js 会把整页包在 Suspense 边界里：\n\n```tsx\n// app/blog/loading.tsx\nexport default function Loading() {\n  return <div>加载中…</div>;\n}\n```\n\n### 用 `<Suspense>` 精细控制\n\n只让慢的片段走流式：\n\n```tsx\nimport { Suspense } from 'react';\nimport BlogList from '@/components/BlogList';\nimport BlogListSkeleton from '@/components/BlogListSkeleton';\n\nexport default function BlogPage() {\n  return (\n    <div>\n      <header>\n        <h1>欢迎来到博客</h1>\n      </header>\n      <main>\n        <Suspense fallback={<BlogListSkeleton />}>\n          <BlogList />\n        </Suspense>\n      </main>\n    </div>\n  );\n}\n```\n\n### 什么是「有意义的加载态」\n\n不要一句 \"Loading…\" 糊弄用户：骨架屏、占位卡片、封面图和标题都是更好的选择，能让用户感觉到应用在稳定响应。\n\n## 在客户端组件获取数据\n\n### React 的 `use` API：服务端→客户端流式\n\n先在服务端发起 Promise，把 Promise 传给客户端组件，客户端 `use()` 读取：\n\n```tsx\n// 服务端组件\nimport Posts from '@/components/Posts';\nimport { Suspense } from 'react';\n\nexport default function Page() {\n  const postsPromise = fetch('https://api.vercel.app/blog').then(r => r.json());\n  return (\n    <Suspense fallback={<p>Loading…</p>}>\n      <Posts postsPromise={postsPromise} />\n    </Suspense>\n  );\n}\n```\n\n```tsx\n'use client';\nimport { use } from 'react';\n\nexport default function Posts({ postsPromise }: { postsPromise: Promise<any[]> }) {\n  const posts = use(postsPromise);\n  return (\n    <ul>\n      {posts.map(p => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n### 社区库：SWR / React Query\n\n客户端的缓存、重试、分页等场景，用 SWR 或 TanStack Query 最顺手：\n\n```tsx\n'use client';\nimport useSWR from 'swr';\n\nconst fetcher = (url: string) => fetch(url).then(r => r.json());\n\nexport function Posts() {\n  const { data, error, isLoading } = useSWR('/api/posts', fetcher);\n  if (isLoading) return <p>加载中…</p>;\n  if (error) return <p>加载失败</p>;\n  return (\n    <ul>\n      {data.map((p: any) => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n## 与 Nuxt 对比\n\nNuxt 里你会用 `useFetch` / `useAsyncData`；Next.js 的「原生 `fetch`」其实更贴近标准。两者的共同点是：在服务端拿到的数据序列化到 HTML 中，客户端复用，首屏 0 往返。\n","/books/nextjs/chapters/zh_Hans_CN/09-mutating-data.md":"# 数据修改与 Server Actions\n\nNext.js 使用 React 的 **Server Functions** 来处理写入。带有 `use server` 指令的异步函数可以被客户端通过网络调用。当它和 `<form>` 或 `formAction` 搭配时，就是大家常说的 **Server Action**。\n\n## 创建 Server Function\n\n在文件级别放 `'use server'`，导出的函数都会变成 Server Function：\n\n```ts\n// app/actions.ts\n'use server';\n\nimport { auth } from '@/lib/auth';\n\nexport async function createPost(formData: FormData) {\n  const session = await auth();\n  if (!session?.user) throw new Error('Unauthorized');\n\n  const title = formData.get('title');\n  const content = formData.get('content');\n  // 写数据库…\n}\n```\n\n> 安全警告：Server Function 会暴露成一个 POST 端点，**必须在函数内自行做鉴权与授权**，不能只依赖 UI 层的按钮显示与否。\n\n也可以在函数顶部写 `'use server'`，仅标记单个函数：\n\n```tsx\nexport default function Page() {\n  async function createPost(formData: FormData) {\n    'use server';\n    // …\n  }\n  return <form action={createPost}>…</form>;\n}\n```\n\n## 在表单中调用\n\nReact 扩展了 `<form>` 的 `action` prop，可以直接传 Server Function：\n\n```tsx\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  return (\n    <form action={createPost}>\n      <input name=\"title\" />\n      <textarea name=\"content\" />\n      <button type=\"submit\">发布</button>\n    </form>\n  );\n}\n```\n\n- 即便 JS 还没加载，表单也能提交（渐进增强）。\n- 提交后，Next.js 会在同一次请求里返回更新后的 UI。\n\n## 在事件里调用\n\n客户端组件里用 `onClick` 等事件调用：\n\n```tsx\n'use client';\nimport { incrementLike } from './actions';\nimport { useState } from 'react';\n\nexport default function LikeButton({ initialLikes }: { initialLikes: number }) {\n  const [likes, setLikes] = useState(initialLikes);\n  return (\n    <button onClick={async () => setLikes(await incrementLike())}>\n      ❤ {likes}\n    </button>\n  );\n}\n```\n\n## 处理期望内的错误\n\n对于表单校验这类「可预期错误」，不要 `throw`，直接把错误当 **返回值** 返回：\n\n```ts\n'use server';\nexport async function createPost(prevState: any, formData: FormData) {\n  const res = await fetch('https://api.vercel.app/posts', {\n    method: 'POST',\n    body: JSON.stringify({\n      title: formData.get('title'),\n      content: formData.get('content'),\n    }),\n  });\n  if (!res.ok) return { message: '创建失败' };\n  return { message: '' };\n}\n```\n\n在客户端用 `useActionState` 管理：\n\n```tsx\n'use client';\nimport { useActionState } from 'react';\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  const [state, formAction, pending] = useActionState(createPost, { message: '' });\n  return (\n    <form action={formAction}>\n      <input name=\"title\" required />\n      <textarea name=\"content\" required />\n      {state?.message && <p aria-live=\"polite\">{state.message}</p>}\n      <button disabled={pending}>发布</button>\n    </form>\n  );\n}\n```\n\n## 写入后刷新 UI\n\n提交完通常需要刷新数据。按需选择：\n\n```ts\n'use server';\nimport { revalidatePath, revalidateTag } from 'next/cache';\nimport { redirect } from 'next/navigation';\n\nexport async function createPost(formData: FormData) {\n  // 写数据…\n  revalidatePath('/posts');   // 按路径失效\n  // revalidateTag('posts'); // 按 tag 失效\n  redirect('/posts');         // 重定向\n}\n```\n\n- `revalidatePath` / `revalidateTag`：让缓存失效，下次访问拉最新数据。\n- `refresh()`（来自 `next/cache`）：刷新客户端路由，但不会让 tag 缓存失效。\n- `redirect()`：跳转到新页面，后续代码不会执行。\n\n## 操作 Cookie\n\n```ts\n'use server';\nimport { cookies } from 'next/headers';\n\nexport async function signIn() {\n  const store = await cookies();\n  store.set('token', 'xxx');    // 设置\n  store.get('token')?.value;    // 读取\n  store.delete('token');         // 删除\n}\n```\n\nNext.js 在 Server Action 设置 cookie 后会自动重新渲染当前页与布局，让 UI 跟上。\n","/books/nextjs/chapters/zh_Hans_CN/10-caching.md":"# 缓存\n\n缓存是把「计算结果」存下来，下次直接复用。Next.js 在 App Router 上引入了 **Cache Components** 模型，用 `use cache` 指令显式声明可以缓存的部分，其它需要运行时数据的部分则用 `<Suspense>` 包起来。整体思路叫 **Partial Prerendering (PPR)**。\n\n## 启用 Cache Components\n\n在 `next.config.ts` 开启：\n\n```ts\nimport type { NextConfig } from 'next';\n\nconst nextConfig: NextConfig = {\n  cacheComponents: true,\n};\n\nexport default nextConfig;\n```\n\n## `use cache` 指令\n\n`use cache` 缓存异步函数或组件的返回值。可以放在两个粒度：\n\n### 数据级缓存\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport async function getUsers() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM users');\n}\n```\n\n函数的 **参数** 和闭包变量会自动成为缓存 key 的一部分，因此不同入参会产生不同的缓存条目。\n\n### UI 级缓存\n\n整个组件/页面都可以缓存：\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport default async function Page() {\n  'use cache';\n  cacheLife('hours');\n\n  const users = await db.query('SELECT * FROM users');\n  return (\n    <ul>\n      {users.map(u => <li key={u.id}>{u.name}</li>)}\n    </ul>\n  );\n}\n```\n\n> 如果在文件顶部写 `'use cache'`，该文件里所有导出都会被缓存。\n\n## 流式未缓存的数据\n\n对于每次请求都要最新的数据（比如调用 API 或数据库），**不要** 加 `'use cache'`，用 `<Suspense>` 包起来即可：\n\n```tsx\nimport { Suspense } from 'react';\n\nasync function LatestPosts() {\n  const data = await fetch('https://api.example.com/posts');\n  const posts = await data.json();\n  return <ul>{posts.map((p: any) => <li key={p.id}>{p.title}</li>)}</ul>;\n}\n\nexport default function Page() {\n  return (\n    <>\n      <h1>我的博客</h1>\n      <Suspense fallback={<p>加载最新文章…</p>}>\n        <LatestPosts />\n      </Suspense>\n    </>\n  );\n}\n```\n\nfallback 会进入静态 HTML 外壳，真实内容在请求时流式注入。\n\n## 运行时 API\n\n`cookies`、`headers`、`searchParams`、`params`（除非提供了 `generateStaticParams`）只能在请求时拿到。访问它们的组件 **必须包在 `<Suspense>` 里**：\n\n```tsx\nimport { cookies } from 'next/headers';\nimport { Suspense } from 'react';\n\nasync function UserGreeting() {\n  const theme = (await cookies()).get('theme')?.value || 'light';\n  return <p>你的主题：{theme}</p>;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<p>加载中…</p>}>\n      <UserGreeting />\n    </Suspense>\n  );\n}\n```\n\n## 把运行时值喂给缓存函数\n\n从运行时拿到值后，可以当参数传给 `use cache` 函数：\n\n```tsx\nimport { cookies } from 'next/headers';\nimport { Suspense } from 'react';\n\nasync function CachedContent({ sessionId }: { sessionId: string }) {\n  'use cache';\n  return <div>{await fetchUserData(sessionId)}</div>;\n}\n\nasync function ProfileContent() {\n  const session = (await cookies()).get('session')?.value;\n  return <CachedContent sessionId={session!} />;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<div>加载中…</div>}>\n      <ProfileContent />\n    </Suspense>\n  );\n}\n```\n\n## 非确定性操作\n\n`Math.random()`、`Date.now()`、`crypto.randomUUID()` 每次结果不同。想每次请求都给不同值？调 `connection()` 让渲染下沉到请求时：\n\n```tsx\nimport { connection } from 'next/server';\nimport { Suspense } from 'react';\n\nasync function Unique() {\n  await connection();\n  return <p>请求 ID: {crypto.randomUUID()}</p>;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<p>加载中…</p>}>\n      <Unique />\n    </Suspense>\n  );\n}\n```\n\n## Partial Prerendering 概览\n\nCache Components 开启后，每条路由在构建期会生成一个 **静态 shell**：\n- `'use cache'` 的结果进入 shell。\n- `<Suspense>` 里未缓存的部分在请求时流式填入。\n- 纯计算（同步 I/O、模块导入）被自动放入 shell。\n\n这就是默认的 PPR 渲染行为，同一条路由能同时做到「首字节超快 + 个性化最新」。\n","/books/nextjs/chapters/zh_Hans_CN/11-revalidating.md":"# 重新验证\n\n缓存爽快，但也要能「让它过期」。Next.js 提供两条路线：**基于时间** 的 `cacheLife`，以及 **按需** 的 `revalidateTag` / `updateTag` / `revalidatePath`。本章讲清楚它们的区别与使用场景。\n\n## 时间维度：`cacheLife`\n\n`cacheLife` 在 `use cache` 作用域里指定缓存寿命：\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport async function getProducts() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM products');\n}\n```\n\n内置档位表：\n\n| profile   | stale | revalidate | expire      |\n|-----------|-------|------------|-------------|\n| `seconds` | 0     | 1s         | 60s         |\n| `minutes` | 5m    | 1m         | 1h          |\n| `hours`   | 5m    | 1h         | 1d          |\n| `days`    | 5m    | 1d         | 1w          |\n| `weeks`   | 5m    | 1w         | 30d         |\n| `max`     | 5m    | 30d        | ~永久        |\n\n也可以传对象自定义：\n\n```tsx\ncacheLife({\n  stale: 3600,       // 1 小时后视为过时\n  revalidate: 7200,  // 2 小时后重新生成\n  expire: 86400,     // 1 天后彻底失效\n});\n```\n\n## Tag 维度：`cacheTag`\n\n给一份缓存打标签，之后按标签批量失效：\n\n```tsx\nimport { cacheTag } from 'next/cache';\n\nexport async function getProducts() {\n  'use cache';\n  cacheTag('products');\n  return db.query('SELECT * FROM products');\n}\n```\n\n## `revalidateTag`：后台刷新（SWR）\n\n适合「晚一点刷新也没问题」的场景（博客、商品目录）。stale-while-revalidate 语义：先把旧数据发出去，后台重新生成。\n\n```tsx\nimport { revalidateTag } from 'next/cache';\n\nexport async function updateUser() {\n  // 写数据…\n  revalidateTag('user', 'max'); // 'max' 允许最长的 stale 窗口\n}\n```\n\n可在 Server Action 或 Route Handler 中调用。\n\n## `updateTag`：立即失效（读自己写）\n\n适合用户「立刻看到自己刚写的东西」的场景：\n\n```tsx\nimport { updateTag } from 'next/cache';\nimport { redirect } from 'next/navigation';\n\nexport async function createPost(formData: FormData) {\n  const post = await db.post.create({\n    data: { title: formData.get('title') },\n  });\n\n  updateTag('posts');\n  redirect(`/posts/${post.id}`);\n}\n```\n\n只能在 **Server Action** 里用。\n\n### 二者对比\n\n|                | `updateTag`         | `revalidateTag`              |\n|----------------|---------------------|------------------------------|\n| 使用场所       | 仅 Server Action    | Server Action + Route Handler |\n| 失效行为       | 立即过期            | stale-while-revalidate        |\n| 典型场景       | 读自己写            | 后台刷新                      |\n\n## `revalidatePath`：按路径失效\n\n不确定要失效哪些 tag 时的粗粒度方案：\n\n```tsx\nimport { revalidatePath } from 'next/cache';\n\nexport async function updateUser() {\n  // 写数据…\n  revalidatePath('/profile');\n}\n```\n\n> 建议：优先使用 tag 失效，精准且不会「误伤」其他缓存。\n\n## 缓存什么？\n\n做一个简单的判断：\n- 不依赖运行时数据（`cookies` / `headers` / `searchParams`）且可以容忍一段时间旧？→ **缓存**。\n- 每次请求都必须最新？→ 不缓存，配 `<Suspense>` 流式输出。\n- CMS 内容？→ 用 tag + 长 `cacheLife`，发布后 `revalidateTag` 触发刷新。\n","/books/nextjs/chapters/zh_Hans_CN/12-error-handling.md":"# 错误处理\n\n错误分两类：\n- **可预期错误**：表单校验失败、请求失败 — 把它们当作「返回值」，不要 `throw`。\n- **未捕获异常**：真正的 bug — 抛出去，由错误边界兜底。\n\n## 处理可预期错误\n\n### Server Function\n\n校验失败、请求非 2xx，应该返回错误对象：\n\n```ts\n'use server';\nexport async function createPost(prevState: any, formData: FormData) {\n  const res = await fetch('https://api.vercel.app/posts', {\n    method: 'POST',\n    body: JSON.stringify({\n      title: formData.get('title'),\n      content: formData.get('content'),\n    }),\n  });\n  if (!res.ok) return { message: '创建失败' };\n  return { message: '' };\n}\n```\n\n客户端用 `useActionState` 读取：\n\n```tsx\n'use client';\nimport { useActionState } from 'react';\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  const [state, formAction, pending] = useActionState(createPost, { message: '' });\n  return (\n    <form action={formAction}>\n      <input name=\"title\" required />\n      <textarea name=\"content\" required />\n      {state?.message && <p aria-live=\"polite\">{state.message}</p>}\n      <button disabled={pending}>提交</button>\n    </form>\n  );\n}\n```\n\n### 在服务端组件中处理\n\n直接根据响应决定渲染分支或重定向：\n\n```tsx\nexport default async function Page() {\n  const res = await fetch('https://…');\n  if (!res.ok) return <p>出错了</p>;\n  const data = await res.json();\n  return <Posts data={data} />;\n}\n```\n\n### 404：`notFound`\n\n在路由段里调用 `notFound()` 就会渲染最近的 `not-found.tsx`：\n\n```tsx\nimport { notFound } from 'next/navigation';\nimport { getPostBySlug } from '@/lib/posts';\n\nexport default async function Page({\n  params,\n}: {\n  params: Promise<{ slug: string }>;\n}) {\n  const { slug } = await params;\n  const post = getPostBySlug(slug);\n  if (!post) notFound();\n  return <div>{post.title}</div>;\n}\n```\n\n配套的 `app/blog/not-found.tsx`：\n\n```tsx\nexport default function NotFound() {\n  return <div>404 - 找不到页面</div>;\n}\n```\n\n## 处理未捕获异常\n\n### 嵌套错误边界\n\n在任意路由段放 `error.tsx` 就会为该子树建立错误边界。**必须加 `'use client'`**：\n\n```tsx\n'use client';\nimport { useEffect } from 'react';\n\nexport default function ErrorPage({\n  error,\n  unstable_retry,\n}: {\n  error: Error & { digest?: string };\n  unstable_retry: () => void;\n}) {\n  useEffect(() => console.error(error), [error]);\n\n  return (\n    <div>\n      <h2>出错了</h2>\n      <button onClick={() => unstable_retry()}>重试</button>\n    </div>\n  );\n}\n```\n\n错误会冒泡到最近的 `error.tsx`，因此你可以分层定制。例如 `/dashboard/settings/error.tsx` 会兜底设置页，其它页面则走上层 `error.tsx`。\n\n### 更细粒度：`unstable_catchError`\n\n想把错误边界挂到组件级别，可以用 `unstable_catchError`：\n\n```tsx\n'use client';\nimport { unstable_catchError as catchError, type ErrorInfo } from 'next/error';\n\nfunction Fallback(\n  props: { title: string },\n  { error, unstable_retry }: ErrorInfo\n) {\n  return (\n    <div>\n      <h2>{props.title}</h2>\n      <p>{error.message}</p>\n      <button onClick={() => unstable_retry()}>重试</button>\n    </div>\n  );\n}\n\nexport default catchError(Fallback);\n```\n\n### 事件里的错误\n\n错误边界只捕获渲染期错误。事件处理函数、async 回调里的错误要自己 `try/catch` 并用 `useState` 存起来：\n\n```tsx\n'use client';\nimport { useState } from 'react';\n\nexport function Button() {\n  const [error, setError] = useState<any>(null);\n  function handleClick() {\n    try {\n      throw new Error('Oops');\n    } catch (e) {\n      setError(e);\n    }\n  }\n  if (error) return <p>发生错误：{error.message}</p>;\n  return <button onClick={handleClick}>点我</button>;\n}\n```\n\n注意：`startTransition` 里的错误会 **冒泡到最近的错误边界**。\n\n### 全局兜底：`global-error.js`\n\n根布局崩了怎么办？在 `app/global-error.tsx` 提供最外层兜底，它会替换根布局：\n\n```tsx\n'use client';\n\nexport default function GlobalError({\n  error,\n  unstable_retry,\n}: {\n  error: Error & { digest?: string };\n  unstable_retry: () => void;\n}) {\n  return (\n    <html>\n      <body>\n        <h2>出错了</h2>\n        <button onClick={() => unstable_retry()}>重试</button>\n      </body>\n    </html>\n  );\n}\n```\n\n注意 `global-error` 自己必须写 `<html>` 和 `<body>`。\n","/books/nextjs/chapters/zh_Hans_CN/13-css-styling.md":"# CSS 样式方案\n\nNext.js 支持一系列成熟的 CSS 方案：**Tailwind CSS**、**CSS Modules**、**全局 CSS**、**外部样式表**、**Sass**、**CSS-in-JS**。这一章重点讲前四种常用方式。\n\n## Tailwind CSS（推荐）\n\n`create-next-app` 默认就帮你装好了 Tailwind。手动安装也很简单：\n\n```bash\nnpm install -D tailwindcss @tailwindcss/postcss\n```\n\n在 `postcss.config.mjs` 启用：\n\n```js\nexport default {\n  plugins: {\n    '@tailwindcss/postcss': {},\n  },\n};\n```\n\n在全局 CSS 里导入：\n\n```css\n/* app/globals.css */\n@import 'tailwindcss';\n```\n\n在根布局里导入 CSS：\n\n```tsx\nimport './globals.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-CN\">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n接下来，随手写工具类：\n\n```tsx\nexport default function Page() {\n  return (\n    <main className=\"flex min-h-screen flex-col items-center justify-between p-24\">\n      <h1 className=\"text-4xl font-bold\">欢迎使用 Next.js!</h1>\n    </main>\n  );\n}\n```\n\n## CSS Modules\n\n适合组件级定制样式，避免全局类名冲突。文件命名为 `*.module.css`：\n\n```css\n/* blog.module.css */\n.blog {\n  padding: 24px;\n}\n```\n\n```tsx\nimport styles from './blog.module.css';\n\nexport default function Page() {\n  return <main className={styles.blog} />;\n}\n```\n\nNext.js 会生成唯一类名，跨文件同名也不会冲突。\n\n## 全局 CSS\n\n用在真正全局的样式上（比如 Tailwind 基础层、reset）：\n\n```css\n/* app/global.css */\nbody {\n  padding: 20px 20px 60px;\n  max-width: 680px;\n  margin: 0 auto;\n}\n```\n\n```tsx\nimport './global.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-CN\">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n> 提示：导航时，全局样式不会自动被卸载，可能彼此叠加造成冲突。建议只放「真正全局」的样式。\n\n## 外部样式表\n\n第三方包的 CSS 可以在 `app` 目录里任意位置 import：\n\n```tsx\nimport 'bootstrap/dist/css/bootstrap.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-CN\">\n      <body className=\"container\">{children}</body>\n    </html>\n  );\n}\n```\n\n## 排序与合并\n\n生产构建时 Next.js 会合并并代码分割 CSS。CSS 的最终顺序取决于你的 `import` 顺序。\n\n例如：\n\n```tsx\nimport { BaseButton } from './base-button';  // 内部 import base-button.module.css\nimport styles from './page.module.css';\n```\n\n顺序是 `base-button.module.css` → `page.module.css`。\n\n实践建议：\n- CSS `import` 集中在单一入口文件。\n- 全局样式、Tailwind 入口都放在应用根部。\n- 大多数样式用 Tailwind，组件私有样式用 CSS Modules，极少用全局 CSS。\n- 禁用 `sort-imports` 之类自动排序 import 的 lint 规则，以免影响 CSS 顺序。\n- 开发和构建下 CSS 顺序可能略有差异，务必通过 `next build` 验证。\n\n## 开发与生产\n\n- `next dev` 下 CSS 热更新，秒级反馈。\n- `next build` 把所有 CSS 压缩、拆包，路由只加载所需的最小集合。\n- 生产禁用 JS 仍能加载 CSS；开发下需要 JS 才能 Fast Refresh。\n","/books/nextjs/chapters/zh_Hans_CN/14-images.md":"# 图片优化\n\n`next/image` 是 Next.js 官方的 `<img>` 替代品，自动完成：\n- **尺寸优化**：按设备分发合适尺寸 + WebP。\n- **视觉稳定**：自动防止加载时的布局抖动（CLS）。\n- **更快首屏**：懒加载 + 可选 blur 占位图。\n- **资源灵活**：本地或远程图片都能按需缩放。\n\n## 基本用法\n\n```tsx\nimport Image from 'next/image';\n\nexport default function Page() {\n  return <Image src=\"/profile.png\" alt=\"头像\" width={500} height={500} />;\n}\n```\n\n`src` 可以是本地路径或远程 URL。\n\n## 本地图片\n\n把静态资源放在根目录的 `public/` 下，直接用 `/xxx.png` 引用。\n\n推荐方式：**静态 import**，这样 `width`、`height`、`blurDataURL` 都会自动填上：\n\n```tsx\nimport Image from 'next/image';\nimport ProfileImage from './profile.png';\n\nexport default function Page() {\n  return (\n    <Image\n      src={ProfileImage}\n      alt=\"头像\"\n      // width / height / blurDataURL 自动提供\n      placeholder=\"blur\"\n    />\n  );\n}\n```\n\n### 文件名不定？用动态 import\n\n博客封面之类路径来自数据的场景，可以在服务端组件里用 `import()`：\n\n```tsx\nimport Image from 'next/image';\n\nasync function PostImage({ imageFilename, alt }: { imageFilename: string; alt: string }) {\n  const { default: image } = await import(`../content/blog/images/${imageFilename}`);\n  return <Image src={image} alt={alt} />;\n}\n```\n\n路径里必须带一个 **静态前缀**（`../content/blog/images/`），Next.js 会把这个目录下所有文件打进产物，外部无法跳出该目录。\n\n## 远程图片\n\n远程 URL 要自己提供 `width` / `height`（或用 `fill`），因为构建期 Next.js 并不能访问远端图：\n\n```tsx\n<Image\n  src=\"https://s3.amazonaws.com/my-bucket/profile.png\"\n  alt=\"头像\"\n  width={500}\n  height={500}\n/>\n```\n\n同时需要在 `next.config.ts` 白名单中允许该域名，防止 URL 被滥用：\n\n```ts\nimport type { NextConfig } from 'next';\n\nconst config: NextConfig = {\n  images: {\n    remotePatterns: [\n      {\n        protocol: 'https',\n        hostname: 's3.amazonaws.com',\n        pathname: '/my-bucket/**',\n      },\n    ],\n  },\n};\n\nexport default config;\n```\n\n写得越具体越安全。\n\n## `fill` 模式\n\n当你希望图片撑满父容器（父容器要 `position: relative` 且有尺寸）：\n\n```tsx\n<div style={{ position: 'relative', width: 320, height: 180 }}>\n  <Image src=\"/cover.jpg\" alt=\"封面\" fill sizes=\"320px\" />\n</div>\n```\n\n`sizes` 帮 Next.js 选用最合适的响应式图源，务必提供。\n\n## 性能建议\n\n- **首屏的关键图像** 加 `priority`，让它提前加载。\n- **列表缩略图** 用合理的 `sizes`，浏览器才能选出最优尺寸。\n- 静态 import 能带来免费的 blur 占位，体验显著更好。\n- 不要对装饰性图标使用 `Image`，直接 `<img>` 或 SVG 更轻量。\n","/books/nextjs/chapters/zh_Hans_CN/15-fonts.md":"# 字体优化\n\n`next/font` 能自动优化字体加载：\n- 所有字体 **自托管** 成静态资源，浏览器不再向 Google 发请求。\n- 自动消除布局抖动（CLS）。\n- 字体只在用到的组件范围内生效，天然范围化。\n\n## Google Fonts\n\n从 `next/font/google` 里直接导入字体并作为函数调用即可：\n\n```tsx\nimport { Geist } from 'next/font/google';\n\nconst geist = Geist({ subsets: ['latin'] });\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-CN\" className={geist.className}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n- **推荐使用 variable font**，灵活且体积合理。\n- 普通字体则需要明确指定 `weight`：\n\n```tsx\nimport { Roboto } from 'next/font/google';\n\nconst roboto = Roboto({ weight: '400', subsets: ['latin'] });\n```\n\n## 本地字体\n\n把 `.woff2` 放到项目里（比如 `app/fonts/` 或 `public/fonts/`），用 `next/font/local`：\n\n```tsx\nimport localFont from 'next/font/local';\n\nconst myFont = localFont({ src: './my-font.woff2' });\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-CN\" className={myFont.className}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n一组多权重/斜体？用数组：\n\n```tsx\nconst roboto = localFont({\n  src: [\n    { path: './Roboto-Regular.woff2', weight: '400', style: 'normal' },\n    { path: './Roboto-Italic.woff2',  weight: '400', style: 'italic' },\n    { path: './Roboto-Bold.woff2',    weight: '700', style: 'normal' },\n    { path: './Roboto-BoldItalic.woff2', weight: '700', style: 'italic' },\n  ],\n});\n```\n\n## 作用域\n\n字体是「用在哪里就作用于哪里」。把它写在根布局可全站生效，写在 `app/blog/layout.tsx` 就只影响博客区。这样你可以在不同路由用不同字体，互不干扰。\n\n## 与 Tailwind 搭配\n\n给字体起一个 CSS 变量，在 Tailwind 配置里映射成字体家族：\n\n```tsx\nimport { Geist } from 'next/font/google';\n\nconst geist = Geist({\n  subsets: ['latin'],\n  variable: '--font-geist',\n});\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html className={geist.variable}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n```css\n/* app/globals.css */\n@theme {\n  --font-sans: var(--font-geist);\n}\n```\n\n这样 `font-sans` 工具类就会用上 Geist。\n","/books/nextjs/chapters/zh_Hans_CN/16-metadata.md":"# 元数据与 OG 图片\n\nMetadata API 帮你为每个页面定义 `<title>`、`<meta>`、`<link>`，进而优化 SEO 和社交分享。你有三条路径：\n1. **静态 `metadata` 对象**\n2. **动态 `generateMetadata` 函数**\n3. **基于文件的约定**（`favicon.ico`、`opengraph-image.tsx`、`robots.txt`、`sitemap.xml` 等）\n\n> `metadata` 与 `generateMetadata` 只在 **服务端组件** 中生效。\n\n## 默认标签\n\n即便你不写任何元数据，Next.js 也会自动注入：\n\n```html\n<meta charset=\"utf-8\" />\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" />\n```\n\n## 静态元数据\n\n在 `layout.tsx` 或 `page.tsx` 里导出一个 `Metadata` 对象：\n\n```tsx\nimport type { Metadata } from 'next';\n\nexport const metadata: Metadata = {\n  title: '我的博客',\n  description: '关于前端与产品的思考',\n};\n\nexport default function Layout() {\n  /* … */\n}\n```\n\n## 动态元数据\n\n根据 `params` 拉取数据再生成标题描述：\n\n```tsx\nimport type { Metadata } from 'next';\n\ntype Props = {\n  params: Promise<{ slug: string }>;\n};\n\nexport async function generateMetadata({ params }: Props): Promise<Metadata> {\n  const { slug } = await params;\n  const post = await fetch(`https://api.vercel.app/blog/${slug}`).then(r => r.json());\n\n  return {\n    title: post.title,\n    description: post.description,\n  };\n}\n\nexport default function Page() {\n  /* … */\n}\n```\n\n### 避免重复请求\n\n页面本身和 `generateMetadata` 常常要同一份数据。用 React 的 `cache` 包一层，保证只请求一次：\n\n```ts\nimport { cache } from 'react';\nimport { db } from '@/app/lib/db';\n\nexport const getPost = cache(async (slug: string) => {\n  return db.query.posts.findFirst({ where: eq(posts.slug, slug) });\n});\n```\n\n### 流式元数据\n\n对动态渲染的页面，Next.js 会把元数据 **单独流式** 注入 HTML，不阻塞视觉内容。Twitterbot、Slackbot 这类必须在 `<head>` 里拿到 meta 的爬虫会自动降级为「等全部生成完再返回」。\n\n## 文件约定\n\n这些特殊文件可以静态放置，也可以用代码动态生成：\n\n- `favicon.ico`、`icon.jpg`、`apple-icon.jpg`\n- `opengraph-image.jpg`、`twitter-image.jpg`\n- `robots.txt`、`sitemap.xml`\n\n### 静态 favicon / OG\n\n把 `favicon.ico` 放在 `app/` 根部，把 `opengraph-image.jpg` 放在需要定制的路由里。Next.js 会自动注入对应 `<meta>` 标签，且就近路由的 OG 优先级更高。\n\n### 动态 OG 图片\n\n用 `ImageResponse` 按 JSX + CSS 生成 1200×630 的 PNG，非常适合博客每篇文章一张独有封面：\n\n```tsx\n// app/blog/[slug]/opengraph-image.tsx\nimport { ImageResponse } from 'next/og';\nimport { getPost } from '@/app/lib/data';\n\nexport const size = { width: 1200, height: 630 };\nexport const contentType = 'image/png';\n\nexport default async function Image({ params }: { params: { slug: string } }) {\n  const post = await getPost(params.slug);\n  return new ImageResponse(\n    (\n      <div\n        style={{\n          fontSize: 128,\n          background: 'white',\n          width: '100%',\n          height: '100%',\n          display: 'flex',\n          alignItems: 'center',\n          justifyContent: 'center',\n        }}\n      >\n        {post.title}\n      </div>\n    )\n  );\n}\n```\n\n`ImageResponse` 底层使用 `satori + resvg`，只支持 flexbox 等子集，grid 不可用。把它当做「可程序化的截图器」而非完整浏览器。\n","/books/nextjs/chapters/zh_Hans_CN/17-route-handlers.md":"# Route Handlers\n\n**Route Handlers** 让你在 App Router 里直接基于 Web 标准的 `Request` / `Response` API 写接口。它们是 `pages/api/*` 的现代替代品，不需要额外的 API Routes。\n\n## 约定\n\n在 `app` 目录任意位置新建 `route.ts`（或 `.js`），导出对应 HTTP 方法的函数：\n\n```ts\n// app/api/hello/route.ts\nexport async function GET(request: Request) {\n  return Response.json({ hello: 'world' });\n}\n```\n\n支持的方法：`GET`、`POST`、`PUT`、`PATCH`、`DELETE`、`HEAD`、`OPTIONS`。未实现的方法会自动返回 `405 Method Not Allowed`。\n\n> 约束：同一个路由段下，`route.ts` 和 `page.tsx` 不能共存。\n\n## 使用 `NextRequest` / `NextResponse`\n\nNext.js 在原生 API 上扩展了更便捷的工具：\n\n```ts\nimport { NextRequest, NextResponse } from 'next/server';\n\nexport async function GET(req: NextRequest) {\n  const q = req.nextUrl.searchParams.get('q') ?? '';\n  return NextResponse.json({ query: q });\n}\n```\n\n## 缓存\n\nRoute Handlers **默认不缓存**。想让 `GET` 缓存？在文件里导出：\n\n```ts\nexport const dynamic = 'force-static';\n\nexport async function GET() {\n  const res = await fetch('https://data.mongodb-api.com/…', {\n    headers: { 'API-Key': process.env.DATA_API_KEY! },\n  });\n  return Response.json({ data: await res.json() });\n}\n```\n\n其它 HTTP 方法（POST 等）即使和 `GET` 在同一个文件里也不会被缓存。\n\n### 开启 Cache Components 后\n\n启用 `cacheComponents: true` 后，`GET` Route Handlers 与页面遵循同一套渲染模型：\n- 不访问运行时 API、没有非确定性操作时会在构建期预渲染。\n- 访问 `headers()` / `cookies()` / `Math.random()` / 请求对象属性时自动下沉到请求时。\n- 需要缓存数据库查询等耗时操作时，把它抽成带 `'use cache'` 的帮助函数。\n\n```ts\nimport { cacheLife } from 'next/cache';\n\nexport async function GET() {\n  const products = await getProducts();\n  return Response.json(products);\n}\n\nasync function getProducts() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM products');\n}\n```\n\n> `'use cache'` 不能直接放在 handler 函数体里，必须放到帮助函数。\n\n## 用 TypeScript 类型化参数\n\nNext.js 内置 `RouteContext` 帮你拿到强类型的动态段：\n\n```ts\n// app/users/[id]/route.ts\nimport type { NextRequest } from 'next/server';\n\nexport async function GET(_req: NextRequest, ctx: RouteContext<'/users/[id]'>) {\n  const { id } = await ctx.params;\n  return Response.json({ id });\n}\n```\n\n类型会在 `next dev` / `next build` 时自动生成。\n\n## 典型用法\n\n### 表单或 Webhook\n\n```ts\nexport async function POST(req: Request) {\n  const data = await req.json();\n  // 处理 webhook / 写库…\n  return new Response('ok');\n}\n```\n\n### 流式响应\n\n直接返回 `ReadableStream`：\n\n```ts\nexport async function GET() {\n  const stream = new ReadableStream({\n    async start(controller) {\n      controller.enqueue('hello ');\n      controller.enqueue('world');\n      controller.close();\n    },\n  });\n  return new Response(stream, { headers: { 'Content-Type': 'text/plain' } });\n}\n```\n\n### 特殊 Route Handlers\n\n`sitemap.ts`、`opengraph-image.tsx`、`icon.tsx` 这些特殊文件也都是 Route Handler，默认静态化，除非使用了运行时 API 或显式配置成动态。\n\n## 何时用 Route Handler、何时用 Server Action？\n\n| 场景                                   | 选哪个                     |\n|----------------------------------------|---------------------------|\n| 内部表单、UI 触发的写操作              | **Server Action**          |\n| 外部集成（webhook、第三方 OAuth 回调）  | **Route Handler**          |\n| 给移动端 / 第三方客户端提供 API        | **Route Handler**          |\n| 需要严格的 HTTP 方法语义               | **Route Handler**          |\n\n总体原则：面向自家 UI 用 Server Action，面向外部或需要纯 HTTP 语义时用 Route Handler。\n","/books/nextjs/chapters/zh_Hans_CN/18-deploying.md":"# 部署\n\nNext.js 可以用多种方式部署：托管平台（Vercel、Netlify、Cloudflare…）、自建 Node.js 服务器、Docker、静态导出，甚至通过 Adapter 对接其它运行时。本章概览这些路径，帮你在生产环境做出合适选择。\n\n## 构建产物\n\n```bash\npnpm build   # next build\npnpm start   # next start\n```\n\n`next build` 会输出：\n- `.next/server/` —— 服务端渲染代码、Server Components、Route Handlers。\n- `.next/static/` —— 客户端 JS、CSS、静态资源（带内容哈希，长缓存安全）。\n- `.next/standalone/`（可选） —— 只包含运行所需的最小 Node 代码 + `node_modules`。\n\n`next start` 以生产模式启动 Node.js 服务。\n\n## 一、托管到 Vercel 等平台\n\nNext.js 的 “zero-config” 部署体验最完整的就是 **Vercel**。你只需把仓库连上去，平台会自动识别：\n- Server Components / SSR / ISR\n- Partial Prerendering（PPR）\n- Image Optimization\n- Middleware / Edge Runtime\n- Cron Jobs、Skew Protection 等\n\nNetlify、Cloudflare Pages、AWS Amplify 等通过各自的 **Adapter** 也提供近似能力。\n\n## 二、自建 Node.js 服务器\n\n只要运行环境支持 **Node.js 20.9+**，就可以直接用 `next start`：\n\n```bash\nnode_modules/.bin/next start -p 3000\n```\n\n常见部署做法：\n1. CI 中执行 `pnpm install --prod=false && pnpm build`。\n2. 把 `.next/`、`public/`、`package.json`、`node_modules`（或 `standalone`）拷贝到服务器。\n3. 用 PM2 / systemd / supervisor 守护进程。\n4. 前面放 Nginx / Caddy 做 TLS、gzip、静态资源缓存。\n\n### Standalone 输出\n\n在 `next.config.js` 开启：\n\n```js\nmodule.exports = { output: 'standalone' };\n```\n\n构建后复制 `.next/standalone` + `.next/static` + `public`，启动：\n\n```bash\nnode .next/standalone/server.js\n```\n\n镜像体积会小非常多，非常适合容器化部署。\n\n## 三、Docker 部署\n\n基于 `output: 'standalone'` 的典型多阶段 Dockerfile：\n\n```dockerfile\nFROM node:20-alpine AS deps\nWORKDIR /app\nCOPY package.json pnpm-lock.yaml ./\nRUN corepack enable && pnpm install --frozen-lockfile\n\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY --from=deps /app/node_modules ./node_modules\nCOPY . .\nRUN corepack enable && pnpm build\n\nFROM node:20-alpine AS runner\nWORKDIR /app\nENV NODE_ENV=production\nCOPY --from=builder /app/public ./public\nCOPY --from=builder /app/.next/standalone ./\nCOPY --from=builder /app/.next/static ./.next/static\nEXPOSE 3000\nCMD [\"node\", \"server.js\"]\n```\n\n## 四、静态导出\n\n如果站点完全静态（无 Server Action、无 SSR），可以直接导出成静态文件：\n\n```js\n// next.config.js\nmodule.exports = { output: 'export' };\n```\n\n产物在 `out/`，可托管到任意静态服务器（Nginx、CDN、GitHub Pages）。注意：\n- 不能用 Server Actions、Route Handlers 的动态 API、`next/image` 默认优化等。\n- 可以用 `next/image` 的 `unoptimized` 模式或第三方 loader。\n\n## 五、Adapter：面向其它运行时\n\nNext.js 通过 Adapter 支持 Cloudflare Workers、Netlify Edge、Deno 等：\n- `@opennextjs/cloudflare`\n- `@netlify/next`\n- `@vercel/next`（官方）\n\n在 `next.config.js` 里配置对应 Adapter，再按平台文档部署即可。Adapter 会负责把构建产物转成运行时可执行的格式。\n\n## 生产环境的几个“要点”\n\n1. **设置环境变量**：数据库、第三方密钥通过平台环境变量注入；本地用 `.env.local`，不要提交。\n2. **缓存策略**：`.next/static/` 长缓存（immutable），页面响应结合 `cacheLife` / `revalidateTag` 管理。\n3. **镜像安全**：多阶段构建，`runner` 阶段用非 root 用户。\n4. **观测**：启用 `instrumentation.ts`、接入 OpenTelemetry / Sentry / Datadog。\n5. **Skew Protection**：长时间用户仍能访问旧版本资源；在 Vercel 自动启用，自托管可用 `deploymentId` 实现。\n\n## 跟 Nuxt 比较\n\n- Nuxt 用 Nitro，一键生成 Node / Cloudflare / Lambda 等 preset。\n- Next.js 的 Adapter 方向接近 Nitro preset，只是需要额外适配包。\n\n选择部署方式前，先确认以下三件事：\n- 是否需要 SSR / Server Actions；\n- 是否需要细粒度缓存与边缘能力；\n- 运维成本（托管 vs 自建）能承受到什么程度。\n\n匹配好这三点，再从上面任选一条路径上线即可。\n","/books/nextjs/chapters/zh_Hant_HK/01-introduction.md":"# 初識 Next.js\n\nNext.js 是由 Vercel 維護的 React 全端框架，內建伺服器渲染、靜態產生、API 路由等能力。\n\n## 建立專案\n\n```bash\nnpx create-next-app@latest my-next-app\ncd my-next-app\nnpm run dev\n```\n\n## 項目結構（App Router）\n\nNext.js 13 起建議使用 App Router：\n\n```\nmy-next-app/\n├── app/\n│   ├── layout.tsx       # 根版面\n│   ├── page.tsx         # 首頁\n│   ├── globals.css      # 全域樣式\n│   └── blog/\n│       └── page.tsx     # /blog 頁面\n├── public/              # 靜態資源\n├── next.config.js       # 設定檔\n└── package.json\n```\n\n## 第一個頁面\n\n```tsx\n// app/page.tsx\nexport default function Home() {\n  return (\n    <main>\n      <h1>Hello Next.js!</h1>\n      <p>這是我的第一個 Next.js 頁面</p>\n    </main>\n  )\n}\n```\n\n## 與 Nuxt 對照\n\n| 特性 | Next.js | Nuxt.js |\n|------|---------|---------|\n| 基礎框架 | React | Vue |\n| 路由方式 | 檔案系統路由 | 檔案系統路由 |\n| 渲染模式 | SSR / SSG / ISR | SSR / SSG / SPA |\n| 部署 | Vercel / 自建 | 任意 Node 環境 |\n| 狀態管理 | Zustand / Redux | Pinia |\n\n兩者皆以「約定優於設定」組織路由；差異主要在生態（React 與 Vue）與預設渲染模型。\n","/books/nextjs/chapters/zh_Hant_HK/02-installation.md":'# 安裝與建立專案\n\n本章帶你從零搭建一個 Next.js 應用，了解系統要求、CLI 工具、手動安裝流程，以及 TypeScript、ESLint、路徑別名等基礎配置。\n\n## 系統要求\n\n- Node.js **20.9** 及以上。\n- macOS、Windows（含 WSL）或 Linux。\n- 瀏覽器：Chrome 111+、Edge 111+、Firefox 111+、Safari 16.4+。\n\n## 用 CLI 快速建立\n\n建議使用官方腳手架 `create-next-app`，一鍵生成帶最佳實踐的專案：\n\n```bash\npnpm create next-app@latest my-app --yes\ncd my-app\npnpm dev\n```\n\n`--yes` 會略過提示，使用預設配置：**TypeScript + Tailwind CSS + ESLint + App Router + Turbopack**，並自動設定 `@/*` 路徑別名。\n\n去掉 `--yes` 則會出現互動提示：\n\n```txt\nWould you like to use TypeScript? Yes\nWhich linter would you like to use? ESLint / Biome / None\nWould you like to use React Compiler? No\nWould you like to use Tailwind CSS? Yes\nWould you like your code inside a `src/` directory? No\nWould you like to use App Router? Yes\nWould you like to customize the import alias? No\n```\n\n## 手動安裝\n\n如果你想對專案有完全掌控，也可以手動初始化：\n\n```bash\nnpm i next@latest react@latest react-dom@latest\n```\n\n然後在 `package.json` 寫入 scripts：\n\n```json\n{\n  "scripts": {\n    "dev": "next dev",\n    "build": "next build",\n    "start": "next start",\n    "lint": "eslint"\n  }\n}\n```\n\n> Turbopack 已成為預設打包工具。如需使用 Webpack 可執行 `next dev --webpack`。\n\n## 建立 `app` 目錄\n\nNext.js 採用檔案系統路由。在根目錄新建 `app/`，並建立 **根佈局**（必須包含 `html` 與 `body`）：\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return (\n    <html lang="zh-HK">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n再建立首頁：\n\n```tsx\n// app/page.tsx\nexport default function Page() {\n  return <h1>Hello, Next.js!</h1>;\n}\n```\n\n執行 `npm run dev`，瀏覽 `http://localhost:3000` 即可看到結果。\n\n## 靜態資源目錄\n\n在專案根新建 `public/` 用於放置圖片、字型等靜態資源，檔案可直接以 `/` 根路徑存取：\n\n```tsx\nimport Image from \'next/image\';\n\n<Image src="/profile.png" alt="Profile" width={100} height={100} />;\n```\n\n## TypeScript 與路徑別名\n\nNext.js 原生支援 TypeScript，只需將檔案改為 `.ts` / `.tsx`，再執行 `next dev`，便會自動寫入 `tsconfig.json`。\n\n在 `tsconfig.json` 或 `jsconfig.json` 中可設定絕對引用，避免冗長的相對路徑：\n\n```json\n{\n  "compilerOptions": {\n    "baseUrl": ".",\n    "paths": {\n      "@/components/*": ["components/*"],\n      "@/styles/*": ["styles/*"]\n    }\n  }\n}\n```\n\n從此可以寫 `import { Button } from \'@/components/button\'`，不再需要 `../../../components/button`。\n',"/books/nextjs/chapters/zh_Hant_HK/03-app-router.md":'# App Router\n\nNext.js 13 的 App Router 以 React Server Components 為基礎，強化路由與版面。\n\n## 路由約定\n\n`app/` 下資料夾即 URL；常見特殊檔：`page.tsx`（頁面）、`layout.tsx`（版面）、`loading.tsx`、`error.tsx`、`not-found.tsx`。\n\n```\napp/page.tsx → /；app/about/page.tsx → /about；app/blog/[slug]/page.tsx → /blog/:slug\n```\n\n## Layout（版面）\n\n版面包覆子路由，以 `children` 插入內容：\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang="zh-Hant">\n      <body>\n        <nav>導覽列</nav>\n        {children}\n        <footer>頁尾</footer>\n      </body>\n    </html>\n  )\n}\n```\n\n## 動態路由\n\n方括號 `[param]` 為動態區段：\n\n```tsx\n// app/blog/[slug]/page.tsx\nexport default function BlogPost({ params }: { params: { slug: string } }) {\n  return <h1>文章：{params.slug}</h1>\n}\n```\n\n## 路由組\n\n圓括號 `(group)` 僅用於整理目錄，不會出現在網址：\n\n```\napp/(marketing)/about/page.tsx    → /about\napp/(dashboard)/settings/page.tsx → /settings\n```\n\n同一層可有多組 `(group)`，便於依功能拆分版面與載入邏輯。\n',"/books/nextjs/chapters/zh_Hant_HK/04-layouts-and-pages.md":'# 佈局與頁面\n\nNext.js 以檔案系統作為路由：資料夾決定 URL 段落，`page.tsx` 與 `layout.tsx` 則決定每段對應的 UI。本章講清楚兩者如何協作，以及動態段與 `searchParams` 的用法。\n\n## 建立頁面\n\n在 `app` 之下放一個 `page.tsx` 並預設匯出元件，該路由便會公開可存取：\n\n```tsx\n// app/page.tsx → /\nexport default function Page() {\n  return <h1>Hello Next.js!</h1>;\n}\n```\n\n## 建立佈局\n\n佈局是多個頁面共用的 UI。在客戶端導航時佈局會保留狀態、保持互動性，且不會重新渲染。\n\n```tsx\n// app/layout.tsx\nexport default function RootLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return (\n    <html lang="zh-HK">\n      <body>\n        <main>{children}</main>\n      </body>\n    </html>\n  );\n}\n```\n\n位於 `app/` 根部的叫 **根佈局**，它是必要的，而且只有它能寫 `<html>` 和 `<body>`。\n\n## 巢狀路由與巢狀佈局\n\n巢狀資料夾 = 巢狀 URL 段。例如 `/blog/[slug]` 對應：\n\n```\napp/\n├── layout.tsx         # 根佈局\n├── blog/\n│   ├── layout.tsx     # 網誌區佈局（可選）\n│   ├── page.tsx       # /blog\n│   └── [slug]/\n│       └── page.tsx   # /blog/hello-world\n```\n\n佈局會自動巢狀包裹：根佈局 → 網誌佈局 → 網誌頁。可在各層依需求複用導航、側欄、麵包屑等結構。\n\n```tsx\n// app/blog/layout.tsx\nexport default function BlogLayout({\n  children,\n}: {\n  children: React.ReactNode;\n}) {\n  return <section className="blog">{children}</section>;\n}\n```\n\n## 動態段\n\n用方括號建立動態段：`[slug]`、`[id]`。Next.js 15+ 的 `params` 是一個 Promise，需要 `await`：\n\n```tsx\n// app/blog/[slug]/page.tsx\nexport default async function Post({\n  params,\n}: {\n  params: Promise<{ slug: string }>;\n}) {\n  const { slug } = await params;\n  const post = await getPost(slug);\n\n  return (\n    <article>\n      <h1>{post.title}</h1>\n      <p>{post.content}</p>\n    </article>\n  );\n}\n```\n\n配合 `generateStaticParams` 可以在建置時預生所有動態路由，獲得純靜態站台的效能。\n\n## searchParams\n\n在伺服器元件頁面中透過 `searchParams` 讀取查詢參數：\n\n```tsx\nexport default async function Page({\n  searchParams,\n}: {\n  searchParams: Promise<{ [k: string]: string | string[] | undefined }>;\n}) {\n  const { q } = await searchParams;\n  return <p>搜尋詞：{q}</p>;\n}\n```\n\n> 注意：使用 `searchParams` 會令頁面自動變成 **動態渲染**，因為它依賴進入的請求。\n\n客戶端元件可改用 `useSearchParams` 這個 hook。\n\n## 頁面間導航\n\n使用 `next/link` 實作客戶端導航（帶自動預載）：\n\n```tsx\nimport Link from \'next/link\';\n\nexport default async function Posts() {\n  const posts = await getPosts();\n  return (\n    <ul>\n      {posts.map(p => (\n        <li key={p.slug}>\n          <Link href={`/blog/${p.slug}`}>{p.title}</Link>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```\n\n下一章會深入講解 `Link` 的預取、串流渲染與客戶端過渡。\n',"/books/nextjs/chapters/zh_Hant_HK/05-linking-and-navigating.md":"# 連結與導航\n\nNext.js 的路由預設在伺服器端渲染，為了讓使用者在切換路由時也感覺流暢，框架內建了 **預取**、**串流** 與 **客戶端過渡** 三大優化。本章講透它們如何協作，以及怎樣為慢網路與動態路由做優化。\n\n## 導航的四層原理\n\n1. **Server Rendering**：佈局與頁面預設為 RSC（React Server Components），伺服器端產出 RSC Payload 後發給客戶端。\n2. **Prefetching**：當連結進入視窗時，Next.js 自動在背景預取目標路由。\n3. **Streaming**：允許伺服器邊渲染邊傳送，配合 `loading.tsx` 立刻顯示載入骨架。\n4. **Client-side Transitions**：`<Link>` 使用客戶端過渡替換頁面內容，保留共用佈局、捲動狀態與互動。\n\n## 用 `<Link>` 取代 `<a>`\n\n```tsx\nimport Link from 'next/link';\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <html>\n      <body>\n        <nav>\n          <Link href=\"/blog\">網誌</Link>        {/* 自動預取 */}\n          <a href=\"/contact\">聯絡我們</a>        {/* 不會預取 */}\n        </nav>\n        {children}\n      </body>\n    </html>\n  );\n}\n```\n\n- **靜態路由**：整條路由都會被預取。\n- **動態路由**：預設不預取；若存在 `loading.tsx`，則會部分預取（共用佈局 + 骨架）。\n\n## 用 `loading.tsx` 開啟串流渲染\n\n在路由資料夾裡放 `loading.tsx`，Next.js 會自動把 `page.tsx` 包在 `<Suspense>` 內：\n\n```tsx\n// app/blog/loading.tsx\nexport default function Loading() {\n  return <BlogListSkeleton />;\n}\n```\n\n效果：使用者點擊連結後立刻看到骨架屏，真實內容準備好再替換進來。對 **Core Web Vitals**（TTFB / FCP / TTI）都有顯著提升。\n\n## 動態段建議加 `generateStaticParams`\n\n若動態段本可以在建置期預渲染，卻沒有 `generateStaticParams`，每次存取都會退化為請求時渲染：\n\n```tsx\nexport async function generateStaticParams() {\n  const posts = await fetch('https://.../posts').then(r => r.json());\n  return posts.map((p: any) => ({ slug: p.slug }));\n}\n```\n\n## 慢網路下的體驗優化\n\n網路不穩時預取可能未完成，骨架也來不及顯示。用 `useLinkStatus` 立即給使用者視覺回饋：\n\n```tsx\n'use client';\nimport { useLinkStatus } from 'next/link';\n\nexport default function LinkIndicator() {\n  const { pending } = useLinkStatus();\n  return <span aria-hidden className={pending ? 'is-pending' : ''} />;\n}\n```\n\n配合 CSS 延遲動畫（例如 100ms 才顯示）可避免快速導航時的「閃屏」。\n\n## 按需關閉預取\n\n長清單（如無限捲動表格）預取會浪費資源，可直接關閉：\n\n```tsx\n<Link prefetch={false} href=\"/blog\">網誌</Link>\n```\n\n或只在滑鼠懸停時預取，兼顧資源與體驗：\n\n```tsx\n'use client';\nimport Link from 'next/link';\nimport { useState } from 'react';\n\nfunction HoverPrefetchLink({ href, children }: { href: string; children: React.ReactNode }) {\n  const [active, setActive] = useState(false);\n  return (\n    <Link\n      href={href}\n      prefetch={active ? null : false}\n      onMouseEnter={() => setActive(true)}\n    >\n      {children}\n    </Link>\n  );\n}\n```\n\n## 使用原生 History API\n\n不想觸發 UI 重新渲染但又想同步 URL，可直接使用 `pushState`：\n\n```tsx\n'use client';\nimport { useSearchParams } from 'next/navigation';\n\nexport default function SortProducts() {\n  const searchParams = useSearchParams();\n  function update(order: string) {\n    const params = new URLSearchParams(searchParams.toString());\n    params.set('sort', order);\n    window.history.pushState(null, '', `?${params.toString()}`);\n  }\n  return (\n    <>\n      <button onClick={() => update('asc')}>升序</button>\n      <button onClick={() => update('desc')}>降序</button>\n    </>\n  );\n}\n```\n\n`pushState` / `replaceState` 都會與 `usePathname`、`useSearchParams` 同步。\n","/books/nextjs/chapters/zh_Hant_HK/06-server-and-client-components.md":"# 伺服器與客戶端元件\n\nApp Router 中，佈局與頁面預設都是 **伺服器元件（Server Components）**。它們在伺服器上執行，可直接存取資料庫、使用密鑰，並把極少量 JavaScript 傳到瀏覽器。需要互動時，再加入 **客戶端元件（Client Components）**。\n\n## 何時選哪一種？\n\n**選伺服器元件**：\n- 在資料來源附近取得資料（資料庫、API）。\n- 使用 API Key、Token 等不能暴露給客戶端的機密。\n- 減少送到瀏覽器的 JS，優化 FCP。\n- 漸進式串流輸出。\n\n**選客戶端元件**：\n- 需要 `useState`、`useEffect` 等 hooks。\n- 需要瀏覽器 API（`localStorage`、`window`、`Navigator.geolocation`）。\n- 監聽 `onClick`、`onChange` 等事件。\n\n## 在 Next.js 中如何運作？\n\n**伺服器端**：\n- 伺服器元件會被渲染成一種特殊的資料格式：**RSC Payload**。\n- 它與客戶端元件的佔位符一起，在伺服器端拼出首屏 HTML。\n\n**客戶端首次載入**：\n1. HTML 立即呈現一個非互動預覽。\n2. RSC Payload 協調伺服器／客戶端元件樹。\n3. JavaScript 載入並 **水合** 客戶端元件，令頁面可互動。\n\n**後續導航**：\n- RSC Payload 會被預取並快取，切換瞬間完成。\n- 客戶端元件完全在瀏覽器中渲染。\n\n## 撰寫一個客戶端元件\n\n在檔案頂部加上 `'use client'` 指令：\n\n```tsx\n'use client';\n\nimport { useState } from 'react';\n\nexport default function Counter() {\n  const [n, setN] = useState(0);\n  return (\n    <div>\n      <p>{n} 次點擊</p>\n      <button onClick={() => setN(n + 1)}>點我</button>\n    </div>\n  );\n}\n```\n\n`'use client'` 聲明了一條分界線：自此檔案起，所有的 import 皆屬於客戶端 bundle，不必每個檔案都重寫指令。\n\n## 縮小 JS 體積的竅門\n\n**把 `'use client'` 放在盡可能小的葉子元件上**，而不是整塊頁面：\n\n```tsx\n// 佈局：伺服器元件\nimport Search from './search';   // 客戶端元件\nimport Logo from './logo';       // 伺服器元件\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      <nav>\n        <Logo />\n        <Search />\n      </nav>\n      <main>{children}</main>\n    </>\n  );\n}\n```\n\n整個佈局仍然是伺服器元件，只有 `Search` 是客戶端 JS。\n\n## 從伺服器傳遞資料到客戶端\n\n直接用 props 即可（props 必須可序列化）：\n\n```tsx\n// 伺服器元件\nimport LikeButton from '@/app/ui/like-button';\nimport { getPost } from '@/lib/data';\n\nexport default async function Page({\n  params,\n}: {\n  params: Promise<{ id: string }>;\n}) {\n  const { id } = await params;\n  const post = await getPost(id);\n  return <LikeButton likes={post.likes} />;\n}\n```\n\n```tsx\n// 客戶端元件\n'use client';\nexport default function LikeButton({ likes }: { likes: number }) {\n  // ...\n}\n```\n\n## 在客戶端元件內嵌套伺服器元件\n\n客戶端元件不能直接 `import` 伺服器元件，但可以透過 **children 插槽** 將它傳進來：\n\n```tsx\n// 客戶端元件：Modal\n'use client';\nexport default function Modal({ children }: { children: React.ReactNode }) {\n  return <div className=\"modal\">{children}</div>;\n}\n```\n\n```tsx\n// 伺服器元件：Page\nimport Modal from './ui/modal';\nimport Cart from './ui/cart'; // 伺服器元件\n\nexport default function Page() {\n  return (\n    <Modal>\n      <Cart />\n    </Modal>\n  );\n}\n```\n\n這種「客戶端外殼 + 伺服器內容」的組合非常常見。\n\n## Context Provider\n\nReact Context 不被伺服器元件支援。把 Provider 封成客戶端元件，在根佈局中使用：\n\n```tsx\n'use client';\nimport { createContext } from 'react';\n\nexport const ThemeContext = createContext({});\n\nexport default function ThemeProvider({ children }: { children: React.ReactNode }) {\n  return <ThemeContext.Provider value=\"dark\">{children}</ThemeContext.Provider>;\n}\n```\n\n## 防止「環境污染」\n\n伺服器程式碼可能被意外 import 至客戶端。用 `server-only` 套件在建置期拋出錯誤：\n\n```ts\nimport 'server-only';\n\nexport async function getData() {\n  const res = await fetch('https://external-service.com/data', {\n    headers: { authorization: process.env.API_KEY },\n  });\n  return res.json();\n}\n```\n\n對應也有 `client-only` 用於標記瀏覽器專屬模組。\n","/books/nextjs/chapters/zh_Hant_HK/07-rendering.md":"# 渲染模式\n\nNext.js 可依頁面選擇 SSR、SSG、ISR 或以客戶端為主。\n\n## Server Components（伺服器元件）\n\n預設在伺服器執行，可 `fetch`、存取後端資源，並減少送到瀏覽器的 JS。\n\n```tsx\nasync function PostList() {\n  const data = await fetch('https://api.example.com/posts').then(r => r.json())\n  return (\n    <ul>{data.map((p: { id: string; title: string }) => <li key={p.id}>{p.title}</li>)}</ul>\n  )\n}\n```\n\n## Client Components（客戶端元件）\n\n需要 `useState`、事件或瀏覽器 API 時，檔案頂端加 `'use client'`：\n\n```tsx\n'use client'\nimport { useState } from 'react'\n\nexport default function Counter() {\n  const [count, setCount] = useState(0)\n  return (\n    <button type=\"button\" onClick={() => setCount(count + 1)}>\n      計數：{count}\n    </button>\n  )\n}\n```\n\n## Static Generation（靜態產生）\n\n建置時產生路徑與 HTML，例如動態路由搭配 `generateStaticParams`：\n\n```tsx\nexport async function generateStaticParams() {\n  const posts = await fetch('https://api.example.com/posts').then(r => r.json())\n  return posts.map((post: { slug: string }) => ({ slug: post.slug }))\n}\n```\n\n## ISR（增量靜態再生）\n\n`fetch` 的 `next.revalidate` 指定間隔（秒），過期後背景再驗證：\n\n```tsx\nasync function Page() {\n  const res = await fetch('https://api.example.com/data', {\n    next: { revalidate: 60 },\n  })\n  const data = await res.json()\n  return <div>{/* 使用 data */}</div>\n}\n```\n\n## 選擇指引\n\n| 情境 | 建議 |\n|------|------|\n| 部落格、文件等靜態內容 | SSG |\n| 動態內容且需 SEO | SSR |\n| 變動不頻繁的列表／內容頁 | ISR |\n| 後台、大量互動 | 以 Client Component（CSR）為主 |\n","/books/nextjs/chapters/zh_Hant_HK/08-fetching-data.md":"# 資料獲取\n\n在 App Router 中，取得資料是最自然的事：伺服器元件直接 `await`，客戶端元件則用 `use`、SWR 或 React Query。本章涵蓋兩端的取得方式，以及用 Suspense 串流不確定耗時的部分。\n\n## 在伺服器元件取得資料\n\n### 使用 `fetch`\n\n把元件寫成 `async` 函式，直接 `await`：\n\n```tsx\nexport default async function Page() {\n  const res = await fetch('https://api.vercel.app/blog');\n  const posts = await res.json();\n  return (\n    <ul>\n      {posts.map((p: any) => (\n        <li key={p.id}>{p.title}</li>\n      ))}\n    </ul>\n  );\n}\n```\n\n要點：\n- React 會自動對同一元件樹中相同的 `fetch` 做 **memoization**，你可以在真正需要的元件中取，不必逐層傳 props。\n- `fetch` 預設 **不快取**。如需快取，請搭配 `use cache` 指令（第 10 章）或在 `fetch` 上使用 `next: { revalidate }`。\n\n### 使用 ORM 或資料庫客戶端\n\n既然伺服器元件只在伺服器端執行，可直接呼叫 ORM：\n\n```tsx\nimport { db, posts } from '@/lib/db';\n\nexport default async function Page() {\n  const all = await db.select().from(posts);\n  return (\n    <ul>\n      {all.map(p => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n> 仍需對每個查詢做驗證與授權，詳見官方 *Data Security* 指南。\n\n## 串流渲染\n\n若某些資料較慢，讓它們先串流，頁面其餘部分立即可見。\n\n### `loading.js`\n\n在路由資料夾放 `loading.tsx`，Next.js 會把整頁包在 Suspense 邊界內：\n\n```tsx\n// app/blog/loading.tsx\nexport default function Loading() {\n  return <div>載入中…</div>;\n}\n```\n\n### 用 `<Suspense>` 精細控制\n\n只讓慢的片段走串流：\n\n```tsx\nimport { Suspense } from 'react';\nimport BlogList from '@/components/BlogList';\nimport BlogListSkeleton from '@/components/BlogListSkeleton';\n\nexport default function BlogPage() {\n  return (\n    <div>\n      <header>\n        <h1>歡迎來到網誌</h1>\n      </header>\n      <main>\n        <Suspense fallback={<BlogListSkeleton />}>\n          <BlogList />\n        </Suspense>\n      </main>\n    </div>\n  );\n}\n```\n\n### 什麼是「有意義的載入狀態」？\n\n別用一句 \"Loading…\" 應付使用者：骨架屏、佔位卡片、封面圖與標題都是更好的選擇，能讓使用者感受到應用穩定回應。\n\n## 在客戶端元件取得資料\n\n### React 的 `use` API：伺服器→客戶端串流\n\n先在伺服器端發起 Promise，將 Promise 傳給客戶端元件，客戶端以 `use()` 讀取：\n\n```tsx\n// 伺服器元件\nimport Posts from '@/components/Posts';\nimport { Suspense } from 'react';\n\nexport default function Page() {\n  const postsPromise = fetch('https://api.vercel.app/blog').then(r => r.json());\n  return (\n    <Suspense fallback={<p>Loading…</p>}>\n      <Posts postsPromise={postsPromise} />\n    </Suspense>\n  );\n}\n```\n\n```tsx\n'use client';\nimport { use } from 'react';\n\nexport default function Posts({ postsPromise }: { postsPromise: Promise<any[]> }) {\n  const posts = use(postsPromise);\n  return (\n    <ul>\n      {posts.map(p => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n### 社群函式庫：SWR / React Query\n\n客戶端的快取、重試、分頁等場景，用 SWR 或 TanStack Query 最順手：\n\n```tsx\n'use client';\nimport useSWR from 'swr';\n\nconst fetcher = (url: string) => fetch(url).then(r => r.json());\n\nexport function Posts() {\n  const { data, error, isLoading } = useSWR('/api/posts', fetcher);\n  if (isLoading) return <p>載入中…</p>;\n  if (error) return <p>載入失敗</p>;\n  return (\n    <ul>\n      {data.map((p: any) => <li key={p.id}>{p.title}</li>)}\n    </ul>\n  );\n}\n```\n\n## 與 Nuxt 比較\n\nNuxt 裡會用 `useFetch` / `useAsyncData`；Next.js 的「原生 `fetch`」反而更貼近 Web 標準。兩者共同點是：伺服器端取得的資料序列化至 HTML 中，客戶端重用，首屏 0 往返。\n","/books/nextjs/chapters/zh_Hant_HK/09-mutating-data.md":"# 資料修改與 Server Actions\n\nNext.js 使用 React 的 **Server Functions** 處理寫入。帶有 `use server` 指令的非同步函式，可被客戶端透過網路呼叫；與 `<form>` 或 `formAction` 搭配時，就是常說的 **Server Action**。\n\n## 建立 Server Function\n\n在檔案層級放 `'use server'`，所有匯出函式都會變成 Server Function：\n\n```ts\n// app/actions.ts\n'use server';\n\nimport { auth } from '@/lib/auth';\n\nexport async function createPost(formData: FormData) {\n  const session = await auth();\n  if (!session?.user) throw new Error('Unauthorized');\n\n  const title = formData.get('title');\n  const content = formData.get('content');\n  // 寫入資料庫…\n}\n```\n\n> 安全警告：Server Function 會被暴露為一個 POST 端點，**必須在函式內自行做驗證與授權**，不能只靠 UI 層的按鈕是否顯示。\n\n也可以在函式頂部寫 `'use server'`，僅標記單一函式：\n\n```tsx\nexport default function Page() {\n  async function createPost(formData: FormData) {\n    'use server';\n    // …\n  }\n  return <form action={createPost}>…</form>;\n}\n```\n\n## 在表單中呼叫\n\nReact 擴展了 `<form>` 的 `action` prop，可直接傳 Server Function：\n\n```tsx\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  return (\n    <form action={createPost}>\n      <input name=\"title\" />\n      <textarea name=\"content\" />\n      <button type=\"submit\">發佈</button>\n    </form>\n  );\n}\n```\n\n- 即使 JS 尚未載入，表單仍能提交（漸進增強）。\n- 送出後，Next.js 會在同一次請求返回更新後的 UI。\n\n## 在事件中呼叫\n\n客戶端元件中可用 `onClick` 等事件呼叫：\n\n```tsx\n'use client';\nimport { incrementLike } from './actions';\nimport { useState } from 'react';\n\nexport default function LikeButton({ initialLikes }: { initialLikes: number }) {\n  const [likes, setLikes] = useState(initialLikes);\n  return (\n    <button onClick={async () => setLikes(await incrementLike())}>\n      ❤ {likes}\n    </button>\n  );\n}\n```\n\n## 處理預期內的錯誤\n\n對於表單驗證這類「可預期錯誤」，請勿 `throw`，直接把錯誤當 **回傳值** 返回：\n\n```ts\n'use server';\nexport async function createPost(prevState: any, formData: FormData) {\n  const res = await fetch('https://api.vercel.app/posts', {\n    method: 'POST',\n    body: JSON.stringify({\n      title: formData.get('title'),\n      content: formData.get('content'),\n    }),\n  });\n  if (!res.ok) return { message: '建立失敗' };\n  return { message: '' };\n}\n```\n\n在客戶端以 `useActionState` 管理：\n\n```tsx\n'use client';\nimport { useActionState } from 'react';\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  const [state, formAction, pending] = useActionState(createPost, { message: '' });\n  return (\n    <form action={formAction}>\n      <input name=\"title\" required />\n      <textarea name=\"content\" required />\n      {state?.message && <p aria-live=\"polite\">{state.message}</p>}\n      <button disabled={pending}>發佈</button>\n    </form>\n  );\n}\n```\n\n## 寫入後刷新 UI\n\n提交後通常需要刷新資料。按需選擇：\n\n```ts\n'use server';\nimport { revalidatePath, revalidateTag } from 'next/cache';\nimport { redirect } from 'next/navigation';\n\nexport async function createPost(formData: FormData) {\n  // 寫入資料…\n  revalidatePath('/posts');   // 依路徑失效\n  // revalidateTag('posts'); // 依 tag 失效\n  redirect('/posts');         // 重新導向\n}\n```\n\n- `revalidatePath` / `revalidateTag`：令快取失效，下次存取拉取最新資料。\n- `refresh()`（來自 `next/cache`）：刷新客戶端路由，但不會令 tag 快取失效。\n- `redirect()`：跳轉到新頁面，其後程式碼不會執行。\n\n## 操作 Cookie\n\n```ts\n'use server';\nimport { cookies } from 'next/headers';\n\nexport async function signIn() {\n  const store = await cookies();\n  store.set('token', 'xxx');    // 設定\n  store.get('token')?.value;    // 讀取\n  store.delete('token');         // 刪除\n}\n```\n\nNext.js 在 Server Action 設定 cookie 後會自動重新渲染目前頁與佈局，讓 UI 同步。\n","/books/nextjs/chapters/zh_Hant_HK/10-caching.md":"# 快取\n\n快取是把「計算結果」儲存起來，下次直接重用。Next.js 在 App Router 中引入了 **Cache Components** 模型：用 `use cache` 指令顯式聲明可快取的部分，其餘需要執行時資料的部分則用 `<Suspense>` 包起來。整體稱為 **Partial Prerendering (PPR)**。\n\n## 啟用 Cache Components\n\n在 `next.config.ts` 啟用：\n\n```ts\nimport type { NextConfig } from 'next';\n\nconst nextConfig: NextConfig = {\n  cacheComponents: true,\n};\n\nexport default nextConfig;\n```\n\n## `use cache` 指令\n\n`use cache` 會快取非同步函式或元件的回傳值，可用於兩個層級：\n\n### 資料級快取\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport async function getUsers() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM users');\n}\n```\n\n函式的 **參數** 與閉包變數會自動成為 cache key 的一部分，因此不同入參會產生不同的快取條目。\n\n### UI 級快取\n\n整個元件／頁面都可以被快取：\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport default async function Page() {\n  'use cache';\n  cacheLife('hours');\n\n  const users = await db.query('SELECT * FROM users');\n  return (\n    <ul>\n      {users.map(u => <li key={u.id}>{u.name}</li>)}\n    </ul>\n  );\n}\n```\n\n> 若在檔案頂部寫 `'use cache'`，該檔案所有匯出皆會被快取。\n\n## 串流未快取的資料\n\n對於每次請求都需最新的資料（如呼叫 API 或資料庫），**切勿** 加 `'use cache'`，改用 `<Suspense>` 包住：\n\n```tsx\nimport { Suspense } from 'react';\n\nasync function LatestPosts() {\n  const data = await fetch('https://api.example.com/posts');\n  const posts = await data.json();\n  return <ul>{posts.map((p: any) => <li key={p.id}>{p.title}</li>)}</ul>;\n}\n\nexport default function Page() {\n  return (\n    <>\n      <h1>我的網誌</h1>\n      <Suspense fallback={<p>載入最新文章…</p>}>\n        <LatestPosts />\n      </Suspense>\n    </>\n  );\n}\n```\n\nfallback 會進入靜態 HTML 外殼，真實內容於請求時串流注入。\n\n## 執行時 API\n\n`cookies`、`headers`、`searchParams`、`params`（除非提供了 `generateStaticParams`）只能在請求時取得。存取它們的元件 **必須包在 `<Suspense>` 中**：\n\n```tsx\nimport { cookies } from 'next/headers';\nimport { Suspense } from 'react';\n\nasync function UserGreeting() {\n  const theme = (await cookies()).get('theme')?.value || 'light';\n  return <p>你的主題：{theme}</p>;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<p>載入中…</p>}>\n      <UserGreeting />\n    </Suspense>\n  );\n}\n```\n\n## 將執行時值傳入快取函式\n\n取得執行時值後，可作為參數傳給 `use cache` 函式：\n\n```tsx\nimport { cookies } from 'next/headers';\nimport { Suspense } from 'react';\n\nasync function CachedContent({ sessionId }: { sessionId: string }) {\n  'use cache';\n  return <div>{await fetchUserData(sessionId)}</div>;\n}\n\nasync function ProfileContent() {\n  const session = (await cookies()).get('session')?.value;\n  return <CachedContent sessionId={session!} />;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<div>載入中…</div>}>\n      <ProfileContent />\n    </Suspense>\n  );\n}\n```\n\n## 非確定性操作\n\n`Math.random()`、`Date.now()`、`crypto.randomUUID()` 每次結果不同。若要每次請求都給不同值？呼叫 `connection()` 讓渲染下沉到請求時：\n\n```tsx\nimport { connection } from 'next/server';\nimport { Suspense } from 'react';\n\nasync function Unique() {\n  await connection();\n  return <p>請求 ID: {crypto.randomUUID()}</p>;\n}\n\nexport default function Page() {\n  return (\n    <Suspense fallback={<p>載入中…</p>}>\n      <Unique />\n    </Suspense>\n  );\n}\n```\n\n## Partial Prerendering 概覽\n\n啟用 Cache Components 後，每條路由在建置期都會產出一個 **靜態 shell**：\n- `'use cache'` 的結果進入 shell。\n- `<Suspense>` 內未快取的部分於請求時串流填入。\n- 純計算（同步 I/O、模組匯入）自動納入 shell。\n\n這是預設的 PPR 渲染行為，同一條路由同時做到「首位元組超快 + 個性化最新」。\n","/books/nextjs/chapters/zh_Hant_HK/11-revalidating.md":"# 重新驗證\n\n有快取就要能「令其過期」。Next.js 提供兩種策略：**基於時間** 的 `cacheLife`，以及 **按需** 的 `revalidateTag` / `updateTag` / `revalidatePath`。本章講清楚它們的差異與使用時機。\n\n## 時間維度：`cacheLife`\n\n`cacheLife` 於 `use cache` 範圍內設定快取壽命：\n\n```tsx\nimport { cacheLife } from 'next/cache';\n\nexport async function getProducts() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM products');\n}\n```\n\n內建檔位表：\n\n| profile   | stale | revalidate | expire      |\n|-----------|-------|------------|-------------|\n| `seconds` | 0     | 1s         | 60s         |\n| `minutes` | 5m    | 1m         | 1h          |\n| `hours`   | 5m    | 1h         | 1d          |\n| `days`    | 5m    | 1d         | 1w          |\n| `weeks`   | 5m    | 1w         | 30d         |\n| `max`     | 5m    | 30d        | ~永久        |\n\n也可以傳入物件自訂：\n\n```tsx\ncacheLife({\n  stale: 3600,       // 1 小時後視為過時\n  revalidate: 7200,  // 2 小時後重新生成\n  expire: 86400,     // 1 天後完全失效\n});\n```\n\n## Tag 維度：`cacheTag`\n\n替一份快取加上標籤，之後按標籤批次失效：\n\n```tsx\nimport { cacheTag } from 'next/cache';\n\nexport async function getProducts() {\n  'use cache';\n  cacheTag('products');\n  return db.query('SELECT * FROM products');\n}\n```\n\n## `revalidateTag`：背景刷新（SWR）\n\n適合「延遲刷新可接受」的場景（網誌、商品目錄）。stale-while-revalidate 語意：先送舊資料，背景再重新生成。\n\n```tsx\nimport { revalidateTag } from 'next/cache';\n\nexport async function updateUser() {\n  // 寫資料…\n  revalidateTag('user', 'max'); // 'max' 允許最長的 stale 窗口\n}\n```\n\n可在 Server Action 或 Route Handler 中呼叫。\n\n## `updateTag`：立即失效（讀自己寫）\n\n適合使用者「立刻看到自己剛寫的內容」場景：\n\n```tsx\nimport { updateTag } from 'next/cache';\nimport { redirect } from 'next/navigation';\n\nexport async function createPost(formData: FormData) {\n  const post = await db.post.create({\n    data: { title: formData.get('title') },\n  });\n\n  updateTag('posts');\n  redirect(`/posts/${post.id}`);\n}\n```\n\n只能在 **Server Action** 中使用。\n\n### 兩者比較\n\n|                | `updateTag`         | `revalidateTag`              |\n|----------------|---------------------|------------------------------|\n| 使用場所       | 僅 Server Action    | Server Action + Route Handler |\n| 失效行為       | 立即過期            | stale-while-revalidate        |\n| 典型場景       | 讀自己寫            | 背景刷新                      |\n\n## `revalidatePath`：按路徑失效\n\n不確定要失效哪些 tag 時的粗粒度方案：\n\n```tsx\nimport { revalidatePath } from 'next/cache';\n\nexport async function updateUser() {\n  // 寫資料…\n  revalidatePath('/profile');\n}\n```\n\n> 建議：優先使用 tag 失效，精準且不會「誤傷」其他快取。\n\n## 應該快取什麼？\n\n簡單判斷：\n- 不依賴執行時資料（`cookies` / `headers` / `searchParams`），且可容忍一段時間舊？→ **快取**。\n- 每次請求都須最新？→ 不快取，搭配 `<Suspense>` 串流輸出。\n- CMS 內容？→ 用 tag + 長 `cacheLife`，發佈後以 `revalidateTag` 觸發刷新。\n","/books/nextjs/chapters/zh_Hant_HK/12-error-handling.md":"# 錯誤處理\n\n錯誤可分為兩類：\n- **可預期錯誤**：表單驗證失敗、請求失敗 — 把它們視為「回傳值」，切勿 `throw`。\n- **未捕捉例外**：真正的 bug — 拋出去，由錯誤邊界兜底。\n\n## 處理可預期錯誤\n\n### Server Function\n\n驗證失敗、回應非 2xx，應返回錯誤物件：\n\n```ts\n'use server';\nexport async function createPost(prevState: any, formData: FormData) {\n  const res = await fetch('https://api.vercel.app/posts', {\n    method: 'POST',\n    body: JSON.stringify({\n      title: formData.get('title'),\n      content: formData.get('content'),\n    }),\n  });\n  if (!res.ok) return { message: '建立失敗' };\n  return { message: '' };\n}\n```\n\n客戶端以 `useActionState` 讀取：\n\n```tsx\n'use client';\nimport { useActionState } from 'react';\nimport { createPost } from '@/app/actions';\n\nexport function Form() {\n  const [state, formAction, pending] = useActionState(createPost, { message: '' });\n  return (\n    <form action={formAction}>\n      <input name=\"title\" required />\n      <textarea name=\"content\" required />\n      {state?.message && <p aria-live=\"polite\">{state.message}</p>}\n      <button disabled={pending}>送出</button>\n    </form>\n  );\n}\n```\n\n### 於伺服器元件中處理\n\n直接依回應決定渲染分支或重新導向：\n\n```tsx\nexport default async function Page() {\n  const res = await fetch('https://…');\n  if (!res.ok) return <p>發生錯誤</p>;\n  const data = await res.json();\n  return <Posts data={data} />;\n}\n```\n\n### 404：`notFound`\n\n在路由段內呼叫 `notFound()` 會渲染最接近的 `not-found.tsx`：\n\n```tsx\nimport { notFound } from 'next/navigation';\nimport { getPostBySlug } from '@/lib/posts';\n\nexport default async function Page({\n  params,\n}: {\n  params: Promise<{ slug: string }>;\n}) {\n  const { slug } = await params;\n  const post = getPostBySlug(slug);\n  if (!post) notFound();\n  return <div>{post.title}</div>;\n}\n```\n\n配套 `app/blog/not-found.tsx`：\n\n```tsx\nexport default function NotFound() {\n  return <div>404 - 找不到頁面</div>;\n}\n```\n\n## 處理未捕捉例外\n\n### 巢狀錯誤邊界\n\n在任何路由段放 `error.tsx`，即會為該子樹建立錯誤邊界。**必須加 `'use client'`**：\n\n```tsx\n'use client';\nimport { useEffect } from 'react';\n\nexport default function ErrorPage({\n  error,\n  unstable_retry,\n}: {\n  error: Error & { digest?: string };\n  unstable_retry: () => void;\n}) {\n  useEffect(() => console.error(error), [error]);\n\n  return (\n    <div>\n      <h2>發生錯誤</h2>\n      <button onClick={() => unstable_retry()}>重試</button>\n    </div>\n  );\n}\n```\n\n錯誤會冒泡至最近的 `error.tsx`，因此你可以分層客製。例如 `/dashboard/settings/error.tsx` 只兜底設定頁，其他頁面走上層 `error.tsx`。\n\n### 更細粒度：`unstable_catchError`\n\n若想把錯誤邊界掛到元件層級，可用 `unstable_catchError`：\n\n```tsx\n'use client';\nimport { unstable_catchError as catchError, type ErrorInfo } from 'next/error';\n\nfunction Fallback(\n  props: { title: string },\n  { error, unstable_retry }: ErrorInfo\n) {\n  return (\n    <div>\n      <h2>{props.title}</h2>\n      <p>{error.message}</p>\n      <button onClick={() => unstable_retry()}>重試</button>\n    </div>\n  );\n}\n\nexport default catchError(Fallback);\n```\n\n### 事件中的錯誤\n\n錯誤邊界只會捕捉渲染期錯誤。事件處理函式、async callback 內的錯誤需自行 `try/catch` 並以 `useState` 儲存：\n\n```tsx\n'use client';\nimport { useState } from 'react';\n\nexport function Button() {\n  const [error, setError] = useState<any>(null);\n  function handleClick() {\n    try {\n      throw new Error('Oops');\n    } catch (e) {\n      setError(e);\n    }\n  }\n  if (error) return <p>發生錯誤：{error.message}</p>;\n  return <button onClick={handleClick}>點我</button>;\n}\n```\n\n注意：`startTransition` 內的錯誤會 **冒泡至最近的錯誤邊界**。\n\n### 全域兜底：`global-error.js`\n\n如根佈局崩潰怎麼辦？在 `app/global-error.tsx` 提供最外層兜底，它會替換根佈局：\n\n```tsx\n'use client';\n\nexport default function GlobalError({\n  error,\n  unstable_retry,\n}: {\n  error: Error & { digest?: string };\n  unstable_retry: () => void;\n}) {\n  return (\n    <html>\n      <body>\n        <h2>發生錯誤</h2>\n        <button onClick={() => unstable_retry()}>重試</button>\n      </body>\n    </html>\n  );\n}\n```\n\n注意 `global-error` 自身必須寫上 `<html>` 與 `<body>`。\n","/books/nextjs/chapters/zh_Hant_HK/13-css-styling.md":"# CSS 樣式方案\n\nNext.js 支援多種成熟的 CSS 方案：**Tailwind CSS**、**CSS Modules**、**全域 CSS**、**外部樣式表**、**Sass**、**CSS-in-JS**。本章聚焦前四種常用做法。\n\n## Tailwind CSS（建議）\n\n`create-next-app` 預設已安裝 Tailwind。若手動安裝：\n\n```bash\nnpm install -D tailwindcss @tailwindcss/postcss\n```\n\n於 `postcss.config.mjs` 啟用：\n\n```js\nexport default {\n  plugins: {\n    '@tailwindcss/postcss': {},\n  },\n};\n```\n\n在全域 CSS 中匯入：\n\n```css\n/* app/globals.css */\n@import 'tailwindcss';\n```\n\n在根佈局中匯入 CSS：\n\n```tsx\nimport './globals.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-HK\">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n接著即可直接使用工具類：\n\n```tsx\nexport default function Page() {\n  return (\n    <main className=\"flex min-h-screen flex-col items-center justify-between p-24\">\n      <h1 className=\"text-4xl font-bold\">歡迎使用 Next.js!</h1>\n    </main>\n  );\n}\n```\n\n## CSS Modules\n\n適合元件層級自訂樣式，避免全域類名衝突。檔名以 `*.module.css` 結尾：\n\n```css\n/* blog.module.css */\n.blog {\n  padding: 24px;\n}\n```\n\n```tsx\nimport styles from './blog.module.css';\n\nexport default function Page() {\n  return <main className={styles.blog} />;\n}\n```\n\nNext.js 會生成唯一類名，跨檔案同名不會衝突。\n\n## 全域 CSS\n\n用於真正全域的樣式（如 Tailwind 基礎層、reset）：\n\n```css\n/* app/global.css */\nbody {\n  padding: 20px 20px 60px;\n  max-width: 680px;\n  margin: 0 auto;\n}\n```\n\n```tsx\nimport './global.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-HK\">\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n> 提示：導航時全域樣式不會自動卸載，彼此可能疊加造成衝突。僅放「真正全域」的樣式。\n\n## 外部樣式表\n\n第三方套件的 CSS 可在 `app` 目錄任意位置 import：\n\n```tsx\nimport 'bootstrap/dist/css/bootstrap.css';\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-HK\">\n      <body className=\"container\">{children}</body>\n    </html>\n  );\n}\n```\n\n## 排序與合併\n\n生產建置時 Next.js 會合併並程式碼分割 CSS。最終順序依 `import` 順序而定。\n\n例如：\n\n```tsx\nimport { BaseButton } from './base-button';  // 內部 import base-button.module.css\nimport styles from './page.module.css';\n```\n\n順序為 `base-button.module.css` → `page.module.css`。\n\n實務建議：\n- CSS `import` 集中於單一入口檔。\n- 全域樣式、Tailwind 入口都放在應用根部。\n- 多數樣式使用 Tailwind，元件私有樣式使用 CSS Modules，極少使用全域 CSS。\n- 關閉 `sort-imports` 類自動排序 import 的 lint 規則，避免影響 CSS 順序。\n- 開發與建置下 CSS 順序可能略有不同，務必透過 `next build` 驗證。\n\n## 開發與生產\n\n- `next dev` 下 CSS 熱更新，秒級回饋。\n- `next build` 會壓縮並拆分 CSS，路由僅載入所需最小集合。\n- 生產即使禁用 JS 也可載入 CSS；開發則需 JS 以支援 Fast Refresh。\n","/books/nextjs/chapters/zh_Hant_HK/14-images.md":"# 圖片優化\n\n`next/image` 是 Next.js 官方的 `<img>` 替代品，自動完成：\n- **尺寸優化**：依裝置分發合適尺寸 + WebP。\n- **視覺穩定**：自動防止載入時的版面抖動（CLS）。\n- **更快首屏**：延遲載入 + 可選 blur 佔位圖。\n- **資源靈活**：本地或遠端圖片皆可按需縮放。\n\n## 基本用法\n\n```tsx\nimport Image from 'next/image';\n\nexport default function Page() {\n  return <Image src=\"/profile.png\" alt=\"頭像\" width={500} height={500} />;\n}\n```\n\n`src` 可為本地路徑或遠端 URL。\n\n## 本地圖片\n\n把靜態資源放在根目錄 `public/` 下，直接以 `/xxx.png` 引用。\n\n建議做法：**靜態 import**，`width`、`height`、`blurDataURL` 都會自動填入：\n\n```tsx\nimport Image from 'next/image';\nimport ProfileImage from './profile.png';\n\nexport default function Page() {\n  return (\n    <Image\n      src={ProfileImage}\n      alt=\"頭像\"\n      // width / height / blurDataURL 自動提供\n      placeholder=\"blur\"\n    />\n  );\n}\n```\n\n### 檔名不定？用動態 import\n\n網誌封面這類路徑來自資料的情境，可在伺服器元件中使用 `import()`：\n\n```tsx\nimport Image from 'next/image';\n\nasync function PostImage({ imageFilename, alt }: { imageFilename: string; alt: string }) {\n  const { default: image } = await import(`../content/blog/images/${imageFilename}`);\n  return <Image src={image} alt={alt} />;\n}\n```\n\n路徑需帶 **靜態前綴**（`../content/blog/images/`），Next.js 會把該目錄下所有檔案打包，外部無法跳出此目錄。\n\n## 遠端圖片\n\n遠端 URL 必須自行提供 `width` / `height`（或使用 `fill`），因建置期 Next.js 無法存取遠端檔：\n\n```tsx\n<Image\n  src=\"https://s3.amazonaws.com/my-bucket/profile.png\"\n  alt=\"頭像\"\n  width={500}\n  height={500}\n/>\n```\n\n另外需在 `next.config.ts` 白名單允許此網域，避免 URL 被濫用：\n\n```ts\nimport type { NextConfig } from 'next';\n\nconst config: NextConfig = {\n  images: {\n    remotePatterns: [\n      {\n        protocol: 'https',\n        hostname: 's3.amazonaws.com',\n        pathname: '/my-bucket/**',\n      },\n    ],\n  },\n};\n\nexport default config;\n```\n\n寫得愈具體愈安全。\n\n## `fill` 模式\n\n當你希望圖片填滿父容器（父容器需 `position: relative` 並有尺寸）：\n\n```tsx\n<div style={{ position: 'relative', width: 320, height: 180 }}>\n  <Image src=\"/cover.jpg\" alt=\"封面\" fill sizes=\"320px\" />\n</div>\n```\n\n`sizes` 有助 Next.js 選擇最合適的響應式圖源，務必提供。\n\n## 效能建議\n\n- **首屏關鍵圖像** 加 `priority`，讓它優先載入。\n- **清單縮圖** 指定合理的 `sizes`，瀏覽器才能挑出最佳尺寸。\n- 靜態 import 可帶來免費 blur 佔位，體驗顯著更佳。\n- 勿對裝飾性圖示使用 `Image`，直接 `<img>` 或 SVG 更輕量。\n","/books/nextjs/chapters/zh_Hant_HK/15-fonts.md":"# 字體優化\n\n`next/font` 會自動優化字體載入：\n- 所有字體 **自行托管** 為靜態資源，瀏覽器不再向 Google 發請求。\n- 自動消除版面抖動（CLS）。\n- 字體只於用到的元件範圍內生效，天然範圍化。\n\n## Google Fonts\n\n從 `next/font/google` 直接 import 字體並呼叫即可：\n\n```tsx\nimport { Geist } from 'next/font/google';\n\nconst geist = Geist({ subsets: ['latin'] });\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-HK\" className={geist.className}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n- **建議採用 variable font**，彈性且體積合理。\n- 一般字體則需明確指定 `weight`：\n\n```tsx\nimport { Roboto } from 'next/font/google';\n\nconst roboto = Roboto({ weight: '400', subsets: ['latin'] });\n```\n\n## 本地字體\n\n將 `.woff2` 放入專案（如 `app/fonts/` 或 `public/fonts/`），使用 `next/font/local`：\n\n```tsx\nimport localFont from 'next/font/local';\n\nconst myFont = localFont({ src: './my-font.woff2' });\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"zh-HK\" className={myFont.className}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n一組多重量／斜體？使用陣列：\n\n```tsx\nconst roboto = localFont({\n  src: [\n    { path: './Roboto-Regular.woff2', weight: '400', style: 'normal' },\n    { path: './Roboto-Italic.woff2',  weight: '400', style: 'italic' },\n    { path: './Roboto-Bold.woff2',    weight: '700', style: 'normal' },\n    { path: './Roboto-BoldItalic.woff2', weight: '700', style: 'italic' },\n  ],\n});\n```\n\n## 作用域\n\n字體「用於何處即作用於何處」。寫在根佈局可全站生效，寫在 `app/blog/layout.tsx` 僅影響網誌區。不同路由可使用不同字體且互不干擾。\n\n## 與 Tailwind 搭配\n\n為字體建立 CSS 變數，在 Tailwind 設定中映射為字體家族：\n\n```tsx\nimport { Geist } from 'next/font/google';\n\nconst geist = Geist({\n  subsets: ['latin'],\n  variable: '--font-geist',\n});\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html className={geist.variable}>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n```css\n/* app/globals.css */\n@theme {\n  --font-sans: var(--font-geist);\n}\n```\n\n如此 `font-sans` 工具類便會使用 Geist。\n","/books/nextjs/chapters/zh_Hant_HK/16-metadata.md":"# 元資料與 OG 圖片\n\nMetadata API 協助你為每個頁面定義 `<title>`、`<meta>`、`<link>`，從而優化 SEO 與社交分享。有三條路徑：\n1. **靜態 `metadata` 物件**\n2. **動態 `generateMetadata` 函式**\n3. **檔案慣例**（`favicon.ico`、`opengraph-image.tsx`、`robots.txt`、`sitemap.xml` 等）\n\n> `metadata` 與 `generateMetadata` 僅於 **伺服器元件** 中生效。\n\n## 預設標籤\n\n即便不寫任何元資料，Next.js 仍會自動注入：\n\n```html\n<meta charset=\"utf-8\" />\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" />\n```\n\n## 靜態元資料\n\n在 `layout.tsx` 或 `page.tsx` 匯出 `Metadata` 物件：\n\n```tsx\nimport type { Metadata } from 'next';\n\nexport const metadata: Metadata = {\n  title: '我的網誌',\n  description: '關於前端與產品的思考',\n};\n\nexport default function Layout() {\n  /* … */\n}\n```\n\n## 動態元資料\n\n依據 `params` 拉取資料再生成標題與描述：\n\n```tsx\nimport type { Metadata } from 'next';\n\ntype Props = {\n  params: Promise<{ slug: string }>;\n};\n\nexport async function generateMetadata({ params }: Props): Promise<Metadata> {\n  const { slug } = await params;\n  const post = await fetch(`https://api.vercel.app/blog/${slug}`).then(r => r.json());\n\n  return {\n    title: post.title,\n    description: post.description,\n  };\n}\n\nexport default function Page() {\n  /* … */\n}\n```\n\n### 避免重複請求\n\n頁面與 `generateMetadata` 常需要相同資料。用 React 的 `cache` 包裹一層，確保只請求一次：\n\n```ts\nimport { cache } from 'react';\nimport { db } from '@/app/lib/db';\n\nexport const getPost = cache(async (slug: string) => {\n  return db.query.posts.findFirst({ where: eq(posts.slug, slug) });\n});\n```\n\n### 串流元資料\n\n對動態渲染的頁面，Next.js 會把元資料 **單獨串流** 注入 HTML，不阻塞視覺內容。Twitterbot、Slackbot 這類必須於 `<head>` 內取得 meta 的爬蟲會自動降級為「等全部生成完再返回」。\n\n## 檔案慣例\n\n這些特殊檔案可靜態放置，亦可以程式動態生成：\n\n- `favicon.ico`、`icon.jpg`、`apple-icon.jpg`\n- `opengraph-image.jpg`、`twitter-image.jpg`\n- `robots.txt`、`sitemap.xml`\n\n### 靜態 favicon / OG\n\n將 `favicon.ico` 放在 `app/` 根部，將 `opengraph-image.jpg` 放在需要客製的路由。Next.js 會自動注入對應 `<meta>`，且就近路由的 OG 優先級較高。\n\n### 動態 OG 圖片\n\n以 `ImageResponse` 按 JSX + CSS 生成 1200×630 的 PNG，非常適合網誌每篇文章獨有封面：\n\n```tsx\n// app/blog/[slug]/opengraph-image.tsx\nimport { ImageResponse } from 'next/og';\nimport { getPost } from '@/app/lib/data';\n\nexport const size = { width: 1200, height: 630 };\nexport const contentType = 'image/png';\n\nexport default async function Image({ params }: { params: { slug: string } }) {\n  const post = await getPost(params.slug);\n  return new ImageResponse(\n    (\n      <div\n        style={{\n          fontSize: 128,\n          background: 'white',\n          width: '100%',\n          height: '100%',\n          display: 'flex',\n          alignItems: 'center',\n          justifyContent: 'center',\n        }}\n      >\n        {post.title}\n      </div>\n    )\n  );\n}\n```\n\n`ImageResponse` 底層使用 `satori + resvg`，僅支援 flexbox 等子集，grid 無法使用。請將其視為「可程序化的截圖器」，而非完整瀏覽器。\n","/books/nextjs/chapters/zh_Hant_HK/17-route-handlers.md":"# Route Handlers\n\n**Route Handlers** 讓你在 App Router 直接基於 Web 標準的 `Request` / `Response` API 撰寫 API。它們是 `pages/api/*` 的現代替代品，不需再搭配 API Routes。\n\n## 約定\n\n在 `app` 目錄任意位置新建 `route.ts`（或 `.js`），匯出對應 HTTP 方法的函式：\n\n```ts\n// app/api/hello/route.ts\nexport async function GET(request: Request) {\n  return Response.json({ hello: 'world' });\n}\n```\n\n支援方法：`GET`、`POST`、`PUT`、`PATCH`、`DELETE`、`HEAD`、`OPTIONS`。未實作的方法會自動回傳 `405 Method Not Allowed`。\n\n> 限制：同一路由段下，`route.ts` 與 `page.tsx` 不可共存。\n\n## 使用 `NextRequest` / `NextResponse`\n\nNext.js 在原生 API 之上擴充了更便捷的工具：\n\n```ts\nimport { NextRequest, NextResponse } from 'next/server';\n\nexport async function GET(req: NextRequest) {\n  const q = req.nextUrl.searchParams.get('q') ?? '';\n  return NextResponse.json({ query: q });\n}\n```\n\n## 快取\n\nRoute Handlers **預設不快取**。若要 `GET` 被快取，可在檔案中匯出：\n\n```ts\nexport const dynamic = 'force-static';\n\nexport async function GET() {\n  const res = await fetch('https://data.mongodb-api.com/…', {\n    headers: { 'API-Key': process.env.DATA_API_KEY! },\n  });\n  return Response.json({ data: await res.json() });\n}\n```\n\n其他 HTTP 方法（POST 等）即使與 `GET` 在同一檔案，也不會被快取。\n\n### 啟用 Cache Components 後\n\n當 `cacheComponents: true` 時，`GET` Route Handlers 與頁面遵循同一套渲染模型：\n- 不存取執行時 API、也沒有非確定性操作時，會於建置期預渲染。\n- 存取 `headers()` / `cookies()` / `Math.random()` / 請求物件屬性時，自動下沉到請求時。\n- 若需快取資料庫查詢等耗時操作，將其抽成帶 `'use cache'` 的輔助函式。\n\n```ts\nimport { cacheLife } from 'next/cache';\n\nexport async function GET() {\n  const products = await getProducts();\n  return Response.json(products);\n}\n\nasync function getProducts() {\n  'use cache';\n  cacheLife('hours');\n  return db.query('SELECT * FROM products');\n}\n```\n\n> `'use cache'` 不能直接寫在 handler 函式本體內，必須置於輔助函式。\n\n## 以 TypeScript 型別化參數\n\nNext.js 內建 `RouteContext` 幫你取得強型別動態段：\n\n```ts\n// app/users/[id]/route.ts\nimport type { NextRequest } from 'next/server';\n\nexport async function GET(_req: NextRequest, ctx: RouteContext<'/users/[id]'>) {\n  const { id } = await ctx.params;\n  return Response.json({ id });\n}\n```\n\n型別會於 `next dev` / `next build` 時自動生成。\n\n## 典型用法\n\n### 表單或 Webhook\n\n```ts\nexport async function POST(req: Request) {\n  const data = await req.json();\n  // 處理 webhook / 寫入資料庫…\n  return new Response('ok');\n}\n```\n\n### 串流回應\n\n直接回傳 `ReadableStream`：\n\n```ts\nexport async function GET() {\n  const stream = new ReadableStream({\n    async start(controller) {\n      controller.enqueue('hello ');\n      controller.enqueue('world');\n      controller.close();\n    },\n  });\n  return new Response(stream, { headers: { 'Content-Type': 'text/plain' } });\n}\n```\n\n### 特殊 Route Handlers\n\n`sitemap.ts`、`opengraph-image.tsx`、`icon.tsx` 這些特殊檔案亦屬 Route Handler，預設靜態化，除非使用執行時 API 或明確設為動態。\n\n## 何時用 Route Handler，何時用 Server Action？\n\n| 情境                                   | 建議                       |\n|----------------------------------------|----------------------------|\n| 內部表單、UI 觸發的寫入                | **Server Action**          |\n| 外部整合（webhook、第三方 OAuth 回呼） | **Route Handler**          |\n| 提供行動端／第三方客戶端 API           | **Route Handler**          |\n| 需嚴格 HTTP 方法語意                   | **Route Handler**          |\n\n原則：面向自家 UI 用 Server Action，面向外部或需純 HTTP 語意時用 Route Handler。\n","/books/nextjs/chapters/zh_Hant_HK/18-deploying.md":"# 部署\n\nNext.js 可以透過多種方式部署：代管平台（Vercel、Netlify、Cloudflare…）、自建 Node.js 伺服器、Docker、靜態匯出，甚至經由 Adapter 接上其他執行時。本章概覽這些路徑，協助你在正式環境做出合適選擇。\n\n## 建置產物\n\n```bash\npnpm build   # next build\npnpm start   # next start\n```\n\n`next build` 會輸出：\n- `.next/server/` —— 伺服器端渲染程式碼、Server Components、Route Handlers。\n- `.next/static/` —— 客戶端 JS、CSS、靜態資源（附內容雜湊，可安全長快取）。\n- `.next/standalone/`（可選） —— 僅包含執行所需的最小 Node 程式碼 + `node_modules`。\n\n`next start` 以正式模式啟動 Node.js 伺服器。\n\n## 一、部署至 Vercel 等平台\n\nNext.js 的 “zero-config” 體驗最完整者非 **Vercel** 莫屬。只需接上儲存庫，平台會自動辨識：\n- Server Components / SSR / ISR\n- Partial Prerendering（PPR）\n- Image Optimization\n- Middleware / Edge Runtime\n- Cron Jobs、Skew Protection 等\n\nNetlify、Cloudflare Pages、AWS Amplify 等透過各自的 **Adapter** 也能提供近似能力。\n\n## 二、自建 Node.js 伺服器\n\n只要執行環境支援 **Node.js 20.9+**，即可直接使用 `next start`：\n\n```bash\nnode_modules/.bin/next start -p 3000\n```\n\n常見流程：\n1. CI 中執行 `pnpm install --prod=false && pnpm build`。\n2. 把 `.next/`、`public/`、`package.json`、`node_modules`（或 `standalone`）複製至伺服器。\n3. 以 PM2 / systemd / supervisor 維持處理程序。\n4. 前面放 Nginx / Caddy 處理 TLS、gzip、靜態資源快取。\n\n### Standalone 輸出\n\n在 `next.config.js` 啟用：\n\n```js\nmodule.exports = { output: 'standalone' };\n```\n\n建置後將 `.next/standalone` + `.next/static` + `public` 一同部署，啟動：\n\n```bash\nnode .next/standalone/server.js\n```\n\n映像檔體積大幅降低，極適合容器化。\n\n## 三、Docker 部署\n\n基於 `output: 'standalone'` 的多階段 Dockerfile 範例：\n\n```dockerfile\nFROM node:20-alpine AS deps\nWORKDIR /app\nCOPY package.json pnpm-lock.yaml ./\nRUN corepack enable && pnpm install --frozen-lockfile\n\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY --from=deps /app/node_modules ./node_modules\nCOPY . .\nRUN corepack enable && pnpm build\n\nFROM node:20-alpine AS runner\nWORKDIR /app\nENV NODE_ENV=production\nCOPY --from=builder /app/public ./public\nCOPY --from=builder /app/.next/standalone ./\nCOPY --from=builder /app/.next/static ./.next/static\nEXPOSE 3000\nCMD [\"node\", \"server.js\"]\n```\n\n## 四、靜態匯出\n\n若網站完全靜態（無 Server Action、無 SSR），可直接匯出靜態檔案：\n\n```js\n// next.config.js\nmodule.exports = { output: 'export' };\n```\n\n產物位於 `out/`，可上架至任意靜態伺服器（Nginx、CDN、GitHub Pages）。注意：\n- 無法使用 Server Actions、Route Handlers 的動態 API、`next/image` 預設優化等。\n- 可使用 `next/image` 的 `unoptimized` 模式或第三方 loader。\n\n## 五、Adapter：支援其他執行時\n\nNext.js 透過 Adapter 支援 Cloudflare Workers、Netlify Edge、Deno 等：\n- `@opennextjs/cloudflare`\n- `@netlify/next`\n- `@vercel/next`（官方）\n\n在 `next.config.js` 中設定對應 Adapter，再依平台文件部署即可。Adapter 會將建置產物轉換成執行時可用的格式。\n\n## 正式環境幾個重點\n\n1. **環境變數**：資料庫、第三方金鑰一律由平台注入；本地用 `.env.local`，切勿提交。\n2. **快取策略**：`.next/static/` 長期快取（immutable），頁面回應搭配 `cacheLife` / `revalidateTag` 管理。\n3. **映像安全**：多階段建置，`runner` 階段使用非 root 使用者。\n4. **觀測性**：啟用 `instrumentation.ts`，串接 OpenTelemetry / Sentry / Datadog。\n5. **Skew Protection**：長時間停留的使用者仍能存取舊版資源；Vercel 自動啟用，自建可用 `deploymentId` 實作。\n\n## 對比 Nuxt\n\n- Nuxt 以 Nitro 為核心，一鍵產出 Node / Cloudflare / Lambda 等 preset。\n- Next.js 的 Adapter 機制方向接近 Nitro preset，只是需額外的適配套件。\n\n選擇部署方式前，先確認三件事：\n- 是否需要 SSR / Server Actions；\n- 是否需要細粒度快取與邊緣能力；\n- 營運成本（代管 vs 自建）可承受至何程度。\n\n依此三點配對，再從上述路徑任選其一上線即可。\n","/books/nuxtjs/chapters/en_US/01-introduction.md":"# Introduction to Nuxt\n\n**Nuxt** is an open-source full-stack framework built on top of **Vue 3** and **Nitro**. It turns a collection of `.vue` files into a production-grade application with server-side rendering, file-based routing, auto-imports, and universal deployment — all out of the box.\n\n## Why pick Nuxt?\n\n- **Conventions over configuration** — an opinionated directory layout automates the boring parts.\n- **SSR by default** — fast first paint, great SEO, works on low-powered devices.\n- **Deploy anywhere** — Node, Deno, static hosting, serverless, edge. No vendor lock-in.\n- **Universal fetching** — composables like `useFetch` / `useAsyncData` work on server and client alike.\n- **Zero-config TypeScript** — auto-generated types and `tsconfig.json`, no setup required.\n- **Modular ecosystem** — Pinia, Tailwind, Content, Image, i18n, Auth Utils… one line to install.\n\n## What's in the box\n\n- **File-based routing** — every file in `app/pages/` becomes a route.\n- **Code splitting** — each route ships the minimum JS.\n- **Auto-imports** — components, composables, and utilities are lifted into scope automatically.\n- **Built-in SEO helpers** — `useHead`, `useSeoMeta`, `<Title>`, `<Meta>`.\n- **Error boundaries** — `error.vue`, `NuxtErrorBoundary`, `createError`.\n- **Hybrid rendering** — per-route `routeRules` for SSR, ISR, SWR, prerender, CSR-only.\n\n## Server engine: Nitro\n\nNitro is the HTTP engine powering Nuxt. It uses `h3` internally and unlocks:\n- Full control over the server side of your app (`server/api`, `server/middleware`).\n- Universal deployment via 15+ presets (Vercel, Netlify, Cloudflare Workers, AWS Lambda, Deno, …).\n- Hybrid rendering through `routeRules`.\n\nIn production Nitro emits a single `.output/` directory that runs on any JavaScript runtime.\n\n## Architecture at a glance\n\nNuxt is a collection of focused packages:\n\n- Core engine: `nuxt`\n- Bundlers: `@nuxt/vite-builder`, `@nuxt/rspack-builder`, `@nuxt/webpack-builder`\n- CLI: `@nuxt/cli`\n- Server engine: `nitro`\n- Dev kit: `@nuxt/kit` (for authoring modules and layers)\n\n## Nuxt vs. Next.js\n\n| Axis                 | Nuxt (Vue)                         | Next.js (React)                       |\n|----------------------|------------------------------------|---------------------------------------|\n| Routing              | File-based under `app/pages/`      | File-based under `app/`               |\n| Rendering            | SSR / SSG / ISR / CSR via `routeRules` | SSR / SSG / PPR / ISR via App Router |\n| Data fetching        | `useFetch`, `useAsyncData`, `$fetch` | Server Components, `fetch`, `use()`  |\n| Server primitives    | Nitro + `server/api`               | Route Handlers + Server Actions       |\n| Runtime targets      | Node, Deno, Workers, Edge, static  | Node, Vercel, adapters                |\n\nBoth ship opinionated app routers. Nuxt leans into conventions and presets; Next.js leans into React Server Components.\n\n## What you'll build in this book\n\nAcross 16 chapters you'll get a practical tour of Nuxt 4:\n- How the filesystem maps to your app (views, pages, layouts, assets).\n- How to fetch, mutate, cache, and share data safely on the server and client.\n- How to ship production features: SEO, transitions, error handling, hybrid rendering.\n- How to extend Nuxt with layers, modules, and custom deployments.\n\nReady? Let's install it and build the first page.\n","/books/nuxtjs/chapters/en_US/02-installation.md":'# Installation\n\nScaffolding a new Nuxt 4 project takes one command. This chapter covers the system prerequisites, the official CLI, the manual path, and the folder layout you\'ll live in for the rest of the book.\n\n## Prerequisites\n\n- **Node.js 20.x** or newer. Prefer an even-numbered LTS release (20, 22, …).\n- **A terminal** and a text editor. VS Code + the Vue (Volar) extension or WebStorm both work great.\n- Windows users: WSL + `127.0.0.1` avoids slow DNS and flaky HMR.\n\n## Scaffold with the CLI\n\n```bash\n# pnpm\npnpm create nuxt@latest my-app\n\n# npm\nnpm create nuxt@latest my-app\n\n# bun\nbun create nuxt@latest my-app\n```\n\nThe CLI asks you to choose a package manager, linter, CSS framework, and so on. Pick what you want, then:\n\n```bash\ncd my-app\npnpm install\npnpm dev -o   # -o opens the browser\n```\n\nOpen `http://localhost:3000` and you should see the default welcome page.\n\n## Manual install\n\nIf you want to understand what\'s happening, start from scratch:\n\n```bash\nmkdir my-app && cd my-app\npnpm init\npnpm add -D nuxt\n```\n\nThen add a minimal `package.json`:\n\n```json\n{\n  "scripts": {\n    "dev": "nuxt dev",\n    "build": "nuxt build",\n    "preview": "nuxt preview"\n  }\n}\n```\n\nCreate `app.vue`:\n\n```vue\n<template>\n  <div>Hello Nuxt 4</div>\n</template>\n```\n\n`pnpm dev` now prints `http://localhost:3000`. Nuxt generates the rest on its own.\n\n## Project structure\n\nHere is the canonical Nuxt 4 layout:\n\n```\nmy-app/\n├── app/\n│   ├── app.vue              # Root component\n│   ├── pages/               # File-based routes\n│   ├── layouts/             # Layout wrappers\n│   ├── components/          # Auto-imported components\n│   ├── composables/         # Auto-imported composables\n│   ├── utils/               # Auto-imported helpers\n│   ├── middleware/          # Route middleware\n│   ├── plugins/             # Client/server plugins\n│   └── assets/              # Processed by Vite (styles, images…)\n├── public/                  # Served as-is from /\n├── server/\n│   ├── api/                 # /api/* handlers\n│   ├── middleware/          # Server middleware\n│   └── plugins/             # Nitro plugins\n├── nuxt.config.ts           # Framework config\n├── tsconfig.json            # Auto-extended from .nuxt/\n└── package.json\n```\n\nTwo things to notice:\n- **Everything under `app/` is auto-imported** — no manual `import` for your own components or composables.\n- **`server/` runs on Nitro**, not Vite. Route handlers live here.\n\n## Development server\n\n```bash\npnpm dev       # start the dev server\npnpm dev -o    # and open a browser\npnpm dev --port 4000\n```\n\nHMR, TypeScript, Vite transform, Nitro dev server — all wired up.\n\n## `nuxt.config.ts` essentials\n\nA minimal `nuxt.config.ts` usually enables Nuxt DevTools and registers a few modules:\n\n```ts\nexport default defineNuxtConfig({\n  devtools: { enabled: true },\n  modules: [\'@pinia/nuxt\', \'@nuxtjs/tailwindcss\'],\n})\n```\n\nThat\'s enough to be productive. The next chapter drills into configuration — env overrides, `runtimeConfig` vs `app.config`, TypeScript integration — but at this point you can already build pages.\n\n## Your first page\n\nDelete the contents of `app.vue` and add:\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n```\n\nCreate `app/pages/index.vue`:\n\n```vue\n<template>\n  <section>\n    <h1>Hello Nuxt 4</h1>\n    <p>File-based routing in 30 seconds.</p>\n  </section>\n</template>\n```\n\nRoutes are generated from `app/pages/`. No router config required.\n',"/books/nuxtjs/chapters/en_US/03-configuration.md":"# Configuration\n\nNuxt ships with sane defaults, so most teams only tweak a handful of options. `nuxt.config.ts` is the single source of truth: modules, env overrides, runtime config, Vite/Nitro passthrough, and Vue-specific flags all live here.\n\n## `nuxt.config.ts`\n\n```ts\nexport default defineNuxtConfig({\n  devtools: { enabled: true },\n  modules: ['@pinia/nuxt', '@nuxtjs/tailwindcss'],\n  css: ['~/assets/css/main.css'],\n})\n```\n\n`defineNuxtConfig` is globally available (no import needed) and provides full TypeScript completion.\n\n## Per-environment overrides\n\nNuxt reads `$development`, `$production`, and named `$env.*` keys before merging into the root config:\n\n```ts\nexport default defineNuxtConfig({\n  $production: {\n    routeRules: {\n      '/**': { isr: true },\n    },\n  },\n  $development: {\n    devtools: { enabled: true },\n  },\n  $env: {\n    staging: { app: { baseURL: '/staging/' } },\n  },\n})\n```\n\nSelect an env at build time with `nuxt build --envName staging`. The mechanism is powered by `c12`.\n\n## `runtimeConfig` vs `app.config`\n\nBoth expose values, but they solve different problems:\n\n| Concern                         | `runtimeConfig`            | `app.config`                 |\n|---------------------------------|-----------------------------|------------------------------|\n| Defined in                      | `nuxt.config.ts`            | `app.config.ts`              |\n| Overridable by env vars         | Yes (`NUXT_*`)              | No                           |\n| Available on client             | Only `public` keys          | Fully bundled                |\n| Supports non-primitive JS types | No                          | Yes                          |\n| Reactive                        | Yes                         | Yes                          |\n| Use it for                      | Secrets, URLs, keys         | Theme, site metadata, flags  |\n\n### `runtimeConfig`\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  runtimeConfig: {\n    apiSecret: '',          // server-only\n    public: {\n      apiBase: '/api',      // exposed to client\n    },\n  },\n})\n```\n\nOverride via environment variables:\n\n```ini\nNUXT_API_SECRET=prod_secret\nNUXT_PUBLIC_API_BASE=https://api.example.com\n```\n\nRead it with the composable:\n\n```vue\n<script setup lang=\"ts\">\nconst { apiSecret, public: { apiBase } } = useRuntimeConfig()\n<\/script>\n```\n\n> `apiSecret` is `undefined` on the client.\n\n### `app.config`\n\n```ts\n// app/app.config.ts\nexport default defineAppConfig({\n  title: 'Hello Nuxt',\n  theme: { dark: true, colors: { primary: '#3b82f6' } },\n})\n```\n\n```vue\n<script setup lang=\"ts\">\nconst appConfig = useAppConfig()\n<\/script>\n```\n\nHMR works for `app.config` — theme changes update instantly during dev.\n\n## TypeScript\n\nNuxt generates `.nuxt/tsconfig.json` automatically and your project `tsconfig.json` extends it. No manual setup required.\n\nTo customize, pass options to Nuxt:\n\n```ts\nexport default defineNuxtConfig({\n  typescript: {\n    strict: true,\n    typeCheck: true, // run vue-tsc during build\n  },\n})\n```\n\n## Passing options to Vite / Nitro / PostCSS\n\nNuxt intentionally does **not** read external `vite.config.ts` / `nitro.config.ts`. Configure them through Nuxt keys:\n\n```ts\nexport default defineNuxtConfig({\n  vite: {\n    plugins: [/* … */],\n  },\n  nitro: {\n    preset: 'node-server',\n    compressPublicAssets: true,\n  },\n  postcss: {\n    plugins: {\n      'postcss-nested': {},\n    },\n  },\n})\n```\n\n## Vue-specific knobs\n\n```ts\nexport default defineNuxtConfig({\n  vue: {\n    propsDestructure: true, // experimental\n  },\n})\n```\n\nNuxt also lets you target `@vitejs/plugin-vue` directly via `vite.vue`.\n\n## App-level head\n\nStatic tags that apply to every page live in `app.head`:\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      titleTemplate: '%s · Nuxt Book',\n      htmlAttrs: { lang: 'en' },\n      link: [{ rel: 'icon', href: '/favicon.ico' }],\n    },\n  },\n})\n```\n\nPer-page, dynamic tags belong in `useHead()` — see the SEO chapter.\n\n## When to pick what\n\n- **Quick, static project flag?** → `app.config`\n- **Secret or env-dependent value?** → `runtimeConfig`\n- **Build-time transform?** → `nuxt.config` (Vite/Nitro keys)\n- **Per-env behavior?** → `$development` / `$production` / `$env`\n\nOne file, four knobs, enough to ship.\n","/books/nuxtjs/chapters/en_US/04-views.md":'# Views\n\nNuxt gives you four layers to build the UI: `app.vue`, **components**, **pages**, and **layouts**. Each has a clear job, and Nuxt auto-wires them so you mostly add files and the rest just works.\n\n## `app.vue`\n\nNuxt treats `app.vue` as the entrypoint. If you don\'t need routing at all, you can stop here:\n\n```vue\n<template>\n  <div>\n    <h1>Welcome</h1>\n  </div>\n</template>\n```\n\nWhen you want per-route pages, include `<NuxtPage />`:\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n```\n\nIf you **delete** `app.vue`, Nuxt generates a default entry that mounts `<NuxtLayout><NuxtPage /></NuxtLayout>`.\n\n## Components\n\nAnything under `app/components/` is auto-imported — no more `import Button from \'./Button.vue\'`.\n\n```\napp/components/\n├── AppHeader.vue\n├── AppFooter.vue\n└── AppAlert.vue\n```\n\n```vue\n<template>\n  <div>\n    <AppHeader />\n    <AppAlert>Hello from an auto-imported component.</AppAlert>\n    <AppFooter />\n  </div>\n</template>\n```\n\nNested folders become name prefixes: `app/components/base/Button.vue` → `<BaseButton />`.\n\nTips:\n- Use PascalCase in `<template>` for clarity.\n- Add `Lazy` to defer loading: `<LazyAppHeavy />`.\n\n## Pages\n\nPages live in `app/pages/`. Every `.vue` file is a route:\n\n```\napp/pages/\n├── index.vue          →  /\n├── about.vue          →  /about\n└── blog/\n    ├── index.vue      →  /blog\n    └── [id].vue       →  /blog/:id\n```\n\n```vue\n\x3c!-- app/pages/about.vue --\x3e\n<template>\n  <section>\n    <h1>About us</h1>\n  </section>\n</template>\n```\n\nPages are code-split, prefetched by `<NuxtLink>`, and can declare per-page metadata with `definePageMeta({ … })`.\n\n## Layouts\n\nA layout wraps one or more pages so you can share chrome (header, footer, sidebar). Files live in `app/layouts/`:\n\n```vue\n\x3c!-- app/layouts/default.vue --\x3e\n<template>\n  <div>\n    <AppHeader />\n    <slot />\n    <AppFooter />\n  </div>\n</template>\n```\n\n`default.vue` applies automatically. For a custom layout, opt in per-page:\n\n```vue\n<script setup lang="ts">\ndefinePageMeta({ layout: \'admin\' })\n<\/script>\n\n<template>\n  <div>Dashboard</div>\n</template>\n```\n\n…and put the wrapper at `app/layouts/admin.vue`.\n\n## `<NuxtLayout>` and `<NuxtPage>`\n\n- `<NuxtPage />` is the "outlet" for file-based routes. It knows how to stream, prefetch, and animate.\n- `<NuxtLayout>` decides which layout wraps the current page. It accepts `name` for manual control:\n\n```vue\n<NuxtLayout name="admin">\n  <NuxtPage />\n</NuxtLayout>\n```\n\nIf you have **only one layout**, skip layouts entirely and put the shell directly in `app.vue`.\n\n## Building blocks recap\n\n| Need                                  | Use this                     |\n|---------------------------------------|-------------------------------|\n| Root entry / app-wide chrome          | `app.vue` (or single layout)  |\n| Reusable UI piece                     | Component in `app/components/`|\n| Route + view                          | Page in `app/pages/`          |\n| Shared shell around several pages     | Layout in `app/layouts/`      |\n\n## Advanced: customizing the HTML template\n\nWhen you need to tweak the raw HTML that Nitro emits (beyond `useHead`), register a Nitro plugin with the `render:html` hook:\n\n```ts\n// server/plugins/meta.ts\nexport default defineNitroPlugin((nitroApp) => {\n  nitroApp.hooks.hook(\'render:html\', (html, { event }) => {\n    html.head.push(\'<meta name="description" content="My custom description" />\')\n  })\n})\n```\n\nUse this sparingly — `useHead` covers 99% of cases, and the next chapter ("Assets") plus the SEO chapter show the ergonomic paths.\n',"/books/nuxtjs/chapters/en_US/05-assets.md":"# Assets\n\nNuxt gives you two directories for assets and they behave very differently. Pick the right one and you save yourself broken URLs, big bundles, and cache-busting headaches.\n\n## `public/` — served as-is\n\nEverything inside `public/` is served at the root URL **without processing**:\n\n```\npublic/\n├── favicon.ico\n├── robots.txt\n└── img/\n    └── hero.png          → /img/hero.png\n```\n\nReference it with an absolute path:\n\n```vue\n<template>\n  <img src=\"/img/hero.png\" alt=\"Hero\" />\n</template>\n```\n\nUse `public/` for:\n- Favicons, `robots.txt`, OG defaults, `sitemap.xml`\n- Images you want at stable, predictable URLs\n- Files shared with marketing / CDN config\n\nThere is **no fingerprinting**. Browsers may cache these forever, so rename on change or set custom cache headers.\n\n## `app/assets/` — processed by Vite\n\nEverything inside `app/assets/` goes through Vite: minification, hashing, optimized imports. Reference it with `~/` or `@/`:\n\n```vue\n<template>\n  <img src=\"~/assets/img/hero.png\" alt=\"Hero\" />\n</template>\n```\n\nUse `app/assets/` for:\n- CSS / SCSS / Less files\n- SVGs you want inlined or transformed\n- Images you want fingerprinted and optimized\n- Fonts bundled with the app\n\nFiles here are **not** available at `/assets/…` in the browser. Vite rewrites them to a hashed URL like `/_nuxt/hero-abc123.png`.\n\n## Quick comparison\n\n| Behavior                       | `public/`           | `app/assets/`            |\n|--------------------------------|----------------------|---------------------------|\n| Served at root URL             | Yes (`/foo.png`)     | No (rewritten by Vite)    |\n| Processed / minified           | No                   | Yes                       |\n| Filename fingerprinted         | No                   | Yes                       |\n| Reference from Vue             | `/img/foo.png`       | `~/assets/img/foo.png`    |\n| Good for                       | Stable public URLs   | Optimized build artifacts |\n\n## Images in depth: `<NuxtImg>` and `<NuxtPicture>`\n\nFor serious image work, install `@nuxt/image`:\n\n```bash\npnpm add -D @nuxt/image\n```\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  modules: ['@nuxt/image'],\n  image: {\n    quality: 80,\n    format: ['webp', 'avif'],\n  },\n})\n```\n\nThen use `<NuxtImg>` / `<NuxtPicture>` anywhere:\n\n```vue\n<template>\n  <NuxtImg\n    src=\"/img/hero.png\"\n    width=\"1200\"\n    height=\"630\"\n    loading=\"lazy\"\n    sizes=\"sm:100vw md:50vw lg:400px\"\n  />\n</template>\n```\n\nIt handles:\n- Responsive `srcset`\n- Automatic WebP / AVIF\n- Lazy loading\n- Third-party providers (Cloudinary, IPX, imgix, Vercel, …)\n\n## Fonts\n\nNuxt has a dedicated `@nuxt/fonts` module that self-hosts Google Fonts and local fonts while preventing CLS:\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@nuxt/fonts'],\n  fonts: {\n    families: [\n      { name: 'Inter', provider: 'google' },\n    ],\n  },\n})\n```\n\nThen just use the family in CSS — the module injects the `@font-face` rules for you.\n\nPrefer local fonts? Drop them in `public/fonts/` and declare:\n\n```css\n/* app/assets/css/fonts.css */\n@font-face {\n  font-family: 'Display';\n  src: url('/fonts/display.woff2') format('woff2');\n  font-display: swap;\n}\n```\n\n## Rule of thumb\n\n- **Stable, public URL?** → `public/`\n- **Need hashing / optimization?** → `app/assets/`\n- **Images at scale?** → `@nuxt/image`\n- **Fonts?** → `@nuxt/fonts` or `public/fonts/`\n\nPick once per asset type and stick to the convention.\n","/books/nuxtjs/chapters/en_US/06-styling.md":"# Styling\n\nNuxt is not opinionated about CSS. Write plain CSS, pull in a preprocessor, go all-in on Tailwind or UnoCSS, or embrace CSS Modules — all paths are first-class.\n\n## Local stylesheets\n\nPut your source files under `app/assets/` and reference them from any component:\n\n```vue\n<script>\nimport '~/assets/css/first.css'\n<\/script>\n\n<style>\n@import url(\"~/assets/css/second.css\");\n</style>\n```\n\nTo apply a stylesheet globally, use the `css` key in `nuxt.config.ts`:\n\n```ts\nexport default defineNuxtConfig({\n  css: ['~/assets/css/main.css'],\n})\n```\n\nNuxt inlines global CSS into the server-rendered HTML, so the first paint is already styled.\n\n## External stylesheets\n\nShip a `<link>` from your config:\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      link: [\n        { rel: 'stylesheet', href: 'https://cdn.example.com/reset.css' },\n      ],\n    },\n  },\n})\n```\n\nOr dynamically via `useHead`:\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  link: [{ rel: 'stylesheet', href: 'https://cdn.example.com/theme.css' }],\n})\n<\/script>\n```\n\nExternal stylesheets are render-blocking — prefer bundling them when you can.\n\n## Preprocessors (SCSS / Sass / Less / Stylus)\n\nInstall the preprocessor and Vite picks it up automatically:\n\n```bash\npnpm add -D sass\n```\n\n```vue\n<style lang=\"scss\">\n@use \"~/assets/scss/main.scss\";\n</style>\n```\n\nOr register it globally:\n\n```ts\nexport default defineNuxtConfig({\n  css: ['~/assets/scss/main.scss'],\n})\n```\n\nNeed partials injected into every file (design tokens, variables)? Use Vite's `preprocessorOptions`:\n\n```ts\nexport default defineNuxtConfig({\n  vite: {\n    css: {\n      preprocessorOptions: {\n        scss: { additionalData: '@use \"~/assets/_colors.scss\" as *;' },\n      },\n    },\n  },\n})\n```\n\n## Single-file component styling\n\nThis is where Vue shines. Inside any component:\n\n```vue\n<script setup lang=\"ts\">\nconst isActive = ref(true)\nconst color = ref('crimson')\n<\/script>\n\n<template>\n  <div :class=\"['box', { active: isActive }]\">\n    <p :style=\"{ color }\">hello</p>\n  </div>\n</template>\n\n<style scoped>\n.box { padding: 1rem; }\n.active { outline: 2px solid v-bind(color); }\n</style>\n```\n\nNotable features:\n- **`scoped`** — styles apply only to this component.\n- **`v-bind()`** — reactive values inside `<style>`.\n- **`module`** — CSS Modules via `$style`:\n\n```vue\n<template>\n  <p :class=\"$style.red\">red</p>\n</template>\n<style module>\n.red { color: red; }\n</style>\n```\n\n- **`lang=\"scss\"`** (or `sass`, `less`, `stylus`) — preprocessor per block.\n\n## PostCSS\n\nNuxt ships PostCSS pre-configured with `postcss-import`, `postcss-url`, `autoprefixer`, and `cssnano`. Add more plugins from `nuxt.config.ts`:\n\n```ts\nexport default defineNuxtConfig({\n  postcss: {\n    plugins: {\n      'postcss-nested': {},\n      'postcss-custom-media': {},\n    },\n  },\n})\n```\n\nUse `<style lang=\"postcss\">` for proper highlighting in SFCs.\n\n## Tailwind CSS / UnoCSS\n\nFor utility-first CSS, both integrate via official modules:\n\n```bash\npnpm add -D @nuxtjs/tailwindcss\n```\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@nuxtjs/tailwindcss'],\n})\n```\n\nor UnoCSS:\n\n```bash\npnpm add -D @unocss/nuxt\n```\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@unocss/nuxt'],\n})\n```\n\nBoth auto-inject the generated CSS — no manual `import 'uno.css'` needed.\n\n## Layouts as style boundaries\n\nWhen sections of your app need fundamentally different styling (e.g. a dark admin vs light marketing), layouts are the cleanest split:\n\n```vue\n\x3c!-- app/layouts/admin.vue --\x3e\n<template>\n  <div class=\"admin-shell\"><slot /></div>\n</template>\n\n<style>\n.admin-shell { background: #0f172a; color: #e2e8f0; }\n</style>\n```\n\n## Choosing a strategy\n\n| If you want…                                 | Pick                        |\n|----------------------------------------------|-----------------------------|\n| Utility classes, small teams                 | Tailwind / UnoCSS           |\n| Component-scoped styles                      | `<style scoped>`            |\n| Class-name collisions impossible             | `<style module>`            |\n| A design system with tokens                  | SCSS partials + `additionalData` |\n| A themeable library                          | `app.config` + CSS variables |\n\nThere is no wrong answer — Nuxt lets you mix all of them in one app.\n","/books/nuxtjs/chapters/en_US/07-routing.md":"# Routing\n\nNuxt's router is built on **vue-router**, but you almost never configure it by hand. Instead, every file under `app/pages/` becomes a route — automatic code-splitting, automatic prefetching, automatic types.\n\n## File-based routing\n\n```\napp/pages/\n├── index.vue          →  /\n├── about.vue          →  /about\n└── posts/\n    ├── index.vue      →  /posts\n    └── [id].vue       →  /posts/:id\n```\n\nNuxt emits a fully typed route tree, including typed `to` values for `<NuxtLink>`.\n\n## Dynamic segments\n\nSquare brackets turn into params:\n\n```vue\n\x3c!-- app/pages/posts/[id].vue --\x3e\n<script setup lang=\"ts\">\nconst route = useRoute()\nconsole.log(route.params.id) // string\n<\/script>\n```\n\nMultiple params work too: `app/pages/users/[userId]/posts/[postId].vue` → `/users/:userId/posts/:postId`.\n\n**Catch-all**: `app/pages/docs/[...slug].vue` matches `/docs/a/b/c` with `route.params.slug = ['a', 'b', 'c']`.\n\n## Nested routes\n\nPair `parent.vue` with a `parent/` folder. Use `<NuxtPage />` inside the parent to render children:\n\n```vue\n\x3c!-- app/pages/settings.vue --\x3e\n<template>\n  <section>\n    <aside>\x3c!-- nav --\x3e</aside>\n    <NuxtPage />\n  </section>\n</template>\n```\n\n```\napp/pages/\n├── settings.vue\n└── settings/\n    ├── profile.vue      →  /settings/profile\n    └── billing.vue      →  /settings/billing\n```\n\n## Navigation with `<NuxtLink>`\n\nUse `<NuxtLink>` for every internal navigation:\n\n```vue\n<template>\n  <nav>\n    <NuxtLink to=\"/\">Home</NuxtLink>\n    <NuxtLink to=\"/about\">About</NuxtLink>\n    <NuxtLink :to=\"{ name: 'posts-id', params: { id: 1 } }\">Post 1</NuxtLink>\n  </nav>\n</template>\n```\n\nBehind the scenes:\n- Renders a real `<a>` (great for accessibility).\n- Prefetches the component and payload when the link enters the viewport.\n- Performs client-side transitions after hydration — no full reloads.\n- Adds `.router-link-active` / `.router-link-exact-active` classes.\n\nOpt out of prefetching selectively:\n\n```vue\n<NuxtLink to=\"/heavy\" :prefetch=\"false\">Heavy page</NuxtLink>\n```\n\n## Programmatic navigation\n\n```vue\n<script setup lang=\"ts\">\nconst router = useRouter()\nfunction openPost (id: number) {\n  router.push(`/posts/${id}`)\n}\n<\/script>\n```\n\nIn middleware, plugins, and event handlers prefer `navigateTo()`:\n\n```ts\nexport default defineNuxtRouteMiddleware((to) => {\n  if (!to.path.startsWith('/public')) return navigateTo('/login')\n})\n```\n\n## Route middleware\n\nThree flavors:\n1. **Inline** — defined right in the page via `definePageMeta({ middleware: (to) => {…} })`.\n2. **Named** — file in `app/middleware/auth.ts`, opt-in per page.\n3. **Global** — `app/middleware/foo.global.ts` runs on every navigation.\n\n```ts\n// app/middleware/auth.ts\nexport default defineNuxtRouteMiddleware((to) => {\n  const { user } = useUserSession()\n  if (!user.value) return navigateTo('/login')\n})\n```\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ middleware: 'auth' })\n<\/script>\n```\n\n> Route middleware runs only in the Vue (client & SSR) layer. Server/API requests go through `server/middleware/` instead.\n\n## Route validation\n\nReject invalid params with `validate`:\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  validate (route) {\n    return typeof route.params.id === 'string' && /^\\d+$/.test(route.params.id)\n  },\n})\n<\/script>\n```\n\nA falsy result triggers a 404. Return an object `{ status, statusText }` for custom responses.\n\n## `definePageMeta` at a glance\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  layout: 'admin',          // choose layout\n  middleware: ['auth'],     // route middleware\n  pageTransition: false,    // disable transitions\n  validate: (r) => true,    // validation\n  key: (r) => r.path,       // re-render strategy\n})\n<\/script>\n```\n\n`key` is useful when navigating between same-route params (`/posts/1` → `/posts/2`) and you want the component torn down and re-created.\n\n## Summary\n\n- Put `.vue` files under `app/pages/` — you already have routing.\n- Use `<NuxtLink>` everywhere for prefetching and transitions.\n- Use middleware for auth, redirects, and per-route guards.\n- Use `definePageMeta` to declare layout, middleware, validation, and more.\n","/books/nuxtjs/chapters/en_US/08-seo-meta.md":"# SEO and Meta\n\nNuxt uses [Unhead](https://unhead.unjs.io/) under the hood for head management. You get reactive composables, type-safe SEO helpers, and built-in components — use whichever style fits your team.\n\n## `nuxt.config.ts` — global defaults\n\nSet tags that don't change per page:\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      titleTemplate: '%s · Nuxt Book',\n      htmlAttrs: { lang: 'en' },\n      link: [{ rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' }],\n    },\n  },\n})\n```\n\nNuxt also injects sensible defaults:\n- `<meta charset=\"utf-8\">`\n- `<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">`\n\n## `useHead` — reactive composable\n\n```vue\n<script setup lang=\"ts\">\nconst title = ref('Hello World')\n\nuseHead({\n  title,\n  meta: [{ name: 'description', content: 'My amazing site.' }],\n  bodyAttrs: { class: 'app' },\n  script: [{ innerHTML: 'console.log(\"loaded\")' }],\n})\n<\/script>\n```\n\n`useHead` accepts reactive values and deduplicates tags across components.\n\n## `useSeoMeta` — type-safe SEO\n\n`useSeoMeta` knows the difference between `name` and `property`, so typos and common mistakes are caught by the type system:\n\n```vue\n<script setup lang=\"ts\">\nuseSeoMeta({\n  title: 'My Amazing Site',\n  description: 'This is my amazing site, let me tell you all about it.',\n  ogTitle: 'My Amazing Site',\n  ogDescription: 'This is my amazing site, let me tell you all about it.',\n  ogImage: 'https://example.com/og.png',\n  twitterCard: 'summary_large_image',\n})\n<\/script>\n```\n\nThis is the path I recommend for most SEO work.\n\n## Component-style meta\n\nIf you prefer declarative templates:\n\n```vue\n<script setup lang=\"ts\">\nconst title = ref('Hello World')\n<\/script>\n\n<template>\n  <Head>\n    <Title>{{ title }}</Title>\n    <Meta name=\"description\" :content=\"title\" />\n  </Head>\n</template>\n```\n\nSupported tags: `<Title>`, `<Meta>`, `<Link>`, `<Script>`, `<Style>`, `<Base>`, `<NoScript>`, `<Html>`, `<Body>`.\n\n## Title template\n\nSite-wide titles usually follow a pattern like \"Page — Site Name\":\n\n```vue\n\x3c!-- app.vue --\x3e\n<script setup lang=\"ts\">\nuseHead({\n  titleTemplate: (title) => title ? `${title} · My Site` : 'My Site',\n})\n<\/script>\n```\n\nAny page that calls `useHead({ title: 'Blog' })` shows up as `Blog · My Site`.\n\n## Template params\n\nNeed multiple placeholders?\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  titleTemplate: (title) => title ? `${title} %separator %siteName` : '%siteName',\n  templateParams: { siteName: 'My Site', separator: '·' },\n})\n<\/script>\n```\n\n## Reactivity\n\nEvery property accepts computed, refs, or getters — so you can follow `route` or API data easily:\n\n```vue\n<script setup lang=\"ts\">\nconst route = useRoute()\nconst post = await $fetch(`/api/posts/${route.params.id}`)\n\nuseSeoMeta({\n  title: () => post.title,\n  description: () => post.excerpt,\n  ogImage: () => post.image,\n})\n<\/script>\n```\n\n## Linking from `definePageMeta`\n\nSometimes you want per-page static info that other layers can read. Store it in `definePageMeta` and pipe it into `useHead`:\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ title: 'Some Page' })\n<\/script>\n```\n\n```vue\n\x3c!-- default layout --\x3e\n<script setup lang=\"ts\">\nconst route = useRoute()\nuseHead({\n  meta: [{ property: 'og:title', content: `My Site · ${route.meta.title}` }],\n})\n<\/script>\n```\n\n## `useHead` vs the alternatives\n\n| You want…                         | Reach for…             |\n|-----------------------------------|-------------------------|\n| Site-wide constants               | `app.head` in config    |\n| Reactive, arbitrary head tags     | `useHead`               |\n| SEO tags with type safety         | `useSeoMeta`            |\n| Template-first authors            | `<Head>`, `<Title>`, …  |\n\n## Body tags\n\nSome scripts belong at the bottom of `<body>`. Use `tagPosition`:\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  script: [\n    {\n      src: 'https://third-party-script.com',\n      tagPosition: 'bodyClose',\n    },\n  ],\n})\n<\/script>\n```\n\n## `useHeadSafe`\n\nWhen you need to merge user-provided values into the head, prefer `useHeadSafe`. It strips dangerous attributes (e.g. `innerHTML`, event handlers) to avoid XSS.\n\nWell-wired metadata is one of the cheapest performance wins Nuxt gives you — treat it as code, not an afterthought.\n","/books/nuxtjs/chapters/en_US/09-transitions.md":"# Transitions\n\nNuxt reuses Vue's `<Transition>` to animate between pages and layouts. It also ships an experimental integration with the native View Transitions API for cross-page animations.\n\n## Page transitions\n\nEnable a site-wide page transition in `nuxt.config.ts`:\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    pageTransition: { name: 'page', mode: 'out-in' },\n  },\n})\n```\n\nAdd matching CSS to `app.vue`:\n\n```vue\n<template>\n  <NuxtPage />\n</template>\n\n<style>\n.page-enter-active,\n.page-leave-active {\n  transition: all 0.4s;\n}\n.page-enter-from,\n.page-leave-to {\n  opacity: 0;\n  filter: blur(1rem);\n}\n</style>\n```\n\nEvery route change now fades with a subtle blur.\n\nTo override the transition on a specific page, use `definePageMeta`:\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: { name: 'rotate', mode: 'out-in' },\n})\n<\/script>\n```\n\n## Layout transitions\n\nIf your navigation changes the layout (e.g. default → admin), page transitions won't run. Use `layoutTransition` instead:\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    layoutTransition: { name: 'layout', mode: 'out-in' },\n  },\n})\n```\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n\n<style>\n.layout-enter-active,\n.layout-leave-active { transition: all 0.4s; }\n.layout-enter-from,\n.layout-leave-to { filter: grayscale(1); }\n</style>\n```\n\n## Disabling transitions\n\nPer page:\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ pageTransition: false, layoutTransition: false })\n<\/script>\n```\n\nGlobally:\n\n```ts\nexport default defineNuxtConfig({\n  app: { pageTransition: false, layoutTransition: false },\n})\n```\n\n## JavaScript hooks\n\nFor anything beyond CSS, use the `<Transition>` JS hooks — great with GSAP, Motion One, or anime.js:\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: {\n    name: 'custom-flip',\n    mode: 'out-in',\n    onBeforeEnter: (el) => { /* prepare */ },\n    onEnter: (el, done) => { /* animate then */ done() },\n    onAfterEnter: (el) => { /* cleanup */ },\n  },\n})\n<\/script>\n```\n\n## Dynamic transitions\n\nWant different animations depending on navigation direction (pagination, swipe)? Pick the name inside an inline middleware:\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: { name: 'slide-right', mode: 'out-in' },\n  middleware (to, from) {\n    if (typeof to.meta.pageTransition !== 'boolean') {\n      to.meta.pageTransition!.name =\n        +to.params.id! > +from.params.id! ? 'slide-left' : 'slide-right'\n    }\n  },\n})\n<\/script>\n```\n\n## `<NuxtPage>` transitions\n\nIf you drop layouts and put `<NuxtPage>` directly in `app.vue`, pass the transition inline:\n\n```vue\n<template>\n  <NuxtPage :transition=\"{ name: 'bounce', mode: 'out-in' }\" />\n</template>\n```\n\nTransitions set this way **cannot** be overridden per page.\n\n## View Transitions API (experimental)\n\nThe native View Transitions API lets you animate between entirely different DOM trees — great for hero image transitions between routes.\n\nEnable it:\n\n```ts\nexport default defineNuxtConfig({\n  experimental: { viewTransition: true },\n})\n```\n\nValues: `false`, `true`, `'always'`. `true` respects `prefers-reduced-motion: reduce`.\n\n### Per-page control\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  viewTransition: {\n    enabled: true,\n    types: ['slide'],\n    toTypes: ['slide-in'],\n    fromTypes: ['slide-out'],\n  },\n})\n<\/script>\n```\n\nTypes can also be functions returning an array for dynamic behavior (e.g. \"slide left when id increases\").\n\n### CSS targeting\n\n```css\n::view-transition-old(root),\n::view-transition-new(root) { animation-duration: 0.3s; }\n\nhtml:active-view-transition-type(slide-left) {\n  &::view-transition-old(root) { animation: slide-out-left 0.3s ease-in-out; }\n  &::view-transition-new(root) { animation: slide-in-right 0.3s ease-in-out; }\n}\n```\n\n### Disable Vue transitions when View Transitions are available\n\nAvoid double animations with a tiny middleware:\n\n```ts\n// app/middleware/disable-vue-transitions.global.ts\nexport default defineNuxtRouteMiddleware((to) => {\n  if (false || !document.startViewTransition) return\n  to.meta.pageTransition = false\n  to.meta.layoutTransition = false\n})\n```\n\n## Caveat\n\nView Transitions freeze the DOM while running. If you rely on data fetched inside `<script setup>` that resolves mid-navigation, you may see a visual stall. Stick to Vue transitions until the API stabilizes for that case.\n\nTransitions make navigation feel native — a tiny bit of CSS goes a long way.\n","/books/nuxtjs/chapters/en_US/10-data-fetching.md":"# Data Fetching\n\nNuxt gives you four data-fetching primitives: `useFetch`, `useAsyncData`, `useLazyFetch`, and `$fetch`. They all share one goal — fetch on the server for SSR, hydrate on the client, and dedupe by key — but each solves a different case.\n\n## `useFetch` — the 90% case\n\n```vue\n<script setup lang=\"ts\">\nconst { data, pending, error, refresh, status } = await useFetch('/api/posts')\n<\/script>\n\n<template>\n  <p v-if=\"pending\">Loading…</p>\n  <p v-else-if=\"error\">Something went wrong.</p>\n  <ul v-else>\n    <li v-for=\"p in data\" :key=\"p.id\">{{ p.title }}</li>\n  </ul>\n</template>\n```\n\nWhat it does for free:\n- Runs on the server during SSR and on the client during navigation.\n- Caches by URL + options (reused across components).\n- Deduplicates concurrent requests.\n- Types the response via Nitro's inferred API types.\n\nCommon options:\n\n```ts\nawait useFetch('/api/posts', {\n  query: { page: 1 },\n  headers: { 'x-role': 'admin' },\n  transform: (posts) => posts.map(p => ({ ...p, shortTitle: p.title.slice(0, 32) })),\n  pick: ['id', 'title'],\n  watch: [search],           // refetch when these refs change\n  default: () => [],         // initial value before first resolution\n})\n```\n\n## `useAsyncData` — arbitrary async work\n\nWhen you need more than a `fetch` (e.g. `$fetch` with computed URL, a DB query from a Nitro plugin, or composing several sources), reach for `useAsyncData`. **Supply a stable cache key.**\n\n```vue\n<script setup lang=\"ts\">\nconst route = useRoute()\nconst { data: post } = await useAsyncData(\n  () => `post-${route.params.id}`,\n  () => $fetch(`/api/posts/${route.params.id}`),\n  { watch: [() => route.params.id] }\n)\n<\/script>\n```\n\n`useFetch` is actually a thin wrapper around `useAsyncData(key, () => $fetch(url, opts))`.\n\n## `useLazyFetch` — don't block navigation\n\n`useFetch` awaits by default — the page waits for data before rendering. For secondary sections that should load in the background, use `useLazyFetch` (or `useFetch(..., { lazy: true })`):\n\n```vue\n<script setup lang=\"ts\">\nconst { data, pending } = useLazyFetch('/api/recommendations')\n<\/script>\n\n<template>\n  <SkeletonList v-if=\"pending\" />\n  <RecommendationList v-else :items=\"data\" />\n</template>\n```\n\n## `$fetch` — direct HTTP for events\n\n`$fetch` is the underlying HTTP client (built on `ofetch`). Use it for imperative calls in event handlers, mutations, or server routes — **not** as your primary data source on a page (it won't participate in the SSR payload).\n\n```vue\n<script setup lang=\"ts\">\nasync function createPost () {\n  return await $fetch('/api/posts', {\n    method: 'POST',\n    body: { title: 'New post' },\n  })\n}\n<\/script>\n```\n\n## Refetching\n\n`useFetch` / `useAsyncData` expose `refresh()`:\n\n```ts\nconst { data, refresh } = await useFetch('/api/posts')\n\nasync function addPost () {\n  await $fetch('/api/posts', { method: 'POST', body: {/* … */} })\n  await refresh()\n}\n```\n\nNeed to revalidate a `useAsyncData` call from elsewhere (e.g. after a mutation in another component)? Share the key:\n\n```ts\nawait refreshNuxtData('posts')        // refresh by key\nawait refreshNuxtData()               // refresh everything\n```\n\n## Server routes: where the data comes from\n\nPlace handlers in `server/api/`. Filenames map HTTP methods via a `.<method>.ts` suffix:\n\n```ts\n// server/api/posts.get.ts\nexport default defineEventHandler(() => {\n  return [{ id: 1, title: 'Hello' }]\n})\n```\n\n```ts\n// server/api/posts.post.ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody<{ title: string }>(event)\n  // insert into DB…\n  return { ok: true }\n})\n```\n\nExposed as `/api/posts`. Consumers use `useFetch('/api/posts')` or `$fetch('/api/posts', ...)`.\n\n## Typing API routes\n\nNuxt auto-generates types from your `server/` files, so both `useFetch` and `$fetch` know the response shape:\n\n```ts\nconst { data } = await useFetch('/api/posts') // data: Post[]\n```\n\nTo export the response type for reuse:\n\n```ts\n// server/api/posts.get.ts\nexport type PostsResponse = Awaited<ReturnType<typeof handler>>\nconst handler = defineEventHandler(() => fetchPosts())\nexport default handler\n```\n\n## Rules of thumb\n\n- **Page-level data for SSR?** → `useFetch`.\n- **Custom async logic + stable cache?** → `useAsyncData`.\n- **Non-blocking secondary data?** → `useLazyFetch` / `lazy: true`.\n- **User-triggered mutation?** → `$fetch` + `refresh()`.\n- **Multiple components need the same data?** → Same key in `useAsyncData` / `useFetch`.\n\nDo this, and your pages are SSR-ready, prefetch-friendly, and never make the same request twice.\n","/books/nuxtjs/chapters/en_US/11-state-management.md":"# State Management\n\nNuxt ships `useState` — an SSR-safe `ref` that survives server → client hydration and can be shared across components by key. It covers most \"global state\" needs. For heavier domains, reach for Pinia.\n\n## `useState` basics\n\n```vue\n<script setup lang=\"ts\">\nconst counter = useState('counter', () => Math.round(Math.random() * 1000))\n<\/script>\n\n<template>\n  <div>\n    Counter: {{ counter }}\n    <button @click=\"counter++\">+</button>\n    <button @click=\"counter--\">-</button>\n  </div>\n</template>\n```\n\nKey points:\n- The first arg is a **unique key** — same key means same value across the app.\n- The second arg is an **initializer**, run only when the state is first created.\n- The value is serialized to JSON for SSR hydration. Don't put classes, functions, or symbols in it.\n\n### Why not `const state = ref()` at module scope?\n\nTop-level refs are **shared across requests** on the server — a classic source of cross-user data leaks and memory leaks. Always wrap state in a composable:\n\n```ts\n// app/composables/useCart.ts\nexport const useCart = () => useState('cart', () => [] as CartItem[])\n```\n\nAny component can then do:\n\n```ts\nconst cart = useCart()\n```\n\n## Initializing with async data\n\nUse `callOnce` so the initialization only runs once per request:\n\n```vue\n<script setup lang=\"ts\">\nconst websiteConfig = useState('config', () => null)\n\nawait callOnce(async () => {\n  websiteConfig.value = await $fetch('/api/site-config')\n})\n<\/script>\n```\n\nThis mirrors Nuxt 2's `nuxtServerInit` — populate state once during SSR, and hydrate it to the client.\n\n## Shared, auto-imported composables\n\nBecause `app/composables/` is auto-imported, tiny composables become powerful primitives:\n\n```ts\n// app/composables/useColor.ts\nexport const useColor = () => useState<string>('color', () => 'pink')\n```\n\n```vue\n<script setup lang=\"ts\">\nconst color = useColor()\n<\/script>\n\n<template>\n  <p>Current color: {{ color }}</p>\n</template>\n```\n\n## Clearing state\n\nIf you store temporary data that should reset after navigation or logout:\n\n```ts\nawait clearNuxtState('cart')     // one key\nawait clearNuxtState()           // everything\n```\n\n## Combining server state with client prefs\n\nA realistic pattern: read a server-detected default, then let the client override it:\n\n```ts\nexport const useLocale = () => {\n  return useState<string>('locale', () => useDefaultLocale().value)\n}\n\nexport const useDefaultLocale = (fallback = 'en-US') => {\n  const locale = ref(fallback)\n  if (false) {\n    const reqLocale = useRequestHeaders()['accept-language']?.split(',')[0]\n    if (reqLocale) locale.value = reqLocale\n  } else if (true) {\n    const nav = navigator.language\n    if (nav) locale.value = nav\n  }\n  return locale\n}\n```\n\n## Pinia for larger stores\n\nWhen you have multiple related actions, modules, and computed views, install Pinia:\n\n```bash\npnpm add pinia @pinia/nuxt\n```\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  modules: ['@pinia/nuxt'],\n})\n```\n\n```ts\n// app/stores/website.ts\nexport const useWebsiteStore = defineStore('website', {\n  state: () => ({ name: '', description: '' }),\n  actions: {\n    async fetch () {\n      const info = await $fetch('/api/site-config')\n      this.name = info.name\n      this.description = info.description\n    },\n  },\n})\n```\n\n```vue\n<script setup lang=\"ts\">\nconst website = useWebsiteStore()\nawait callOnce(website.fetch)\n<\/script>\n```\n\nPinia's state is automatically serialized across SSR, just like `useState`.\n\n## Alternatives\n\nNuxt isn't opinionated. If you want:\n- Immutable state → [Harlem](https://nuxt.com/modules/harlem)\n- State machines → [XState](https://nuxt.com/modules/xstate)\n- Lightweight signals → use `ref` / `computed` inside composables\n\nMix and match — the only rule is never hold mutable state at module scope on the server.\n\n## When to reach for what\n\n| Need                              | Use                              |\n|-----------------------------------|----------------------------------|\n| A few values shared site-wide     | `useState` + composable          |\n| Derived, action-heavy logic       | Pinia store                      |\n| Per-request data from the server  | `useState` + `callOnce`          |\n| Formal state machine              | XState + `@nuxtjs/xstate`        |\n\nKeep the footprint small until your UI tells you it needs more.\n","/books/nuxtjs/chapters/en_US/12-error-handling.md":"# Error Handling\n\nNuxt is full-stack, so errors can happen on four fronts: Vue rendering, app startup, Nitro server code, and JS chunk downloads. Each has a distinct toolkit.\n\n## Vue errors\n\nInside components, `onErrorCaptured` works as in any Vue app. Globally, register once via a plugin:\n\n```ts\n// app/plugins/error-report.ts\nexport default defineNuxtPlugin((nuxtApp) => {\n  nuxtApp.vueApp.config.errorHandler = (error, instance, info) => {\n    // ship to Sentry / Datadog / your backend\n  }\n\n  nuxtApp.hook('vue:error', (error, instance, info) => {\n    // runs even for handled errors\n  })\n})\n```\n\n`vue:error` is Nuxt's version of `onErrorCaptured` — it receives every error that propagates up the Vue tree.\n\n## Startup errors\n\nErrors thrown during plugin init, SSR, or client mount fire the `app:error` hook. Listen to it to reset analytics, log, or purge caches.\n\n## Nitro server errors\n\nServer-side errors can't be caught by a Vue boundary. They render the error page instead (see below). For custom behavior, throw `createError({...})` inside your event handler:\n\n```ts\n// server/api/posts/[id].get.ts\nexport default defineEventHandler((event) => {\n  const post = findPost(event.context.params!.id)\n  if (!post) {\n    throw createError({ status: 404, statusText: 'Post not found' })\n  }\n  return post\n})\n```\n\n## Chunk loading errors\n\nWhen a user sits on a page and you redeploy, their cached chunk URLs can 404 on the next navigation. Nuxt ships a built-in hard-reload fallback. To customize, set `experimental.emitRouteChunkError` to `'manual'` and handle the `nuxt:chunk-error` event yourself.\n\n## `error.vue` — the fallback page\n\nWhen Nuxt hits a **fatal** error (uncaught server error, or `createError({ fatal: true })` on the client), it renders `~/error.vue`:\n\n```vue\n\x3c!-- error.vue --\x3e\n<script setup lang=\"ts\">\nimport type { NuxtError } from '#app'\n\nconst props = defineProps<{ error: NuxtError }>()\n\nconst handleError = () => clearError({ redirect: '/' })\n<\/script>\n\n<template>\n  <section>\n    <h1>{{ error.statusCode }}</h1>\n    <p>{{ error.statusMessage }}</p>\n    <button @click=\"handleError\">Go home</button>\n  </section>\n</template>\n```\n\nThings to remember:\n- `error.vue` is a **separate page render**, so route middleware runs again. Use `useError()` to detect when you're in error mode.\n- JSON clients (those sending `Accept: application/json`) get a JSON body instead of the error page.\n\n## Creating errors: `createError`\n\n```ts\nimport { createError } from '#app'\n\nif (!data.value) {\n  throw createError({\n    status: 404,\n    statusText: 'Page Not Found',\n  })\n}\n```\n\n- On the **server** (setup, plugins, handlers), throwing a `createError` triggers the fatal error page.\n- On the **client**, it's a non-fatal error by default; pass `{ fatal: true }` to escalate to the error page.\n\nKeep `statusText` short and ASCII-only. For long descriptions or i18n, use `message` or a payload `data`.\n\n## `showError` and `clearError`\n\n- `showError(err)` → imperatively trigger the error page.\n- `clearError({ redirect: '/' })` → dismiss the current error and optionally navigate.\n\nPrefer `throw createError(...)` in setup / handlers; use `showError` when you already caught the error and want to surface it manually.\n\n## `NuxtErrorBoundary` — localized error UI\n\nDon't blow up the whole page for a broken widget. Wrap it:\n\n```vue\n<template>\n  <NuxtErrorBoundary @error=\"report\">\n    <RemoteChart />\n    <template #error=\"{ error, clearError }\">\n      <p>Chart failed to load.</p>\n      <button @click=\"clearError\">Retry</button>\n    </template>\n  </NuxtErrorBoundary>\n</template>\n\n<script setup lang=\"ts\">\nfunction report (err: unknown) { /* log */ }\n<\/script>\n```\n\nSetting `error = null` re-renders the default slot — so make sure you resolved the cause first, otherwise the slot re-throws.\n\n## `useError()` — read the active error\n\nInside middleware, plugins, or layouts you can check:\n\n```ts\nconst error = useError()\nif (error.value) {\n  // running inside the error page flow\n}\n```\n\nHandy for skipping auth checks or hiding chrome while the error page is active.\n\n## Checklist\n\n- Register a global handler in a plugin (`errorHandler` + `vue:error`).\n- Provide `error.vue` with a friendly retry path.\n- Throw `createError` from Nitro handlers for 4xx/5xx responses.\n- Wrap risky widgets in `NuxtErrorBoundary`.\n- Ship chunk-reload telemetry so you know when deploys break sessions.\n\nGood error UX is a feature — treat it like one.\n","/books/nuxtjs/chapters/en_US/13-server.md":"# Server with Nitro\n\nBehind every Nuxt app lives **Nitro**, the HTTP engine that powers SSR, API routes, middleware, and deployment presets. Everything in your `server/` directory runs on Nitro; Vite stays out of it.\n\n## What Nitro gives you\n\n- A single `.output/` directory that runs anywhere JavaScript runs.\n- 15+ deployment presets (Node, Vercel, Netlify, Cloudflare Workers, AWS Lambda, Deno…).\n- `h3` HTTP primitives — small, fast, portable.\n- Per-route rules for hybrid rendering (SSR / ISR / SWR / prerender / CSR).\n- File-based API routes, middleware, and plugins.\n\n## Project layout\n\n```\nserver/\n├── api/                # /api/* endpoints\n│   ├── posts.get.ts\n│   └── posts.post.ts\n├── routes/             # Arbitrary custom routes\n│   └── sitemap.xml.ts\n├── middleware/         # Runs on every request\n└── plugins/            # Nitro plugins (hooks, services)\n```\n\nHMR and auto-import work the same as on the Vue side.\n\n## API routes\n\nFilenames map HTTP methods via a `.<method>.ts` suffix:\n\n```ts\n// server/api/posts.get.ts\nexport default defineEventHandler(async () => {\n  return await db.posts.findMany()\n})\n```\n\n```ts\n// server/api/posts.post.ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody<{ title: string }>(event)\n  const post = await db.posts.create({ data: body })\n  return post\n})\n```\n\nReturn strings, JSON-serializable objects, `Response`, or `ReadableStream`. Nitro handles serialization.\n\nDynamic params use brackets in the filename:\n\n```ts\n// server/api/posts/[id].get.ts\nexport default defineEventHandler((event) => {\n  const id = event.context.params!.id\n  return db.posts.findUnique({ where: { id: Number(id) } })\n})\n```\n\n## Custom routes (non-API)\n\nNeed `/sitemap.xml` or `/healthz` at the root? Put them in `server/routes/`:\n\n```ts\n// server/routes/healthz.ts\nexport default defineEventHandler(() => 'ok')\n```\n\n## Server middleware\n\nRuns for every server request, before API handlers:\n\n```ts\n// server/middleware/request-id.ts\nexport default defineEventHandler((event) => {\n  event.context.requestId = crypto.randomUUID()\n})\n```\n\nSet-once, read anywhere in your handlers via `event.context`.\n\n## Nitro plugins\n\nHook into the server lifecycle for things like logging, DB connection pools, cron:\n\n```ts\n// server/plugins/db.ts\nimport { prisma } from '~~/server/utils/prisma'\n\nexport default defineNitroPlugin((nitro) => {\n  nitro.hooks.hook('close', async () => {\n    await prisma.$disconnect()\n  })\n})\n```\n\n## h3 utilities\n\nUse the ones that come with Nuxt/Nitro — they're fast and edge-safe:\n\n```ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody(event)           // parse body\n  const query = getQuery(event)                // parse ?query\n  const headers = getRequestHeaders(event)     // read headers\n  setResponseStatus(event, 201)                // set status\n  setResponseHeader(event, 'x-custom', '1')    // set header\n  return { ok: true }\n})\n```\n\n## Errors\n\nThrow structured errors with `createError`:\n\n```ts\nthrow createError({ status: 404, statusText: 'Post Not Found' })\n```\n\nNitro converts that into the right HTTP response (or an error page for HTML clients).\n\n## Hybrid rendering via `routeRules`\n\n`routeRules` is where SSR / ISR / prerender / CSR decisions live:\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },              // static at build time\n    '/blog/**': { swr: 3600 },             // SWR: 1 hour\n    '/api/public/*': { cache: { maxAge: 300 } },\n    '/admin/**': { ssr: false },           // CSR-only\n    '/old': { redirect: { to: '/new', statusCode: 301 } },\n  },\n})\n```\n\nNitro translates these into platform-native behavior — Cloudflare Workers uses cache APIs, Vercel uses ISR, Node caches in memory.\n\n## Deployment context\n\nWhen you're ready, a few presets worth remembering:\n\n```ts\nexport default defineNuxtConfig({\n  nitro: {\n    preset: 'node-server',    // also: 'vercel', 'netlify', 'cloudflare-pages', …\n  },\n})\n```\n\nSee the Deployment chapter for the details — here, just know that everything in `server/` travels with your app into whatever runtime you pick.\n\n## Checklist\n\n- Keep DB connections in a Nitro plugin (one pool per process).\n- Use `server/middleware/` for cross-cutting concerns (request IDs, auth).\n- Prefer `routeRules` over custom logic when choosing rendering strategies.\n- Throw `createError` instead of writing status codes by hand.\n- Separate pure data logic into `server/utils/` — they're auto-imported too.\n\nYou now have a full-stack Nuxt app: Vue on the frontend, Nitro on the backend, all in one repo.\n","/books/nuxtjs/chapters/en_US/14-layers.md":"# Layers\n\nLayers let you extract a set of Nuxt config, components, composables, server routes, and assets into a reusable bundle — local to a repo, published on npm, or pulled from Git. Think of them as \"features you can extend from\".\n\n## Why layers?\n\n- Share reusable config presets across projects (`nuxt.config`, `app.config`).\n- Build component libraries with `app/components/`.\n- Publish utilities and composables via `app/composables/` / `app/utils/`.\n- Ship Nuxt module bundles that come with defaults.\n- Split a monorepo by domain (DDD) — each layer owns its slice.\n- Create themes and starter templates.\n\nLayers use the **same directory structure as a Nuxt app**, so authoring them feels just like writing one.\n\n## Two ways to include a layer\n\n### Auto-registered `~~/layers/*`\n\nSince Nuxt 3.12, any folder under `~~/layers/` is picked up automatically:\n\n```\nmy-app/\n├── layers/\n│   ├── base/\n│   └── admin/\n├── app/\n└── nuxt.config.ts\n```\n\nNuxt also exposes named aliases — e.g. `#layers/admin` — to import from the layer's `srcDir`.\n\n### Explicit `extends`\n\nUse `extends` for layers outside the project, npm packages, or Git sources:\n\n```ts\nexport default defineNuxtConfig({\n  extends: [\n    '../base',                                // relative path\n    '@my-themes/awesome',                     // npm package\n    'github:my-themes/awesome#v1',            // git branch / tag\n    ['github:my-themes/private-awesome',\n      { auth: process.env.GITHUB_TOKEN }],    // private repo\n  ],\n})\n```\n\nRemote layers are fetched via `unjs/giget`; config merging is handled by `unjs/c12`.\n\nYou can rename a layer's alias inline:\n\n```ts\nexport default defineNuxtConfig({\n  extends: [\n    ['github:my-themes/awesome',\n      { meta: { name: 'my-awesome-theme' } }],\n  ],\n})\n```\n\n## Priority & override order\n\nWhen multiple layers define the same file, Nuxt merges with this precedence (high → low):\n\n1. Your project files\n2. Auto-scanned `~~/layers/*` (alphabetical; `Z > A`)\n3. `extends` entries (first > last)\n\nPrefix folders with numbers to control the order:\n\n```\nlayers/\n├── 1.base/          # lowest priority among layers\n├── 2.features/\n└── 3.admin/         # highest priority among layers\n```\n\nYour project files always win unless you explicitly opt out.\n\n## Authoring a layer\n\nA layer is just a mini Nuxt project. Here's a tiny admin layer:\n\n```\nlayers/admin/\n├── app/\n│   ├── components/AdminNav.vue\n│   └── composables/useAdmin.ts\n├── nuxt.config.ts\n└── app.config.ts\n```\n\n```ts\n// layers/admin/nuxt.config.ts\nexport default defineNuxtConfig({\n  css: ['~/assets/admin.css'],\n})\n```\n\nEverything the layer declares is merged into the consuming app: components auto-import, routes under `app/pages/` add to the route tree, and `routeRules` are combined.\n\n## Publishing a layer\n\n1. `package.json` should expose the layer's `nuxt.config.ts`:\n\n   ```json\n   {\n     \"name\": \"@me/nuxt-awesome\",\n     \"type\": \"module\",\n     \"exports\": {\n       \".\": {\n         \"import\": \"./nuxt.config.ts\"\n       }\n     }\n   }\n   ```\n\n2. Users install and register:\n\n   ```ts\n   export default defineNuxtConfig({\n     extends: ['@me/nuxt-awesome'],\n   })\n   ```\n\n3. Document the public API: exposed components, composables, module options, and any required env vars.\n\n## Layer vs module vs plugin\n\n- **Layer** — a whole chunk of app (pages, components, composables, config, server routes). Use when you want to share *user-facing features*.\n- **Module** — programmatic extension of Nuxt's build (inject aliases, add modules, patch config). Use when you want to configure other layers or integrate an external library.\n- **Plugin** — runtime code that runs on startup (client and/or server). Use for providing services at runtime.\n\nA big product often uses all three: a layer for domain UI, a module to auto-wire it, plugins for runtime glue.\n\n## Use cases worth copying\n\n- **Marketing site → docs site → product app** as three layers in one monorepo.\n- **Auth kit** (middleware, login pages, composables) as a reusable layer.\n- **Design system** shared between projects, with `app.config` tokens.\n- **Internal theme** tailored to your company, consumed by every Nuxt repo.\n\nLayers are where Nuxt stops being a framework and starts being a platform for your team.\n","/books/nuxtjs/chapters/en_US/15-rendering-modes.md":"# Rendering Modes\n\nNuxt is one framework with many rendering shapes: SSR, SSG, ISR, SWR, CSR-only. You pick per route via `routeRules`, and Nitro translates your choice into platform-native behavior.\n\n## The four modes\n\n### SSR (Server-Side Rendering) — the default\n\nEvery request is rendered on the server. Best for:\n- Personalized pages (auth, account, cart).\n- Content that changes by user or request context.\n- Pages where SEO of fresh data matters.\n\nNo special config needed — you're already here.\n\n### SSG (Static Site Generation) — `prerender`\n\nRender at build time, serve pure HTML. Best for:\n- Marketing pages, landing pages.\n- Docs and blog posts with known slugs.\n\n```bash\npnpm build && pnpm preview\n# or for fully static hosting:\npnpm generate\n```\n\nPer-route:\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },\n    '/about': { prerender: true },\n  },\n})\n```\n\n`nuxt generate` behaves like `nuxt build` with `prerender: true` everywhere, plus emits `/200.html` and `/404.html` as SPA fallbacks.\n\n### ISR (Incremental Static Regeneration) — `isr`\n\nServe cached HTML; regenerate in the background after it goes stale. Best for:\n- High-traffic pages whose content updates occasionally.\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/blog/**': { isr: 60 },        // 60s cache\n    '/docs/**': { isr: true },      // cache until redeploy\n  },\n})\n```\n\nPlatform support: Vercel, Netlify, Cloudflare (via the chosen preset).\n\n### SWR (Stale-While-Revalidate) — `swr`\n\nSimilar to ISR but implemented via HTTP cache headers / CDN:\n\n```ts\nrouteRules: {\n  '/feed': { swr: 30 },             // serve stale up to 30s\n}\n```\n\n### CSR-only — `ssr: false`\n\nSkip SSR entirely for a section. Best for:\n- Dashboards behind auth.\n- Pages that depend on `window`/`document` at first paint.\n\n```ts\nrouteRules: {\n  '/admin/**': { ssr: false },\n}\n```\n\nFor a fully CSR app, disable SSR globally:\n\n```ts\nexport default defineNuxtConfig({\n  ssr: false,\n})\n```\n\n`nuxt generate` then emits an `index.html` that boots the Vue app client-side — just like a classic SPA.\n\n## `routeRules` — one place to decide\n\nYou can combine concerns on the same rule:\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },\n    '/blog/**': { swr: 3600, headers: { 'cache-control': 's-maxage=3600' } },\n    '/api/public/*': { cache: { maxAge: 300 } },\n    '/admin/**': { ssr: false, appMiddleware: 'auth' },\n    '/legacy': { redirect: { to: '/new', statusCode: 301 } },\n    '/proxy/**': { proxy: 'https://api.example.com/**' },\n  },\n})\n```\n\nAvailable keys include `prerender`, `isr`, `swr`, `ssr`, `cache`, `headers`, `redirect`, `proxy`, `noScripts`, `appMiddleware`. Wildcards: `*` for segments, `**` for deep matches.\n\n## Static vs hybrid hosting\n\n| Mode                         | Output                              | Hosting                           |\n|------------------------------|--------------------------------------|-----------------------------------|\n| Full SSR                     | `.output/server/` + `public/`        | Node, Vercel, Netlify, Cloudflare |\n| Full SSG (`nuxt generate`)   | `.output/public/` only               | Any static host + CDN             |\n| Hybrid (per-route)           | Mix of prerendered + SSR artefacts   | Platforms that understand ISR/SWR |\n| CSR (`ssr: false` + generate)| Static `index.html` + JS bundles     | Any static host                   |\n\n## When to choose what\n\n- **Logged-in dashboards** → SSR (or `ssr: false` if you really don't need SEO).\n- **Marketing pages** → `prerender: true`.\n- **Blog / docs / catalogs** → ISR or SWR.\n- **APIs for your app** → `/api/*` with `cache.maxAge` if idempotent.\n- **Redirects / proxies** → `routeRules`, not custom middleware.\n\n## Gotchas\n\n- **`noScripts: true`** emits HTML without Nuxt's runtime JS — great for pure static landing pages, fatal for interactive ones.\n- **`ssr: false` per route** still builds the shell on the server; the client hydrates only JS.\n- **`prerender` dynamic routes** require crawler hints or an explicit list (`nuxt.config` → `nitro.prerender.routes`).\n- On **CDN edges**, combine `swr`/`isr` with cache-control headers to keep things snappy.\n\nPick rendering mode per route, keep your intent in `routeRules`, and let Nitro handle the rest.\n","/books/nuxtjs/chapters/en_US/16-deployment.md":"# Deployment\n\nThanks to Nitro, a Nuxt app can ship to a bare-metal Node server, a serverless function, a CDN edge runtime, or pure static hosting — with the **same codebase** and usually no more than a preset change.\n\n## Build outputs\n\n`pnpm build` writes two trees:\n\n```\n.output/\n├── server/            # entrypoint + serverless-friendly server code\n└── public/            # static assets & prerendered HTML\n```\n\n`pnpm preview` spins up a production-equivalent local server.\n\nFor fully static hosting, `pnpm generate` prerenders every reachable route and emits only `.output/public/`.\n\n## Node.js server (default preset)\n\nBest for VPS / Docker / traditional PaaS:\n\n```bash\nnode .output/server/index.mjs\n# listens on http://0.0.0.0:3000\n```\n\nRespected env vars:\n- `NITRO_PORT` / `PORT` (default `3000`)\n- `NITRO_HOST` / `HOST` (default `0.0.0.0`)\n- `NITRO_SSL_CERT` / `NITRO_SSL_KEY` — dev-only HTTPS\n\n### Process manager (PM2)\n\n```js\n// ecosystem.config.cjs\nmodule.exports = {\n  apps: [{\n    name: 'my-nuxt-app',\n    port: '3000',\n    exec_mode: 'cluster',\n    instances: 'max',\n    script: './.output/server/index.mjs',\n  }],\n}\n```\n\n`pm2 start ecosystem.config.cjs` and you're in cluster mode across all cores.\n\n### Docker\n\nA minimal multi-stage image:\n\n```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY . .\nRUN corepack enable && pnpm install --frozen-lockfile && pnpm build\n\nFROM node:22-alpine\nWORKDIR /app\nCOPY --from=build /app/.output ./.output\nEXPOSE 3000\nCMD [\"node\", \".output/server/index.mjs\"]\n```\n\n## Static hosting\n\nTwo flavors:\n\n1. **SSG with SSR content** (`ssr: true`, default). `nuxt generate` prerenders your routes and produces `200.html` / `404.html` as SPA fallbacks for dynamic routes.\n2. **Pure CSR SPA** (`ssr: false`). `nuxt generate` emits a standard `index.html` that the browser hydrates.\n\nUpload `.output/public/` to any static host — S3, Cloudflare Pages, Netlify, Vercel, GitHub Pages.\n\n## Serverless / Edge presets\n\nNuxt ships 15+ Nitro presets. Set one in config…\n\n```ts\nexport default defineNuxtConfig({\n  nitro: { preset: 'vercel' },\n})\n```\n\n…or via env when you run `nuxt build`:\n\n```bash\nNITRO_PRESET=vercel pnpm build\nNITRO_PRESET=netlify pnpm build\nNITRO_PRESET=cloudflare-pages pnpm build\nNITRO_PRESET=deno-deploy pnpm build\nNITRO_PRESET=aws-lambda pnpm build\n```\n\nMost managed platforms (Vercel, Netlify, Cloudflare Pages) auto-detect Nuxt and pick a preset for you.\n\n## Environment variables\n\nServer-only secrets live on `runtimeConfig`, overridable with `NUXT_*` env vars:\n\n```ini\nNUXT_API_SECRET=prod_secret\nNUXT_PUBLIC_API_BASE=https://api.example.com\n```\n\nPublic keys go under `runtimeConfig.public` and ship to the client.\n\n## Deploy checklist\n\n- [ ] `pnpm typecheck` / `pnpm lint` / `pnpm test` green.\n- [ ] `pnpm build` succeeds locally.\n- [ ] Required env vars set on the platform (`NUXT_*`, DB URLs, secrets).\n- [ ] `routeRules` verified — prerender what you can, SSR what you must.\n- [ ] Sensible `cache` / `swr` headers for public routes.\n- [ ] Health check endpoint (`/healthz`) if the platform expects one.\n- [ ] Log collection wired up (console is enough for most platforms).\n- [ ] Error reporting plugin active (`vue:error` + server `error` hook).\n\n## CDN / Cloudflare gotchas\n\nIf you proxy through Cloudflare, disable:\n\n1. **Speed → Content Optimization → Rocket Loader™**\n2. **Security → Email Address Obfuscation**\n\nBoth inject scripts that break hydration.\n\n## Rollback plan\n\n- Keep at least two builds warm when deploying to serverless — old chunk URLs need to resolve for tabs open on the previous version. Nuxt's chunk-error handler will hard-reload, but only if the route still responds.\n- Tag releases (`git tag v1.2.3`) so you can redeploy instantly.\n\n## Beyond Node\n\n- **Deno** — `NITRO_PRESET=deno-deploy` or `deno-server`.\n- **Bun** — works with Node presets today; Bun-native preset is in beta.\n- **Workers / Workerd** — `cloudflare-pages`, `cloudflare-workers` presets have first-class `routeRules` mapping.\n\nThanks to the preset system, switching runtimes is rarely more than a config change and a fresh `NITRO_PRESET`. Build once, deploy anywhere — that's the Nuxt promise.\n","/books/nuxtjs/chapters/zh_Hans_CN/01-introduction.md":"# 初识 Nuxt\n\n**Nuxt** 是基于 **Vue 3** 与 **Nitro** 的开源全栈框架。它能把一堆 `.vue` 文件直接变成一个生产级应用：SSR、文件式路由、自动导入、通用部署，全部开箱即用。\n\n## 为什么选 Nuxt？\n\n- **约定优于配置**：一套有主张的目录结构，自动化搞定重复劳动。\n- **SSR 默认开启**：首屏更快、SEO 更好、低性能设备也友好。\n- **随处部署**：Node、Deno、静态托管、Serverless、Edge 都支持，没有厂商锁定。\n- **同构数据获取**：`useFetch` / `useAsyncData` 在服务端与客户端行为一致。\n- **零配置 TypeScript**：类型和 `tsconfig.json` 自动生成，不需要配置。\n- **模块生态成熟**：Pinia、Tailwind、Content、Image、i18n、Auth Utils…… 一行命令装上。\n\n## 框架自带能力\n\n- **文件式路由**：`app/pages/` 下每个文件就是一条路由。\n- **代码分割**：每个路由只加载最小的 JS。\n- **自动导入**：组件、composable、工具函数都不需要手动 `import`。\n- **SEO 辅助**：`useHead`、`useSeoMeta`、`<Title>`、`<Meta>`。\n- **错误边界**：`error.vue`、`NuxtErrorBoundary`、`createError`。\n- **混合渲染**：通过 `routeRules` 为每条路由指定 SSR / ISR / SWR / 预渲染 / 纯 CSR。\n\n## 服务端引擎：Nitro\n\nNitro 是 Nuxt 的 HTTP 引擎，内部基于 `h3`，带来：\n- 完整的服务端能力（`server/api`、`server/middleware`）。\n- 15+ 部署预设，一键输出到 Vercel / Netlify / Cloudflare Workers / AWS Lambda / Deno 等。\n- 通过 `routeRules` 实现混合渲染。\n\n生产环境构建后只生成一个 `.output/` 目录，可以运行在任何 JS 运行时上。\n\n## 架构一瞥\n\nNuxt 由若干单一职责的包组成：\n\n- 核心引擎：`nuxt`\n- 打包器：`@nuxt/vite-builder`、`@nuxt/rspack-builder`、`@nuxt/webpack-builder`\n- CLI：`@nuxt/cli`\n- 服务端引擎：`nitro`\n- 开发套件：`@nuxt/kit`（用于编写模块与 Layer）\n\n## Nuxt 与 Next.js 对照\n\n| 维度           | Nuxt (Vue)                         | Next.js (React)                       |\n|----------------|------------------------------------|---------------------------------------|\n| 路由           | `app/pages/` 文件式                | `app/` 文件式                          |\n| 渲染           | SSR / SSG / ISR / CSR（`routeRules`）| SSR / SSG / PPR / ISR（App Router）  |\n| 数据获取       | `useFetch`、`useAsyncData`、`$fetch` | Server Components、`fetch`、`use()`  |\n| 服务端原语     | Nitro + `server/api`               | Route Handlers + Server Actions       |\n| 运行时目标     | Node / Deno / Workers / Edge / 静态 | Node / Vercel / Adapter              |\n\n两者都推崇“App Router + 约定”。Nuxt 更强调约定与预设，Next.js 更押注 React Server Components。\n\n## 本书会讲什么\n\n整本书 16 章，围绕 Nuxt 4 的实战路线展开：\n- 文件系统如何映射到你的应用（视图、页面、布局、资源）。\n- 如何在服务端和客户端安全地获取、修改、缓存与共享数据。\n- 如何把 SEO、过渡动画、错误处理、混合渲染这些生产特性落地。\n- 如何用 Layer、模块与自定义部署扩展 Nuxt。\n\n准备好了？先装上它，写下第一页。\n","/books/nuxtjs/chapters/zh_Hans_CN/02-installation.md":'# 安装与创建项目\n\n创建一个 Nuxt 4 项目只要一条命令。本章介绍环境要求、官方 CLI、手动安装，以及后面一直会用到的目录结构。\n\n## 环境要求\n\n- **Node.js 20.x** 或更新版本，建议使用偶数 LTS（20、22、……）。\n- **终端 + 编辑器**：VS Code + Vue (Volar) 插件或 WebStorm 都行。\n- Windows 用户建议：用 WSL，并用 `127.0.0.1` 代替 `localhost`，可避免 DNS 慢和 HMR 抖动。\n\n## 用 CLI 初始化\n\n```bash\n# pnpm\npnpm create nuxt@latest my-app\n\n# npm\nnpm create nuxt@latest my-app\n\n# bun\nbun create nuxt@latest my-app\n```\n\nCLI 会询问包管理器、Linter、CSS 方案等。选完后：\n\n```bash\ncd my-app\npnpm install\npnpm dev -o   # -o 会自动打开浏览器\n```\n\n访问 `http://localhost:3000` 就能看到默认欢迎页。\n\n## 手动安装\n\n想搞清楚底层发生了什么，就从零开始：\n\n```bash\nmkdir my-app && cd my-app\npnpm init\npnpm add -D nuxt\n```\n\n在 `package.json` 中加入脚本：\n\n```json\n{\n  "scripts": {\n    "dev": "nuxt dev",\n    "build": "nuxt build",\n    "preview": "nuxt preview"\n  }\n}\n```\n\n新建 `app.vue`：\n\n```vue\n<template>\n  <div>Hello Nuxt 4</div>\n</template>\n```\n\n`pnpm dev` 启动后就能访问 `http://localhost:3000`，其余细节 Nuxt 会自动处理。\n\n## 目录结构\n\nNuxt 4 推荐的目录布局：\n\n```\nmy-app/\n├── app/\n│   ├── app.vue              # 根组件\n│   ├── pages/               # 文件式路由\n│   ├── layouts/             # 布局\n│   ├── components/          # 自动导入的组件\n│   ├── composables/         # 自动导入的组合式函数\n│   ├── utils/               # 自动导入的工具\n│   ├── middleware/          # 路由中间件\n│   ├── plugins/             # 客户端/服务端插件\n│   └── assets/              # 需要 Vite 处理的资源\n├── public/                  # 原样托管到 /\n├── server/\n│   ├── api/                 # /api/* 接口\n│   ├── middleware/          # 服务端中间件\n│   └── plugins/             # Nitro 插件\n├── nuxt.config.ts           # 框架配置\n├── tsconfig.json            # 从 .nuxt/ 继承\n└── package.json\n```\n\n两点特别要记住：\n- **`app/` 下的所有内容都被自动导入** —— 自己的组件与 composable 都不需要写 `import`。\n- **`server/` 跑在 Nitro 上**，不是 Vite。服务端接口都写在这里。\n\n## 开发服务器\n\n```bash\npnpm dev              # 启动 dev 服务器\npnpm dev -o           # 自动打开浏览器\npnpm dev --port 4000  # 换端口\n```\n\nHMR、TypeScript、Vite、Nitro dev server 全部自动配好。\n\n## `nuxt.config.ts` 基本用法\n\n最小 `nuxt.config.ts` 一般会打开 DevTools、注册模块：\n\n```ts\nexport default defineNuxtConfig({\n  devtools: { enabled: true },\n  modules: [\'@pinia/nuxt\', \'@nuxtjs/tailwindcss\'],\n})\n```\n\n这点配置就足够开工了。下一章会细讲环境变量覆盖、`runtimeConfig` 与 `app.config` 区别、TypeScript 集成等内容，但到这一步已经可以写页面。\n\n## 写下第一页\n\n把 `app.vue` 改成：\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n```\n\n新建 `app/pages/index.vue`：\n\n```vue\n<template>\n  <section>\n    <h1>Hello Nuxt 4</h1>\n    <p>30 秒体验文件式路由。</p>\n  </section>\n</template>\n```\n\n路由完全由 `app/pages/` 生成，不需要任何路由表。\n',"/books/nuxtjs/chapters/zh_Hans_CN/03-configuration.md":"# 配置系统\n\nNuxt 自带合理默认值，大多数项目只改几项配置。`nuxt.config.ts` 是唯一的真相源：模块、环境覆盖、runtimeConfig、Vite/Nitro 透传、Vue 开关全写在这里。\n\n## `nuxt.config.ts`\n\n```ts\nexport default defineNuxtConfig({\n  devtools: { enabled: true },\n  modules: ['@pinia/nuxt', '@nuxtjs/tailwindcss'],\n  css: ['~/assets/css/main.css'],\n})\n```\n\n`defineNuxtConfig` 全局可用（不需要 import），并且有完整的 TypeScript 补全。\n\n## 按环境覆盖\n\nNuxt 会在合并前优先读取 `$development`、`$production`、`$env.*`：\n\n```ts\nexport default defineNuxtConfig({\n  $production: {\n    routeRules: {\n      '/**': { isr: true },\n    },\n  },\n  $development: {\n    devtools: { enabled: true },\n  },\n  $env: {\n    staging: { app: { baseURL: '/staging/' } },\n  },\n})\n```\n\n构建时通过 `nuxt build --envName staging` 指定环境。背后由 `c12` 实现。\n\n## `runtimeConfig` 与 `app.config` 的区别\n\n两者都能暴露配置值，但解决的问题不同：\n\n| 关注点                        | `runtimeConfig`             | `app.config`                 |\n|-------------------------------|------------------------------|------------------------------|\n| 定义位置                      | `nuxt.config.ts`             | `app.config.ts`              |\n| 支持环境变量覆盖              | 是（`NUXT_*`）               | 否                           |\n| 客户端可访问                  | 只有 `public` 下的键         | 完整打包                     |\n| 支持非基础 JS 类型            | 否                            | 是                           |\n| 响应式                        | 是                            | 是                           |\n| 适合用来放                    | 密钥、URL、Token             | 主题、站点元信息、开关       |\n\n### `runtimeConfig`\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  runtimeConfig: {\n    apiSecret: '',          // 仅服务端\n    public: {\n      apiBase: '/api',      // 暴露给客户端\n    },\n  },\n})\n```\n\n通过环境变量覆盖：\n\n```ini\nNUXT_API_SECRET=prod_secret\nNUXT_PUBLIC_API_BASE=https://api.example.com\n```\n\n使用 composable 读取：\n\n```vue\n<script setup lang=\"ts\">\nconst { apiSecret, public: { apiBase } } = useRuntimeConfig()\n<\/script>\n```\n\n> 在客户端，`apiSecret` 会是 `undefined`。\n\n### `app.config`\n\n```ts\n// app/app.config.ts\nexport default defineAppConfig({\n  title: 'Hello Nuxt',\n  theme: { dark: true, colors: { primary: '#3b82f6' } },\n})\n```\n\n```vue\n<script setup lang=\"ts\">\nconst appConfig = useAppConfig()\n<\/script>\n```\n\n`app.config` 支持 HMR —— 改主题会立即生效。\n\n## TypeScript\n\nNuxt 会自动生成 `.nuxt/tsconfig.json`，项目的 `tsconfig.json` 继承它，无需手动配置。\n\n需要自定义时：\n\n```ts\nexport default defineNuxtConfig({\n  typescript: {\n    strict: true,\n    typeCheck: true, // 构建时跑 vue-tsc\n  },\n})\n```\n\n## 给 Vite / Nitro / PostCSS 传选项\n\nNuxt **不会** 读取外部的 `vite.config.ts` / `nitro.config.ts`，都要通过 Nuxt 的 key 透传：\n\n```ts\nexport default defineNuxtConfig({\n  vite: {\n    plugins: [/* … */],\n  },\n  nitro: {\n    preset: 'node-server',\n    compressPublicAssets: true,\n  },\n  postcss: {\n    plugins: {\n      'postcss-nested': {},\n    },\n  },\n})\n```\n\n## Vue 相关开关\n\n```ts\nexport default defineNuxtConfig({\n  vue: {\n    propsDestructure: true, // 实验特性\n  },\n})\n```\n\n也可以直接通过 `vite.vue` 给 `@vitejs/plugin-vue` 传参。\n\n## 应用级 head\n\n要应用到所有页面的静态标签放在 `app.head`：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      titleTemplate: '%s · Nuxt 小册',\n      htmlAttrs: { lang: 'zh-CN' },\n      link: [{ rel: 'icon', href: '/favicon.ico' }],\n    },\n  },\n})\n```\n\n页面级、动态标签用 `useHead()` —— 详见 SEO 那章。\n\n## 怎么选？\n\n- **项目里的静态开关？** → `app.config`\n- **密钥或环境相关？** → `runtimeConfig`\n- **构建期转换？** → `nuxt.config`（`vite` / `nitro` key）\n- **按环境切换？** → `$development` / `$production` / `$env`\n\n一个文件，四个旋钮，足够把项目跑起来。\n","/books/nuxtjs/chapters/zh_Hans_CN/04-views.md":'# 视图结构\n\nNuxt 给你 4 层来搭 UI：`app.vue`、**组件**、**页面**、**布局**。每一层分工明确，Nuxt 会自动连线，大多数时候你只要加文件就行。\n\n## `app.vue`\n\n`app.vue` 是应用入口。如果完全不需要路由：\n\n```vue\n<template>\n  <div>\n    <h1>欢迎</h1>\n  </div>\n</template>\n```\n\n想要多页面时挂上 `<NuxtPage />`：\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n```\n\n如果**删掉** `app.vue`，Nuxt 会生成一个默认入口，相当于 `<NuxtLayout><NuxtPage /></NuxtLayout>`。\n\n## 组件\n\n`app/components/` 下的所有内容会自动导入，不用手写 `import`：\n\n```\napp/components/\n├── AppHeader.vue\n├── AppFooter.vue\n└── AppAlert.vue\n```\n\n```vue\n<template>\n  <div>\n    <AppHeader />\n    <AppAlert>Hello，来自自动导入的组件。</AppAlert>\n    <AppFooter />\n  </div>\n</template>\n```\n\n嵌套目录会变成名称前缀：`app/components/base/Button.vue` → `<BaseButton />`。\n\n小建议：\n- 在 `<template>` 里统一用 PascalCase。\n- 在组件名前加 `Lazy` 可以延迟加载：`<LazyAppHeavy />`。\n\n## 页面\n\n页面放在 `app/pages/`，每个 `.vue` 文件就是一条路由：\n\n```\napp/pages/\n├── index.vue          →  /\n├── about.vue          →  /about\n└── blog/\n    ├── index.vue      →  /blog\n    └── [id].vue       →  /blog/:id\n```\n\n```vue\n\x3c!-- app/pages/about.vue --\x3e\n<template>\n  <section>\n    <h1>关于我们</h1>\n  </section>\n</template>\n```\n\n页面会自动分包，`<NuxtLink>` 会帮你预取，`definePageMeta({ … })` 用来声明页面级元信息。\n\n## 布局\n\n布局用来包裹一组页面，负责共用的外壳（头部、底部、侧边栏）。文件放在 `app/layouts/`：\n\n```vue\n\x3c!-- app/layouts/default.vue --\x3e\n<template>\n  <div>\n    <AppHeader />\n    <slot />\n    <AppFooter />\n  </div>\n</template>\n```\n\n`default.vue` 会自动应用。要自定义布局，页面级声明：\n\n```vue\n<script setup lang="ts">\ndefinePageMeta({ layout: \'admin\' })\n<\/script>\n\n<template>\n  <div>Dashboard</div>\n</template>\n```\n\n……对应的文件放到 `app/layouts/admin.vue`。\n\n## `<NuxtLayout>` 与 `<NuxtPage>`\n\n- `<NuxtPage />` 是文件式路由的“出口”，负责流式渲染、预取、过渡。\n- `<NuxtLayout>` 决定当前页面用哪个布局，也可以手动指定 `name`：\n\n```vue\n<NuxtLayout name="admin">\n  <NuxtPage />\n</NuxtLayout>\n```\n\n如果只有一个布局，完全可以跳过 `layouts/`，把外壳直接写在 `app.vue` 里。\n\n## 要点回顾\n\n| 需求                                  | 去哪儿                        |\n|---------------------------------------|-------------------------------|\n| 根入口／全站外壳                      | `app.vue`（或单布局）          |\n| 可复用 UI 块                          | `app/components/`             |\n| 一条路由 + 视图                       | `app/pages/`                  |\n| 多页面共享的外壳                      | `app/layouts/`                |\n\n## 进阶：定制 HTML 模板\n\n如果 `useHead` 还不够（需要改 Nitro 输出的原始 HTML），用 `render:html` 钩子写一个 Nitro 插件：\n\n```ts\n// server/plugins/meta.ts\nexport default defineNitroPlugin((nitroApp) => {\n  nitroApp.hooks.hook(\'render:html\', (html, { event }) => {\n    html.head.push(\'<meta name="description" content="自定义描述" />\')\n  })\n})\n```\n\n这种写法谨慎使用 —— `useHead` 能覆盖 99% 的情况，静态资源和 SEO 章节会介绍更顺手的做法。\n',"/books/nuxtjs/chapters/zh_Hans_CN/05-assets.md":"# 静态资源\n\nNuxt 提供两个放静态资源的目录，行为差别很大。选对了能省掉一大堆坏链、体积膨胀、缓存失效的问题。\n\n## `public/` —— 原样托管\n\n`public/` 下的所有内容会以根路径 URL **原样** 提供：\n\n```\npublic/\n├── favicon.ico\n├── robots.txt\n└── img/\n    └── hero.png          → /img/hero.png\n```\n\n通过绝对路径引用：\n\n```vue\n<template>\n  <img src=\"/img/hero.png\" alt=\"Hero\" />\n</template>\n```\n\n适合放在 `public/` 的：\n- favicon、`robots.txt`、OG 默认图、`sitemap.xml`\n- 需要稳定可预测 URL 的图片\n- 与营销 / CDN 配置共用的文件\n\n这里的文件**不会带指纹**，浏览器可能永远缓存，修改后要改名或手动设置缓存头。\n\n## `app/assets/` —— 由 Vite 处理\n\n`app/assets/` 下的文件会经过 Vite：压缩、打指纹、优化引用。用 `~/` 或 `@/` 引用：\n\n```vue\n<template>\n  <img src=\"~/assets/img/hero.png\" alt=\"Hero\" />\n</template>\n```\n\n适合放在 `app/assets/` 的：\n- CSS / SCSS / Less 文件\n- 需要内联或转换的 SVG\n- 需要打指纹与优化的图片\n- 随应用打包的字体\n\n这里的文件**不会**在浏览器里以 `/assets/…` 访问，Vite 会改写成 `/_nuxt/hero-abc123.png` 这样的带哈希 URL。\n\n## 快速对比\n\n| 行为                           | `public/`           | `app/assets/`            |\n|--------------------------------|----------------------|---------------------------|\n| 根 URL 访问                    | 是（`/foo.png`）     | 否（被 Vite 改写）        |\n| 会压缩 / 处理                  | 否                   | 是                        |\n| 文件名带指纹                   | 否                   | 是                        |\n| 在 Vue 中如何引用              | `/img/foo.png`       | `~/assets/img/foo.png`    |\n| 适合                           | 稳定公共 URL         | 打包优化后的产物          |\n\n## 图片进阶：`<NuxtImg>` 和 `<NuxtPicture>`\n\n做认真的图片优化建议上 `@nuxt/image`：\n\n```bash\npnpm add -D @nuxt/image\n```\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  modules: ['@nuxt/image'],\n  image: {\n    quality: 80,\n    format: ['webp', 'avif'],\n  },\n})\n```\n\n然后就可以用 `<NuxtImg>` / `<NuxtPicture>`：\n\n```vue\n<template>\n  <NuxtImg\n    src=\"/img/hero.png\"\n    width=\"1200\"\n    height=\"630\"\n    loading=\"lazy\"\n    sizes=\"sm:100vw md:50vw lg:400px\"\n  />\n</template>\n```\n\n它负责：\n- 响应式 `srcset`\n- 自动 WebP / AVIF\n- 懒加载\n- 各种 CDN Provider（Cloudinary、IPX、imgix、Vercel……）\n\n## 字体\n\nNuxt 官方有 `@nuxt/fonts` 模块，能自托管 Google Fonts / 本地字体并避免 CLS：\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@nuxt/fonts'],\n  fonts: {\n    families: [\n      { name: 'Inter', provider: 'google' },\n    ],\n  },\n})\n```\n\n在 CSS 里直接用这个字体名即可，模块会自动注入 `@font-face`。\n\n想用本地字体？放进 `public/fonts/` 然后：\n\n```css\n/* app/assets/css/fonts.css */\n@font-face {\n  font-family: 'Display';\n  src: url('/fonts/display.woff2') format('woff2');\n  font-display: swap;\n}\n```\n\n## 一句话指南\n\n- **需要稳定的公共 URL？** → `public/`\n- **需要打指纹 / 优化？** → `app/assets/`\n- **大量图片？** → `@nuxt/image`\n- **字体？** → `@nuxt/fonts` 或 `public/fonts/`\n\n每类资源只挑一个位置，保持团队一致。\n","/books/nuxtjs/chapters/zh_Hans_CN/06-styling.md":"# 样式方案\n\nNuxt 对样式方案几乎不设限：纯 CSS、预处理器、Tailwind / UnoCSS，或 CSS Modules —— 都是“一等公民”。\n\n## 本地样式文件\n\n把源文件放进 `app/assets/`，任何组件里都能引用：\n\n```vue\n<script>\nimport '~/assets/css/first.css'\n<\/script>\n\n<style>\n@import url(\"~/assets/css/second.css\");\n</style>\n```\n\n要全局注入，直接在 `nuxt.config.ts` 中声明：\n\n```ts\nexport default defineNuxtConfig({\n  css: ['~/assets/css/main.css'],\n})\n```\n\n全局 CSS 会被内联到 SSR HTML，首屏一上来就是带样式的。\n\n## 外部样式表\n\n从配置里挂 `<link>`：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      link: [\n        { rel: 'stylesheet', href: 'https://cdn.example.com/reset.css' },\n      ],\n    },\n  },\n})\n```\n\n或通过 `useHead` 动态挂载：\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  link: [{ rel: 'stylesheet', href: 'https://cdn.example.com/theme.css' }],\n})\n<\/script>\n```\n\n外部样式表会阻塞渲染，能本地化时尽量本地化。\n\n## 预处理器（SCSS / Sass / Less / Stylus）\n\n安装预处理器后 Vite 会自动识别：\n\n```bash\npnpm add -D sass\n```\n\n```vue\n<style lang=\"scss\">\n@use \"~/assets/scss/main.scss\";\n</style>\n```\n\n或全局注册：\n\n```ts\nexport default defineNuxtConfig({\n  css: ['~/assets/scss/main.scss'],\n})\n```\n\n想要每个文件都自动注入变量、设计 token？用 Vite 的 `preprocessorOptions`：\n\n```ts\nexport default defineNuxtConfig({\n  vite: {\n    css: {\n      preprocessorOptions: {\n        scss: { additionalData: '@use \"~/assets/_colors.scss\" as *;' },\n      },\n    },\n  },\n})\n```\n\n## 单文件组件样式\n\n这正是 Vue 的强项：\n\n```vue\n<script setup lang=\"ts\">\nconst isActive = ref(true)\nconst color = ref('crimson')\n<\/script>\n\n<template>\n  <div :class=\"['box', { active: isActive }]\">\n    <p :style=\"{ color }\">hello</p>\n  </div>\n</template>\n\n<style scoped>\n.box { padding: 1rem; }\n.active { outline: 2px solid v-bind(color); }\n</style>\n```\n\n几个关键特性：\n- **`scoped`** —— 样式只作用于本组件。\n- **`v-bind()`** —— 在 `<style>` 中使用响应式值。\n- **`module`** —— CSS Modules，通过 `$style` 访问：\n\n```vue\n<template>\n  <p :class=\"$style.red\">red</p>\n</template>\n<style module>\n.red { color: red; }\n</style>\n```\n\n- **`lang=\"scss\"`**（或 `sass`、`less`、`stylus`）—— 每个 `<style>` 块独立选择预处理器。\n\n## PostCSS\n\nNuxt 已经预配置了 `postcss-import`、`postcss-url`、`autoprefixer`、`cssnano`。要添加其他插件：\n\n```ts\nexport default defineNuxtConfig({\n  postcss: {\n    plugins: {\n      'postcss-nested': {},\n      'postcss-custom-media': {},\n    },\n  },\n})\n```\n\nSFC 里用 `<style lang=\"postcss\">` 可以获得正确的语法高亮。\n\n## Tailwind CSS / UnoCSS\n\n想用 utility-first 方案，官方模块是最快路径：\n\n```bash\npnpm add -D @nuxtjs/tailwindcss\n```\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@nuxtjs/tailwindcss'],\n})\n```\n\n或 UnoCSS：\n\n```bash\npnpm add -D @unocss/nuxt\n```\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@unocss/nuxt'],\n})\n```\n\n两者都会自动注入生成的 CSS，不需要手动 `import 'uno.css'`。\n\n## 用布局做风格分隔\n\n当应用里不同区域风格差别很大（比如深色后台 vs 亮色营销页），把外壳拆到布局里最干净：\n\n```vue\n\x3c!-- app/layouts/admin.vue --\x3e\n<template>\n  <div class=\"admin-shell\"><slot /></div>\n</template>\n\n<style>\n.admin-shell { background: #0f172a; color: #e2e8f0; }\n</style>\n```\n\n## 策略选择\n\n| 需求                                         | 选什么                       |\n|----------------------------------------------|-----------------------------|\n| 工具类 + 小团队                              | Tailwind / UnoCSS           |\n| 组件级隔离                                   | `<style scoped>`            |\n| 彻底避免类名冲突                             | `<style module>`            |\n| 设计系统 + 设计 token                        | SCSS + `additionalData`     |\n| 可定制主题                                   | `app.config` + CSS 变量     |\n\n没有所谓“标准答案”，Nuxt 允许你在同一个项目里混着用。\n","/books/nuxtjs/chapters/zh_Hans_CN/07-routing.md":"# 路由系统\n\nNuxt 的路由基于 **vue-router**，但你几乎不用手写配置。`app/pages/` 下每个文件就是一条路由 —— 自动分包、自动预取、自动类型。\n\n## 文件式路由\n\n```\napp/pages/\n├── index.vue          →  /\n├── about.vue          →  /about\n└── posts/\n    ├── index.vue      →  /posts\n    └── [id].vue       →  /posts/:id\n```\n\nNuxt 会生成完整的类型化路由树，`<NuxtLink>` 的 `to` 值也会带类型。\n\n## 动态段\n\n方括号即 params：\n\n```vue\n\x3c!-- app/pages/posts/[id].vue --\x3e\n<script setup lang=\"ts\">\nconst route = useRoute()\nconsole.log(route.params.id) // string\n<\/script>\n```\n\n多个参数也 OK：`app/pages/users/[userId]/posts/[postId].vue` → `/users/:userId/posts/:postId`。\n\n**Catch-all**：`app/pages/docs/[...slug].vue` 会匹配 `/docs/a/b/c`，`route.params.slug = ['a', 'b', 'c']`。\n\n## 嵌套路由\n\n让 `parent.vue` 和 `parent/` 目录共存，在父组件里放 `<NuxtPage />`：\n\n```vue\n\x3c!-- app/pages/settings.vue --\x3e\n<template>\n  <section>\n    <aside>\x3c!-- 导航 --\x3e</aside>\n    <NuxtPage />\n  </section>\n</template>\n```\n\n```\napp/pages/\n├── settings.vue\n└── settings/\n    ├── profile.vue      →  /settings/profile\n    └── billing.vue      →  /settings/billing\n```\n\n## `<NuxtLink>` 导航\n\n应用内跳转一律用 `<NuxtLink>`：\n\n```vue\n<template>\n  <nav>\n    <NuxtLink to=\"/\">首页</NuxtLink>\n    <NuxtLink to=\"/about\">关于</NuxtLink>\n    <NuxtLink :to=\"{ name: 'posts-id', params: { id: 1 } }\">文章 1</NuxtLink>\n  </nav>\n</template>\n```\n\n它会：\n- 渲染真正的 `<a>`（对无障碍友好）。\n- 链接进入视口时自动预取组件与 payload。\n- Hydration 之后走客户端跳转，不会整页刷新。\n- 自动加上 `.router-link-active` / `.router-link-exact-active` 类名。\n\n想临时关掉预取：\n\n```vue\n<NuxtLink to=\"/heavy\" :prefetch=\"false\">Heavy page</NuxtLink>\n```\n\n## 程序式导航\n\n```vue\n<script setup lang=\"ts\">\nconst router = useRouter()\nfunction openPost (id: number) {\n  router.push(`/posts/${id}`)\n}\n<\/script>\n```\n\n在中间件、插件、事件处理器里优先使用 `navigateTo()`：\n\n```ts\nexport default defineNuxtRouteMiddleware((to) => {\n  if (!to.path.startsWith('/public')) return navigateTo('/login')\n})\n```\n\n## 路由中间件\n\n三种形态：\n1. **内联**：直接写在页面的 `definePageMeta({ middleware: (to) => {…} })` 里。\n2. **命名**：文件放在 `app/middleware/auth.ts`，页面手动声明使用。\n3. **全局**：`app/middleware/foo.global.ts`，每次导航都会跑。\n\n```ts\n// app/middleware/auth.ts\nexport default defineNuxtRouteMiddleware((to) => {\n  const { user } = useUserSession()\n  if (!user.value) return navigateTo('/login')\n})\n```\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ middleware: 'auth' })\n<\/script>\n```\n\n> 路由中间件只在 Vue（客户端 & SSR）层执行。服务端／API 请求走 `server/middleware/`。\n\n## 路由校验\n\n用 `validate` 校验参数：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  validate (route) {\n    return typeof route.params.id === 'string' && /^\\d+$/.test(route.params.id)\n  },\n})\n<\/script>\n```\n\n返回假值会触发 404；返回 `{ status, statusText }` 可以自定义响应。\n\n## `definePageMeta` 概览\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  layout: 'admin',          // 指定布局\n  middleware: ['auth'],     // 路由中间件\n  pageTransition: false,    // 关闭过渡\n  validate: (r) => true,    // 校验\n  key: (r) => r.path,       // 重渲染策略\n})\n<\/script>\n```\n\n在 `/posts/1` → `/posts/2` 这种相同路由不同参数的场景下，加 `key` 可以让组件重新创建。\n\n## 小结\n\n- 把 `.vue` 文件丢进 `app/pages/`，路由就已经有了。\n- 应用内跳转一律 `<NuxtLink>`，获得预取与过渡。\n- 鉴权、重定向、守卫交给中间件。\n- `definePageMeta` 用来声明布局、中间件、校验等。\n","/books/nuxtjs/chapters/zh_Hans_CN/08-seo-meta.md":"# SEO 与 Meta 标签\n\nNuxt 底层的 head 管理由 [Unhead](https://unhead.unjs.io/) 提供。你可以得到响应式的 composable、类型安全的 SEO 工具，以及内置组件 —— 按团队习惯任选风格。\n\n## `nuxt.config.ts` —— 全局默认值\n\n放一些不会随页面变化的标签：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      titleTemplate: '%s · Nuxt 小册',\n      htmlAttrs: { lang: 'zh-CN' },\n      link: [{ rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' }],\n    },\n  },\n})\n```\n\nNuxt 还会自动注入：\n- `<meta charset=\"utf-8\">`\n- `<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">`\n\n## `useHead` —— 响应式 composable\n\n```vue\n<script setup lang=\"ts\">\nconst title = ref('Hello World')\n\nuseHead({\n  title,\n  meta: [{ name: 'description', content: '我的超棒网站。' }],\n  bodyAttrs: { class: 'app' },\n  script: [{ innerHTML: 'console.log(\"loaded\")' }],\n})\n<\/script>\n```\n\n`useHead` 支持响应式值，多处调用也会自动去重。\n\n## `useSeoMeta` —— 类型安全的 SEO\n\n`useSeoMeta` 知道 `name` 和 `property` 的区别，拼错、写错一眼就被类型系统发现：\n\n```vue\n<script setup lang=\"ts\">\nuseSeoMeta({\n  title: '我的超棒网站',\n  description: '这个网站做了啥，来了解一下。',\n  ogTitle: '我的超棒网站',\n  ogDescription: '这个网站做了啥，来了解一下。',\n  ogImage: 'https://example.com/og.png',\n  twitterCard: 'summary_large_image',\n})\n<\/script>\n```\n\n做 SEO 时基本就用它。\n\n## 组件式写法\n\n喜欢声明式模板也可以：\n\n```vue\n<script setup lang=\"ts\">\nconst title = ref('Hello World')\n<\/script>\n\n<template>\n  <Head>\n    <Title>{{ title }}</Title>\n    <Meta name=\"description\" :content=\"title\" />\n  </Head>\n</template>\n```\n\n支持的标签：`<Title>`、`<Meta>`、`<Link>`、`<Script>`、`<Style>`、`<Base>`、`<NoScript>`、`<Html>`、`<Body>`。\n\n## Title template\n\n全站标题一般是 “页面 — 站点名” 的模式：\n\n```vue\n\x3c!-- app.vue --\x3e\n<script setup lang=\"ts\">\nuseHead({\n  titleTemplate: (title) => title ? `${title} · 我的站点` : '我的站点',\n})\n<\/script>\n```\n\n之后任何页面调用 `useHead({ title: 'Blog' })` 就会显示成 `Blog · 我的站点`。\n\n## Template params\n\n需要多个占位符？\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  titleTemplate: (title) => title ? `${title} %separator %siteName` : '%siteName',\n  templateParams: { siteName: '我的站点', separator: '·' },\n})\n<\/script>\n```\n\n## 响应式\n\n所有属性都支持 computed、ref 或 getter，可以直接跟着路由或接口数据走：\n\n```vue\n<script setup lang=\"ts\">\nconst route = useRoute()\nconst post = await $fetch(`/api/posts/${route.params.id}`)\n\nuseSeoMeta({\n  title: () => post.title,\n  description: () => post.excerpt,\n  ogImage: () => post.image,\n})\n<\/script>\n```\n\n## 与 `definePageMeta` 联动\n\n有时希望页面级静态信息被其他层读到，放到 `definePageMeta` 里，再在布局里读：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ title: '某个页面' })\n<\/script>\n```\n\n```vue\n\x3c!-- 默认布局 --\x3e\n<script setup lang=\"ts\">\nconst route = useRoute()\nuseHead({\n  meta: [{ property: 'og:title', content: `我的站点 · ${route.meta.title}` }],\n})\n<\/script>\n```\n\n## 怎么选？\n\n| 你想做的事                        | 用哪个                   |\n|-----------------------------------|---------------------------|\n| 全站固定标签                      | `app.head`                |\n| 响应式、任意 head 标签            | `useHead`                 |\n| 类型安全的 SEO 标签               | `useSeoMeta`              |\n| 偏爱模板写法                      | `<Head>` / `<Title>` 等   |\n\n## Body 尾部标签\n\n有些脚本要放在 `<body>` 末尾，用 `tagPosition`：\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  script: [\n    {\n      src: 'https://third-party-script.com',\n      tagPosition: 'bodyClose',\n    },\n  ],\n})\n<\/script>\n```\n\n## `useHeadSafe`\n\n当你要把用户输入合并进 head，优先用 `useHeadSafe`。它会过滤掉危险属性（如 `innerHTML`、事件处理器），防止 XSS。\n\n把元信息当作代码认真维护，这是 Nuxt 里最廉价也最划算的优化之一。\n","/books/nuxtjs/chapters/zh_Hans_CN/09-transitions.md":"# 页面过渡动画\n\nNuxt 复用 Vue 的 `<Transition>`，让页面与布局之间有过渡效果；同时还实验性地接入了浏览器原生的 View Transitions API，可以做跨页面的动画。\n\n## 页面过渡\n\n在 `nuxt.config.ts` 里开启全站页面过渡：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    pageTransition: { name: 'page', mode: 'out-in' },\n  },\n})\n```\n\n在 `app.vue` 里配 CSS：\n\n```vue\n<template>\n  <NuxtPage />\n</template>\n\n<style>\n.page-enter-active,\n.page-leave-active {\n  transition: all 0.4s;\n}\n.page-enter-from,\n.page-leave-to {\n  opacity: 0;\n  filter: blur(1rem);\n}\n</style>\n```\n\n之后每次路由切换都会有淡入淡出 + 模糊效果。\n\n单页覆写全局过渡，用 `definePageMeta`：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: { name: 'rotate', mode: 'out-in' },\n})\n<\/script>\n```\n\n## 布局过渡\n\n如果路由导致布局变化（比如 default → admin），页面过渡不会触发，得用 `layoutTransition`：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    layoutTransition: { name: 'layout', mode: 'out-in' },\n  },\n})\n```\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n\n<style>\n.layout-enter-active,\n.layout-leave-active { transition: all 0.4s; }\n.layout-enter-from,\n.layout-leave-to { filter: grayscale(1); }\n</style>\n```\n\n## 禁用过渡\n\n单页：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ pageTransition: false, layoutTransition: false })\n<\/script>\n```\n\n全局：\n\n```ts\nexport default defineNuxtConfig({\n  app: { pageTransition: false, layoutTransition: false },\n})\n```\n\n## JavaScript 钩子\n\nCSS 搞不定的时候用 `<Transition>` 的 JS 钩子，配合 GSAP、Motion One、anime.js 都顺手：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: {\n    name: 'custom-flip',\n    mode: 'out-in',\n    onBeforeEnter: (el) => { /* 准备 */ },\n    onEnter: (el, done) => { /* 动画结束调 */ done() },\n    onAfterEnter: (el) => { /* 清理 */ },\n  },\n})\n<\/script>\n```\n\n## 动态过渡\n\n想根据跳转方向切换动画（比如翻页、滑动）？在内联中间件里决定：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: { name: 'slide-right', mode: 'out-in' },\n  middleware (to, from) {\n    if (typeof to.meta.pageTransition !== 'boolean') {\n      to.meta.pageTransition!.name =\n        +to.params.id! > +from.params.id! ? 'slide-left' : 'slide-right'\n    }\n  },\n})\n<\/script>\n```\n\n## `<NuxtPage>` 上的过渡\n\n如果不用布局，把 `<NuxtPage>` 直接放在 `app.vue`，可以内联传入：\n\n```vue\n<template>\n  <NuxtPage :transition=\"{ name: 'bounce', mode: 'out-in' }\" />\n</template>\n```\n\n这种写法**不能**被页面级覆写。\n\n## View Transitions API（实验）\n\n原生 View Transitions API 可以让完全不同 DOM 结构之间也有平滑过渡，非常适合做 hero 图跨页动画。\n\n启用：\n\n```ts\nexport default defineNuxtConfig({\n  experimental: { viewTransition: true },\n})\n```\n\n可取值：`false`、`true`、`'always'`。`true` 会尊重 `prefers-reduced-motion: reduce`。\n\n### 单页控制\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  viewTransition: {\n    enabled: true,\n    types: ['slide'],\n    toTypes: ['slide-in'],\n    fromTypes: ['slide-out'],\n  },\n})\n<\/script>\n```\n\n`types` 还能写成函数返回数组，实现类似“id 递增就向左滑”的动态动画。\n\n### CSS 描述\n\n```css\n::view-transition-old(root),\n::view-transition-new(root) { animation-duration: 0.3s; }\n\nhtml:active-view-transition-type(slide-left) {\n  &::view-transition-old(root) { animation: slide-out-left 0.3s ease-in-out; }\n  &::view-transition-new(root) { animation: slide-in-right 0.3s ease-in-out; }\n}\n```\n\n### 两者同时启用时\n\n避免 View Transition 和 Vue `<Transition>` 同时跑，用一小段中间件关掉 Vue 过渡：\n\n```ts\n// app/middleware/disable-vue-transitions.global.ts\nexport default defineNuxtRouteMiddleware((to) => {\n  if (false || !document.startViewTransition) return\n  to.meta.pageTransition = false\n  to.meta.layoutTransition = false\n})\n```\n\n## 注意事项\n\nView Transitions 在执行期间会冻结 DOM。如果 `<script setup>` 里的数据请求在导航过程中才解决，可能会看到停顿。这类场景继续使用 Vue 过渡比较稳。\n\n过渡能让导航有原生应用的质感，少量 CSS 就能把体验拉一截。\n","/books/nuxtjs/chapters/zh_Hans_CN/10-data-fetching.md":"# 数据获取\n\nNuxt 给了 4 个数据获取工具：`useFetch`、`useAsyncData`、`useLazyFetch`、`$fetch`。它们的共同目标是 —— 在服务端获取、客户端复用、按 key 去重 —— 但各自适用的场景不同。\n\n## `useFetch` —— 90% 的场景\n\n```vue\n<script setup lang=\"ts\">\nconst { data, pending, error, refresh, status } = await useFetch('/api/posts')\n<\/script>\n\n<template>\n  <p v-if=\"pending\">加载中…</p>\n  <p v-else-if=\"error\">出错了</p>\n  <ul v-else>\n    <li v-for=\"p in data\" :key=\"p.id\">{{ p.title }}</li>\n  </ul>\n</template>\n```\n\n它自动做了：\n- SSR 阶段在服务端请求，客户端导航再走一次。\n- 按 URL + 选项缓存，多个组件可复用。\n- 合并并发请求。\n- 通过 Nitro 推导的 API 类型给响应加上类型。\n\n常用选项：\n\n```ts\nawait useFetch('/api/posts', {\n  query: { page: 1 },\n  headers: { 'x-role': 'admin' },\n  transform: (posts) => posts.map(p => ({ ...p, shortTitle: p.title.slice(0, 32) })),\n  pick: ['id', 'title'],\n  watch: [search],           // 这些 ref 变化时重新请求\n  default: () => [],         // 首次未返回前的默认值\n})\n```\n\n## `useAsyncData` —— 任意异步逻辑\n\n不是简单 `fetch`（比如 URL 需要计算、要查询数据库、要合并多个来源），用 `useAsyncData`，并给一个**稳定的 key**：\n\n```vue\n<script setup lang=\"ts\">\nconst route = useRoute()\nconst { data: post } = await useAsyncData(\n  () => `post-${route.params.id}`,\n  () => $fetch(`/api/posts/${route.params.id}`),\n  { watch: [() => route.params.id] }\n)\n<\/script>\n```\n\n其实 `useFetch` 就是 `useAsyncData(key, () => $fetch(url, opts))` 的薄封装。\n\n## `useLazyFetch` —— 不阻塞导航\n\n`useFetch` 默认 `await`，整个页面要等数据就位才渲染。次要内容可以用 `useLazyFetch`（或 `useFetch(..., { lazy: true })`）后台加载：\n\n```vue\n<script setup lang=\"ts\">\nconst { data, pending } = useLazyFetch('/api/recommendations')\n<\/script>\n\n<template>\n  <SkeletonList v-if=\"pending\" />\n  <RecommendationList v-else :items=\"data\" />\n</template>\n```\n\n## `$fetch` —— 事件中的直连 HTTP\n\n`$fetch` 是底层 HTTP 客户端（基于 `ofetch`），用来在事件处理器、提交动作、服务端路由里做命令式调用。**不要**拿来当页面主数据源 —— 它不参与 SSR payload。\n\n```vue\n<script setup lang=\"ts\">\nasync function createPost () {\n  return await $fetch('/api/posts', {\n    method: 'POST',\n    body: { title: '新文章' },\n  })\n}\n<\/script>\n```\n\n## 重新拉取\n\n`useFetch` / `useAsyncData` 都返回了 `refresh()`：\n\n```ts\nconst { data, refresh } = await useFetch('/api/posts')\n\nasync function addPost () {\n  await $fetch('/api/posts', { method: 'POST', body: {/* … */} })\n  await refresh()\n}\n```\n\n其他组件里想刷新？用同一个 key：\n\n```ts\nawait refreshNuxtData('posts')        // 按 key 刷新\nawait refreshNuxtData()               // 全部刷新\n```\n\n## 服务端路由：数据从哪里来\n\n处理器放在 `server/api/`，文件名后缀映射 HTTP 方法：\n\n```ts\n// server/api/posts.get.ts\nexport default defineEventHandler(() => {\n  return [{ id: 1, title: 'Hello' }]\n})\n```\n\n```ts\n// server/api/posts.post.ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody<{ title: string }>(event)\n  // 写库 …\n  return { ok: true }\n})\n```\n\n暴露为 `/api/posts`，页面里用 `useFetch('/api/posts')` 或 `$fetch('/api/posts', ...)` 调用。\n\n## API 路由的类型\n\nNuxt 会自动根据 `server/` 文件生成类型，`useFetch` 与 `$fetch` 都能拿到响应类型：\n\n```ts\nconst { data } = await useFetch('/api/posts') // data: Post[]\n```\n\n想导出响应类型复用：\n\n```ts\n// server/api/posts.get.ts\nexport type PostsResponse = Awaited<ReturnType<typeof handler>>\nconst handler = defineEventHandler(() => fetchPosts())\nexport default handler\n```\n\n## 选择原则\n\n- **页面级 SSR 数据？** → `useFetch`\n- **自定义异步逻辑 + 稳定缓存？** → `useAsyncData`\n- **次要的非阻塞数据？** → `useLazyFetch` / `lazy: true`\n- **用户触发的写操作？** → `$fetch` + `refresh()`\n- **多个组件要同一份数据？** → 用相同的 key\n\n照这个路子，页面就能 SSR 到位、预取顺滑、不做重复请求。\n","/books/nuxtjs/chapters/zh_Hans_CN/11-state-management.md":"# 状态管理\n\nNuxt 自带 `useState` —— 一个 SSR 友好、能在服务端到客户端之间保活、按 key 共享的 `ref` 替代品。大多数“全局状态”用它就够。业务更重时再上 Pinia。\n\n## `useState` 基础\n\n```vue\n<script setup lang=\"ts\">\nconst counter = useState('counter', () => Math.round(Math.random() * 1000))\n<\/script>\n\n<template>\n  <div>\n    Counter: {{ counter }}\n    <button @click=\"counter++\">+</button>\n    <button @click=\"counter--\">-</button>\n  </div>\n</template>\n```\n\n关键点：\n- 第一个参数是**唯一 key**，相同 key 在全应用共享同一份值。\n- 第二个参数是**初始化函数**，只在首次创建时执行。\n- 值会被 JSON 序列化以完成 SSR hydration。别塞类、函数或 symbol。\n\n### 为什么不要写 `const state = ref()` 作为模块级变量？\n\n模块级 ref 在服务端会**跨请求共享** —— 这是经典的跨用户数据泄露与内存泄露来源。永远把状态包进 composable：\n\n```ts\n// app/composables/useCart.ts\nexport const useCart = () => useState('cart', () => [] as CartItem[])\n```\n\n组件里直接用：\n\n```ts\nconst cart = useCart()\n```\n\n## 用异步数据初始化\n\n用 `callOnce` 让初始化每次请求只跑一次：\n\n```vue\n<script setup lang=\"ts\">\nconst websiteConfig = useState('config', () => null)\n\nawait callOnce(async () => {\n  websiteConfig.value = await $fetch('/api/site-config')\n})\n<\/script>\n```\n\n效果类似 Nuxt 2 的 `nuxtServerInit` —— 服务端一次填充，客户端 hydration 直接拿到。\n\n## 共享的、自动导入的 composable\n\n`app/composables/` 会自动导入，小小 composable 就是强大的基元：\n\n```ts\n// app/composables/useColor.ts\nexport const useColor = () => useState<string>('color', () => 'pink')\n```\n\n```vue\n<script setup lang=\"ts\">\nconst color = useColor()\n<\/script>\n\n<template>\n  <p>Current color: {{ color }}</p>\n</template>\n```\n\n## 清理状态\n\n临时数据在导航或登出后要清掉：\n\n```ts\nawait clearNuxtState('cart')     // 某个 key\nawait clearNuxtState()           // 清空所有\n```\n\n## 结合服务端检测 + 客户端偏好\n\n一个真实模式：先取服务端默认值，再让客户端覆盖：\n\n```ts\nexport const useLocale = () => {\n  return useState<string>('locale', () => useDefaultLocale().value)\n}\n\nexport const useDefaultLocale = (fallback = 'zh-CN') => {\n  const locale = ref(fallback)\n  if (false) {\n    const reqLocale = useRequestHeaders()['accept-language']?.split(',')[0]\n    if (reqLocale) locale.value = reqLocale\n  } else if (true) {\n    const nav = navigator.language\n    if (nav) locale.value = nav\n  }\n  return locale\n}\n```\n\n## 大型 store 用 Pinia\n\n当你有多个相互关联的 action、模块和派生视图时，上 Pinia：\n\n```bash\npnpm add pinia @pinia/nuxt\n```\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  modules: ['@pinia/nuxt'],\n})\n```\n\n```ts\n// app/stores/website.ts\nexport const useWebsiteStore = defineStore('website', {\n  state: () => ({ name: '', description: '' }),\n  actions: {\n    async fetch () {\n      const info = await $fetch('/api/site-config')\n      this.name = info.name\n      this.description = info.description\n    },\n  },\n})\n```\n\n```vue\n<script setup lang=\"ts\">\nconst website = useWebsiteStore()\nawait callOnce(website.fetch)\n<\/script>\n```\n\nPinia 的 state 也会自动跨 SSR 序列化，跟 `useState` 一样。\n\n## 其它选择\n\nNuxt 不押注某一种方案，需要时可选：\n- 不可变状态 → [Harlem](https://nuxt.com/modules/harlem)\n- 状态机 → [XState](https://nuxt.com/modules/xstate)\n- 轻量 signals → 直接在 composable 里写 `ref` / `computed`\n\n可以自由组合，唯一铁律：**服务端不要在模块顶层持有可变状态**。\n\n## 怎么选？\n\n| 需求                              | 用哪个                             |\n|-----------------------------------|------------------------------------|\n| 全站共享几个变量                  | `useState` + composable            |\n| 大量派生逻辑、多 action           | Pinia store                        |\n| 每次请求的服务端数据              | `useState` + `callOnce`            |\n| 正式的状态机                      | XState + `@nuxtjs/xstate`          |\n\n等业务确实需要，再升级到更重的方案。\n","/books/nuxtjs/chapters/zh_Hans_CN/12-error-handling.md":"# 错误处理\n\nNuxt 是全栈框架，错误可能来自四个方向：Vue 渲染、应用启动、Nitro 服务端、JS chunk 下载。每一种都有对应的处理工具。\n\n## Vue 错误\n\n在组件内用 `onErrorCaptured` 捕获；全局一次性注册用插件：\n\n```ts\n// app/plugins/error-report.ts\nexport default defineNuxtPlugin((nuxtApp) => {\n  nuxtApp.vueApp.config.errorHandler = (error, instance, info) => {\n    // 上报到 Sentry / Datadog / 自己的后端\n  }\n\n  nuxtApp.hook('vue:error', (error, instance, info) => {\n    // 即使被处理过的错误也会触发\n  })\n})\n```\n\n`vue:error` 就是 Nuxt 版的 `onErrorCaptured` —— 所有冒泡到顶层的错误都会过这里。\n\n## 启动错误\n\n插件初始化、SSR、客户端挂载阶段的错误会触发 `app:error`。可在这里做埋点、清缓存、重置状态。\n\n## Nitro 服务端错误\n\n服务端错误不会被 Vue 边界捕获，会直接进入错误页面。要自定义响应，在 event handler 里 `throw createError({...})`：\n\n```ts\n// server/api/posts/[id].get.ts\nexport default defineEventHandler((event) => {\n  const post = findPost(event.context.params!.id)\n  if (!post) {\n    throw createError({ status: 404, statusText: 'Post not found' })\n  }\n  return post\n})\n```\n\n## Chunk 加载失败\n\n用户停留在旧页面、你又重新部署时，旧的 chunk URL 可能 404。Nuxt 默认会触发硬刷新。想自定义，把 `experimental.emitRouteChunkError` 设为 `'manual'`，然后自己监听 `nuxt:chunk-error`。\n\n## `error.vue` —— 兜底页面\n\n触发**致命错误**（未捕获的服务端错误、或客户端 `createError({ fatal: true })`）时，Nuxt 会渲染 `~/error.vue`：\n\n```vue\n\x3c!-- error.vue --\x3e\n<script setup lang=\"ts\">\nimport type { NuxtError } from '#app'\n\nconst props = defineProps<{ error: NuxtError }>()\n\nconst handleError = () => clearError({ redirect: '/' })\n<\/script>\n\n<template>\n  <section>\n    <h1>{{ error.statusCode }}</h1>\n    <p>{{ error.statusMessage }}</p>\n    <button @click=\"handleError\">回首页</button>\n  </section>\n</template>\n```\n\n几个细节：\n- `error.vue` 是一次**单独的页面渲染**，路由中间件会重新跑。可以用 `useError()` 判断“当前是不是错误页”。\n- `Accept: application/json` 的请求不会拿到 HTML，而是 JSON 响应。\n\n## 抛错：`createError`\n\n```ts\nimport { createError } from '#app'\n\nif (!data.value) {\n  throw createError({\n    status: 404,\n    statusText: 'Page Not Found',\n  })\n}\n```\n\n- 在**服务端**（setup / 插件 / handler）抛出，会直接进错误页面。\n- 在**客户端**默认是非致命的；要进错误页，加 `{ fatal: true }`。\n\n`statusText` 请保持简短、仅 ASCII。长描述或国际化文案放到 `message` 或 `data` 里。\n\n## `showError` 与 `clearError`\n\n- `showError(err)`：命令式触发错误页面。\n- `clearError({ redirect: '/' })`：清掉当前错误，可选跳转。\n\nsetup / handler 里优先 `throw createError(...)`；已经 catch 住再手动暴露时再用 `showError`。\n\n## `NuxtErrorBoundary` —— 局部错误 UI\n\n不要因为一个组件挂了就崩整页，包一层：\n\n```vue\n<template>\n  <NuxtErrorBoundary @error=\"report\">\n    <RemoteChart />\n    <template #error=\"{ error, clearError }\">\n      <p>图表加载失败。</p>\n      <button @click=\"clearError\">重试</button>\n    </template>\n  </NuxtErrorBoundary>\n</template>\n\n<script setup lang=\"ts\">\nfunction report (err: unknown) { /* 上报 */ }\n<\/script>\n```\n\n`error = null` 会重新渲染默认 slot，所以要先解决根因，否则 slot 立刻再抛。\n\n## `useError()` —— 读取当前错误\n\n在中间件、插件、布局里都能读：\n\n```ts\nconst error = useError()\nif (error.value) {\n  // 正处于错误页流程\n}\n```\n\n用来跳过登录检查、隐藏外壳都很方便。\n\n## Checklist\n\n- 插件里注册全局 handler（`errorHandler` + `vue:error`）。\n- 提供 `error.vue`，给用户一条友好的恢复路径。\n- Nitro handler 用 `createError` 抛 4xx / 5xx。\n- 高风险组件包 `NuxtErrorBoundary`。\n- 上报 chunk-error 指标，及时发现发版导致的会话中断。\n\n好的错误体验本身就是一个功能，认真对待。\n","/books/nuxtjs/chapters/zh_Hans_CN/13-server.md":"# 服务端与 Nitro\n\n每个 Nuxt 应用背后都有 **Nitro** —— 负责 SSR、API 路由、中间件、部署预设的 HTTP 引擎。`server/` 目录下的一切都跑在 Nitro 上，Vite 不参与。\n\n## Nitro 的能力\n\n- 构建出一个 `.output/` 目录，能运行在所有 JS 运行时。\n- 15+ 部署预设（Node、Vercel、Netlify、Cloudflare Workers、AWS Lambda、Deno……）。\n- 基于 `h3` 的 HTTP 原语 —— 小、快、可移植。\n- 通过 `routeRules` 做混合渲染（SSR / ISR / SWR / 预渲染 / CSR）。\n- 文件式 API 路由、中间件、插件。\n\n## 目录布局\n\n```\nserver/\n├── api/                # /api/* 接口\n│   ├── posts.get.ts\n│   └── posts.post.ts\n├── routes/             # 自定义路由\n│   └── sitemap.xml.ts\n├── middleware/         # 每个请求都会跑\n└── plugins/            # Nitro 插件（钩子、长连接）\n```\n\nHMR 与自动导入机制跟 Vue 层一致。\n\n## API 路由\n\n文件名后缀映射 HTTP 方法：\n\n```ts\n// server/api/posts.get.ts\nexport default defineEventHandler(async () => {\n  return await db.posts.findMany()\n})\n```\n\n```ts\n// server/api/posts.post.ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody<{ title: string }>(event)\n  const post = await db.posts.create({ data: body })\n  return post\n})\n```\n\n可以返回字符串、可 JSON 序列化对象、`Response` 或 `ReadableStream`，Nitro 负责序列化。\n\n动态参数用方括号：\n\n```ts\n// server/api/posts/[id].get.ts\nexport default defineEventHandler((event) => {\n  const id = event.context.params!.id\n  return db.posts.findUnique({ where: { id: Number(id) } })\n})\n```\n\n## 自定义路由（非 API）\n\n想要根路径的 `/sitemap.xml`、`/healthz`？放 `server/routes/`：\n\n```ts\n// server/routes/healthz.ts\nexport default defineEventHandler(() => 'ok')\n```\n\n## 服务端中间件\n\n在所有 API handler 之前，每个服务端请求都会跑：\n\n```ts\n// server/middleware/request-id.ts\nexport default defineEventHandler((event) => {\n  event.context.requestId = crypto.randomUUID()\n})\n```\n\n通过 `event.context` 往下游传值最方便。\n\n## Nitro 插件\n\n挂到服务端生命周期上做日志、数据库连接池、定时任务等：\n\n```ts\n// server/plugins/db.ts\nimport { prisma } from '~~/server/utils/prisma'\n\nexport default defineNitroPlugin((nitro) => {\n  nitro.hooks.hook('close', async () => {\n    await prisma.$disconnect()\n  })\n})\n```\n\n## h3 常用工具\n\nNuxt / Nitro 内置的这些工具又快又兼容 Edge：\n\n```ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody(event)           // 解析 body\n  const query = getQuery(event)                // 解析 ?query\n  const headers = getRequestHeaders(event)     // 读取 headers\n  setResponseStatus(event, 201)                // 设置状态码\n  setResponseHeader(event, 'x-custom', '1')    // 设置响应头\n  return { ok: true }\n})\n```\n\n## 错误\n\n用 `createError` 抛结构化错误：\n\n```ts\nthrow createError({ status: 404, statusText: 'Post Not Found' })\n```\n\nNitro 会转成正确的 HTTP 响应（面向 HTML 请求还会渲染错误页）。\n\n## 通过 `routeRules` 做混合渲染\n\nSSR / ISR / 预渲染 / CSR 的策略都放在 `routeRules` 里：\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },              // 构建期静态化\n    '/blog/**': { swr: 3600 },             // SWR 1 小时\n    '/api/public/*': { cache: { maxAge: 300 } },\n    '/admin/**': { ssr: false },           // 只 CSR\n    '/old': { redirect: { to: '/new', statusCode: 301 } },\n  },\n})\n```\n\nNitro 会把这些规则翻译成各平台原生能力：Cloudflare Workers 用 Cache API，Vercel 用 ISR，Node 用内存缓存。\n\n## 部署相关\n\n部署预设常见几个：\n\n```ts\nexport default defineNuxtConfig({\n  nitro: {\n    preset: 'node-server',    // 也可 'vercel'、'netlify'、'cloudflare-pages' 等\n  },\n})\n```\n\n详见“部署”那一章 —— 这里只要记住 `server/` 里的东西都会随应用一起进入你选的运行时。\n\n## Checklist\n\n- 数据库连接放 Nitro 插件里，每个进程一个连接池。\n- 横切关注点（请求 ID、鉴权）用 `server/middleware/`。\n- 渲染策略能用 `routeRules` 就用，别自己写轮子。\n- 抛错用 `createError`，不要手写状态码。\n- 纯数据逻辑抽到 `server/utils/`，它们也会被自动导入。\n\n至此你就拥有了一个完整的全栈 Nuxt 应用：前端 Vue + 后端 Nitro，都在同一个仓库里。\n","/books/nuxtjs/chapters/zh_Hans_CN/14-layers.md":"# 分层与主题\n\nLayer 能把一组 Nuxt 配置、组件、composable、服务端路由和资源打包成一个可复用的单元 —— 本地目录、npm 包，或 Git 源都行。可以把它当作“可继承的功能”。\n\n## 为什么用 Layer？\n\n- 跨项目复用配置预设（`nuxt.config`、`app.config`）。\n- 用 `app/components/` 搭建组件库。\n- 用 `app/composables/` / `app/utils/` 发布工具函数。\n- 发布带默认值的 Nuxt 模块套装。\n- 在 monorepo 里按领域拆分（DDD），每层只管自己那片。\n- 做主题和脚手架模板。\n\nLayer 的目录结构**跟 Nuxt 应用本身一模一样**，写 layer 就像写个小号 Nuxt 项目。\n\n## 两种引入方式\n\n### 自动注册 `~~/layers/*`\n\n从 Nuxt 3.12 起，`~~/layers/` 下的任何目录都会被自动注册：\n\n```\nmy-app/\n├── layers/\n│   ├── base/\n│   └── admin/\n├── app/\n└── nuxt.config.ts\n```\n\nNuxt 也会暴露别名，例如 `#layers/admin`，指向该 layer 的 `srcDir`。\n\n### 显式 `extends`\n\n项目外、npm 包、Git 上的 layer 用 `extends`：\n\n```ts\nexport default defineNuxtConfig({\n  extends: [\n    '../base',                                // 相对路径\n    '@my-themes/awesome',                     // npm 包\n    'github:my-themes/awesome#v1',            // Git 分支/tag\n    ['github:my-themes/private-awesome',\n      { auth: process.env.GITHUB_TOKEN }],    // 私有仓库\n  ],\n})\n```\n\n远程 layer 由 `unjs/giget` 下载，配置合并由 `unjs/c12` 负责。\n\n也可以改 layer 的别名：\n\n```ts\nexport default defineNuxtConfig({\n  extends: [\n    ['github:my-themes/awesome',\n      { meta: { name: 'my-awesome-theme' } }],\n  ],\n})\n```\n\n## 优先级与覆盖顺序\n\n多个 layer 定义同一个文件时，Nuxt 按以下优先级合并（高 → 低）：\n\n1. 你自己项目的文件\n2. 自动扫描的 `~~/layers/*`（字母序，`Z > A`）\n3. `extends` 中的条目（靠前 > 靠后）\n\n用数字前缀控制顺序：\n\n```\nlayers/\n├── 1.base/          # layer 中优先级最低\n├── 2.features/\n└── 3.admin/         # layer 中优先级最高\n```\n\n项目自身的文件始终优先，除非显式不覆盖。\n\n## 编写一个 Layer\n\n一个 layer 就是迷你版 Nuxt 项目。比如一个 admin layer：\n\n```\nlayers/admin/\n├── app/\n│   ├── components/AdminNav.vue\n│   └── composables/useAdmin.ts\n├── nuxt.config.ts\n└── app.config.ts\n```\n\n```ts\n// layers/admin/nuxt.config.ts\nexport default defineNuxtConfig({\n  css: ['~/assets/admin.css'],\n})\n```\n\nLayer 中声明的一切都会合入消费端：组件自动导入、`app/pages/` 下的路由加入路由表、`routeRules` 一并合并。\n\n## 发布 Layer\n\n1. `package.json` 暴露 `nuxt.config.ts`：\n\n   ```json\n   {\n     \"name\": \"@me/nuxt-awesome\",\n     \"type\": \"module\",\n     \"exports\": {\n       \".\": {\n         \"import\": \"./nuxt.config.ts\"\n       }\n     }\n   }\n   ```\n\n2. 用户安装后注册：\n\n   ```ts\n   export default defineNuxtConfig({\n     extends: ['@me/nuxt-awesome'],\n   })\n   ```\n\n3. 记得把对外 API 写进文档：暴露的组件、composable、模块选项、需要的环境变量。\n\n## Layer、Module、Plugin 的差别\n\n- **Layer**：整块的应用切片（页面、组件、composable、配置、服务端路由）。适合复用**面向用户的功能**。\n- **Module**：程序式扩展 Nuxt 构建（注入别名、追加模块、修改配置）。适合配置其他 layer 或集成外部库。\n- **Plugin**：运行时代码（客户端/服务端），启动时执行。适合在运行时提供服务。\n\n一个大产品往往三者都有：Layer 提供业务 UI，Module 把它自动接上，Plugin 做运行时胶水。\n\n## 值得抄作业的场景\n\n- 同一个 monorepo 里把「营销站 → 文档站 → 产品应用」拆成三个 layer。\n- 把登录（中间件、登录页、composable）打包成可复用 layer。\n- 跨项目共用设计系统，用 `app.config` 下发 token。\n- 面向公司定制的内部主题，所有 Nuxt 项目都来继承。\n\nLayer 让 Nuxt 从“框架”进化成“你们团队的平台”。\n","/books/nuxtjs/chapters/zh_Hans_CN/15-rendering-modes.md":"# 渲染模式\n\nNuxt 是“一个框架多种形态”：SSR、SSG、ISR、SWR、纯 CSR。通过 `routeRules` 按路由选择，Nitro 会把你的决定翻译成每个平台的原生能力。\n\n## 四种模式\n\n### SSR（服务端渲染）—— 默认\n\n每次请求都在服务端渲染。适合：\n- 个性化页面（鉴权、账户、购物车）。\n- 随用户或请求变化的内容。\n- 对实时 SEO 有要求的页面。\n\n不用额外配置，你一开始就在这个模式里。\n\n### SSG（静态站点生成）—— `prerender`\n\n构建期渲染，直接发纯 HTML。适合：\n- 营销页、落地页。\n- 已知 slug 的文档、博客。\n\n```bash\npnpm build && pnpm preview\n# 或者纯静态托管：\npnpm generate\n```\n\n按路由声明：\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },\n    '/about': { prerender: true },\n  },\n})\n```\n\n`nuxt generate` 相当于对所有路由开启 `prerender`，并额外生成 `/200.html`、`/404.html` 作为 SPA fallback。\n\n### ISR（增量静态再生）—— `isr`\n\n先发缓存 HTML，过期后在后台重新生成。适合：\n- 流量大但内容偶尔更新的页面。\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/blog/**': { isr: 60 },        // 缓存 60 秒\n    '/docs/**': { isr: true },      // 缓存到下次发版\n  },\n})\n```\n\n平台支持：Vercel、Netlify、Cloudflare（视预设而定）。\n\n### SWR（stale-while-revalidate）—— `swr`\n\n思路跟 ISR 类似，但靠 HTTP 缓存头 / CDN 实现：\n\n```ts\nrouteRules: {\n  '/feed': { swr: 30 },             // 30 秒内允许发旧内容\n}\n```\n\n### 纯 CSR —— `ssr: false`\n\n完全跳过 SSR。适合：\n- 登录后访问的仪表板。\n- 首屏就依赖 `window` / `document` 的页面。\n\n```ts\nrouteRules: {\n  '/admin/**': { ssr: false },\n}\n```\n\n想全站 CSR，全局关掉 SSR：\n\n```ts\nexport default defineNuxtConfig({\n  ssr: false,\n})\n```\n\n再 `nuxt generate` 会得到一个 `index.html`，由它在客户端启动 Vue —— 经典 SPA。\n\n## `routeRules` —— 决策集中在一起\n\n一条规则可以组合多个关注点：\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },\n    '/blog/**': { swr: 3600, headers: { 'cache-control': 's-maxage=3600' } },\n    '/api/public/*': { cache: { maxAge: 300 } },\n    '/admin/**': { ssr: false, appMiddleware: 'auth' },\n    '/legacy': { redirect: { to: '/new', statusCode: 301 } },\n    '/proxy/**': { proxy: 'https://api.example.com/**' },\n  },\n})\n```\n\n可用 key：`prerender`、`isr`、`swr`、`ssr`、`cache`、`headers`、`redirect`、`proxy`、`noScripts`、`appMiddleware`。通配符：`*` 单段、`**` 多段。\n\n## 静态托管 vs 混合托管\n\n| 模式                         | 输出                                  | 托管位置                           |\n|------------------------------|---------------------------------------|------------------------------------|\n| 全 SSR                       | `.output/server/` + `public/`         | Node、Vercel、Netlify、Cloudflare  |\n| 全 SSG（`nuxt generate`）    | 只有 `.output/public/`                | 任意静态托管 + CDN                 |\n| 混合（按路由）               | 预渲染 + SSR 产物混合                 | 支持 ISR / SWR 的平台              |\n| CSR（`ssr: false` + generate）| 静态 `index.html` + JS bundle        | 任意静态托管                       |\n\n## 怎么选？\n\n- **登录后看板** → SSR（没 SEO 需求可以 `ssr: false`）。\n- **营销页** → `prerender: true`。\n- **博客 / 文档 / 目录** → ISR 或 SWR。\n- **自家 API** → `/api/*` 加 `cache.maxAge`（幂等接口）。\n- **重定向 / 代理** → 走 `routeRules`，别自己写中间件。\n\n## 易踩的坑\n\n- **`noScripts: true`** 不会发 Nuxt 运行时 JS —— 做纯静态落地页香，交互页面慎用。\n- **按路由 `ssr: false`** 也会在服务端构建外壳，客户端只 hydrate JS。\n- **`prerender` 动态路由**需要爬虫提示或显式列表（`nitro.prerender.routes`）。\n- **CDN 边缘**场景下把 `swr` / `isr` 配上合适的 cache-control，体验会更顺。\n\n每条路由挑一种渲染模式，意图统一写在 `routeRules`，剩下的交给 Nitro。\n","/books/nuxtjs/chapters/zh_Hans_CN/16-deployment.md":"# 部署\n\n得益于 Nitro，**同一份代码**可以部署到裸机 Node 服务器、Serverless、Edge 运行时或纯静态托管 —— 通常只要换个 preset 就行。\n\n## 构建产物\n\n`pnpm build` 会生成两个目录：\n\n```\n.output/\n├── server/            # 入口 + 面向 Serverless 的服务端代码\n└── public/            # 静态资源与预渲染 HTML\n```\n\n`pnpm preview` 起一个本地等价于生产的服务器。\n\n纯静态托管用 `pnpm generate`，它会预渲染所有可达路由，只输出 `.output/public/`。\n\n## Node.js 服务器（默认 preset）\n\n适合 VPS / Docker / 传统 PaaS：\n\n```bash\nnode .output/server/index.mjs\n# 监听 http://0.0.0.0:3000\n```\n\n支持的环境变量：\n- `NITRO_PORT` / `PORT`（默认 `3000`）\n- `NITRO_HOST` / `HOST`（默认 `0.0.0.0`）\n- `NITRO_SSL_CERT` / `NITRO_SSL_KEY` —— 开发用 HTTPS\n\n### 进程管理（PM2）\n\n```js\n// ecosystem.config.cjs\nmodule.exports = {\n  apps: [{\n    name: 'my-nuxt-app',\n    port: '3000',\n    exec_mode: 'cluster',\n    instances: 'max',\n    script: './.output/server/index.mjs',\n  }],\n}\n```\n\n`pm2 start ecosystem.config.cjs` 就开启多核集群模式。\n\n### Docker\n\n一份最小多阶段镜像：\n\n```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY . .\nRUN corepack enable && pnpm install --frozen-lockfile && pnpm build\n\nFROM node:22-alpine\nWORKDIR /app\nCOPY --from=build /app/.output ./.output\nEXPOSE 3000\nCMD [\"node\", \".output/server/index.mjs\"]\n```\n\n## 静态托管\n\n两种玩法：\n\n1. **SSG 保留 SSR 内容**（`ssr: true`，默认）。`nuxt generate` 预渲染路由，生成 `200.html` / `404.html` 作为动态路由的 SPA fallback。\n2. **纯 CSR SPA**（`ssr: false`）。`nuxt generate` 会输出一个标准 `index.html`，在浏览器里 hydrate。\n\n把 `.output/public/` 上传到任意静态托管 —— S3、Cloudflare Pages、Netlify、Vercel、GitHub Pages 都行。\n\n## Serverless / Edge 预设\n\nNuxt 自带 15+ Nitro preset。配置里指定：\n\n```ts\nexport default defineNuxtConfig({\n  nitro: { preset: 'vercel' },\n})\n```\n\n或者通过环境变量：\n\n```bash\nNITRO_PRESET=vercel pnpm build\nNITRO_PRESET=netlify pnpm build\nNITRO_PRESET=cloudflare-pages pnpm build\nNITRO_PRESET=deno-deploy pnpm build\nNITRO_PRESET=aws-lambda pnpm build\n```\n\n大多数托管平台（Vercel、Netlify、Cloudflare Pages）能自动识别 Nuxt 并选中 preset。\n\n## 环境变量\n\n仅服务端的密钥放 `runtimeConfig`，用 `NUXT_*` 环境变量覆盖：\n\n```ini\nNUXT_API_SECRET=prod_secret\nNUXT_PUBLIC_API_BASE=https://api.example.com\n```\n\n客户端需要的 key 放到 `runtimeConfig.public`。\n\n## 发版 checklist\n\n- [ ] `pnpm typecheck` / `pnpm lint` / `pnpm test` 全部通过。\n- [ ] 本地 `pnpm build` 成功。\n- [ ] 平台上配好必要环境变量（`NUXT_*`、DB URL、密钥）。\n- [ ] `routeRules` 检查一遍 —— 能预渲染的预渲染，必须 SSR 的才 SSR。\n- [ ] 公共路由配好 `cache` / `swr` 头。\n- [ ] 需要健康检查的平台加上 `/healthz`。\n- [ ] 日志收集已接入（大多数平台 `console` 就够）。\n- [ ] 错误上报插件已启用（`vue:error` + 服务端 `error` 钩子）。\n\n## CDN / Cloudflare 注意事项\n\n用 Cloudflare 代理时，关掉这两项：\n\n1. **Speed → Content Optimization → Rocket Loader™**\n2. **Security → Email Address Obfuscation**\n\n它们注入的脚本会破坏 hydration。\n\n## 回滚方案\n\n- Serverless 平台至少保留两个构建版本 —— 旧页面 tab 还持有老的 chunk URL，要让它们能拿到响应（或让 Nuxt 的 chunk-error 处理器硬刷新）。\n- 打 Git tag（`git tag v1.2.3`），便于快速回滚。\n\n## 不止 Node\n\n- **Deno** —— `NITRO_PRESET=deno-deploy` 或 `deno-server`。\n- **Bun** —— 目前用 Node preset 也能跑；原生 Bun preset 处于 beta。\n- **Workers / Workerd** —— `cloudflare-pages`、`cloudflare-workers` preset 一等公民支持 `routeRules` 映射。\n\n有了 preset 系统，换运行时就是一次配置更改 + `NITRO_PRESET`。“一次构建、随处部署”，这就是 Nuxt 给你的承诺。\n","/books/nuxtjs/chapters/zh_Hant_HK/01-introduction.md":"# 初識 Nuxt\n\n**Nuxt** 是建基於 **Vue 3** 與 **Nitro** 的開源全棧框架。它能把一堆 `.vue` 檔案直接變成生產級應用：SSR、檔案式路由、自動匯入、通用部署，全部開箱即用。\n\n## 為甚麼選 Nuxt？\n\n- **約定優於設定**：一套有主張的目錄結構，自動化處理重複工作。\n- **預設 SSR**：首屏更快、SEO 更佳、在低效能裝置上也順暢。\n- **隨處部署**：Node、Deno、靜態代管、Serverless、Edge 全都支援，沒有廠商鎖定。\n- **同構資料獲取**：`useFetch` / `useAsyncData` 在伺服器端與客戶端行為一致。\n- **零設定 TypeScript**：類型與 `tsconfig.json` 自動產生，毋須額外設定。\n- **模組生態成熟**：Pinia、Tailwind、Content、Image、i18n、Auth Utils…… 一行指令搞掂。\n\n## 框架內建能力\n\n- **檔案式路由**：`app/pages/` 中的檔案即路由。\n- **程式碼分割**：每條路由只載入最少 JS。\n- **自動匯入**：元件、composable、工具函式無需手動 `import`。\n- **SEO 工具**：`useHead`、`useSeoMeta`、`<Title>`、`<Meta>`。\n- **錯誤邊界**：`error.vue`、`NuxtErrorBoundary`、`createError`。\n- **混合渲染**：透過 `routeRules` 為每條路由指定 SSR / ISR / SWR / 預渲染 / 純 CSR。\n\n## 伺服器引擎：Nitro\n\nNitro 是 Nuxt 的 HTTP 引擎，內部基於 `h3`，帶來：\n- 完整伺服器端能力（`server/api`、`server/middleware`）。\n- 15+ 部署預設，一鍵輸出至 Vercel / Netlify / Cloudflare Workers / AWS Lambda / Deno 等。\n- 透過 `routeRules` 實作混合渲染。\n\n正式環境建置後僅產生一個 `.output/` 目錄，可於任何 JS 執行時運行。\n\n## 架構一覽\n\nNuxt 由多個單一職責的套件組成：\n\n- 核心引擎：`nuxt`\n- 打包器：`@nuxt/vite-builder`、`@nuxt/rspack-builder`、`@nuxt/webpack-builder`\n- CLI：`@nuxt/cli`\n- 伺服器引擎：`nitro`\n- 開發套件：`@nuxt/kit`（用於撰寫模組與 Layer）\n\n## Nuxt 與 Next.js 對照\n\n| 面向           | Nuxt (Vue)                          | Next.js (React)                       |\n|----------------|-------------------------------------|---------------------------------------|\n| 路由           | `app/pages/` 檔案式                 | `app/` 檔案式                          |\n| 渲染           | SSR / SSG / ISR / CSR（`routeRules`）| SSR / SSG / PPR / ISR（App Router）  |\n| 資料獲取       | `useFetch`、`useAsyncData`、`$fetch`  | Server Components、`fetch`、`use()`  |\n| 伺服器原語     | Nitro + `server/api`                | Route Handlers + Server Actions       |\n| 執行時目標     | Node / Deno / Workers / Edge / 靜態  | Node / Vercel / Adapter              |\n\n兩者皆擁抱「App Router + 約定」路線。Nuxt 更偏向約定與預設，Next.js 則押注 React Server Components。\n\n## 本書會涵蓋甚麼\n\n全書 16 章，圍繞 Nuxt 4 的實戰路線展開：\n- 檔案系統如何對應到應用結構（視圖、頁面、佈局、資源）。\n- 如何於伺服器端與客戶端安全地獲取、修改、快取並分享資料。\n- 如何將 SEO、過渡動畫、錯誤處理、混合渲染等正式特性落地。\n- 如何用 Layer、模組與自訂部署擴充 Nuxt。\n\n準備好了？先裝起來，動手寫下第一頁。\n","/books/nuxtjs/chapters/zh_Hant_HK/02-installation.md":'# 安裝與建立專案\n\n建立一個 Nuxt 4 專案只需一條指令。本章介紹環境需求、官方 CLI、手動安裝，以及往後會一直用到的目錄結構。\n\n## 環境需求\n\n- **Node.js 20.x** 或更新版本，建議使用偶數 LTS（20、22、……）。\n- **終端機 + 編輯器**：VS Code + Vue (Volar) 擴充套件或 WebStorm 皆可。\n- Windows 使用者建議：採用 WSL，並以 `127.0.0.1` 取代 `localhost`，可避免 DNS 慢與 HMR 抖動。\n\n## 以 CLI 初始化\n\n```bash\n# pnpm\npnpm create nuxt@latest my-app\n\n# npm\nnpm create nuxt@latest my-app\n\n# bun\nbun create nuxt@latest my-app\n```\n\nCLI 會詢問套件管理器、Linter、CSS 方案等。選擇完畢後：\n\n```bash\ncd my-app\npnpm install\npnpm dev -o   # -o 自動開啟瀏覽器\n```\n\n開啟 `http://localhost:3000` 即可看到預設歡迎頁。\n\n## 手動安裝\n\n想了解底層運作，就從零做起：\n\n```bash\nmkdir my-app && cd my-app\npnpm init\npnpm add -D nuxt\n```\n\n在 `package.json` 新增指令：\n\n```json\n{\n  "scripts": {\n    "dev": "nuxt dev",\n    "build": "nuxt build",\n    "preview": "nuxt preview"\n  }\n}\n```\n\n新建 `app.vue`：\n\n```vue\n<template>\n  <div>Hello Nuxt 4</div>\n</template>\n```\n\n執行 `pnpm dev` 即可造訪 `http://localhost:3000`，其餘事項 Nuxt 會自動處理。\n\n## 目錄結構\n\nNuxt 4 推薦的目錄：\n\n```\nmy-app/\n├── app/\n│   ├── app.vue              # 根元件\n│   ├── pages/               # 檔案式路由\n│   ├── layouts/             # 佈局\n│   ├── components/          # 自動匯入的元件\n│   ├── composables/         # 自動匯入的組合式函式\n│   ├── utils/               # 自動匯入的工具\n│   ├── middleware/          # 路由中介層\n│   ├── plugins/             # 客戶端／伺服器外掛\n│   └── assets/              # 由 Vite 處理的資源\n├── public/                  # 原樣由 / 提供\n├── server/\n│   ├── api/                 # /api/* 端點\n│   ├── middleware/          # 伺服器中介層\n│   └── plugins/             # Nitro 外掛\n├── nuxt.config.ts           # 框架設定\n├── tsconfig.json            # 由 .nuxt/ 繼承\n└── package.json\n```\n\n兩點必須記住：\n- **`app/` 下的內容皆自動匯入** —— 自家元件與 composable 皆毋須手動 `import`。\n- **`server/` 執行於 Nitro** 而非 Vite，伺服器端點都寫於此。\n\n## 開發伺服器\n\n```bash\npnpm dev              # 啟動 dev 伺服器\npnpm dev -o           # 自動開啟瀏覽器\npnpm dev --port 4000  # 改變連接埠\n```\n\nHMR、TypeScript、Vite、Nitro dev server 全數自動就位。\n\n## `nuxt.config.ts` 基本用法\n\n最簡 `nuxt.config.ts` 通常會啟用 DevTools 並註冊模組：\n\n```ts\nexport default defineNuxtConfig({\n  devtools: { enabled: true },\n  modules: [\'@pinia/nuxt\', \'@nuxtjs/tailwindcss\'],\n})\n```\n\n這樣便足以開工。下一章會詳述環境變數覆寫、`runtimeConfig` 與 `app.config` 的分別、TypeScript 整合等細節，不過目前已可開始寫頁面。\n\n## 寫下第一頁\n\n把 `app.vue` 改為：\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n```\n\n新建 `app/pages/index.vue`：\n\n```vue\n<template>\n  <section>\n    <h1>Hello Nuxt 4</h1>\n    <p>30 秒體驗檔案式路由。</p>\n  </section>\n</template>\n```\n\n路由完全由 `app/pages/` 產生，毋須任何路由表。\n',"/books/nuxtjs/chapters/zh_Hant_HK/03-configuration.md":"# 配置系統\n\nNuxt 內建合理預設，多數專案只需調整數個選項。`nuxt.config.ts` 是唯一真相來源：模組、環境覆寫、runtimeConfig、Vite/Nitro 轉發、Vue 開關皆於此設定。\n\n## `nuxt.config.ts`\n\n```ts\nexport default defineNuxtConfig({\n  devtools: { enabled: true },\n  modules: ['@pinia/nuxt', '@nuxtjs/tailwindcss'],\n  css: ['~/assets/css/main.css'],\n})\n```\n\n`defineNuxtConfig` 全域可用（無需 import），並提供完整 TypeScript 補全。\n\n## 依環境覆寫\n\nNuxt 會在合併前優先讀取 `$development`、`$production`、`$env.*`：\n\n```ts\nexport default defineNuxtConfig({\n  $production: {\n    routeRules: {\n      '/**': { isr: true },\n    },\n  },\n  $development: {\n    devtools: { enabled: true },\n  },\n  $env: {\n    staging: { app: { baseURL: '/staging/' } },\n  },\n})\n```\n\n建置時用 `nuxt build --envName staging` 指定環境。底層由 `c12` 實作。\n\n## `runtimeConfig` 與 `app.config` 的差異\n\n兩者皆可暴露設定值，但用途不同：\n\n| 關注點                        | `runtimeConfig`             | `app.config`                 |\n|-------------------------------|------------------------------|------------------------------|\n| 定義位置                      | `nuxt.config.ts`             | `app.config.ts`              |\n| 支援環境變數覆寫              | 是（`NUXT_*`）               | 否                           |\n| 客戶端可存取                  | 僅 `public` 下的鍵           | 完整打包                     |\n| 支援非基本 JS 型別            | 否                            | 是                           |\n| 響應式                        | 是                            | 是                           |\n| 適合擺放                      | 金鑰、URL、Token             | 主題、站點中繼資料、旗標     |\n\n### `runtimeConfig`\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  runtimeConfig: {\n    apiSecret: '',          // 僅伺服器端\n    public: {\n      apiBase: '/api',      // 曝露給客戶端\n    },\n  },\n})\n```\n\n以環境變數覆寫：\n\n```ini\nNUXT_API_SECRET=prod_secret\nNUXT_PUBLIC_API_BASE=https://api.example.com\n```\n\n以 composable 讀取：\n\n```vue\n<script setup lang=\"ts\">\nconst { apiSecret, public: { apiBase } } = useRuntimeConfig()\n<\/script>\n```\n\n> 客戶端取到的 `apiSecret` 會是 `undefined`。\n\n### `app.config`\n\n```ts\n// app/app.config.ts\nexport default defineAppConfig({\n  title: 'Hello Nuxt',\n  theme: { dark: true, colors: { primary: '#3b82f6' } },\n})\n```\n\n```vue\n<script setup lang=\"ts\">\nconst appConfig = useAppConfig()\n<\/script>\n```\n\n`app.config` 支援 HMR —— 改主題會立刻生效。\n\n## TypeScript\n\nNuxt 會自動產生 `.nuxt/tsconfig.json`，專案 `tsconfig.json` 繼承它，毋須手動設定。\n\n需要自訂時：\n\n```ts\nexport default defineNuxtConfig({\n  typescript: {\n    strict: true,\n    typeCheck: true, // 建置時執行 vue-tsc\n  },\n})\n```\n\n## 向 Vite / Nitro / PostCSS 傳選項\n\nNuxt **不會** 讀取外部的 `vite.config.ts` / `nitro.config.ts`，需透過 Nuxt 的 key 轉發：\n\n```ts\nexport default defineNuxtConfig({\n  vite: {\n    plugins: [/* … */],\n  },\n  nitro: {\n    preset: 'node-server',\n    compressPublicAssets: true,\n  },\n  postcss: {\n    plugins: {\n      'postcss-nested': {},\n    },\n  },\n})\n```\n\n## Vue 相關開關\n\n```ts\nexport default defineNuxtConfig({\n  vue: {\n    propsDestructure: true, // 實驗特性\n  },\n})\n```\n\n也可直接透過 `vite.vue` 對 `@vitejs/plugin-vue` 傳參。\n\n## 應用層級 head\n\n欲套用至所有頁面的靜態標籤放在 `app.head`：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      titleTemplate: '%s · Nuxt 小冊',\n      htmlAttrs: { lang: 'zh-Hant' },\n      link: [{ rel: 'icon', href: '/favicon.ico' }],\n    },\n  },\n})\n```\n\n頁面層級、動態標籤改用 `useHead()` —— 詳見 SEO 章節。\n\n## 如何抉擇？\n\n- **專案內的靜態旗標？** → `app.config`\n- **金鑰或環境相關？** → `runtimeConfig`\n- **建置期轉換？** → `nuxt.config`（`vite` / `nitro` key）\n- **按環境切換？** → `$development` / `$production` / `$env`\n\n一個檔案、四個旋鈕，足以把專案跑起來。\n","/books/nuxtjs/chapters/zh_Hant_HK/04-views.md":'# 視圖結構\n\nNuxt 提供 4 層介面建構：`app.vue`、**元件**、**頁面**、**佈局**。各司其職，Nuxt 會自動串接，多數時候只需新增檔案即可。\n\n## `app.vue`\n\n`app.vue` 是應用入口。若完全不需路由：\n\n```vue\n<template>\n  <div>\n    <h1>歡迎</h1>\n  </div>\n</template>\n```\n\n需要多頁面時掛上 `<NuxtPage />`：\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n```\n\n若**刪除** `app.vue`，Nuxt 會產生預設入口，等同於 `<NuxtLayout><NuxtPage /></NuxtLayout>`。\n\n## 元件\n\n`app/components/` 下的所有內容會自動匯入，毋須手寫 `import`：\n\n```\napp/components/\n├── AppHeader.vue\n├── AppFooter.vue\n└── AppAlert.vue\n```\n\n```vue\n<template>\n  <div>\n    <AppHeader />\n    <AppAlert>Hello，來自自動匯入的元件。</AppAlert>\n    <AppFooter />\n  </div>\n</template>\n```\n\n巢狀目錄會成為名稱前綴：`app/components/base/Button.vue` → `<BaseButton />`。\n\n小建議：\n- `<template>` 一律採用 PascalCase。\n- 前置 `Lazy` 可延遲載入：`<LazyAppHeavy />`。\n\n## 頁面\n\n頁面存放於 `app/pages/`，每個 `.vue` 檔即一條路由：\n\n```\napp/pages/\n├── index.vue          →  /\n├── about.vue          →  /about\n└── blog/\n    ├── index.vue      →  /blog\n    └── [id].vue       →  /blog/:id\n```\n\n```vue\n\x3c!-- app/pages/about.vue --\x3e\n<template>\n  <section>\n    <h1>關於我們</h1>\n  </section>\n</template>\n```\n\n頁面會自動分包，`<NuxtLink>` 會協助預取，`definePageMeta({ … })` 用於宣告頁面層級中繼資料。\n\n## 佈局\n\n佈局用於包裹多個頁面，承載共用外殼（頁首、頁尾、側欄）。檔案置於 `app/layouts/`：\n\n```vue\n\x3c!-- app/layouts/default.vue --\x3e\n<template>\n  <div>\n    <AppHeader />\n    <slot />\n    <AppFooter />\n  </div>\n</template>\n```\n\n`default.vue` 會自動套用。如需自訂佈局，於頁面宣告：\n\n```vue\n<script setup lang="ts">\ndefinePageMeta({ layout: \'admin\' })\n<\/script>\n\n<template>\n  <div>Dashboard</div>\n</template>\n```\n\n……對應檔案放於 `app/layouts/admin.vue`。\n\n## `<NuxtLayout>` 與 `<NuxtPage>`\n\n- `<NuxtPage />` 為檔案式路由的「出口」，負責串流渲染、預取與過渡動畫。\n- `<NuxtLayout>` 決定當前頁面採用哪個佈局，亦可透過 `name` 手動指定：\n\n```vue\n<NuxtLayout name="admin">\n  <NuxtPage />\n</NuxtLayout>\n```\n\n若僅有一個佈局，可跳過 `layouts/`，將外殼直接寫於 `app.vue`。\n\n## 要點回顧\n\n| 需求                                  | 該往哪放                       |\n|---------------------------------------|--------------------------------|\n| 根入口／全站外殼                      | `app.vue`（或單一佈局）          |\n| 可重用的 UI 區塊                      | `app/components/`              |\n| 一條路由 + 視圖                       | `app/pages/`                   |\n| 多頁共享的外殼                        | `app/layouts/`                 |\n\n## 進階：自訂 HTML 模板\n\n若 `useHead` 無法滿足（需改動 Nitro 輸出的原始 HTML），可透過 `render:html` 鉤子撰寫 Nitro 外掛：\n\n```ts\n// server/plugins/meta.ts\nexport default defineNitroPlugin((nitroApp) => {\n  nitroApp.hooks.hook(\'render:html\', (html, { event }) => {\n    html.head.push(\'<meta name="description" content="自訂描述" />\')\n  })\n})\n```\n\n此方式宜節制使用 —— `useHead` 已涵蓋 99% 情境，靜態資源與 SEO 章節將介紹更順手的做法。\n',"/books/nuxtjs/chapters/zh_Hant_HK/05-assets.md":"# 靜態資源\n\nNuxt 提供兩個擺放靜態資源的目錄，行為大不相同。選對可省下許多壞連結、體積膨脹、快取失效的麻煩。\n\n## `public/` —— 原樣對外提供\n\n`public/` 的所有內容會以根 URL **原樣** 提供：\n\n```\npublic/\n├── favicon.ico\n├── robots.txt\n└── img/\n    └── hero.png          → /img/hero.png\n```\n\n以絕對路徑引用：\n\n```vue\n<template>\n  <img src=\"/img/hero.png\" alt=\"Hero\" />\n</template>\n```\n\n`public/` 適合放：\n- favicon、`robots.txt`、OG 預設圖、`sitemap.xml`\n- 需要穩定、可預測 URL 的圖片\n- 與行銷／CDN 設定共用的檔案\n\n此處檔案**不帶指紋**，瀏覽器可能永久快取，修改後需更名或自訂快取標頭。\n\n## `app/assets/` —— 由 Vite 處理\n\n`app/assets/` 下的檔案會經 Vite：壓縮、加指紋、優化引用。以 `~/` 或 `@/` 引用：\n\n```vue\n<template>\n  <img src=\"~/assets/img/hero.png\" alt=\"Hero\" />\n</template>\n```\n\n`app/assets/` 適合放：\n- CSS / SCSS / Less 檔案\n- 需要內嵌或轉換的 SVG\n- 需要加指紋與優化的圖片\n- 隨應用打包的字型\n\n此處檔案**不會**於瀏覽器以 `/assets/…` 存取，Vite 會改寫為 `/_nuxt/hero-abc123.png` 之類帶雜湊 URL。\n\n## 快速對照\n\n| 行為                           | `public/`           | `app/assets/`            |\n|--------------------------------|----------------------|---------------------------|\n| 根 URL 存取                    | 是（`/foo.png`）     | 否（由 Vite 改寫）        |\n| 會壓縮／處理                   | 否                   | 是                        |\n| 檔名帶指紋                     | 否                   | 是                        |\n| 於 Vue 中如何引用              | `/img/foo.png`       | `~/assets/img/foo.png`    |\n| 適合                           | 穩定的公共 URL       | 優化後的建置產物          |\n\n## 圖片進階：`<NuxtImg>` 與 `<NuxtPicture>`\n\n認真處理圖片建議採用 `@nuxt/image`：\n\n```bash\npnpm add -D @nuxt/image\n```\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  modules: ['@nuxt/image'],\n  image: {\n    quality: 80,\n    format: ['webp', 'avif'],\n  },\n})\n```\n\n然後即可使用 `<NuxtImg>` / `<NuxtPicture>`：\n\n```vue\n<template>\n  <NuxtImg\n    src=\"/img/hero.png\"\n    width=\"1200\"\n    height=\"630\"\n    loading=\"lazy\"\n    sizes=\"sm:100vw md:50vw lg:400px\"\n  />\n</template>\n```\n\n它負責：\n- 響應式 `srcset`\n- 自動 WebP / AVIF\n- 延遲載入\n- 各大 CDN Provider（Cloudinary、IPX、imgix、Vercel……）\n\n## 字型\n\nNuxt 官方提供 `@nuxt/fonts` 模組，可自代管 Google Fonts／本地字型並避免 CLS：\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@nuxt/fonts'],\n  fonts: {\n    families: [\n      { name: 'Inter', provider: 'google' },\n    ],\n  },\n})\n```\n\n於 CSS 直接使用該字型名稱即可，模組會自動注入 `@font-face`。\n\n想用本地字型？置於 `public/fonts/` 後：\n\n```css\n/* app/assets/css/fonts.css */\n@font-face {\n  font-family: 'Display';\n  src: url('/fonts/display.woff2') format('woff2');\n  font-display: swap;\n}\n```\n\n## 一句話指南\n\n- **需要穩定公共 URL？** → `public/`\n- **需要加指紋／優化？** → `app/assets/`\n- **大量圖片？** → `@nuxt/image`\n- **字型？** → `@nuxt/fonts` 或 `public/fonts/`\n\n每類資源只挑一個位置，讓團隊慣例一致。\n","/books/nuxtjs/chapters/zh_Hant_HK/06-styling.md":"# 樣式方案\n\nNuxt 對樣式方案幾乎不設限：純 CSS、預處理器、Tailwind／UnoCSS，或 CSS Modules —— 皆為「一等公民」。\n\n## 本地樣式檔\n\n把原始檔放於 `app/assets/`，任一元件皆可引用：\n\n```vue\n<script>\nimport '~/assets/css/first.css'\n<\/script>\n\n<style>\n@import url(\"~/assets/css/second.css\");\n</style>\n```\n\n若要全域注入，於 `nuxt.config.ts` 宣告：\n\n```ts\nexport default defineNuxtConfig({\n  css: ['~/assets/css/main.css'],\n})\n```\n\n全域 CSS 會被內嵌到 SSR HTML，首屏即為已套用樣式的版本。\n\n## 外部樣式表\n\n自設定掛上 `<link>`：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      link: [\n        { rel: 'stylesheet', href: 'https://cdn.example.com/reset.css' },\n      ],\n    },\n  },\n})\n```\n\n或透過 `useHead` 動態掛載：\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  link: [{ rel: 'stylesheet', href: 'https://cdn.example.com/theme.css' }],\n})\n<\/script>\n```\n\n外部樣式表會阻塞渲染，可本地化者盡量本地化。\n\n## 預處理器（SCSS / Sass / Less / Stylus）\n\n安裝預處理器後 Vite 會自動識別：\n\n```bash\npnpm add -D sass\n```\n\n```vue\n<style lang=\"scss\">\n@use \"~/assets/scss/main.scss\";\n</style>\n```\n\n或全域註冊：\n\n```ts\nexport default defineNuxtConfig({\n  css: ['~/assets/scss/main.scss'],\n})\n```\n\n希望每個檔都自動注入變數、設計 token？使用 Vite 的 `preprocessorOptions`：\n\n```ts\nexport default defineNuxtConfig({\n  vite: {\n    css: {\n      preprocessorOptions: {\n        scss: { additionalData: '@use \"~/assets/_colors.scss\" as *;' },\n      },\n    },\n  },\n})\n```\n\n## 單檔元件樣式\n\n這正是 Vue 的強項：\n\n```vue\n<script setup lang=\"ts\">\nconst isActive = ref(true)\nconst color = ref('crimson')\n<\/script>\n\n<template>\n  <div :class=\"['box', { active: isActive }]\">\n    <p :style=\"{ color }\">hello</p>\n  </div>\n</template>\n\n<style scoped>\n.box { padding: 1rem; }\n.active { outline: 2px solid v-bind(color); }\n</style>\n```\n\n重點特性：\n- **`scoped`** —— 樣式僅作用於當前元件。\n- **`v-bind()`** —— 於 `<style>` 中使用響應式值。\n- **`module`** —— CSS Modules，透過 `$style` 存取：\n\n```vue\n<template>\n  <p :class=\"$style.red\">red</p>\n</template>\n<style module>\n.red { color: red; }\n</style>\n```\n\n- **`lang=\"scss\"`**（或 `sass`、`less`、`stylus`）—— 每個 `<style>` 可單獨選擇預處理器。\n\n## PostCSS\n\nNuxt 已預先設定 `postcss-import`、`postcss-url`、`autoprefixer`、`cssnano`。欲追加其他外掛：\n\n```ts\nexport default defineNuxtConfig({\n  postcss: {\n    plugins: {\n      'postcss-nested': {},\n      'postcss-custom-media': {},\n    },\n  },\n})\n```\n\nSFC 內使用 `<style lang=\"postcss\">` 可獲得正確語法突顯。\n\n## Tailwind CSS / UnoCSS\n\n採用 utility-first 方案時，官方模組是最快路徑：\n\n```bash\npnpm add -D @nuxtjs/tailwindcss\n```\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@nuxtjs/tailwindcss'],\n})\n```\n\n或 UnoCSS：\n\n```bash\npnpm add -D @unocss/nuxt\n```\n\n```ts\nexport default defineNuxtConfig({\n  modules: ['@unocss/nuxt'],\n})\n```\n\n兩者皆會自動注入所產生之 CSS，毋須手動 `import 'uno.css'`。\n\n## 以佈局區隔風格\n\n當應用不同區域風格差異顯著（如深色後台 vs 淺色行銷頁），把外殼拆到佈局最乾淨：\n\n```vue\n\x3c!-- app/layouts/admin.vue --\x3e\n<template>\n  <div class=\"admin-shell\"><slot /></div>\n</template>\n\n<style>\n.admin-shell { background: #0f172a; color: #e2e8f0; }\n</style>\n```\n\n## 策略選擇\n\n| 需求                                         | 選擇                         |\n|----------------------------------------------|------------------------------|\n| 工具類 + 小團隊                              | Tailwind / UnoCSS            |\n| 元件層級隔離                                 | `<style scoped>`             |\n| 徹底避免類名衝突                             | `<style module>`             |\n| 設計系統 + 設計 token                        | SCSS + `additionalData`      |\n| 可自訂主題                                   | `app.config` + CSS 變數      |\n\n沒有所謂「標準答案」，Nuxt 允許你於同一專案混合使用。\n","/books/nuxtjs/chapters/zh_Hant_HK/07-routing.md":"# 路由系統\n\nNuxt 的路由基於 **vue-router**，但你幾乎毋須手寫設定。`app/pages/` 下每個檔案即一條路由 —— 自動分包、自動預取、自動型別。\n\n## 檔案式路由\n\n```\napp/pages/\n├── index.vue          →  /\n├── about.vue          →  /about\n└── posts/\n    ├── index.vue      →  /posts\n    └── [id].vue       →  /posts/:id\n```\n\nNuxt 會產生完整的型別化路由樹，`<NuxtLink>` 的 `to` 亦具型別。\n\n## 動態段\n\n方括號即 params：\n\n```vue\n\x3c!-- app/pages/posts/[id].vue --\x3e\n<script setup lang=\"ts\">\nconst route = useRoute()\nconsole.log(route.params.id) // string\n<\/script>\n```\n\n多個參數亦可：`app/pages/users/[userId]/posts/[postId].vue` → `/users/:userId/posts/:postId`。\n\n**Catch-all**：`app/pages/docs/[...slug].vue` 會匹配 `/docs/a/b/c`，`route.params.slug = ['a', 'b', 'c']`。\n\n## 巢狀路由\n\n讓 `parent.vue` 與 `parent/` 目錄共存，父元件內使用 `<NuxtPage />`：\n\n```vue\n\x3c!-- app/pages/settings.vue --\x3e\n<template>\n  <section>\n    <aside>\x3c!-- 導覽 --\x3e</aside>\n    <NuxtPage />\n  </section>\n</template>\n```\n\n```\napp/pages/\n├── settings.vue\n└── settings/\n    ├── profile.vue      →  /settings/profile\n    └── billing.vue      →  /settings/billing\n```\n\n## `<NuxtLink>` 導航\n\n應用內跳轉一律使用 `<NuxtLink>`：\n\n```vue\n<template>\n  <nav>\n    <NuxtLink to=\"/\">首頁</NuxtLink>\n    <NuxtLink to=\"/about\">關於</NuxtLink>\n    <NuxtLink :to=\"{ name: 'posts-id', params: { id: 1 } }\">文章 1</NuxtLink>\n  </nav>\n</template>\n```\n\n它會：\n- 渲染真正的 `<a>`（對無障礙友善）。\n- 連結進入視窗時自動預取元件與 payload。\n- Hydration 後走客戶端跳轉，不會整頁重新整理。\n- 自動附加 `.router-link-active` / `.router-link-exact-active` 類別。\n\n若要暫時關閉預取：\n\n```vue\n<NuxtLink to=\"/heavy\" :prefetch=\"false\">Heavy page</NuxtLink>\n```\n\n## 程式化導航\n\n```vue\n<script setup lang=\"ts\">\nconst router = useRouter()\nfunction openPost (id: number) {\n  router.push(`/posts/${id}`)\n}\n<\/script>\n```\n\n於中介層、外掛、事件處理器中，優先使用 `navigateTo()`：\n\n```ts\nexport default defineNuxtRouteMiddleware((to) => {\n  if (!to.path.startsWith('/public')) return navigateTo('/login')\n})\n```\n\n## 路由中介層\n\n三種形態：\n1. **內嵌**：直接寫在頁面的 `definePageMeta({ middleware: (to) => {…} })`。\n2. **具名**：檔案置於 `app/middleware/auth.ts`，頁面手動宣告使用。\n3. **全域**：`app/middleware/foo.global.ts`，每次導航皆會執行。\n\n```ts\n// app/middleware/auth.ts\nexport default defineNuxtRouteMiddleware((to) => {\n  const { user } = useUserSession()\n  if (!user.value) return navigateTo('/login')\n})\n```\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ middleware: 'auth' })\n<\/script>\n```\n\n> 路由中介層僅於 Vue（客戶端 & SSR）層執行。伺服器／API 請求請改用 `server/middleware/`。\n\n## 路由驗證\n\n以 `validate` 驗證參數：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  validate (route) {\n    return typeof route.params.id === 'string' && /^\\d+$/.test(route.params.id)\n  },\n})\n<\/script>\n```\n\n回傳假值會觸發 404；回傳 `{ status, statusText }` 可自訂回應。\n\n## `definePageMeta` 一覽\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  layout: 'admin',          // 指定佈局\n  middleware: ['auth'],     // 路由中介層\n  pageTransition: false,    // 關閉過渡\n  validate: (r) => true,    // 驗證\n  key: (r) => r.path,       // 重新渲染策略\n})\n<\/script>\n```\n\n於 `/posts/1` → `/posts/2` 這類同路由不同參數的場景，加上 `key` 可讓元件重新建立。\n\n## 小結\n\n- 把 `.vue` 丟進 `app/pages/`，路由即已就位。\n- 應用內跳轉一律 `<NuxtLink>`，自動享受預取與過渡。\n- 鑑權、重導向、守衛交給中介層。\n- `definePageMeta` 用於宣告佈局、中介層、驗證等。\n","/books/nuxtjs/chapters/zh_Hant_HK/08-seo-meta.md":"# SEO 與 Meta 標籤\n\nNuxt 底層的 head 管理由 [Unhead](https://unhead.unjs.io/) 提供。你可獲得響應式 composable、型別安全的 SEO 工具及內建元件 —— 依團隊習慣任選風格。\n\n## `nuxt.config.ts` —— 全域預設\n\n擺放不隨頁面變化的標籤：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      titleTemplate: '%s · Nuxt 小冊',\n      htmlAttrs: { lang: 'zh-Hant' },\n      link: [{ rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' }],\n    },\n  },\n})\n```\n\nNuxt 亦會自動注入：\n- `<meta charset=\"utf-8\">`\n- `<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">`\n\n## `useHead` —— 響應式 composable\n\n```vue\n<script setup lang=\"ts\">\nconst title = ref('Hello World')\n\nuseHead({\n  title,\n  meta: [{ name: 'description', content: '我的超棒網站。' }],\n  bodyAttrs: { class: 'app' },\n  script: [{ innerHTML: 'console.log(\"loaded\")' }],\n})\n<\/script>\n```\n\n`useHead` 支援響應式值，多處呼叫亦會自動去重。\n\n## `useSeoMeta` —— 型別安全的 SEO\n\n`useSeoMeta` 知道 `name` 與 `property` 的差異，拼錯或寫錯會即時被型別系統攔截：\n\n```vue\n<script setup lang=\"ts\">\nuseSeoMeta({\n  title: '我的超棒網站',\n  description: '此站做了啥，來了解一下。',\n  ogTitle: '我的超棒網站',\n  ogDescription: '此站做了啥，來了解一下。',\n  ogImage: 'https://example.com/og.png',\n  twitterCard: 'summary_large_image',\n})\n<\/script>\n```\n\n做 SEO 時基本就用它。\n\n## 元件式寫法\n\n偏好宣告式模板者亦可：\n\n```vue\n<script setup lang=\"ts\">\nconst title = ref('Hello World')\n<\/script>\n\n<template>\n  <Head>\n    <Title>{{ title }}</Title>\n    <Meta name=\"description\" :content=\"title\" />\n  </Head>\n</template>\n```\n\n支援標籤：`<Title>`、`<Meta>`、`<Link>`、`<Script>`、`<Style>`、`<Base>`、`<NoScript>`、`<Html>`、`<Body>`。\n\n## Title template\n\n全站標題通常為「頁面 — 站點名」模式：\n\n```vue\n\x3c!-- app.vue --\x3e\n<script setup lang=\"ts\">\nuseHead({\n  titleTemplate: (title) => title ? `${title} · 我的站點` : '我的站點',\n})\n<\/script>\n```\n\n任何頁面呼叫 `useHead({ title: 'Blog' })` 都會顯示為 `Blog · 我的站點`。\n\n## Template params\n\n需要多個佔位符？\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  titleTemplate: (title) => title ? `${title} %separator %siteName` : '%siteName',\n  templateParams: { siteName: '我的站點', separator: '·' },\n})\n<\/script>\n```\n\n## 響應式\n\n所有屬性皆支援 computed、ref 或 getter，可直接跟隨路由或 API 資料：\n\n```vue\n<script setup lang=\"ts\">\nconst route = useRoute()\nconst post = await $fetch(`/api/posts/${route.params.id}`)\n\nuseSeoMeta({\n  title: () => post.title,\n  description: () => post.excerpt,\n  ogImage: () => post.image,\n})\n<\/script>\n```\n\n## 與 `definePageMeta` 聯動\n\n若希望頁面層級的靜態資訊被其他層讀取，可放於 `definePageMeta`，再於佈局內讀：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ title: '某個頁面' })\n<\/script>\n```\n\n```vue\n\x3c!-- 預設佈局 --\x3e\n<script setup lang=\"ts\">\nconst route = useRoute()\nuseHead({\n  meta: [{ property: 'og:title', content: `我的站點 · ${route.meta.title}` }],\n})\n<\/script>\n```\n\n## 該選哪個？\n\n| 需求                              | 用甚麼                   |\n|-----------------------------------|--------------------------|\n| 全站固定標籤                      | `app.head`               |\n| 響應式、任意 head 標籤            | `useHead`                |\n| 型別安全的 SEO 標籤               | `useSeoMeta`             |\n| 偏好模板寫法                      | `<Head>` / `<Title>` 等  |\n\n## Body 底部標籤\n\n部分腳本需置於 `<body>` 尾端，使用 `tagPosition`：\n\n```vue\n<script setup lang=\"ts\">\nuseHead({\n  script: [\n    {\n      src: 'https://third-party-script.com',\n      tagPosition: 'bodyClose',\n    },\n  ],\n})\n<\/script>\n```\n\n## `useHeadSafe`\n\n當需將使用者輸入合併進 head 時，優先採用 `useHeadSafe`，它會過濾危險屬性（如 `innerHTML`、事件處理器）以防 XSS。\n\n把中繼資料當作程式碼認真維護，是 Nuxt 最便宜也最划算的優化之一。\n","/books/nuxtjs/chapters/zh_Hant_HK/09-transitions.md":"# 頁面過渡動畫\n\nNuxt 復用 Vue 的 `<Transition>`，為頁面與佈局之間提供過渡效果；並以實驗形式接入瀏覽器原生的 View Transitions API，可做跨頁面動畫。\n\n## 頁面過渡\n\n於 `nuxt.config.ts` 啟用全站頁面過渡：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    pageTransition: { name: 'page', mode: 'out-in' },\n  },\n})\n```\n\n於 `app.vue` 設定 CSS：\n\n```vue\n<template>\n  <NuxtPage />\n</template>\n\n<style>\n.page-enter-active,\n.page-leave-active {\n  transition: all 0.4s;\n}\n.page-enter-from,\n.page-leave-to {\n  opacity: 0;\n  filter: blur(1rem);\n}\n</style>\n```\n\n此後每次路由切換皆會有淡入淡出 + 模糊效果。\n\n單頁覆寫全站過渡，使用 `definePageMeta`：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: { name: 'rotate', mode: 'out-in' },\n})\n<\/script>\n```\n\n## 佈局過渡\n\n若路由導致佈局變更（例如 default → admin），頁面過渡不會觸發，需改用 `layoutTransition`：\n\n```ts\nexport default defineNuxtConfig({\n  app: {\n    layoutTransition: { name: 'layout', mode: 'out-in' },\n  },\n})\n```\n\n```vue\n<template>\n  <NuxtLayout>\n    <NuxtPage />\n  </NuxtLayout>\n</template>\n\n<style>\n.layout-enter-active,\n.layout-leave-active { transition: all 0.4s; }\n.layout-enter-from,\n.layout-leave-to { filter: grayscale(1); }\n</style>\n```\n\n## 停用過渡\n\n單頁：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({ pageTransition: false, layoutTransition: false })\n<\/script>\n```\n\n全域：\n\n```ts\nexport default defineNuxtConfig({\n  app: { pageTransition: false, layoutTransition: false },\n})\n```\n\n## JavaScript 鉤子\n\nCSS 無法滿足時，改用 `<Transition>` 的 JS 鉤子，搭配 GSAP、Motion One、anime.js 都很順手：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: {\n    name: 'custom-flip',\n    mode: 'out-in',\n    onBeforeEnter: (el) => { /* 準備 */ },\n    onEnter: (el, done) => { /* 動畫結束呼叫 */ done() },\n    onAfterEnter: (el) => { /* 清理 */ },\n  },\n})\n<\/script>\n```\n\n## 動態過渡\n\n想依跳轉方向切換動畫（例如翻頁、滑動）？於內嵌中介層中決定：\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  pageTransition: { name: 'slide-right', mode: 'out-in' },\n  middleware (to, from) {\n    if (typeof to.meta.pageTransition !== 'boolean') {\n      to.meta.pageTransition!.name =\n        +to.params.id! > +from.params.id! ? 'slide-left' : 'slide-right'\n    }\n  },\n})\n<\/script>\n```\n\n## `<NuxtPage>` 上的過渡\n\n若不使用佈局，直接將 `<NuxtPage>` 放於 `app.vue`，可內嵌傳入：\n\n```vue\n<template>\n  <NuxtPage :transition=\"{ name: 'bounce', mode: 'out-in' }\" />\n</template>\n```\n\n此寫法**無法**被頁面層級覆寫。\n\n## View Transitions API（實驗）\n\n原生 View Transitions API 能讓結構完全不同的 DOM 也有流暢過渡，非常適合製作 hero 圖跨頁動畫。\n\n啟用：\n\n```ts\nexport default defineNuxtConfig({\n  experimental: { viewTransition: true },\n})\n```\n\n可選值：`false`、`true`、`'always'`。`true` 會尊重 `prefers-reduced-motion: reduce`。\n\n### 單頁控制\n\n```vue\n<script setup lang=\"ts\">\ndefinePageMeta({\n  viewTransition: {\n    enabled: true,\n    types: ['slide'],\n    toTypes: ['slide-in'],\n    fromTypes: ['slide-out'],\n  },\n})\n<\/script>\n```\n\n`types` 也可寫成函式回傳陣列，實作「id 遞增則左滑」這類動態動畫。\n\n### CSS 描述\n\n```css\n::view-transition-old(root),\n::view-transition-new(root) { animation-duration: 0.3s; }\n\nhtml:active-view-transition-type(slide-left) {\n  &::view-transition-old(root) { animation: slide-out-left 0.3s ease-in-out; }\n  &::view-transition-new(root) { animation: slide-in-right 0.3s ease-in-out; }\n}\n```\n\n### 兩者同時啟用時\n\n避免 View Transition 與 Vue `<Transition>` 同時執行，可用簡短中介層關閉 Vue 過渡：\n\n```ts\n// app/middleware/disable-vue-transitions.global.ts\nexport default defineNuxtRouteMiddleware((to) => {\n  if (false || !document.startViewTransition) return\n  to.meta.pageTransition = false\n  to.meta.layoutTransition = false\n})\n```\n\n## 注意事項\n\nView Transitions 執行期間會凍結 DOM。若 `<script setup>` 內的資料請求於導航期間才解決，可能會出現停頓。此類情境建議繼續使用 Vue 過渡。\n\n過渡能為導航增添原生應用般的質感，少量 CSS 即可顯著提升體驗。\n","/books/nuxtjs/chapters/zh_Hant_HK/10-data-fetching.md":"# 資料獲取\n\nNuxt 提供 4 種資料獲取工具：`useFetch`、`useAsyncData`、`useLazyFetch`、`$fetch`。共同目標為 —— 於伺服器獲取、客戶端重用、依 key 去重 —— 但各自適用情境不同。\n\n## `useFetch` —— 90% 情境\n\n```vue\n<script setup lang=\"ts\">\nconst { data, pending, error, refresh, status } = await useFetch('/api/posts')\n<\/script>\n\n<template>\n  <p v-if=\"pending\">載入中…</p>\n  <p v-else-if=\"error\">發生錯誤</p>\n  <ul v-else>\n    <li v-for=\"p in data\" :key=\"p.id\">{{ p.title }}</li>\n  </ul>\n</template>\n```\n\n它自動處理：\n- SSR 階段於伺服器請求，客戶端導航再執行一次。\n- 以 URL + 選項快取，多個元件可重用。\n- 合併並發請求。\n- 透過 Nitro 推導的 API 型別賦予回應型別。\n\n常用選項：\n\n```ts\nawait useFetch('/api/posts', {\n  query: { page: 1 },\n  headers: { 'x-role': 'admin' },\n  transform: (posts) => posts.map(p => ({ ...p, shortTitle: p.title.slice(0, 32) })),\n  pick: ['id', 'title'],\n  watch: [search],           // 這些 ref 變化時重新請求\n  default: () => [],         // 首次回應前的預設值\n})\n```\n\n## `useAsyncData` —— 任意非同步邏輯\n\n非單純 `fetch`（如 URL 需計算、要查資料庫、需合併多個來源）時使用 `useAsyncData`，並提供**穩定的 key**：\n\n```vue\n<script setup lang=\"ts\">\nconst route = useRoute()\nconst { data: post } = await useAsyncData(\n  () => `post-${route.params.id}`,\n  () => $fetch(`/api/posts/${route.params.id}`),\n  { watch: [() => route.params.id] }\n)\n<\/script>\n```\n\n實際上 `useFetch` 就是 `useAsyncData(key, () => $fetch(url, opts))` 的薄封裝。\n\n## `useLazyFetch` —— 不阻塞導航\n\n`useFetch` 預設 `await`，整頁需等資料就緒才渲染。次要內容可用 `useLazyFetch`（或 `useFetch(..., { lazy: true })`）背景載入：\n\n```vue\n<script setup lang=\"ts\">\nconst { data, pending } = useLazyFetch('/api/recommendations')\n<\/script>\n\n<template>\n  <SkeletonList v-if=\"pending\" />\n  <RecommendationList v-else :items=\"data\" />\n</template>\n```\n\n## `$fetch` —— 事件中的直連 HTTP\n\n`$fetch` 是底層 HTTP 客戶端（基於 `ofetch`），用於事件處理器、提交動作、伺服器路由中的命令式呼叫。**請勿**當頁面主資料來源 —— 它不參與 SSR payload。\n\n```vue\n<script setup lang=\"ts\">\nasync function createPost () {\n  return await $fetch('/api/posts', {\n    method: 'POST',\n    body: { title: '新文章' },\n  })\n}\n<\/script>\n```\n\n## 重新請求\n\n`useFetch` / `useAsyncData` 皆回傳 `refresh()`：\n\n```ts\nconst { data, refresh } = await useFetch('/api/posts')\n\nasync function addPost () {\n  await $fetch('/api/posts', { method: 'POST', body: {/* … */} })\n  await refresh()\n}\n```\n\n其他元件需刷新？共用相同 key：\n\n```ts\nawait refreshNuxtData('posts')        // 依 key 刷新\nawait refreshNuxtData()               // 全部刷新\n```\n\n## 伺服器路由：資料從哪裡來\n\n處理器置於 `server/api/`，檔名後綴對應 HTTP 方法：\n\n```ts\n// server/api/posts.get.ts\nexport default defineEventHandler(() => {\n  return [{ id: 1, title: 'Hello' }]\n})\n```\n\n```ts\n// server/api/posts.post.ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody<{ title: string }>(event)\n  // 寫入資料庫 …\n  return { ok: true }\n})\n```\n\n暴露為 `/api/posts`，頁面內以 `useFetch('/api/posts')` 或 `$fetch('/api/posts', ...)` 呼叫。\n\n## API 路由型別\n\nNuxt 會自動依 `server/` 檔案產生型別，`useFetch` 與 `$fetch` 皆可取得回應型別：\n\n```ts\nconst { data } = await useFetch('/api/posts') // data: Post[]\n```\n\n欲匯出回應型別重用：\n\n```ts\n// server/api/posts.get.ts\nexport type PostsResponse = Awaited<ReturnType<typeof handler>>\nconst handler = defineEventHandler(() => fetchPosts())\nexport default handler\n```\n\n## 選擇原則\n\n- **頁面層級 SSR 資料？** → `useFetch`\n- **自訂非同步邏輯 + 穩定快取？** → `useAsyncData`\n- **次要非阻塞資料？** → `useLazyFetch` / `lazy: true`\n- **使用者觸發的寫入？** → `$fetch` + `refresh()`\n- **多個元件共享同一份資料？** → 使用相同 key\n\n如此一來，頁面可 SSR 正確、預取順暢、避免重複請求。\n","/books/nuxtjs/chapters/zh_Hant_HK/11-state-management.md":"# 狀態管理\n\nNuxt 內建 `useState` —— 一個 SSR 友善、能於伺服器到客戶端保活、依 key 共享的 `ref` 替代品。多數「全域狀態」使用它已足夠。業務更重時再引入 Pinia。\n\n## `useState` 基礎\n\n```vue\n<script setup lang=\"ts\">\nconst counter = useState('counter', () => Math.round(Math.random() * 1000))\n<\/script>\n\n<template>\n  <div>\n    Counter: {{ counter }}\n    <button @click=\"counter++\">+</button>\n    <button @click=\"counter--\">-</button>\n  </div>\n</template>\n```\n\n重點：\n- 第一個參數是**唯一 key**，相同 key 全應用共享同一份值。\n- 第二個參數是**初始化函式**，僅於首次建立時執行。\n- 數值會以 JSON 序列化完成 SSR hydration。請勿放類別、函式或 symbol。\n\n### 為甚麼不要寫 `const state = ref()` 作為模組層級變數？\n\n模組層級 ref 於伺服器會**跨請求共享** —— 是經典的跨使用者資料洩露與記憶體洩漏來源。永遠把狀態包進 composable：\n\n```ts\n// app/composables/useCart.ts\nexport const useCart = () => useState('cart', () => [] as CartItem[])\n```\n\n元件直接使用：\n\n```ts\nconst cart = useCart()\n```\n\n## 以非同步資料初始化\n\n使用 `callOnce` 讓初始化每次請求只執行一次：\n\n```vue\n<script setup lang=\"ts\">\nconst websiteConfig = useState('config', () => null)\n\nawait callOnce(async () => {\n  websiteConfig.value = await $fetch('/api/site-config')\n})\n<\/script>\n```\n\n效果近似 Nuxt 2 的 `nuxtServerInit` —— 伺服器一次填充，客戶端 hydration 即可取得。\n\n## 共享、自動匯入的 composable\n\n`app/composables/` 會自動匯入，小小 composable 即為強大基元：\n\n```ts\n// app/composables/useColor.ts\nexport const useColor = () => useState<string>('color', () => 'pink')\n```\n\n```vue\n<script setup lang=\"ts\">\nconst color = useColor()\n<\/script>\n\n<template>\n  <p>Current color: {{ color }}</p>\n</template>\n```\n\n## 清理狀態\n\n臨時資料在導航或登出後應清除：\n\n```ts\nawait clearNuxtState('cart')     // 某個 key\nawait clearNuxtState()           // 全部清除\n```\n\n## 結合伺服器偵測 + 客戶端偏好\n\n真實情境：先取伺服器預設值，再讓客戶端覆寫：\n\n```ts\nexport const useLocale = () => {\n  return useState<string>('locale', () => useDefaultLocale().value)\n}\n\nexport const useDefaultLocale = (fallback = 'zh-HK') => {\n  const locale = ref(fallback)\n  if (false) {\n    const reqLocale = useRequestHeaders()['accept-language']?.split(',')[0]\n    if (reqLocale) locale.value = reqLocale\n  } else if (true) {\n    const nav = navigator.language\n    if (nav) locale.value = nav\n  }\n  return locale\n}\n```\n\n## 大型 store 採用 Pinia\n\n當擁有多個相關 action、模組與派生視圖時，採用 Pinia：\n\n```bash\npnpm add pinia @pinia/nuxt\n```\n\n```ts\n// nuxt.config.ts\nexport default defineNuxtConfig({\n  modules: ['@pinia/nuxt'],\n})\n```\n\n```ts\n// app/stores/website.ts\nexport const useWebsiteStore = defineStore('website', {\n  state: () => ({ name: '', description: '' }),\n  actions: {\n    async fetch () {\n      const info = await $fetch('/api/site-config')\n      this.name = info.name\n      this.description = info.description\n    },\n  },\n})\n```\n\n```vue\n<script setup lang=\"ts\">\nconst website = useWebsiteStore()\nawait callOnce(website.fetch)\n<\/script>\n```\n\nPinia 的 state 也會自動跨 SSR 序列化，與 `useState` 相同。\n\n## 其他選擇\n\nNuxt 未押注單一方案，依需擇一：\n- 不可變狀態 → [Harlem](https://nuxt.com/modules/harlem)\n- 狀態機 → [XState](https://nuxt.com/modules/xstate)\n- 輕量 signals → 於 composable 內直接寫 `ref` / `computed`\n\n可自由組合，唯一鐵律：**伺服器端不要於模組頂層持有可變狀態**。\n\n## 如何抉擇？\n\n| 需求                              | 用哪個                             |\n|-----------------------------------|------------------------------------|\n| 全站共享數個變數                  | `useState` + composable            |\n| 大量派生邏輯、多 action           | Pinia store                        |\n| 每次請求的伺服器資料              | `useState` + `callOnce`            |\n| 正式狀態機                        | XState + `@nuxtjs/xstate`          |\n\n待業務確實需要，再升級至更重方案。\n","/books/nuxtjs/chapters/zh_Hant_HK/12-error-handling.md":"# 錯誤處理\n\nNuxt 是全棧框架，錯誤來源可能有四處：Vue 渲染、應用啟動、Nitro 伺服器、JS chunk 下載。各自有對應的工具。\n\n## Vue 錯誤\n\n於元件內以 `onErrorCaptured` 捕捉；全域一次性註冊請用外掛：\n\n```ts\n// app/plugins/error-report.ts\nexport default defineNuxtPlugin((nuxtApp) => {\n  nuxtApp.vueApp.config.errorHandler = (error, instance, info) => {\n    // 回報至 Sentry / Datadog / 自家後端\n  }\n\n  nuxtApp.hook('vue:error', (error, instance, info) => {\n    // 即便已被處理的錯誤亦會觸發\n  })\n})\n```\n\n`vue:error` 即為 Nuxt 版的 `onErrorCaptured` —— 冒泡至頂層的錯誤皆會經此。\n\n## 啟動錯誤\n\n外掛初始化、SSR、客戶端掛載階段的錯誤會觸發 `app:error`。可於此埋點、清快取、重設狀態。\n\n## Nitro 伺服器錯誤\n\n伺服器錯誤不會被 Vue 邊界捕捉，會直接進入錯誤頁面。若需自訂回應，於 event handler 內 `throw createError({...})`：\n\n```ts\n// server/api/posts/[id].get.ts\nexport default defineEventHandler((event) => {\n  const post = findPost(event.context.params!.id)\n  if (!post) {\n    throw createError({ status: 404, statusText: 'Post not found' })\n  }\n  return post\n})\n```\n\n## Chunk 載入失敗\n\n使用者停留於舊頁、你再部署新版本時，舊 chunk URL 可能 404。Nuxt 預設會觸發強制重新整理。如需自訂，將 `experimental.emitRouteChunkError` 設為 `'manual'`，再自行監聽 `nuxt:chunk-error`。\n\n## `error.vue` —— 兜底頁面\n\n觸發**致命錯誤**（未捕捉的伺服器錯誤、或客戶端 `createError({ fatal: true })`）時，Nuxt 會渲染 `~/error.vue`：\n\n```vue\n\x3c!-- error.vue --\x3e\n<script setup lang=\"ts\">\nimport type { NuxtError } from '#app'\n\nconst props = defineProps<{ error: NuxtError }>()\n\nconst handleError = () => clearError({ redirect: '/' })\n<\/script>\n\n<template>\n  <section>\n    <h1>{{ error.statusCode }}</h1>\n    <p>{{ error.statusMessage }}</p>\n    <button @click=\"handleError\">回首頁</button>\n  </section>\n</template>\n```\n\n幾個細節：\n- `error.vue` 屬**獨立頁面渲染**，路由中介層會重新執行。可用 `useError()` 判斷當前是否處於錯誤頁流程。\n- `Accept: application/json` 的請求會收到 JSON 回應，而非 HTML 錯誤頁。\n\n## 拋錯：`createError`\n\n```ts\nimport { createError } from '#app'\n\nif (!data.value) {\n  throw createError({\n    status: 404,\n    statusText: 'Page Not Found',\n  })\n}\n```\n\n- 於**伺服器端**（setup／外掛／handler）拋出，會直接進入錯誤頁。\n- 於**客戶端**預設為非致命；若要進錯誤頁，加 `{ fatal: true }`。\n\n`statusText` 請保持簡短、僅 ASCII。長描述或國際化文案請放 `message` 或 `data`。\n\n## `showError` 與 `clearError`\n\n- `showError(err)`：命令式觸發錯誤頁。\n- `clearError({ redirect: '/' })`：清除當前錯誤，可選擇跳轉。\n\nsetup／handler 內優先 `throw createError(...)`；已捕捉後再手動呈現時才用 `showError`。\n\n## `NuxtErrorBoundary` —— 局部錯誤 UI\n\n勿因單一元件故障致整頁崩潰，可包裹：\n\n```vue\n<template>\n  <NuxtErrorBoundary @error=\"report\">\n    <RemoteChart />\n    <template #error=\"{ error, clearError }\">\n      <p>圖表載入失敗。</p>\n      <button @click=\"clearError\">重試</button>\n    </template>\n  </NuxtErrorBoundary>\n</template>\n\n<script setup lang=\"ts\">\nfunction report (err: unknown) { /* 回報 */ }\n<\/script>\n```\n\n將 `error = null` 會重新渲染預設 slot，請先解決根因，否則 slot 會立即再度拋出。\n\n## `useError()` —— 讀取當前錯誤\n\n於中介層、外掛、佈局中皆可讀取：\n\n```ts\nconst error = useError()\nif (error.value) {\n  // 正處於錯誤頁流程\n}\n```\n\n可用於跳過登入檢查、隱藏外殼。\n\n## Checklist\n\n- 於外掛註冊全域 handler（`errorHandler` + `vue:error`）。\n- 提供 `error.vue`，給使用者友善的復原路徑。\n- Nitro handler 以 `createError` 拋出 4xx／5xx。\n- 高風險元件包 `NuxtErrorBoundary`。\n- 監控 chunk-error 指標，及時掌握部署造成的 session 中斷。\n\n良好的錯誤體驗本身即為功能，請認真對待。\n","/books/nuxtjs/chapters/zh_Hant_HK/13-server.md":"# 伺服器與 Nitro\n\n每個 Nuxt 應用背後皆有 **Nitro** —— 負責 SSR、API 路由、中介層、部署預設的 HTTP 引擎。`server/` 目錄下的一切皆運行於 Nitro，Vite 不參與。\n\n## Nitro 的能力\n\n- 建置產生單一 `.output/` 目錄，可運行於所有 JS 執行時。\n- 15+ 部署預設（Node、Vercel、Netlify、Cloudflare Workers、AWS Lambda、Deno……）。\n- 基於 `h3` 的 HTTP 原語 —— 小、快、可攜。\n- 透過 `routeRules` 進行混合渲染（SSR / ISR / SWR / 預渲染 / CSR）。\n- 檔案式 API 路由、中介層與外掛。\n\n## 目錄佈局\n\n```\nserver/\n├── api/                # /api/* 端點\n│   ├── posts.get.ts\n│   └── posts.post.ts\n├── routes/             # 自訂路由\n│   └── sitemap.xml.ts\n├── middleware/         # 每個請求皆執行\n└── plugins/            # Nitro 外掛（鉤子、長連線）\n```\n\nHMR 與自動匯入機制與 Vue 層一致。\n\n## API 路由\n\n檔名後綴對應 HTTP 方法：\n\n```ts\n// server/api/posts.get.ts\nexport default defineEventHandler(async () => {\n  return await db.posts.findMany()\n})\n```\n\n```ts\n// server/api/posts.post.ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody<{ title: string }>(event)\n  const post = await db.posts.create({ data: body })\n  return post\n})\n```\n\n可回傳字串、可 JSON 序列化物件、`Response` 或 `ReadableStream`，Nitro 負責序列化。\n\n動態參數用方括號：\n\n```ts\n// server/api/posts/[id].get.ts\nexport default defineEventHandler((event) => {\n  const id = event.context.params!.id\n  return db.posts.findUnique({ where: { id: Number(id) } })\n})\n```\n\n## 自訂路由（非 API）\n\n需要根路徑 `/sitemap.xml` 或 `/healthz`？置於 `server/routes/`：\n\n```ts\n// server/routes/healthz.ts\nexport default defineEventHandler(() => 'ok')\n```\n\n## 伺服器中介層\n\n於所有 API handler 之前，每個伺服器請求皆會執行：\n\n```ts\n// server/middleware/request-id.ts\nexport default defineEventHandler((event) => {\n  event.context.requestId = crypto.randomUUID()\n})\n```\n\n透過 `event.context` 向下游傳遞資料最方便。\n\n## Nitro 外掛\n\n掛在伺服器生命週期上處理日誌、資料庫連線池、排程等：\n\n```ts\n// server/plugins/db.ts\nimport { prisma } from '~~/server/utils/prisma'\n\nexport default defineNitroPlugin((nitro) => {\n  nitro.hooks.hook('close', async () => {\n    await prisma.$disconnect()\n  })\n})\n```\n\n## h3 常用工具\n\nNuxt／Nitro 內建的工具既快又相容 Edge：\n\n```ts\nexport default defineEventHandler(async (event) => {\n  const body = await readBody(event)           // 解析 body\n  const query = getQuery(event)                // 解析 ?query\n  const headers = getRequestHeaders(event)     // 讀取 headers\n  setResponseStatus(event, 201)                // 設定狀態碼\n  setResponseHeader(event, 'x-custom', '1')    // 設定回應標頭\n  return { ok: true }\n})\n```\n\n## 錯誤\n\n以 `createError` 拋結構化錯誤：\n\n```ts\nthrow createError({ status: 404, statusText: 'Post Not Found' })\n```\n\nNitro 會轉為正確的 HTTP 回應（對 HTML 請求還會渲染錯誤頁）。\n\n## 以 `routeRules` 做混合渲染\n\nSSR / ISR / 預渲染 / CSR 策略皆寫於 `routeRules`：\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },              // 建置期靜態化\n    '/blog/**': { swr: 3600 },             // SWR 1 小時\n    '/api/public/*': { cache: { maxAge: 300 } },\n    '/admin/**': { ssr: false },           // 僅 CSR\n    '/old': { redirect: { to: '/new', statusCode: 301 } },\n  },\n})\n```\n\nNitro 會將這些規則翻譯為各平台原生能力：Cloudflare Workers 採 Cache API，Vercel 採 ISR，Node 採記憶體快取。\n\n## 部署相關\n\n常見部署預設：\n\n```ts\nexport default defineNuxtConfig({\n  nitro: {\n    preset: 'node-server',    // 亦可 'vercel'、'netlify'、'cloudflare-pages' 等\n  },\n})\n```\n\n詳情見「部署」章節 —— 此處只需記住 `server/` 內容會隨應用一同進入所選執行時。\n\n## Checklist\n\n- 資料庫連線置於 Nitro 外掛，每個 process 一個連線池。\n- 橫切關注點（請求 ID、鑑權）用 `server/middleware/`。\n- 渲染策略能用 `routeRules` 就用，勿自行造輪子。\n- 拋錯用 `createError`，勿手寫狀態碼。\n- 純資料邏輯抽至 `server/utils/`，它們亦會自動匯入。\n\n至此你已擁有完整的全棧 Nuxt 應用：前端 Vue + 後端 Nitro，皆位於同一儲存庫。\n","/books/nuxtjs/chapters/zh_Hant_HK/14-layers.md":"# 分層與主題\n\nLayer 能把一組 Nuxt 設定、元件、composable、伺服器路由與資源封裝為可重用單元 —— 本地目錄、npm 套件、Git 來源皆可。可將之視為「可繼承的功能」。\n\n## 為甚麼用 Layer？\n\n- 跨專案重用設定預設（`nuxt.config`、`app.config`）。\n- 以 `app/components/` 打造元件庫。\n- 以 `app/composables/` / `app/utils/` 發佈工具函式。\n- 發佈帶預設值的 Nuxt 模組套裝。\n- 於 monorepo 中按領域拆分（DDD），每層各司其職。\n- 製作主題與起始模板。\n\nLayer 的目錄結構**與 Nuxt 應用本身一致**，撰寫 layer 就像寫一個小型 Nuxt 專案。\n\n## 兩種引入方式\n\n### 自動註冊 `~~/layers/*`\n\n自 Nuxt 3.12 起，`~~/layers/` 下任何目錄皆會自動註冊：\n\n```\nmy-app/\n├── layers/\n│   ├── base/\n│   └── admin/\n├── app/\n└── nuxt.config.ts\n```\n\nNuxt 亦會曝露別名，例如 `#layers/admin`，指向該 layer 的 `srcDir`。\n\n### 明確 `extends`\n\n專案外、npm 套件、Git 上的 layer 以 `extends` 引入：\n\n```ts\nexport default defineNuxtConfig({\n  extends: [\n    '../base',                                // 相對路徑\n    '@my-themes/awesome',                     // npm 套件\n    'github:my-themes/awesome#v1',            // Git 分支／tag\n    ['github:my-themes/private-awesome',\n      { auth: process.env.GITHUB_TOKEN }],    // 私有儲存庫\n  ],\n})\n```\n\n遠端 layer 由 `unjs/giget` 下載，設定合併由 `unjs/c12` 負責。\n\n亦可更改 layer 的別名：\n\n```ts\nexport default defineNuxtConfig({\n  extends: [\n    ['github:my-themes/awesome',\n      { meta: { name: 'my-awesome-theme' } }],\n  ],\n})\n```\n\n## 優先級與覆寫順序\n\n多個 layer 定義同一檔案時，Nuxt 依以下優先級合併（高 → 低）：\n\n1. 專案自身檔案\n2. 自動掃描的 `~~/layers/*`（字母序，`Z > A`）\n3. `extends` 條目（越前面越優先）\n\n以數字前綴控制順序：\n\n```\nlayers/\n├── 1.base/          # layer 中優先級最低\n├── 2.features/\n└── 3.admin/         # layer 中優先級最高\n```\n\n專案自身檔案永遠優先，除非明確放棄覆寫。\n\n## 撰寫 Layer\n\n一個 layer 即是迷你 Nuxt 專案。以下是 admin layer 範例：\n\n```\nlayers/admin/\n├── app/\n│   ├── components/AdminNav.vue\n│   └── composables/useAdmin.ts\n├── nuxt.config.ts\n└── app.config.ts\n```\n\n```ts\n// layers/admin/nuxt.config.ts\nexport default defineNuxtConfig({\n  css: ['~/assets/admin.css'],\n})\n```\n\nLayer 所宣告者都會合入消費端：元件自動匯入、`app/pages/` 內路由加入路由表、`routeRules` 亦一併合併。\n\n## 發佈 Layer\n\n1. `package.json` 曝露 `nuxt.config.ts`：\n\n   ```json\n   {\n     \"name\": \"@me/nuxt-awesome\",\n     \"type\": \"module\",\n     \"exports\": {\n       \".\": {\n         \"import\": \"./nuxt.config.ts\"\n       }\n     }\n   }\n   ```\n\n2. 使用者安裝後註冊：\n\n   ```ts\n   export default defineNuxtConfig({\n     extends: ['@me/nuxt-awesome'],\n   })\n   ```\n\n3. 將對外 API 記錄於文件：曝露的元件、composable、模組選項、所需環境變數。\n\n## Layer、Module、Plugin 的差異\n\n- **Layer**：整塊應用切片（頁面、元件、composable、設定、伺服器路由）。適合重用**面向使用者的功能**。\n- **Module**：程式化擴充 Nuxt 建置（注入別名、追加模組、修改設定）。適合設定其他 layer 或整合外部函式庫。\n- **Plugin**：執行時程式碼（客戶端／伺服器端），啟動時執行。適合於執行時提供服務。\n\n大型產品常三者並用：Layer 提供業務 UI、Module 將其自動接入、Plugin 擔任執行時黏著劑。\n\n## 值得借鑑的場景\n\n- 同一 monorepo 內把「行銷站 → 文件站 → 產品應用」拆成三個 layer。\n- 將登入（中介層、登入頁、composable）封裝為可重用 layer。\n- 跨專案共用設計系統，以 `app.config` 下發 token。\n- 面向公司自訂的內部主題，所有 Nuxt 專案皆繼承之。\n\nLayer 讓 Nuxt 從「框架」升級為「你們團隊的平台」。\n","/books/nuxtjs/chapters/zh_Hant_HK/15-rendering-modes.md":"# 渲染模式\n\nNuxt 是「一個框架多種形態」：SSR、SSG、ISR、SWR、純 CSR。透過 `routeRules` 按路由選擇，Nitro 會將決定翻譯成各平台的原生能力。\n\n## 四種模式\n\n### SSR（伺服器渲染）—— 預設\n\n每個請求皆於伺服器渲染。適合：\n- 個人化頁面（鑑權、帳戶、購物車）。\n- 隨使用者或請求變化的內容。\n- 對即時 SEO 有要求的頁面。\n\n不需額外設定，初始即為此模式。\n\n### SSG（靜態站點生成）—— `prerender`\n\n建置期渲染，直接傳送純 HTML。適合：\n- 行銷頁、著陸頁。\n- 已知 slug 的文件、部落格。\n\n```bash\npnpm build && pnpm preview\n# 或純靜態代管：\npnpm generate\n```\n\n按路由宣告：\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },\n    '/about': { prerender: true },\n  },\n})\n```\n\n`nuxt generate` 等同於對所有路由開啟 `prerender`，並額外產生 `/200.html`、`/404.html` 作為 SPA fallback。\n\n### ISR（增量靜態再生）—— `isr`\n\n先提供快取 HTML，過期後在背景重新產生。適合：\n- 流量高但內容偶爾更新的頁面。\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/blog/**': { isr: 60 },        // 快取 60 秒\n    '/docs/**': { isr: true },      // 快取至下次部署\n  },\n})\n```\n\n平台支援：Vercel、Netlify、Cloudflare（依預設而定）。\n\n### SWR（stale-while-revalidate）—— `swr`\n\n與 ISR 概念相近，但靠 HTTP 快取標頭／CDN 實作：\n\n```ts\nrouteRules: {\n  '/feed': { swr: 30 },             // 30 秒內允許提供舊內容\n}\n```\n\n### 純 CSR —— `ssr: false`\n\n完全跳過 SSR。適合：\n- 登入後的儀表板。\n- 首屏即依賴 `window`／`document` 的頁面。\n\n```ts\nrouteRules: {\n  '/admin/**': { ssr: false },\n}\n```\n\n若整站 CSR，全域關閉 SSR：\n\n```ts\nexport default defineNuxtConfig({\n  ssr: false,\n})\n```\n\n再執行 `nuxt generate` 會得到 `index.html`，由它在客戶端啟動 Vue —— 即經典 SPA。\n\n## `routeRules` —— 決策集中於一處\n\n一條規則可組合多個關注點：\n\n```ts\nexport default defineNuxtConfig({\n  routeRules: {\n    '/': { prerender: true },\n    '/blog/**': { swr: 3600, headers: { 'cache-control': 's-maxage=3600' } },\n    '/api/public/*': { cache: { maxAge: 300 } },\n    '/admin/**': { ssr: false, appMiddleware: 'auth' },\n    '/legacy': { redirect: { to: '/new', statusCode: 301 } },\n    '/proxy/**': { proxy: 'https://api.example.com/**' },\n  },\n})\n```\n\n可用 key：`prerender`、`isr`、`swr`、`ssr`、`cache`、`headers`、`redirect`、`proxy`、`noScripts`、`appMiddleware`。萬用字元：`*` 單段、`**` 多段。\n\n## 靜態代管 vs 混合代管\n\n| 模式                         | 輸出                                  | 代管位置                           |\n|------------------------------|---------------------------------------|------------------------------------|\n| 全 SSR                       | `.output/server/` + `public/`         | Node、Vercel、Netlify、Cloudflare  |\n| 全 SSG（`nuxt generate`）    | 僅 `.output/public/`                  | 任意靜態代管 + CDN                 |\n| 混合（按路由）               | 預渲染 + SSR 產物混合                 | 支援 ISR／SWR 的平台               |\n| CSR（`ssr: false` + generate）| 靜態 `index.html` + JS bundle        | 任意靜態代管                       |\n\n## 如何抉擇？\n\n- **登入後儀表板** → SSR（無 SEO 需求可設 `ssr: false`）。\n- **行銷頁** → `prerender: true`。\n- **部落格／文件／目錄** → ISR 或 SWR。\n- **自家 API** → `/api/*` 搭配 `cache.maxAge`（冪等端點）。\n- **重定向／代理** → 走 `routeRules`，勿自行撰寫中介層。\n\n## 常見陷阱\n\n- **`noScripts: true`** 不會輸出 Nuxt 執行時 JS —— 純靜態著陸頁很好，但互動頁面請慎用。\n- **按路由 `ssr: false`** 仍會於伺服器端建置外殼，客戶端僅 hydrate JS。\n- **`prerender` 動態路由**需爬蟲提示或明確清單（`nitro.prerender.routes`）。\n- **CDN 邊緣**場景下為 `swr`／`isr` 配合適當 cache-control，體驗更佳。\n\n每條路由挑一種渲染模式，意圖統一寫於 `routeRules`，其餘交給 Nitro。\n","/books/nuxtjs/chapters/zh_Hant_HK/16-deployment.md":"# 部署\n\n藉由 Nitro，**同一份程式碼**可部署至裸機 Node 伺服器、Serverless、Edge 執行時或純靜態代管 —— 通常只需更換 preset。\n\n## 建置產物\n\n`pnpm build` 會產生兩個目錄：\n\n```\n.output/\n├── server/            # 入口 + 面向 Serverless 的伺服器程式碼\n└── public/            # 靜態資源與預渲染 HTML\n```\n\n`pnpm preview` 會啟動與正式環境等效的本地伺服器。\n\n純靜態代管用 `pnpm generate`，它會預渲染所有可達路由，僅輸出 `.output/public/`。\n\n## Node.js 伺服器（預設 preset）\n\n適合 VPS／Docker／傳統 PaaS：\n\n```bash\nnode .output/server/index.mjs\n# 監聽 http://0.0.0.0:3000\n```\n\n支援的環境變數：\n- `NITRO_PORT` / `PORT`（預設 `3000`）\n- `NITRO_HOST` / `HOST`（預設 `0.0.0.0`）\n- `NITRO_SSL_CERT` / `NITRO_SSL_KEY` —— 開發用 HTTPS\n\n### 進程管理（PM2）\n\n```js\n// ecosystem.config.cjs\nmodule.exports = {\n  apps: [{\n    name: 'my-nuxt-app',\n    port: '3000',\n    exec_mode: 'cluster',\n    instances: 'max',\n    script: './.output/server/index.mjs',\n  }],\n}\n```\n\n`pm2 start ecosystem.config.cjs` 即進入多核叢集模式。\n\n### Docker\n\n最小多階段映像：\n\n```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY . .\nRUN corepack enable && pnpm install --frozen-lockfile && pnpm build\n\nFROM node:22-alpine\nWORKDIR /app\nCOPY --from=build /app/.output ./.output\nEXPOSE 3000\nCMD [\"node\", \".output/server/index.mjs\"]\n```\n\n## 靜態代管\n\n兩種玩法：\n\n1. **SSG 保留 SSR 內容**（`ssr: true`，預設）。`nuxt generate` 預渲染路由，產生 `200.html`／`404.html` 作為動態路由的 SPA fallback。\n2. **純 CSR SPA**（`ssr: false`）。`nuxt generate` 會輸出標準 `index.html`，於瀏覽器 hydrate。\n\n將 `.output/public/` 上傳至任一靜態代管 —— S3、Cloudflare Pages、Netlify、Vercel、GitHub Pages 皆可。\n\n## Serverless／Edge 預設\n\nNuxt 內建 15+ Nitro preset。於設定指定：\n\n```ts\nexport default defineNuxtConfig({\n  nitro: { preset: 'vercel' },\n})\n```\n\n或透過環境變數：\n\n```bash\nNITRO_PRESET=vercel pnpm build\nNITRO_PRESET=netlify pnpm build\nNITRO_PRESET=cloudflare-pages pnpm build\nNITRO_PRESET=deno-deploy pnpm build\nNITRO_PRESET=aws-lambda pnpm build\n```\n\n多數代管平台（Vercel、Netlify、Cloudflare Pages）會自動識別 Nuxt 並選中 preset。\n\n## 環境變數\n\n僅伺服器端的金鑰置於 `runtimeConfig`，以 `NUXT_*` 環境變數覆寫：\n\n```ini\nNUXT_API_SECRET=prod_secret\nNUXT_PUBLIC_API_BASE=https://api.example.com\n```\n\n客戶端需要的 key 置於 `runtimeConfig.public`。\n\n## 發版 checklist\n\n- [ ] `pnpm typecheck`／`pnpm lint`／`pnpm test` 全數通過。\n- [ ] 本地 `pnpm build` 成功。\n- [ ] 平台已設定必要環境變數（`NUXT_*`、DB URL、金鑰）。\n- [ ] `routeRules` 再次檢查 —— 能預渲染的預渲染，必須 SSR 的才 SSR。\n- [ ] 公共路由設定合適的 `cache`／`swr` 標頭。\n- [ ] 需要健康檢查的平台加上 `/healthz`。\n- [ ] 日誌收集已接入（多數平台 `console` 即可）。\n- [ ] 錯誤回報外掛已啟用（`vue:error` + 伺服器 `error` 鉤子）。\n\n## CDN／Cloudflare 注意事項\n\n採用 Cloudflare 代理時，請關閉：\n\n1. **Speed → Content Optimization → Rocket Loader™**\n2. **Security → Email Address Obfuscation**\n\n它們注入的指令碼會破壞 hydration。\n\n## 回滾方案\n\n- Serverless 平台至少保留兩個建置版本 —— 舊頁面 tab 仍持有舊 chunk URL，需令其能取得回應（或讓 Nuxt 的 chunk-error 處理器強制重新整理）。\n- 打 Git tag（`git tag v1.2.3`），便於快速回滾。\n\n## 不止 Node\n\n- **Deno** —— `NITRO_PRESET=deno-deploy` 或 `deno-server`。\n- **Bun** —— 目前用 Node preset 亦可；原生 Bun preset 處於 beta。\n- **Workers／Workerd** —— `cloudflare-pages`、`cloudflare-workers` preset 為一等公民，支援 `routeRules` 映射。\n\n有了 preset 系統，切換執行時僅為一次設定變更 + `NITRO_PRESET`。「一次建置、隨處部署」，正是 Nuxt 的承諾。\n"}),p=g(),h=p.params.book,N=p.params.chapter,L=x(!1),A=x(),{lang:j,resolveBookConfig:I}=k();C(A),y((()=>p.params.chapter),(()=>{L.value=!1}));const H=t[`/books/${h}/config.js`];if(!H)throw v({statusCode:404,message:e("书籍不存在")});const E=H.chapters.findIndex((n=>n.slug===N));if(-1===E)throw v({statusCode:404,message:e("章节不存在")});const D=I(H);function U(n){const e=`/books/${h}/chapters/zh_Hans_CN/${N}.md`;return i[`/books/${h}/chapters/${n}/${N}.md`]||i[e]||""}const O=["zh_Hans_CN","zh_Hant_HK","en_US"].map((n=>({lang:n,content:U(n)}))),F=b((()=>E>0?D.value.chapters[E-1]:null)),z=b((()=>E<H.chapters.length-1?D.value.chapters[E+1]:null));return S((()=>{const n=D.value.chapters[E];n&&T().setPageHead({title:`${n.title} - ${D.value.title}`,description:D.value.description})})),(n,e)=>{const t=_,i=M,p=K;return a(),s("div",Y,[e[3]||(e[3]=o("div",{class:"u-cartoon-sky","aria-hidden":"true"},null,-1)),o("div",{ref_key:"container",ref:A,class:"page-container pt-30 pb-60"},[o("div",X,[o("div",Q,[r(t,{class:d(["u-cartoon-box u-cartoon-box--no-hover",{"sidebar-open":f(L)}]),"book-config":f(D),"book-slug":f(h),"current-chapter":f(N)},null,8,["class","book-config","book-slug","current-chapter"])])]),o("div",Z,[o("div",{class:d(["multi-lang-stage",f(j)])},[(a(!0),s(l,null,u(f(O),(n=>(a(),c(i,{key:n.lang,class:"multi-lang-pane","data-lang":n.lang,content:n.content},null,8,["data-lang","content"])))),128))],2),r(p,{"book-slug":f(h),prev:f(F),next:f(z)},null,8,["book-slug","prev","next"])])],512),o("button",{class:"sidebar-toggle",onClick:e[0]||(e[0]=n=>L.value=!f(L))},e[2]||(e[2]=[o("i",{class:"iconfont icon-caidan text-18"},null,-1)])),f(L)?(a(),s("div",{key:0,class:"sidebar-overlay",onClick:e[1]||(e[1]=n=>L.value=!1)})):w("",!0)])}}})}}}));
